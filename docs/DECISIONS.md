# Decisions

## 2026-06-27: Completion Monitor Requires Scan-Task Health

The strict-forward completion monitor must not be able to report fresh dependencies when the scheduled scan feeders are broken. Scheduler health for `\OptionsStrictForward30Collector` proves the repeated collector wrapper is configured, but it does not prove the 11:00 and 11:30 scan tasks still point at the forced frozen-cohort sweep and no-append readback chain.

Durable decision: `scripts/build_regular_options_strict_forward_30_completion_monitor.py` loads `regular_options_strict_forward_scan_task_health_latest.json`, requires it to be loaded, fresh relative to the collector, and `scan_tasks_ready_for_next_market_window`, and includes its status in the monitor/source artifacts. `scripts/run_strict_forward_30_auto_window_collector.bat`, `scripts/run_scan_picks.bat`, and `scripts/run_scan_picks_safety_net.bat` refresh scan-task health after scheduler health and before candidate review/completion. This is read-only dependency verification; it does not run scans outside approved windows, append rows, enable live validation, enable auto-track, submit broker orders, import quotes, lower proof bars, or count historical rows as forward proof.

## 2026-06-27: Auto-Window Collection Pauses On Pending Candidate Review

Repeated market-window collection must not destroy an already staged candidate batch before operator review. The capture runner intentionally deletes stale canonical candidate JSONL files when a fresh capture finds zero rows, but that behavior is unsafe if a prior same-day no-append capture already produced an append-eligible candidate batch that has not yet been reviewed.

Durable decision: `scripts/run_regular_options_strict_forward_30_auto_window_collector.py` detects a current candidate JSONL whose path and SHA-256 match the latest `candidate_rows_valid_no_append` capture. During an open market window, the auto-window wrapper pauses new collection and reports `auto_window_collector_paused_pending_candidate_review` so candidate review can validate the existing batch. The pause applies to both the dedicated collector path and the scan-batch `--skip-scan-sweep` path. It does not append rows, bypass candidate validation, enable live validation, enable auto-track, submit broker orders, import quotes, mutate evidence stores, lower proof bars, or treat historical rows as forward proof.

## 2026-06-27: Preserve Scanner Entry Quote Provenance In Strict-Forward Handoffs

Strict-forward Phase 2 handoffs must preserve scanner-observed entry quote provenance instead of relying on generated defaults or later reconstruction. The scanner already has the executable quote source and quote timestamp context when it emits a pick, and the scan log is the durable source artifact consumed by candidate staging.

Durable decision: scanner candidates carry explicit `entry_quote_source`, `entry_quote_timestamp_utc`, and `entry_quote_timestamp_et`; `scripts/log_scan_picks.py` logs those fields and writes `scanner_run_id` alongside the existing `scan_run_id`. The Phase 2 stager preserves `scanner_run_id`, `source_scan_run_id`, or legacy `scan_run_id` as scanner provenance. It still validates/fails closed on trusted source labels, timestamps, frozen lanes, frozen symbols, and policy hash, but future natural same-day rows should not lose real provenance in the scan-pick log or candidate JSONL.

This is a provenance-preservation change only. It does not synthesize quote sources, append cohort rows, enable live validation, enable auto-track, submit broker orders, import quotes, lower proof bars, or count historical rows as forward proof.

## 2026-06-27: Closed-Window Strict-Forward Readbacks Use The Latest Completed Market Day

Closed-window strict-forward refreshes must not report a missing scheduled scan session for a weekend or pre-open date that could not have produced a market-window scan yet. Open-window runs still use the current trade date, and the safe next-window command still targets the next market day.

Durable decision: the strict-forward 30-row goal loop, auto-window wrapper, and standalone forward-candidate throughput audit expose a `default_selection_date` for readbacks. Open market windows use the current market date; pre-open and non-market-day closed refreshes use the latest completed market day; after-close market-day refreshes use that just-completed market date. This keeps Saturday/Sunday and pre-open scheduler/throughput readbacks from creating false `blocked_forward_cohort_scheduled_scan_session_missing` states while preserving no-append/no-live/no-broker/no-autotrack behavior and the next safe collection command.

## 2026-06-27: Paper-Shadow Evidence Planning Follows The Trade-Qualification Decision

The paper-shadow evidence plan must not drop the current best evidence lane only because lane-promotion state still reports `promotion_state=diagnostic`. The authoritative evidence-collection decision is the trade-qualification lane row: `decision=paper_shadow_collect` with `disposition=paper_shadow`.

Durable decision: `scripts/build_regular_options_paper_shadow_evidence_plan.py` accepts the current best lane when the matching trade-qualification row has `decision=paper_shadow_collect` and `disposition=paper_shadow` or blank. The promotion-state token remains visible in the plan for context, but it is not the sole gate for paper-shadow evidence collection. This keeps `volatility_expansion_observation` actionable for paper-shadow evidence while live validation, auto-track, broker orders, append, quote import, proof-bar changes, and historical rows as forward proof remain forbidden.

## 2026-06-27: Completion Monitor Embeds The Phase 2 Report Inline

The strict-forward completion monitor must not imply that `data/forward-tracking/phase2_regular_options_forward_paper_shadow_report_latest.json` is loaded when that report writer is intentionally disabled. The monitor computes the Phase 2 forward report inline and embeds that payload as completion evidence.

Durable decision: `source_artifacts.phase2_report` uses `source=computed_inline`, `path=null`, and separate `proposed_report_path` / `proposed_report_exists` metadata. The completion source of truth remains `data/forward-tracking/regular_options_strict_forward_30_completion_monitor_latest.json`, not a missing proposed Phase 2 latest report file.

This is a readback/handoff clarification only. It does not write Phase 2 evidence, append cohort rows, enable live validation, enable auto-track, submit broker orders, import quotes, lower proof bars, or treat historical rows as forward proof.

## 2026-06-27: Strict Forward Exact Proof Requires Trusted Executable Quote Sources

Strict-forward exact entry and exit provenance must require more than a non-empty source label. Accepted executable quote-source labels are `opra_nbbo`, `trusted_opra_nbbo`, `trusted_intraday_opra_nbbo`, `thetadata_opra_nbbo_1m`, `alpaca_opra`, and `alpaca_opra_daily_snapshot`, paired with timestamp and bid/ask provenance. Arbitrary source strings, midpoint/manual/diagnostic basis labels, or stale/display-only marks cannot prove exact entry or exact exit evidence.

The Phase 2 forward report now rejects exact-entry, exact-exit, and open policy-waiting rows without trusted executable entry quote provenance, and exposes `exact_entry_missing_entry_quote_provenance` in candidate append validation. The Phase 2 candidate stager must not default a missing source to `opra_nbbo`; it now rejects missing or untrusted entry quote sources before candidate JSONL creation. The strict-forward exit-completion stager rejects untrusted exit evidence as `untrusted_exit_quote_source`.

This hardens the existing proof bar; it does not authorize append, live validation, auto-track, broker orders, quote import, protected-holdout use, proof-bar lowering, or historical rows as forward proof.

## 2026-06-27: Strict Forward Candidate Entry Points Use New York Market Date Defaults

Standalone strict-forward throughput, staging, and passive capture refreshes must not default target selection date from the UTC calendar date. Evening New York runs can occur after midnight UTC while still reviewing the same regular market date, and a UTC default can falsely report missing scheduled scan sessions or reject same-day scan picks as stale for the next UTC day.

Durable decision: `scripts/build_regular_options_forward_candidate_throughput_audit.py` and `scripts/build_phase2_regular_options_forward_paper_shadow_candidate_rows.py` default an omitted `--selection-date` from the `America/New_York` date derived from `generated_at_utc`, matching the strict-forward goal loop and auto-window collector. Row-level fallback selection dates also use the New York market date for timezone-aware `selection_timestamp_utc` / `logged_at` values when explicit `selection_date` / `scan_date` is absent; naive timestamps keep their literal date. The strict Phase 2 forward report uses the same New York market-date fallback when validating candidate rows, acceptance rows, and date/month concentration from `selection_timestamp_utc`. The passive capture runner inherits the stager behavior. Explicit `--selection-date`, `selection_date`, and `scan_date` still take precedence. This preserves coherent readbacks for after-hours review and does not run scans, append rows, import quotes, mutate evidence stores, enable live validation, enable auto-track, submit broker orders, lower proof bars, or treat historical rows as forward proof.

## 2026-06-27: Use The Strict-Forward Completion Monitor Before Closing The 30-Trade Goal

The active profitability loop's `30`-trade target is closed only when the strict-forward completion monitor reports enough completed Phase 2 forward rows and accepted profitability. Collector, scheduler, candidate-review, or throughput artifacts are supporting state, not completion proof.

The monitor recomputes strict completed rows from the Phase 2 forward paper-shadow report, which enforces post-freeze frozen-lane scope, real source provenance, exact entry and exact exit evidence, OPRA/NBBO quote provenance, policy exit condition, and `net_pnl_usd`. Current monitor status is `completion_monitor_waiting_for_first_cohort_row` with `0/30` strict rows, so the goal remains active.

This decision does not authorize append, live validation, auto-track, broker orders, quote import, protected-holdout use, proof-bar changes, or historical rows as forward proof.

## 2026-06-27: Scheduled Scans Feed The Strict-Forward Collector Without Append

The active `30`-row strict forward-audit goal should not depend on a human remembering a separate capture command after scheduled market-window scans. The existing scheduled scan tasks already run inside the regular market window and now sweep both frozen Phase 2 lanes, so the strict-forward collector should follow that sweep immediately.

Durable decision: `scripts/run_strict_forward_30_auto_window_collector.bat` is the standalone repeated collector wrapper for Windows Task Scheduler and forces `OPTIONS_SCAN_AUTO_TRACK=0` while keeping portfolio/profitability gates enabled. Registered task `\OptionsStrictForward30Collector` runs that wrapper on weekdays starting at 07:35 local and repeats every 30 minutes for 6.5 hours. The wrapper invokes the auto-window collector with `--max-attempts 3 --sleep-seconds 300 --json`, which increases bounded no-append market-window sampling while staying inside the 45-minute task execution limit. `scripts/run_scan_picks.bat` and `scripts/run_scan_picks_safety_net.bat` also run `scripts/run_regular_options_strict_forward_30_auto_window_collector.py --skip-scan-sweep --json` after `scripts/run_forward_cohort_scan_sweep.py --force`. The 11:30 safety-net batch is intentionally a second passive frozen-cohort sample. `scripts/build_regular_options_strict_forward_scan_task_health.py`, exposed as `npm run options:goal-loop:strict-forward-scan-task-health`, verifies the two scheduled scan tasks and batch contents. This stages/refreshes strict-forward candidate evidence from scheduled market-window collection without appending rows. Append remains limited to the existing guarded tokened path after explicit review; this does not fabricate rows, count historical rows as forward proof, lower proof bars, enable live validation, enable auto-track, submit broker orders, import quotes, consume holdout, or promote lanes.

The same scheduled wrappers also refresh scan-task health, the read-only candidate-review packet, exit-completion stager, lifecycle audit, and strict-forward completion monitor after the collector. These readbacks are status/proof surfaces only; they do not append rows or authorize mutation.

## 2026-06-26: Oracle Packet Ranks VRP And Term Through Current Bounded Gates

The Oracle profitability loop packet must not rank older VRP or term-structure replay-readiness artifacts when newer structure harness and bounded replay gates exist. Those older artifacts still have provenance value, but their engine blocker arrays are stale relative to the current harness/bounded gates.

Durable decision: `scripts/build_options_oracle_profit_loop_packet.py` loads `regular-options-vrp-credit-spread-structure-harness`, `regular-options-vrp-credit-spread-bounded-replay`, `regular-options-term-structure-calendar-structure-harness`, and `regular-options-term-structure-calendar-bounded-replay`. The current packet blocker arrays use the bounded replay gates: VRP remains blocked only on `missing_index_credit_spread_quote_surface`; term structure remains blocked only on `missing_index_calendar_quote_surface` and `missing_point_in_time_term_structure_inputs`. This is not replay, not accepted profitability, not quote import, not evidence mutation, not live/broker/auto-track permission, not holdout use, not proof-bar change, and not promotion.

## 2026-06-26: Dispersion Pair Mechanics Are Cleared As Readiness Blockers

The dispersion-proxy hybrid readiness audit no longer treats pair construction, side-aware all-leg pair pricing, pair max-loss/collateral convention, full denominator mapping, or strict-new dedupe as current blockers when the preregistered dispersion playbook is loaded. The playbook already freezes those design/formula/denominator contracts, and the readiness artifact now includes the preregistration artifact in its evidence search.

Durable decision: `index_constituent_dispersion_proxy_defined_risk_hybrid_v1` remains blocked only on `missing_dispersion_or_concentration_proxy_inputs` until a trusted point-in-time dispersion/concentration source exists. This is not replay, not accepted profitability, not quote import, not evidence mutation, not live/broker/auto-track permission, not holdout use, not proof-bar change, and not promotion.

## 2026-06-26: Term-Structure Geometry And Strict-New Are Cleared As Local Harness Blockers

The term-structure calendar/diagonal branch no longer treats `missing_preregistered_calendar_diagonal_geometry` or `missing_strict_new_dedupe` as current blockers. The preregistered playbook already carries frozen geometry through `concept.frozen_design.term_structure_selection` plus `exit_policy`, and the bounded replay gate already has duplicate strict-new identity handling against the 157-row clean base stack.

Durable decision: the structure harness must recognize the real preregistered playbook shape, surface `candidate_geometry_ready=true` and `strict_new_dedupe_ready=true`, and mark those stale blockers as satisfied by harness. The branch remains blocked on real source/quote blockers only: `missing_point_in_time_term_structure_inputs` and `missing_index_calendar_quote_surface`. This is not replay, not accepted profitability, not quote import, not evidence mutation, not live/broker/auto-track permission, not holdout use, not proof-bar change, and not promotion.

## 2026-06-26: Profitability Blocker Goal Requires Whole-Surface Inventory And Agent Consensus

The current blocker-removal goal is not satisfied by clearing stale VIX text alone. `docs/regular-options-profitability-blocker-inventory.md` is now the current whole-surface inventory for regular-options profitability blockers.

Durable decision: direct VIX is cleared by current artifacts (`direct_vix_source_import_materialized` and `point_in_time_vix_bucket_ready` with `505` / `505` coverage), and stale VIX propagation has been patched in current source/selector/replay readbacks. The remaining profitability blockers are non-VIX: `0/30` strict forward proof, missing real forward cohort rows, frozen 13-symbol candidate generation at `0/24` months and `0` selected rows, missing trusted underlying/opening, macro-event, flow, dispersion/concentration, trend/regime, breadth/momentum inputs, missing quote surfaces for VRP/skew/term/PMCC structures, incomplete replay/engine surfaces, ThetaTerminal/source availability, explicit source/materialization approval boundaries, and future market-window forward capture.

The preregistered selector must consume current readiness gates, including momentum bounded replay, before ranking a blocker-free candidate. A blocker-removal goal is complete only when all locally removable blockers are fixed, remaining blockers are explicitly categorized, verification passes, and the six-agent debate agrees that the done/not-done conclusion is evidence-backed. Source, approval, provider, and market-window blockers cannot be erased by rewriting docs.

## 2026-06-26: Post-Event IV-Crush Readiness Gap Is Now A Concrete Blocker Audit

`scripts/build_regular_options_post_event_iv_crush_replay_readiness.py`, exposed as `npm run options:research:post-event-iv-crush-replay-readiness`, now owns the read-only readiness audit for `post_event_iv_crush_index_iron_condor_v1`. The preregistered selector and Oracle packet consume and validate this artifact, so the branch is no longer blocked by a missing structure-specific readiness audit.

Durable decision: the current blockers for this branch are non-VIX blockers: missing macro-event calendar source/category coverage, missing IV/event-premium proxy, and insufficient full-window/latest-four/train-month quote-surface coverage for four-leg iron condor/butterfly replay. The artifact is not replay, not accepted profitability, not source or quote import, not evidence mutation, not live validation, not auto-track, not broker permission, not holdout use, not scanner/strategy/proof-bar change, and not promotion.

## 2026-06-26: Dreaming Is Reviewable Memory, Not Options Proof

The local agent control plane now supports session transcript provenance and out-of-band dream proposals, while `C:\Users\kalec\projects-memory` provides a computer-wide memory index and schema. The options repo keeps `data/agent-control/agent_control.db` as its authoritative runtime graph; the global graph is only a pointer/provenance layer.

Durable decision: dream proposals are non-authoritative until accepted through `npm run agent:control -- dream accept`. Accepted dream entries become visibly marked operating memory with `origin=dreaming`, `non_authoritative=true`, and `does_not_authorize_trading_or_evidence_mutation=true`. They cannot prove profitability, approve evidence mutation, change scanner policy, promote lanes, open broker-paper or live-capital paths, consume holdout, or override living docs, generated readbacks, gateboard state, exact OPRA/NBBO evidence, or code.

## 2026-06-25: Oracle Profitability Prompt Must Rank Current Evidence, Not Stale Packets

The same-session GPT-5.5 profitability loop packet is now an approval-aware reviewer and next-slice selector, not a generic handoff prompt. `scripts/build_options_oracle_profit_loop_packet.py`, exposed as `npm run options:oracle-loop:packet`, starts with a current fact table and evidence-precedence rules so stale embedded blockers cannot outrank newer structured artifacts.

Durable decision: the packet must preserve strict forward proof at `0/30` until real approved forward-cohort rows with exact executable entry and exit evidence exist. Historical, replay, simulated-forward, dashboard, research/backfill, midpoint, stale, EOD, last, model, manual, synthetic, lookahead, diagnostic, or repaired historical rows can rank hypotheses but cannot count as accepted profitability. Current VIX-ready artifacts override older missing-VIX text; 59-symbol ThetaTerminal retry stays parked until provider/source state changes; 13-symbol quote depth remains insufficient without candidate-generation proof; underlying daily OHLCV is a first-class blocker but cannot be solved by another packet-only plan or local `market_data.db` shortcut; and bullish-pullback `layer_4_clean_exact` must be explicitly ranked as a profitable historical economics branch whose remaining path is natural forward capture and approval, not promotion.

Future GPT-5.5 outputs must include branch-local status, approval requirements, stale blockers ignored, a safe fallback when a top branch is approval/source/market-window blocked, and measurable replay or forward-row metrics. Source-row writes, default source-row materialization, quote import, cohort-log append, evidence-store mutation, protected-holdout use, live validation, auto-track, broker/order action, promotion, and production scanner/strategy/stop/sizing/proof-bar changes still require separate exact approval.

## 2026-06-25: Underlying Daily Acquisition Is A Source Intake Gate, Not Profitability Proof

The next safe blocker-reducing slice for the regular-options profitability loop is not another parser plan and not a local `market_data.db` shortcut. `scripts/build_regular_options_underlying_daily_source_acquisition_packet.py`, exposed as `npm run options:source-acquisition:underlying-daily-history`, now owns the read-only intake preflight for staged trusted `point_in_time_underlying_daily_ohlcv_adjusted_v1` CSVs.

Latest status is `blocked_underlying_daily_source_acquisition_missing`: no CSV exists under `data/import-staging/underlying_daily`, so there are `0` candidate files and `0` ready candidates. The packet reuses the strict source parser/validator, rejects leakage fields, manual/synthetic/source-mark rows, late known-at rows, and local provenance shortcuts such as `market_data.db:daily_history`, historical reconstruction, or inferred known-at policy. It emits the exact future command `npm run options:source-import:underlying-daily-history -- --source-file data/import-staging/underlying_daily/point_in_time_underlying_daily_ohlcv_adjusted_v1.csv --approval-token APPROVE_UNDERLYING_DAILY_HISTORY_SOURCE_IMPORT --no-replay --json` only as a future materialization path.

Durable decision: the acquisition packet is a fail-closed source intake boundary. It writes no source rows, runs no replay, imports no quotes, mutates no evidence stores, consumes no protected holdout, and proves no profitability. The historical frozen scanner adapter now has scanner/as-of/no-write contract blockers cleared, but the 13-symbol historical path remains blocked on missing trusted underlying daily source rows and downstream point-in-time input surfaces until a real full-window source CSV is staged and separately approved for tokened import.

## 2026-06-24: Frozen 13-Symbol Entrypoint Exists But Daily Candidate Decisions Are Still Missing

GPT-5.5 Pro selected `candidate_generation_repair:frozen_13_symbol_reusable_candidate_generation_entrypoint_v1` after the profitability-first blocker map identified candidate throughput as the highest-leverage blocker. `scripts/regular_options_frozen_candidate_generation_entrypoint.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-entrypoint`, now owns the reusable read-only/no-write frozen daily candidate/no-pick entrypoint.

Latest status is `blocked_frozen_13_symbol_candidate_generation_entrypoint`: the artifact emits `6,916` lane/symbol/date rows across the frozen Phase 2 13-symbol cohort from `2024-06-01` through `2026-05-31`, but all rows are blocked by `missing_daily_candidate_generation_diagnostics`; the source is still not an exact frozen 13-symbol daily decision source, covered months are `0/24`, and selected candidates are `0`. The frozen source-surface materializer now consumes this entrypoint, and the historical simulated-forward audit now defaults to the frozen source surface instead of the old broad selected-trade source. Current audit status remains `blocked_historical_simulated_forward_audit` with `0` selected rows, `0` train months, `0` audit months, and `0/30` exact audit trades.

Durable decision: the reusable entrypoint is implemented, but it does not clear candidate-generation proof. Do not claim historical or forward profitability from old broad selected rows, dashboard rows, or quote-depth-only rows. The next same-session Oracle loop should treat the remaining blocker as real daily candidate-generation/source repair, source materialization, replay-engine work, or another ranked profitability path. No broker orders, live validation, auto-track, quote import, evidence mutation, protected-holdout consumption, production scanner/strategy/stop/sizing/proof-bar change, or promotion occurred.

## 2026-06-24: Direct VIX Source Is Materialized; Macro And Flow Need Trusted Source Files

The direct VIX source repair packet is no longer merely an import-decision artifact. `scripts/import_regular_options_direct_vix_source.py`, exposed as `npm run options:source-import:direct-vix`, materialized official Cboe VIX daily history into `data/profitability-lab/regular-options-point-in-time-vix-bucket/source_rows.jsonl` under the token `APPROVE_DIRECT_VIX_SOURCE_IMPORT`, wrote the frozen VIX bucket policy at `data/contracts/regular-options-vix-bucket-policy.json`, and did not mutate `options_history.db`.

Latest direct VIX readback is `direct_vix_source_import_materialized`; the downstream point-in-time VIX bucket is `point_in_time_vix_bucket_ready` with `505` / `505` requested feature-store dates covered, `100.0%` coverage, `0` late-known-at rows, `0` leakage rejects, and no blockers. VIX should no longer be named as a blocker for macro-event, flow-extreme, PMCC, momentum, dispersion, skew, or VRP readiness unless a future artifact becomes missing, stale, malformed, or policy-incompatible.

`scripts/import_regular_options_macro_event_calendar.py` and `scripts/import_regular_options_flow_extreme_volume_oi.py` now provide exact tokened materialization paths for the next easy source repairs, but they still require trusted input CSVs. Macro-event remains blocked by missing scheduled-event source rows, and flow-extreme remains blocked by missing trusted SPY/QQQ daily option volume/open-interest source rows. These importers do not authorize broker orders, live validation, auto-track, protected-holdout consumption, production scanner/strategy/stop/sizing/proof-bar changes, evidence DB mutation outside their generated source-row artifacts, promotion, or treating historical rows as forward proof.

## 2026-06-24: Flow-Extreme Volume/OI Source Repair Packet Is Ready For Future Import Decision

GPT-5.5 Pro selected `trusted_flow_volume_oi_source_repair_packet_v1` after the macro-event calendar source repair packet reached import-decision state. `scripts/build_regular_options_flow_extreme_source_repair_packet.py`, exposed as `npm run options:source-plan:flow-extreme-volume-oi`, now owns the read-only flow-extreme volume/open-interest source repair packet.

Latest status is `flow_extreme_source_repair_packet_ready_for_operator_import_decision`. The packet reproduces the current flow baseline (`point_in_time_flow_extreme_input_status=blocked_point_in_time_flow_extreme_input`, `flow_extreme_volume_oi_source_rows_status=blocked_flow_extreme_volume_oi_source_rows`, `covered_month_count=0`, `date_coverage_pct=0.0`), defines `trusted_option_volume_open_interest_daily_v1` required fields for SPY/QQQ only, rejects same-day aggregate use for same-day entries, freezes the prior-row percentile policy, validates late-known-at and missing-value fixture rejects, and emits a future tokened import/materialization command using `APPROVE_FLOW_EXTREME_VOLUME_OI_SOURCE_IMPORT`.

Durable decision: this task authorizes only a future approval decision, not import. Flow source import/materialization, source-row writes, downstream flow input materialization, replay, and profitability claims remain not run. No evidence store mutation, protected-holdout consumption, live validation, auto-track, broker/order action, scanner/strategy/stop/sizing/proof-bar change, or promotion occurred. The same-session Oracle handoff prompt now starts with the profitability-first blocker-ranking prompt and explicitly forbids repeating the completed VIX, macro-event, flow-source, and ThetaTerminal retry branches unless source state changes.

## 2026-06-24: Macro-Event Calendar Source Repair Packet Is Ready For Future Import Decision

GPT-5.5 Pro selected `macro_event_calendar_source_repair_packet_v1` after the direct VIX source repair packet reached import-decision state. `scripts/build_regular_options_macro_event_calendar_source_repair_packet.py`, exposed as `npm run options:source-plan:macro-event-calendar`, now owns the read-only macro-event source repair packet.

Latest status is `macro_event_calendar_source_repair_packet_ready_for_operator_import_decision`. The packet reproduces the current macro-event baseline (`macro_event_calendar_status=blocked_macro_event_calendar_source_missing`, `event_count=0`, `covered_categories=[]`), defines `scheduled_macro_event_calendar_v1` required fields and frozen categories (`cpi`, `fomc_minutes`, `fomc_rate_decision`, `nonfarm_payrolls`, `pce`, `scheduled_fed_chair_testimony`), validates a fixture with before-market, during-market, after-market, and holiday/weekend-adjacent cases, rejects surprise/outcome/reaction/P&L fields, and emits a future tokened import/materialization command using `APPROVE_MACRO_EVENT_CALENDAR_SOURCE_IMPORT`. The packet identifies macro-event long strangle and post-event IV-crush iron condor branches whose event-calendar blocker could clear after a valid future source.

Durable decision: this task authorizes only a future approval decision, not import. Macro-event source import/materialization, source-row writes, replay, and profitability claims remain not run. No evidence store mutation, protected-holdout consumption, live validation, auto-track, broker/order action, scanner/strategy/stop/sizing/proof-bar change, or promotion occurred.

## 2026-06-24: Direct VIX Source Repair Packet Is Ready For Future Import Decision

GPT-5.5 Pro selected `direct_point_in_time_vix_source_repair_packet_v1` after the 59-symbol ThetaTerminal resume retry parked. `scripts/build_regular_options_direct_vix_source_repair_packet.py`, exposed as `npm run options:source-plan:direct-vix`, now owns the read-only VIX source repair packet.

Historical superseded status was `direct_vix_source_repair_packet_ready_for_operator_import_decision`. That earlier packet reproduced the pre-import VIX baseline, defined `direct_vix_daily_close` required fields, froze known-at policy so date D VIX close is tradable no earlier than the next regular market session, froze `vix_prior_close_fixed_buckets_v1`, validated a CBOE-style fixture with a holiday/weekend gap, and emitted future commands for a tokened direct VIX import plus downstream VIX-bucket materialization. The current direct VIX packet is superseded by the materialized source and ready point-in-time VIX bucket; do not use this older paragraph as current VIX blocker state.

Durable decision: this task authorizes only a future approval decision, not import. VIX source import/materialization, source-row writes, downstream VIX-bucket materialization, replay, and profitability claims remain not run. No evidence store mutation, protected-holdout consumption, live validation, auto-track, broker/order action, scanner/strategy/stop/sizing/proof-bar change, or promotion occurred.

## 2026-06-24: Park 59-Symbol Resume Retry On ThetaTerminal Availability Only

GPT-5.5 Pro selected `scoped_59_symbol_thetadata_opra_nbbo_import_resume_and_split_audit_v2` after the 13-symbol frozen engine branch parked. `scripts/run_regular_options_59_symbol_thetadata_opra_import_repair.py` now accepts the resume/provider-recheck flags, and `scripts/run_regular_options_59_symbol_thetadata_opra_import_resume.py` provides a resume-specific entrypoint and artifacts.

Latest tokened resume readback is `blocked_thetaterminal_source_unavailable_retry`: `approval_token_valid=true`, local ThetaTerminal refused `http://127.0.0.1:25503/v2/system/status`, `import_attempted=false`, `imported_rows=0`, `quotes_imported=false`, shared trusted imported quote dates remain `260` from `2025-05-22` through `2026-06-04`, `11,565` symbol-date gaps remain, `protected_holdout_overlap_rows=0`, and `outside_universe_import_rows=0`. The split-aware historical simulated-forward audit was not rerun because no import or coverage improvement occurred.

Durable decision: do not retry the same 59-symbol ThetaData provider-down branch until ThetaTerminal/source availability changes. This is not accepted profitability, not forward proof, not live validation, not auto-track, not broker/order permission, not scanner/strategy/stop/sizing/proof-bar change, not protected-holdout consumption, and not promotion. The same-session Oracle loop should pivot to the next non-live/non-broker source family or causal branch.

## 2026-06-24: Park 13-Symbol Candidate-Generation Engine On Missing Reusable Frozen Entrypoint

GPT-5.5 Pro selected `frozen_13_symbol_candidate_generation_engine_daily_diagnostics_v1` after the approved 59-symbol source repair was blocked by local ThetaTerminal availability. `scripts/build_regular_options_13_symbol_frozen_candidate_generation_engine.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-engine`, now owns the read-only engine/daily diagnostics artifact.

Latest status is `blocked_frozen_13_symbol_candidate_generation_engine`, with decision `blocked_missing_reusable_candidate_generation_entrypoint`. The artifact loads the 13-symbol feature store, forward-cohort preregistration, forward-holdout contract, source-quality policy, base clean stack ledger, no-write runner support, prior frozen source surface, and denominator-v2 readbacks. It emits `6,916` daily diagnostics rows across the frozen Phase 2 lane/symbol pairs, but all rows are blocked because no reusable frozen no-write candidate-generation entrypoint is advertised. Candidate-generation coverage remains `0/24` months, train months `0`, audit months `0`, latest-audit exact trades `0`, selected candidates `0`, and the historical simulated-forward audit was not rerun because coverage was not proven.

Durable decision: do not repeat the 13-symbol source-surface audit, no-write runner support, denominator-v2, or frozen engine diagnostics unless a real reusable frozen candidate-generation entrypoint/source artifact is added. Quote depth remains insufficient by itself. This is not accepted profitability, not forward proof, not live validation, not auto-track, not broker/order permission, not quote import, not evidence DB mutation, not protected-holdout consumption, not scanner/strategy/stop/sizing/proof-bar change, and not promotion.

## 2026-06-24: Do Not Treat ThetaTerminal Down As An Earned Profitability Stop

The operator approved the scoped 59-symbol ThetaData OPRA/NBBO source-repair/import slice requested by GPT-5.5 Pro, limited to non-live/non-broker research data repair over `2024-05-22` through `2026-06-04`. The tokened run recognized `APPROVE_SCOPED_59_SYMBOL_THETADATA_OPRA_IMPORT`, but local ThetaTerminal refused connection at `http://127.0.0.1:25503/v2/system/status`, so `scripts/run_regular_options_59_symbol_thetadata_opra_import_repair.py` generated `blocked_thetaterminal_source_unavailable`.

Latest readback: `dry_run=false`, `approval_token_valid=true`, `import_attempted=false`, `imported_rows=0`, `quotes_imported=false`, `accepted_profitability=false`, shared trusted imported quote dates remain `260` from `2025-05-22` through `2026-06-04`, and `11,565` symbol-date gaps remain. This did not improve the historical simulated-forward blocker and did not create any forward proof rows.

Durable decision: ThetaTerminal being down is a provider-availability/source-state blocker, not an operator-approval blocker and not an earned `stop_exception` for the profitability goal. The same-session Oracle/GPT-5.5 loop should continue to the next meaningful non-live/non-broker research branch instead of stopping on approval language. Still forbidden without a separate exact decision: broker orders, live validation, auto-track, production scanner release, stop/sizing/proof-bar changes, protected-holdout consumption, promotion, and treating historical rows as forward proof.

## 2026-06-23: Phase 2 Forward Capture Cannot Append Without Natural Selections

After the user approved the forward paper-shadow append gate, GPT-5.5 Pro selected `phase2_forward_paper_shadow_market_window_capture_append_v1` as the smallest direct path toward the `30` strict completed forward rows target. The existing passive capture command now has a compact audit readback at `docs/regular-options-phase2-forward-paper-shadow-market-window-capture.md` and `data/profitability-lab/regular-options-phase2-forward-paper-shadow-market-window-capture/latest.json`.

Latest status is `no_phase2_natural_selections_no_append`. The capture runner staged `0` candidate rows from `data/forward-tracking/scan_picks.jsonl`, did not write `phase2_regular_options_forward_paper_shadow_candidate_rows.jsonl`, and therefore validation and append were not run. Existing scan-pick rows failed the Phase 2 gate as `non_phase2_lane=468`, `non_preregistered_symbol=50`, `pre_freeze_selection=31`, and `not_current_market_window_selection=1`. The goal-loop readback still reports `cohort_log_status=missing`, `cohort_log_row_count=0`, `post_freeze_strict_exact_completed_rows=0`, and `strict_rows_remaining_to_minimum=30`.

Durable decision: forward append is approved only for real same-day Phase 2 natural selections that pass the existing validator with `append_allowed=true`. No fabricated, fixture, stale, historical, non-frozen-lane, non-preregistered-symbol, or pre-freeze rows may be appended. If repeated market-window captures continue to produce zero natural selections, pivot to the newly approved scoped source repair/import plan for the smallest named blocker instead of rerunning the same empty capture indefinitely. This run is not live validation, not auto-track, not broker/order action, not scanner/strategy/proof-bar change, not quote import, not protected-holdout consumption, not promotion, and not accepted profitability.

## 2026-06-23: Park Existing Read-Only Input Surfaces Under Current Repository Data

GPT-5.5 Pro selected a read-only existing repository point-in-time input/source surface atlas after the all-local quote-minute atlas parked quote-surface-only replayability. `scripts/build_regular_options_existing_input_surface_atlas.py`, exposed as `npm run options:research:existing-input-surface-atlas`, now owns that generated artifact.

Latest status is `research_only_input_surfaces_exhausted_under_current_repository`. The artifact opens `data/options-validation/options_history.db` in SQLite read-only mode, inventories DB tables, generated data artifacts, and required missing-input families for `2024-06-01` through `2026-05-31` as of `2026-06-04`, and refuses to mark a surface ready unless it has known-at safety, no leakage, no protected-holdout overlap, `20` train months, all `4` latest months, and at least `90%` date coverage. It found `0` ready source surfaces and emitted `stop_exception_candidate=true`. The only candidate with 20+4 month coverage was `option_quote_snapshots_term_structure_skew_quote_proxy` (`26,579,741` usable rows, `95.0%` date coverage, `96.47%` latest-four coverage), but that is only the already-parked quote surface and cannot clear missing VIX, flow, event, underlying, trend, or candidate-generation blockers. Direct underlying/IV/volume/OI fields are limited to `3` dates in `2026-05`; VIX, flow, macro/earnings events, dispersion, and candidate-generation diagnostics remain missing or unsafe; fresh forward collection remains approval-required.

Durable decision: under current repository data and current prohibitions, there is no remaining read-only existing input/source surface that can honestly support a materially different proof-safe branch. Do not repeat this source-surface atlas, fixed quote matrix, all-local quote atlas, opening-range, synthetic-forward, PMCC, VIX, flow, macro-event, dispersion, or candidate-generation source-surface slices unless a new trusted source, explicit data repair/import approval, forward-cohort append approval, protected-holdout decision, or promotion review changes the source state. This artifact is not accepted profitability, not replay, not P&L proof, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park All-Local Quote-Surface-Only Replayability On Insufficient Train Coverage

GPT-5.5 Pro selected a read-only all-local stable quote-minute structure capability atlas after the fixed 13-symbol structure matrix found dense latest-four rows but inadequate train-month coverage. `scripts/build_regular_options_all_local_quote_minute_structure_capability_atlas.py`, exposed as `npm run options:research:all-local-quote-minute-structure-capability-atlas`, now owns that generated artifact.

Latest status is `all_local_quote_surface_replayability_exhausted_under_current_data`. The artifact opens `data/options-validation/options_history.db` in SQLite read-only mode, inventories existing trusted OPRA/NBBO bid/ask rows across all local underlyings and all 5-minute quote buckets from 09:35 through 15:55 ET for `2024-06-01` through `2026-05-31` as of `2026-06-04`, and ranks surfaces by quote coverage only, not P&L or future outcomes. It found `66` trusted local underlyings, `2,520` symbol/bucket inventory rows, `40` selected detailed coverage surfaces, `324,576` daily bucket/structure status rows, and `191,641` representative opportunities, but `0` replay-feasible surfaces and no next replay candidate. The strongest surfaces had substantial full/latest-four depth, such as `TLT:same_expiration_same_type_verticals:14:45-15:45:21-45` with `740,239` full-window and `239,195` latest-four constructible opportunities, but only `9` train months. The best selected broader-history coverage surface was `IWM` with `15` train months. The active source-quality policy excludes `CVX` from feasible replay surfaces.

Durable decision: under current trusted local quote rows and current prohibitions, all-local quote-surface-only replayability is parked because the available executable OPRA/NBBO quote surface does not provide the required `20` train months, even when all local underlyings and all available quote-minute buckets are searched. Do not repeat fixed-bucket structure inventory, all-local quote-minute atlas work, opening-range replay, or synthetic-forward source-surface work unless a new trusted source/data repair changes train coverage, underlying/opening bucket availability, or same-minute call-put-pair blockers. This artifact is not accepted profitability, not replay, not P&L proof, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park Local Quote-Surface-Only Structures On Insufficient Train Coverage

GPT-5.5 Pro selected a read-only local OPRA/NBBO structure capability matrix after the opening-range and synthetic-forward branches failed on source-surface availability. `scripts/build_regular_options_local_quote_structure_capability_matrix.py`, exposed as `npm run options:research:local-quote-structure-capability-matrix`, now owns that generated artifact.

Latest status is `local_quote_surface_only_structures_exhausted_under_current_data`. The artifact opens `data/options-validation/options_history.db` in SQLite read-only mode and inspects the 13-symbol proof set at fixed 10:40/14:30 ET entry buckets and a 15:50 ET exit bucket over `2024-06-01` through `2026-05-31` as of `2026-06-04`. It emitted `12,864` daily structure status rows and `1,333` representative opportunities, but `0` structures cleared replay feasibility. Same-expiration same-type verticals had `27,428` full-window and `20,358` latest-four constructible opportunities, but only `4` train months; single legs had `10,116` full-window and `6,544` latest-four opportunities but only `6` train months; butterflies, condors, calendars/diagonals, and bounded ratio/backspreads also failed train-month or latest-month gates; straddles and iron structures failed full-window/latest-four depth.

Durable decision: under current local quote rows and current prohibitions, local quote-surface-only structures are not replay-feasible because they do not cover the required `20` train months plus all `4` latest-four months despite dense recent quotes. Do not repeat quote-surface-only structure inventory, opening-range replay, or synthetic-forward source-surface work unless a new trusted source/data repair changes train coverage, opening/underlying, or same-minute pair blockers. This artifact is not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park Synthetic-Forward Opening-Bucket Surface On Missing Same-Minute Call-Put Pairs

GPT-5.5 Pro selected a read-only quote-derived synthetic-forward input surface to test whether existing OPRA/NBBO option quotes can replace the missing underlying/opening-bucket source for the opening-range branch. `scripts/build_regular_options_quote_derived_synthetic_forward_surface.py`, exposed as `npm run options:research:quote-derived-synthetic-forward-surface`, now owns that generated artifact.

Latest status is `blocked_quote_derived_synthetic_forward_surface`. The script opens `data/options-validation/options_history.db` in SQLite read-only mode and checks SPY/QQQ/IWM/DIA over `2024-06-01` through `2026-05-31` as of `2026-06-04` at 09:35, 10:35, 10:40, and 15:50 ET. The real readback produced `1,976` symbol-date surface rows, `7,904` requested symbol/date/bucket checks, `0` ready buckets, `0.0%` bucket coverage, `0` train months covered, `0` latest-four months covered, and no next replay command.

Durable decision: same-minute OPRA/NBBO call-put pairs in the current local store do not provide the requested research-only synthetic-forward opening-bucket surface. Do not rerun the opening-range replay or this synthetic-forward surface expecting progress unless a trusted opening-bucket/underlying source, different approved source surface, or explicit data repair changes the missing-pair blocker. This artifact is not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park Quote-Surface Opening-Range Reversal On Missing Underlying Price

GPT-5.5 Pro selected a materially different read-only replay for `quote_surface_opening_range_reversal_vertical_v1`. `scripts/build_regular_options_quote_surface_opening_range_reversal_replay.py`, exposed as `npm run options:research:quote-surface-opening-range-reversal-replay`, now owns that generated artifact.

Latest status is `blocked_quote_surface_opening_range_reversal_replay`. The script opens `data/options-validation/options_history.db` in SQLite read-only mode, uses the base clean stack identity ledger for strict-new dedupe, and emits daily denominator and candidate JSONL artifacts for SPY/QQQ/IWM/DIA over `2024-06-01` through `2026-05-31` as of `2026-06-04`. The real readback produced `1,976` daily denominator rows, all `1,976` blocked by `blocked_missing_underlying_price` / missing opening-range snapshot evidence, `0` candidate rows, `0` latest-four strict executable completed rows, and smallest blocker `blocked_missing_quote_surface_underlying_price`.

Durable decision: the current OPRA/NBBO option quote table is not a point-in-time underlying/opening-range source for this concept. Do not rerun this opening-range reversal branch unless a trusted underlying-price/opening-bucket source or explicitly approved research-only source surface changes that blocker. This artifact is not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park 13-Symbol Denominator v2 On Missing Frozen Candidate-Generation Engine

GPT-5.5 Pro selected a read-only frozen 13-symbol candidate-generation denominator v2 after the PMCC readiness branch parked. `scripts/build_regular_options_13_symbol_frozen_candidate_generation_denominator_v2.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-denominator-v2`, now owns that generated daily denominator artifact.

Latest status is `blocked_13_symbol_frozen_candidate_generation_denominator_v2`. The 13-symbol quote surface still has `24` quote months, the no-write/as-of runner support is available, and the artifact emits `494` daily denominator rows for `2024-06-01` through `2026-05-31` as of `2026-06-04`. All `494` rows are blocked, the frozen source surface still has `0/24` candidate-generation months, latest-four strict-new candidate rows after opportunity dedupe are `0`, and the smallest next blocker is `missing_frozen_13_symbol_candidate_generation_engine`.

Durable decision: quote depth and no-write runner controls are not daily candidate/no-pick proof. Do not repeat the v2 denominator, no-write runner, or frozen source-surface materializer unless a real frozen 13-symbol candidate-generation engine or source artifact changes the blocker. This artifact is not replay, not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Park PMCC Diagonal Readiness On Missing Trend And Long-DTE Quote Surface

GPT-5.5 Pro selected a read-only replay-readiness audit for `low_mid_vix_index_pmcc_diagonal_income_v1`. `scripts/build_regular_options_pmcc_diagonal_replay_readiness.py`, exposed as `npm run options:research:pmcc-diagonal-replay-readiness`, now owns that artifact.

Current refreshed status is `blocked_pmcc_diagonal_replay_readiness`. The preregistered PMCC design is valid and remains design-only, undefined/uncapped short-call risk is false, side-aware diagonal entry/roll/exit formulas are registered, short-call roll/assignment/ex-dividend/expiration handling is registered, max-loss/collateral and denominator statuses are registered, the base clean stack identity ledger is ready, protected-holdout guard is loaded, and current VIX is cleared. The real blockers are `missing_point_in_time_trend_or_regime_inputs` and `missing_trusted_pmcc_diagonal_quote_surface`; read-only `options_history.db` inspection found `1,992,676` trusted SPY/QQQ call quote rows but `0` long-DTE call rows for the PMCC surface.

Durable decision: do not proceed to PMCC replay and do not repeat this readiness audit unless a point-in-time trend/regime input surface or trusted long-DTE diagonal quote surface changes. This artifact is not replay, not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Do Not Treat Empty Trusted Volume/OI Columns Or Research-Grade Flow As Flow Proof

GPT-5.5 Pro selected a read-only volume/open-interest source-row generator for the flow-extreme ratio/backspread branch. `scripts/build_regular_options_flow_extreme_volume_oi_source_rows.py`, exposed as `npm run options:research:flow-extreme-volume-oi-source-rows`, now owns that artifact.

Latest status is `blocked_flow_extreme_volume_oi_source_rows`. The generator opened `data/options-validation/options_history.db` read-only, filtered to trusted `thetadata_opra_nbbo_1m` intraday SPY/QQQ rows, and found `1,635` aggregate rows across `501` dates but `0` usable volume/open-interest aggregates. No point-in-time source rows were written, coverage stayed `0/24` months and `0.0%`, and blockers are `missing_trusted_volume_open_interest_source_rows`, `trusted_rows_have_null_volume_open_interest`, `insufficient_month_coverage`, and `insufficient_date_coverage`.

Durable decision: empty trusted volume/open-interest columns do not clear the flow-input blocker, and research-grade daily rows with volume/OI must not be relabeled as trusted proof without a separate explicit data-trust/source decision. Do not repeat the volume/OI source-row slice unless a new trusted point-in-time volume/OI source, approved data-trust repair, or explicit flow-source strategy changes the blocker. This artifact is not replay, not accepted profitability, not forward proof, not quote import, not evidence mutation, not scanner/strategy/proof-bar change, not live validation, not auto-track, not broker permission, not protected-holdout use, and not promotion.

## 2026-06-23: Do Not Pretend Strict-New Dedupe Is Ready Without Row-Level Base Identities

GPT-5.5 Pro selected a read-only denominator and strict-new dedupe bridge for the flow-extreme ratio/backspread branch. `scripts/build_regular_options_flow_extreme_denominator_dedupe_bridge.py`, exposed as `npm run options:research:flow-extreme-denominator-dedupe-bridge`, now owns that bridge artifact.

Latest status is `flow_extreme_denominator_dedupe_bridge_ready`. The bridge defines the full ratio/backspread denominator status contract and point-in-time opportunity identity hash fields, validates a bounded fixture, consumes the existing multi-leg side-aware pricing capability, and now consumes `data/profitability-lab/regular-options-base-clean-stack-identity-ledger/latest.json`.

The base ledger is generated by `scripts/build_regular_options_base_clean_stack_identity_ledger.py`, exposed as `npm run options:research:base-clean-stack-identity-ledger`. Current ledger status is `base_clean_stack_identity_ledger_ready`: `157` expected rows, `157` ledger rows, `157` unique identities, `0` duplicate identities, `0` missing identity rows, `0` future/outcome identity dependencies, and `0` protected-holdout overlaps.

Durable decision: strict-new dedupe must fail closed when the base stack lacks row-level identities, but this specific blocker is now cleared for the flow-extreme branch. Fixture identities and base identity hashes are not proof rows, not replay, not forward proof, and not profitability. Current VIX is cleared, and the flow-extreme branch remains parked on `missing_point_in_time_flow_extreme_input`; do not repeat the base-ledger or denominator-bridge slice unless those artifacts become missing, malformed, unsafe, stale, or no longer consumed by readiness.

## 2026-06-23: Clear Ratio/Backspread Pricing Capability Without Counting Fixtures As Proof

GPT-5.5 Pro selected a read-only cross-branch blocker-clear for multi-leg side-aware pricing and denominator capability. `scripts/build_regular_options_multi_leg_side_aware_pricing_capability.py`, exposed as `npm run options:research:multi-leg-side-aware-pricing-capability`, now owns that capability artifact.

Latest status is `multi_leg_side_aware_pricing_capability_available`. The artifact opens `data/options-validation/options_history.db` in read-only mode, resolves one bounded QQQ ratio/backspread fixture from trusted `thetadata_opra_nbbo_1m` bid/ask rows, rejects midpoint/source-mark/EOD/display/manual/last/model/synthetic/lookahead evidence, and marks fixture rows as `fixture_source_not_proof_eligible=true`. It clears only the flow-readiness blocker `missing_side_aware_ratio_backspread_pricing`.

Durable decision: capability fixtures are not historical replay, accepted profitability, forward proof, or promotion evidence. Current VIX is cleared, and the flow-extreme ratio/backspread branch remains blocked by `missing_point_in_time_flow_extreme_input`; full-denominator mapping, strict-new dedupe, and side-aware ratio/backspread pricing are cleared in current readiness dependencies. Do not repeat this pricing slice unless the capability artifact becomes missing, malformed, unsafe, or no longer consumed by readiness.

## 2026-06-23: Park Flow-Extreme Input On Missing Trusted Flow Source Rows

GPT-5.5 Pro selected a read-only point-in-time flow-extreme input materializer as the smallest blocker-clearing slice for `index_flow_extreme_mean_reversion_ratio_backspread_v1`. `scripts/build_regular_options_point_in_time_flow_extreme_input.py`, exposed as `npm run options:research:point-in-time-flow-extreme-input`, now owns that input artifact.

Latest status is `blocked_point_in_time_flow_extreme_input`. The materializer found no trusted local flow source rows at `data/profitability-lab/regular-options-point-in-time-flow-extreme-input/source_rows.jsonl`, `0/24` covered months, `0.0%` date coverage, and blockers `missing_point_in_time_flow_extreme_source`, `missing_required_flow_fields`, `insufficient_month_coverage`, and `insufficient_date_coverage`. It inventories `options_history.db` read-only and confirms `volume` / `open_interest` columns exist, while `bid_size`, `ask_size`, and `quote_depth` do not; plain bid/ask quote availability is explicitly not treated as flow.

Durable decision: do not rerun the flow-extreme readiness audit expecting progress until a trusted point-in-time flow/extreme source exists. The input materializer is not replay, profitability, forward proof, quote import, evidence mutation, scanner/strategy change, live validation, auto-track, broker permission, protected-holdout use, proof-bar change, or promotion. It only makes the missing flow source explicit for the next Oracle pivot.

## 2026-06-23: Park Dispersion Input On Missing Point-In-Time Return Source

GPT-5.5 Pro selected a read-only point-in-time dispersion/concentration proxy input materializer as the smallest blocker-clearing slice for `index_constituent_dispersion_proxy_defined_risk_hybrid_v1`. `scripts/build_regular_options_point_in_time_dispersion_concentration_proxy.py`, exposed as `npm run options:research:point-in-time-dispersion-concentration-proxy`, now owns that input artifact.

Latest status is `blocked_point_in_time_dispersion_concentration_proxy`. The materializer found no trusted local proxy source rows at `data/profitability-lab/regular-options-point-in-time-dispersion-concentration-proxy/source_rows.jsonl`, `0/24` covered months, `0.0%` date coverage, and `feature_store_missing_underlying_return_fields` because `docs/regular-options-feature-store.md` / latest JSON currently prove option-quote coverage, not underlying return rows. Blockers are `missing_point_in_time_dispersion_proxy_source`, `missing_required_return_fields`, `insufficient_month_coverage`, and `insufficient_date_coverage`.

Durable decision: do not rerun the dispersion readiness audit expecting progress until a trusted point-in-time return/proxy source exists. The input materializer is not replay, profitability, forward proof, quote import, evidence mutation, scanner/strategy change, live validation, auto-track, broker permission, protected-holdout use, proof-bar change, or promotion. It only makes the missing input source explicit for the next Oracle pivot.

## 2026-06-23: Park 13-Symbol Historical Depth On Missing Frozen Source Surface

GPT-5.5 Pro selected a read-only frozen 13-symbol candidate-generation source-surface materializer after runner support was proven. `scripts/build_regular_options_13_symbol_frozen_candidate_generation_source_surface.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-source-surface`, now owns that artifact.

Durable decision: the existing broad `59`-symbol candidate-generation source cannot be post-hoc filtered into 13-symbol proof. The real materializer result is `blocked_13_symbol_frozen_candidate_generation_source_surface` with `0/24` proven candidate-generation months, `0` selected rows, and blockers `missing_frozen_13_symbol_candidate_generation_engine`, `source_artifact_universe_not_13_symbol`, `outside_universe_source_rows_present`, `missing_daily_candidate_generation_diagnostics`, and `candidate_generation_months_0_below_requested_24`.

The 13-symbol no-write runner exists, but runner controls are not candidate-surface proof. The downstream 13-symbol audit and historical simulated-forward audit now consume the fail-closed source surface, so the historical-depth branch is parked unless a separately selected/approved source-engine, data-surface, or repair slice changes those blockers. This materializer is not replay, accepted profitability, forward proof, quote import, evidence mutation, scanner or strategy change, live validation, auto-track, broker permission, protected-holdout use, proof-bar change, or promotion.

## 2026-06-23: Separate 13-Symbol Runner Support From Candidate-Surface Proof

GPT-5.5 Pro selected a read-only no-write/as-of/universe-filter runner-support slice for the 13-symbol historical surface. `scripts/run_regular_options_13_symbol_no_write_candidate_generation.py`, exposed as `npm run options:research:13-symbol-no-write-candidate-generation`, now emits a generated support artifact, and `scripts/build_regular_options_13_symbol_candidate_generation_surface_audit.py` validates that artifact before reporting runner status.

Durable decision: valid runner controls clear only `missing_no_write_runner_support`; they do not make the current broad source surface countable. The refreshed 13-symbol audit has runner status `read_only_no_write_runner_available`, but remains `blocked_13_symbol_candidate_generation_surface_audit` because the frozen source-surface materializer now proves `0/24` candidate-generation months from the current broad `59`-symbol source and refuses post-hoc filtering as proof.

This runner-support slice is not replay, profitability proof, forward proof, quote import, evidence mutation, scanner or strategy change, live validation, auto-track, broker permission, protected-holdout use, proof-bar change, or promotion. Future Oracle tasks should not repeat generic runner-support work unless the generated no-write artifact becomes missing, stale, or invalid.

## 2026-06-23: Fail Closed When Oracle Packet Artifacts Are Missing, Stale, Or Unsafe

The same-session GPT-5.5 Pro loop repeated a completed flow-extreme ratio/backspread readiness task because the packet did not make artifact ingestion/freshness failures explicit enough. `scripts/build_options_oracle_profit_loop_packet.py` now validates the flow-extreme replay-readiness artifact before presenting it to Oracle.

Durable decision: the Oracle loop packet must surface completed branch artifacts as named packet-ingestion states, not as silent nulls. For `index_flow_extreme_mean_reversion_ratio_backspread_v1`, the packet now distinguishes missing, malformed, wrong report ID, wrong concept ID, wrong structure, stale timestamp, and unsafe safety-flag artifacts. A clean loaded artifact keeps its real branch status; the current validated status is `blocked_flow_extreme_ratio_backspread_replay_readiness`, with no packet-ingestion reason codes.

This packet hardening is operator clarity and loop-control infrastructure only. It does not replay the branch, clear any blocker, change scanner or strategy logic, import quotes, mutate evidence stores, consume protected holdout, enable live validation, enable auto-track, allow broker orders, lower proof bars, or promote a lane.

## 2026-06-23: Park Flow-Extreme Ratio/Backspread On Readiness Blockers Before Replay

GPT-5.5 Pro selected a read-only replay-readiness audit for `index_flow_extreme_mean_reversion_ratio_backspread_v1`, and that slice is now implemented. `scripts/build_regular_options_flow_extreme_ratio_backspread_replay_readiness.py`, exposed as `npm run options:research:flow-extreme-ratio-backspread-replay-readiness`, owns the readiness artifact.

Latest status is `blocked_flow_extreme_ratio_backspread_replay_readiness`. The preregistered playbook is valid, undefined/naked ratio risk remains forbidden, the SPY/QQQ trusted feature-store quote surface is present, the protected-holdout guard is loaded, proof-boundary labeling exists, and current VIX, side-aware ratio/backspread pricing, full denominator mapping, and strict-new dedupe are cleared. The branch is blocked by `missing_point_in_time_flow_extreme_input`.

Durable decision: do not replay or implement the flow-extreme ratio/backspread branch until those readiness blockers clear in a separate proof-safe slice. Readiness is not profitability, replay, forward proof, live readiness, auto-track permission, broker permission, scanner release, or promotion. The smallest blocker-clearing slice is point-in-time flow/extreme input provenance; it must not invent source rows, import quotes, mutate evidence stores, consume protected holdout, or weaken proof bars.

## 2026-06-23: Park Dispersion-Proxy Hybrid On Readiness Blockers Before Replay

GPT-5.5 Pro selected a read-only replay-readiness audit for `index_constituent_dispersion_proxy_defined_risk_hybrid_v1`, and that slice is now implemented. `scripts/build_regular_options_dispersion_proxy_hybrid_replay_readiness.py`, exposed as `npm run options:research:dispersion-proxy-hybrid-replay-readiness`, owns the readiness artifact.

Current refreshed status is `blocked_dispersion_proxy_hybrid_replay_readiness`. The preregistered playbook is valid, undefined or uncapped pair risk remains forbidden, the trusted feature store covers the index/constituent symbols, CVX source-quality scope is preserved, the protected-holdout guard is loaded, proof-boundary labeling exists, current VIX is cleared, and the playbook's frozen design/formula/denominator contract now satisfies pair construction, side-aware all-leg pair pricing, pair max-loss/collateral convention, full denominator mapping, and strict-new dedupe. The branch is blocked by `missing_dispersion_or_concentration_proxy_inputs`.

Durable decision: do not replay or implement the dispersion-proxy hybrid branch until the point-in-time dispersion/concentration input blocker clears in a separate proof-safe slice. Readiness is not profitability, replay, forward proof, live readiness, auto-track permission, broker permission, scanner release, or promotion. The smallest blocker-clearing slice is point-in-time dispersion/concentration proxy inputs, and it must not invent source rows, import quotes, mutate evidence stores, consume protected holdout, or weaken proof bars.

## 2026-06-23: Do Not Treat 13-Symbol Quote Depth As Candidate-Generation Proof

GPT-5.5 Pro selected the read-only 13-symbol candidate-generation surface audit, and that slice is now implemented. `scripts/build_regular_options_13_symbol_candidate_generation_surface_audit.py`, exposed as `npm run options:research:13-symbol-candidate-generation-surface-audit`, owns the audit artifact.

Latest status is `blocked_13_symbol_candidate_generation_surface_audit`. The trusted 13-symbol feature store covers all `24` requested months from `2024-06` through `2026-05`, and no-write/as-of/universe-filter runner support is now proven, but the frozen source-surface materializer proves `0/24` candidate-generation months from the current source. The existing source artifacts are broad `59`-symbol replays, not a frozen 13-symbol surface, and cannot be post-hoc filtered into proof.

Durable decision: quote-history depth alone does not count a zero-selection month. A future bounded 13-symbol replay or simulated-forward audit is ready only when every requested month has point-in-time candidate-generation coverage or explicit no-pick proof on the frozen 13-symbol universe, CVX source-quality handling remains enforced, protected-holdout overlap is absent, and the frozen source-surface blocker is cleared. This audit does not authorize quote import, evidence mutation, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, protected-holdout use, or promotion.

## 2026-06-23: Do Not Repeat The Macro-Event Long-Strangle Readiness Slice

GPT-5.5 Pro selected the read-only replay-readiness audit for `low_mid_vix_macro_event_long_strangle_v1`, and that slice is now implemented. `scripts/build_regular_options_macro_event_long_strangle_replay_readiness.py`, exposed as `npm run options:research:macro-event-long-strangle-replay-readiness`, owns the readiness artifact.

Current refreshed status is `blocked_macro_event_long_strangle_replay_readiness`: the preregistered playbook is valid, protected-holdout guard is present, and the artifact defines side-aware long-premium entry/exit/expiry formulas, denominator statuses, strict-new identity, and net USD P&L convention. Current VIX is cleared; the active replay blocker is `macro_event_calendar_source_missing`.

That calendar artifact is now implemented by `scripts/build_regular_options_macro_event_calendar.py`, exposed as `npm run options:research:macro-event-calendar`. Latest real status is `blocked_macro_event_calendar_source_missing` because no trusted local source rows exist at `data/profitability-lab/regular-options-macro-event-calendar/source_rows.jsonl`. The builder may validate source rows when they are explicitly supplied, but it must not invent macro-event dates, fetch quote data, or treat event rows as profitability proof.

The point-in-time VIX bucket validator is implemented by `scripts/build_regular_options_point_in_time_vix_bucket.py`, exposed as `npm run options:research:point-in-time-vix-bucket`. This earlier implementation blocker is superseded by the materialized direct VIX source: current status is `point_in_time_vix_bucket_ready` with `505` / `505` requested feature-store dates covered. The validator still must not invent VIX thresholds, import quotes, mutate evidence stores, treat VIX rows as profitability proof, or pass same-day daily-close VIX rows before they are known for candidate entry.

Research-only implementation, replay-readiness, passive audit, and fixture/temp verification work should not stop on an operator-approval question when the user has already said yes to that category. Still forbidden without a separate explicit decision: broker orders, order prep, live validation, auto-track, production scanner/strategy/stop/sizing/proof-bar changes, quote import, protected-holdout consumption, promotion, and unsafe evidence-store mutation.

## 2026-06-21: Run Oracle-Assisted Profitability Goal Loop Against Proof-Qualified Acceptance Criteria

The active profitability objective is to keep the Codex terminal work loop and Oracle GPT-5.5 Pro review loop moving until a regular-options lane reaches defensible profitability or a concrete blocker is proven. Profit is the target, but the acceptance criteria must stay proof-qualified so the loop cannot declare success from stale, midpoint, display-only, historical-backfill, or synthetic marks.

Durable decision: Oracle is allowed to ask any clarifying questions, request more context, pivot prompts, choose the next narrow slice, and challenge the acceptance criteria, but Codex keeps implementation and verification inside the repo's fail-closed proof boundaries. The current success bar is at least `30` post-freeze fresh exact realized P&L rows for a candidate lane, executable bid/ask entry and policy-defined executable exit evidence, positive net USD P&L, profit factor above `1.0` with a 5% bootstrap lower bound above `1.0`, no material regime or winner-damage failure, no unresolved open-risk governor blocker, and no gateboard blocker other than explicit operator approval. Broker orders, live validation, scanner policy changes, stop/sizing edits, proof-bar changes, protected-holdout consumption, and evidence-store mutation remain disallowed unless separately approved.

The first Oracle-selected slice under this goal is `volatility_expansion_observation` paper-shadow evidence collection, not suggested trade `id=138` or any live trade route. As of the 2026-06-21 refresh, that lane is `paper_probation` / `paper_shadow_collect` with PF `1.83`, avg net P&L `+6.74%`, `0` exact realized P&L rows, `0` promotion-ready rows, and no post-`2026-06-14` volatility candidate rows. The June 4/5 rows are pre-freeze diagnostics and cannot count toward the post-freeze profitability acceptance bar.

Oracle follow-up on 2026-06-21 selected closed-market forward-cohort acceptance hardening as the next slice. `scripts/build_volatility_expansion_forward_paper_shadow_report.py` and `scripts/run_options_goal_loop.py` now surface a strict acceptance snapshot requiring post-freeze strict exact completed rows, net USD P&L, executable entry/exit evidence, no lookahead/non-executable marks, no duplicate or unknown-denominator rows, and scanner-hash consistency before any paper-validation review state can be considered. The current goal-loop readback remains `underpowered_forward_evidence` with `post_freeze_strict_exact_completed_rows=0/30`, `promotion_ready=false`, and no live, auto-track, or broker permission.

The forward cohort ledger remains an append-only evidence artifact, not a closed-market planning artifact. `docs/volatility-expansion-forward-paper-shadow-approval-packet.md` is the operator packet for the separate approval question before creating or appending `data/forward-tracking/volatility_expansion_forward_paper_shadow_cohort.jsonl`; approval covers only frozen-policy paper-shadow denominator rows during valid market-data windows and still forbids live trading, auto-track, broker actions, quote imports, historical repair, scanner/strategy/stop/sizing changes, proof-bar changes, protected-holdout consumption, or promotion.

Durable decision: Phase 2 forward paper-shadow candidate rows require real-source provenance before append eligibility. Fixture/test/synthetic rows may be generated for structural tests, but they must be rejected by validation and appender paths with `fixture_rows_not_append_eligible` and must never be written into the real Phase 2 cohort log. A Phase 2 append-ready candidate row must come from `real_market_window_scan_picks`, carry source artifact path/hash, open market-window status, captured timestamp, frozen lane/symbol/policy hash, and exact evidence fields required by its denominator status. This hardening does not change the proof bar or create proof; the current real state remains `0/30` strict post-freeze completed rows.

Durable decision: the Phase 2 passive capture runner is now implemented and verified, so it should not be returned as the next Oracle task unless its code regresses. A real run with the current `scan_picks.jsonl` found `0` same-day Phase 2 natural selections and did not create a candidate JSONL or cohort log; that branch is waiting on real natural selections, not blocked by approval. The VRP put-credit-spread structure harness resolved the read-only structure/math blockers for `low_mid_vix_index_put_credit_spread_vrp_v1` and now consumes the ready point-in-time VIX bucket. The preregistered playbook now freezes deterministic candidate geometry, and the read-only quote-surface proof shows trusted ThetaData coverage is still insufficient for the full SPY/QQQ/IWM/DIA research window (`SPY 16/24`, `QQQ 17/24`, `IWM 17/24`, `DIA 15/24` covered months). The bounded replay gate confirmed no replay should run yet. The remaining VRP replay-gate blocker is now specifically `missing_index_credit_spread_quote_surface`; do not reframe it as missing approval, and do not solve it by quote import, protected-holdout consumption, scanner changes, live validation, broker flow, proof-bar changes, or promotion without a separate explicit decision.

Durable decision: the term-structure calendar/diagonal structure harness is now implemented and verified, so Oracle should not return it as the next task unless it regresses. It resolved the branch's structure-level mechanics without replay or mutation. Current remaining term-structure blockers are `missing_point_in_time_term_structure_inputs` and `missing_index_calendar_quote_surface`; do not solve them by quote import, protected-holdout consumption, scanner changes, live validation, broker flow, proof-bar changes, or promotion without a separate explicit decision.

Durable decision: the term-structure calendar/diagonal bounded replay gate is now implemented and verified, so Oracle should not return it as the next task unless it regresses. The gate consumes the preregistered playbook and structure harness, opens no live/broker path, and does not run replay while source/quote gates are blocked. Current status is `blocked_term_structure_calendar_bounded_replay`, with blockers `missing_point_in_time_term_structure_inputs` and `missing_index_calendar_quote_surface`. This is a falsification/pivot artifact only; it is not profitability proof.

Durable decision: Oracle profitability-loop continuations should reuse the existing ChatGPT browser conversation when available. Use the stored Oracle session chain and `--followup <latest-session-id> --browser-bundle-files --browser-archive never` so GPT-5.5 Pro keeps prior context while still receiving current implementation files. Do not start a fresh Oracle chat for the loop unless the saved conversation cannot be recovered and that failure is recorded.

Durable decision: the market-window/operator blocker does not stop historical falsification work. Historical selected-trade audits may continue while fresh forward collection is closed, but they must not confuse quote-history depth with selected-trade depth. The failed inline Oracle prompt for this pivot was discarded; future Oracle loop pivots should use a file-backed prompt, dry-run bundle inspection, and the existing ChatGPT session follow-up when available. `scripts/build_regular_options_historical_depth_selected_trades.py`, exposed as `npm run options:build:historical-depth-selected-trades`, now owns the selected-trade calendar-depth readback for `2024-06` through `2026-05`; current status is `blocked_historical_depth_selected_trades` because only `8` selected-entry months and `231` selected rows are proven, with `16` requested months unproven and no protected-holdout overlap. `scripts/build_regular_options_historical_simulated_forward_audit.py`, exposed as `npm run options:audit:historical-simulated-forward`, owns the explicit `20` train months plus latest `4` simulated-forward audit question. The current readback is blocked because the feature store has `505` shared trusted intraday quote dates, but the selected-trade source still has only `8` entry months and `231` accepted exact rows after source-quality scope. The available latest-four-month audit is positive on point PF (`1.4249`) but underpowered on bootstrap PF lower bound (`0.99`), so it may guide research but cannot be called proof-qualified profitability. To make the requested split valid, a point-in-time selected-trade generator must emit explicit calendar coverage over the older trusted quote-history window without consuming protected forward holdout, changing scanner policy, importing quotes, mutating evidence stores, lowering proof bars, overwriting the current multilane latest artifact, or promoting a lane.

Durable decision: explicit zero-selection months require candidate-generation proof, not quote coverage alone. `scripts/build_regular_options_point_in_time_selected_trade_depth.py` and `scripts/build_regular_options_point_in_time_candidate_generation.py` are read-only proof surfaces for that boundary. The candidate-generation reconstructor currently reproduces all `231` selected source rows but proves only `8` diagnostic months from the contributing source artifacts, so the missing `2024-06` through `2025-07`, `2026-04`, and `2026-05` months cannot be counted as valid zero-selection months. The replay runners may expose `--min-imported-calendar-dates` for read-only historical replay depth, but the latest `505`-date attempt failed because the current `59`-symbol source replay universe has only `260` selected shared imported quote dates. Do not mark the `20+4` simulated-forward audit profitable unless candidate-generation calendar coverage is proven, forward strict proof clears, or an explicitly approved quote/universe path changes the blocker without weakening proof bars.

Oracle follow-up `profitabil-after-260-date-blocker` accepted that this is a concrete closed-market blocker, not a prompt or implementation gap. Durable decision: stop this historical-depth implementation loop under the current prohibitions. Optional sleeve-runner null-metric error surfacing is operator clarity only and must not be treated as profitability progress. The next action must be an explicit operator choice among trusted quote import/evidence repair for the existing `59`-symbol source replay universe, a preregistered narrower universe/source audit as a new research question, or waiting for future market-window forward evidence. Until such approval or evidence exists, the status remains not accepted profitable.

Oracle follow-up `profitabil-new-lanes-regime` clarified that the historical-depth stop was branch-scoped and does not mean options lane research is exhausted. Durable decision: new lanes may be researched under current market conditions only through preregistered, read-only lane incubation before implementation. `scripts/build_regular_options_current_regime_lane_incubator.py`, exposed as `npm run options:research:current-regime-lane-incubator`, owns that decision surface. The incubator may rank concepts and ask for operator approval to implement exactly one read-only research playbook, but it must keep `accepted_profitability=false`, `new_lanes_are_research_concepts_only=true`, and all live, auto-track, broker, quote import, evidence mutation, scanner-policy release, protected-holdout, proof-bar, and promotion flags false. Current best approval question is a read-only research playbook for `regime_momentum_continuation_debit_spread`; credit-spread income is blocked until credit replay/assignment/margin proof exists, event/catalyst lanes are blocked until event data is point-in-time and no-lookahead, weak-sector lanes need exact coverage/candidate-generation proof, and new volatility-expansion work must not duplicate or promote the existing paper-shadow candidate.

Durable decision: the first approved edge-throughput test for `regime_momentum_continuation_debit_spread` is a read-only falsification surface, not a scanner implementation. `scripts/build_regular_options_current_regime_momentum_edge.py`, exposed as `npm run options:research:current-regime-momentum-edge`, tests current-regime/momentum-compatible all-planned candidates against a `200` exact-row target using the base clean stack plus strict-new de-duplicated rows, while requiring positive point PF, quote coverage, stress PF, and unpriced-row gates. Current result is `raw_count_available_but_not_countable_profitable_edge`: raw combined counts can exceed `200`, but `0` candidates are countable profitable momentum-edge candidates. The positive raw-count tracked-winner rows are blocked by coverage, unpriced, stress, and rolling-watch failures; the clean index refill remains positive but overlaps too heavily and adds only `6` strict-new rows. Therefore the next Oracle loop should request a new causal read-only playbook or falsification path, not raw overlapping aggregation or proof-bar relaxation.

Oracle follow-up in the same GPT-5.5 Pro browser session selected a stricter countable-throughput frontier before any new playbook. Durable decision: `scripts/build_regular_options_countable_throughput_frontier.py`, exposed as `npm run options:research:countable-throughput-frontier`, is the stop-verdict surface for the current historical all-planned artifact universe. It locks the `157`-row clean baseline, `200` exact-row target, and `43` strict-new gap; evaluates all `44` variants; separates raw count from strict-new tranche economics; and blocks base-subsidized, execution-fragile, negative/flat, holdout-thin, stress-fragile, lower-bound-missing, or overlap-only candidates. Latest status is `current_historical_surface_exhausted_under_current_prohibitions`: `11` raw-count candidates exist, but `0` countable throughput candidates pass. Decision counts are `33` `blocked_below_strict_new_count`, `2` `blocked_execution_quality`, and `9` `rejected_negative_or_flat_edge`. The next step now requires separate operator approval for fresh forward paper-shadow collection, scoped source repair/replay, a new causal playbook, or a new historical data surface/longer lookback; continuing to loop raw historical variants without such approval is rejected.

Durable decision: the Oracle/GPT-5.5 loop handoff must now use a loop packet, not a free-form "what next?" prompt. `scripts/build_options_oracle_profit_loop_packet.py`, exposed as `npm run options:oracle-loop:packet`, writes `data/forward-tracking/options_oracle_profit_loop_packet_latest.json` and `docs/research-decisions/options_oracle_profit_loop_packet_latest.md`. The packet requires GPT-5.5 Pro to return whether the loop continues, whether a significant upgrade remains, the selected branch, an exact operator approval question if needed, one Codex task, allowed files/artifacts, forbidden actions, commands, acceptance criteria, and a branch stop condition. The loop should stop only when GPT-5.5 Pro explicitly returns `continue_loop=false` because no significant upgrade remains under current approvals, or when a safety/proof violation is detected. Significant upgrades must materially move proof-qualified profitability, retire a false branch, or open a bounded causal/data/evidence branch; raw overlapping rows, wording-only improvements, exhausted reruns, proof-bar relaxation, and unapproved live/broker/evidence mutation are not significant.

Superseding prompt-hardening decision on 2026-06-22: because the system is still not forward-audit profitable and strict post-freeze proof is `0/30`, GPT-5.5 Pro may not treat stopping as symmetric with continuing. The packet now frames stopping as `verdict=stop_exception`, with a burden-of-proof check. Before stopping, GPT-5.5 must explicitly evaluate fresh forward collection, scoped source repair/replay, new historical data surfaces or longer lookbacks, new causal playbooks, and new option structures beyond the current directional-spread surface. It must consider volatility-risk-premium, skew, term-structure, event-volatility, post-event drift/crush, momentum, mean-reversion, dispersion-like, and liquidity/flow hypotheses, plus structures such as verticals, calendars, diagonals, broken-wing butterflies, ratio/backspreads, straddles/strangles, iron condors/butterflies, synthetic covered-call/PMCC-style diagonals, and debit/credit hybrids. Generic advice such as "collect more data", "try more strategies", "optimize", or "run more backtests" is rejected unless it includes the exact data, lane, structure, date window, command, and pass/fail threshold. The practical target remains at least `30` profitable strict completed rows in the latest approximately four months / post-freeze forward-style audit window with executable exact net P&L, PF lower-bound discipline, no leakage, and no data defect that could flip the result.

The same-session GPT-5.5 browser handoff for the packet stalled twice after selecting a read-only preregistered causal falsification slice. Durable decision: treat that partial selection as sufficient to implement only the read-only falsification surface, not a strategy change. `scripts/build_regular_options_causal_falsification_slice.py`, exposed as `npm run options:research:causal-falsification`, now owns that slice. Latest status is `existing_surface_falsified_new_causal_branch_still_possible`: raw overlapping count aggregation, tracked-winner count retuning without new causal evidence, clean index/IWM refill as the primary gap closer, and existing current-regime momentum-compatible artifact aggregation are stopped. The only non-falsified branch under current approvals is `new_preregistered_causal_playbook`; GPT-5.5 Pro must next select exactly one read-only causal playbook/design task or return `continue_loop=false`. This does not authorize live validation, auto-track, broker orders, quote import, evidence-store mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, or promotion.

GPT-5.5 Pro follow-up `profitabil-causal-next` selected the one remaining read-only causal design task. Durable decision: `scripts/build_regular_options_preregistered_momentum_continuation_playbook.py`, exposed as `npm run options:research:preregistered-momentum-continuation-playbook`, owns the preregistration artifact for `breadth_confirmed_index_qqq_momentum_continuation_debit_spread_v1`. The artifact defines a design-only, defined-risk call debit-spread concept using index/QQQ momentum, breadth confirmation, and low/mid-VIX preconditions over `SPY`, `QQQ`, `IWM`, `DIA`, `AAPL`, `GOOGL`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, and `NEM`. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro follow-up `profitabil-after-preregiste-playbook` stopped the current read-only loop with `continue_loop=false` and `decision=approval_required_before_next_meaningful_step`. Durable decision: the next meaningful step requires explicit operator approval for exactly one research-only implementation slice for `breadth_confirmed_index_qqq_momentum_continuation_debit_spread_v1`, limited to a non-live/non-broker candidate-generation and replay harness that writes only derived research artifacts and evaluates only existing trusted local OPRA/NBBO data. Without that approval, no further significant read-only upgrade remains under the current loop. Still forbidden without separate approval: broker orders, live validation, auto-track, quote import, evidence-store mutation, protected-holdout consumption, production scanner release, stop/sizing/proof-bar changes, and promotion.

GPT-5.5 Pro Extended follow-up under the hardened stop-exception packet returned `verdict=continue` and selected one further read-only causal design: `low_mid_vix_index_put_credit_spread_vrp_v1`. Durable decision: `scripts/build_regular_options_preregistered_vrp_credit_spread_playbook.py`, exposed as `npm run options:research:preregistered-vrp-credit-spread-playbook`, owns the preregistration artifact for this non-directional volatility-risk-premium credit-spread concept. The artifact is design-only and limited to defined-risk put credit verticals over `SPY`, `QQQ`, `IWM`, and `DIA`, with low/mid-VIX and non-crash-trend entry preconditions, side-aware entry credit `short_put_bid - long_put_ask`, side-aware exit debit `short_put_ask - long_put_bid`, full denominator statuses, assignment/expiration/margin requirements, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, concentration, and unresolved credit-spread mechanics. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-vrp-design` continued the same saved ChatGPT conversation after the prior Oracle controller went stale. Durable decision: `scripts/build_regular_options_vrp_credit_spread_replay_readiness.py`, exposed as `npm run options:research:vrp-credit-spread-replay-readiness`, owns the read-only readiness audit before any VRP implementation/replay approval question. Latest status is `blocked_vrp_credit_spread_replay_readiness`: the repo has related vertical/condor bid-ask logic and net-P&L plumbing, but lacks the exact two-leg credit-spread side-aware entry/exit pricing engine, full denominator mapping, assignment/expiration classifier, frozen credit-spread margin/max-loss convention, point-in-time low/mid-VIX bucket, exact SPY/QQQ/IWM/DIA credit-spread quote-surface proof, and protected-holdout guard required by the preregistration. This readiness artifact is not profitability, replay, or implementation approval; it keeps accepted profitability false and forbids live validation, auto-track, broker orders, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, and promotion.

GPT-5.5 Pro Extended follow-up `profitabil-after-vrp-readiness` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_term_structure_calendar_playbook.py`, exposed as `npm run options:research:preregistered-term-structure-calendar-playbook`, owns the preregistration artifact for `low_mid_vix_index_calendar_term_structure_dislocation_v1`. The artifact is design-only and limited to defined-risk calendar/diagonal debit spreads, with `SPY` and `QQQ` as the initial research universe and `IWM`/`DIA` as future extensions only. It freezes low/mid-VIX and non-crash-trend preconditions, fixed front/back expiry spacing, exact OPRA/NBBO multi-leg bid/ask requirements, side-aware entry debit `long_back_month_ask - short_front_month_bid`, side-aware exit value `long_back_month_bid - short_front_month_ask`, front-leg expiry/roll/assignment requirements, full denominator statuses, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, concentration, and unresolved calendar mechanics. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-term-calendar` selected a read-only readiness audit before any calendar/diagonal implementation approval question. Durable decision: `scripts/build_regular_options_term_structure_calendar_replay_readiness.py`, exposed as `npm run options:research:term-structure-calendar-replay-readiness`, owns the readiness audit for `low_mid_vix_index_calendar_term_structure_dislocation_v1`. Latest status is `blocked_term_structure_calendar_replay_readiness`: current code has calendar/diagonal labels, generic expiration/time-exit paths, trusted OPRA/NBBO infrastructure, net USD P&L plumbing, proof labels, and protected-holdout guards, but lacks the dedicated calendar/diagonal side-aware entry pricing engine, exit/expiry engine, full denominator mapping, front-leg assignment/expiration classifier, frozen roll/expiry policy, point-in-time term-structure inputs, exact SPY/QQQ multi-expiry quote-surface proof, and strict-new dedupe required before a future research-only replay harness can be considered. This readiness artifact is not profitability, replay, or implementation approval; it keeps accepted profitability false and forbids live validation, auto-track, broker orders, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, and promotion.

GPT-5.5 Pro Extended follow-up `profitabil-after-term-readiness` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_skew_broken_wing_playbook.py`, exposed as `npm run options:research:preregistered-skew-broken-wing-playbook`, owns the preregistration artifact for `low_mid_vix_index_skew_broken_wing_put_fly_v1`. The artifact is design-only and limited to defined-risk broken-wing put butterflies over `SPY`, `QQQ`, `IWM`, and `DIA`, with sector ETFs and single names as future extensions only. It freezes low/mid-VIX and non-crash-trend preconditions, point-in-time downside-skew or put-wing richness inputs, fixed DTE/strike/width/debit-or-credit rules, exact OPRA/NBBO all-leg bid/ask requirements, side-aware multi-leg entry/exit/expiry formulas, denominator statuses, assignment/expiration/settlement requirements, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, concentration, and unresolved mechanics. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-term-replay-correction` selected the skew broken-wing put-fly structure harness after accepting the term-structure replay gate correction. Durable decision: `scripts/build_regular_options_skew_broken_wing_put_fly_structure_harness.py`, exposed as `npm run options:research:skew-broken-wing-structure-harness`, owns the read-only readiness harness for `low_mid_vix_index_skew_broken_wing_put_fly_v1`. It resolves BWB-specific side-aware entry/exit pricing, credit/debit entry handling, payoff-grid max-loss, geometry validation, assignment/expiration blocking, strict-new identity, denominator statuses, protected-holdout guard, and proof-boundary labeling without replay or mutation. Current VIX is cleared. Latest blockers are `missing_index_broken_wing_quote_surface` and `missing_point_in_time_downside_skew_inputs`. Do not solve those by quote import, protected-holdout consumption, scanner changes, live validation, broker flow, proof-bar changes, or promotion without a separate explicit decision.

GPT-5.5 Pro Extended follow-up `profitabil-after-skew-harness-correction` selected the skew broken-wing put-fly bounded replay gate after accepting the skew harness correction. Durable decision: `scripts/build_regular_options_skew_broken_wing_put_fly_bounded_replay.py`, exposed as `npm run options:research:skew-broken-wing-bounded-replay`, owns the read-only replay gate for `low_mid_vix_index_skew_broken_wing_put_fly_v1`. The gate consumes the preregistered playbook and structure harness, opens no live/broker path, and does not run replay while the harness is blocked. Current VIX is cleared. Latest blockers are `missing_index_broken_wing_quote_surface` and `missing_point_in_time_downside_skew_inputs`. This is a falsification/pivot artifact only; it is not profitability proof.

GPT-5.5 Pro Extended follow-up `profitabil-after-skew-design` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_macro_event_long_strangle_playbook.py`, exposed as `npm run options:research:preregistered-macro-event-long-strangle-playbook`, owns the preregistration artifact for `low_mid_vix_macro_event_long_strangle_v1`. The artifact is design-only and limited to SPY/QQQ long straddles or strangles around preregistered scheduled macro-event categories: FOMC rate decisions, FOMC minutes, CPI, PCE, nonfarm payrolls, and scheduled Fed-chair testimony. It freezes point-in-time event-calendar and low/mid-VIX requirements, future SPY/QQQ-only initial universe with IWM/DIA as future extensions only, fixed entry window, strike/moneyness/DTE/liquidity thresholds, exact OPRA/NBBO all-leg bid/ask requirements, all-long-leg side-aware entry/exit/expiry formulas, denominator statuses, event-calendar leakage controls, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, event/ticker/month/date/winner dependence, unresolved event provenance, and protected-holdout overlap. Status must remain `preregistered_design_only`: no event calendar implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-macro-event-design` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_post_event_iv_crush_iron_condor_playbook.py`, exposed as `npm run options:research:preregistered-post-event-iv-crush-iron-condor-playbook`, owns the preregistration artifact for `post_event_iv_crush_index_iron_condor_v1`. The artifact is design-only and limited to SPY/QQQ defined-risk short iron condors or iron butterflies around preregistered scheduled macro-event categories, with IWM/DIA as future extensions only. It freezes point-in-time event-calendar, VIX bucket, and IV/event-premium proxy requirements, fixed entry/exit/strike/wing/liquidity rules, exact OPRA/NBBO four-leg bid/ask requirements, short-premium side-aware entry/exit/expiry formulas, max-loss/margin conventions, denominator statuses, assignment/expiration/settlement requirements, leakage controls, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, event/ticker/month/date/winner dependence, unresolved event/IV/margin mechanics, and protected-holdout overlap. Status must remain `preregistered_design_only`: no event calendar implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-ivcrush-condor-design` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_flow_extreme_ratio_backspread_playbook.py`, exposed as `npm run options:research:preregistered-flow-extreme-ratio-backspread-playbook`, owns the preregistration artifact for `index_flow_extreme_mean_reversion_ratio_backspread_v1`. The artifact is design-only and limited to SPY/QQQ defined-risk ratio spreads or backspreads around point-in-time index flow or overextension extremes, with IWM/DIA as future extensions only. It freezes point-in-time overextension/flow/VIX requirements, fixed DTE/strike/ratio/cap/liquidity rules, exact OPRA/NBBO all-leg bid/ask requirements, side-aware multi-leg entry/exit/expiry formulas with quantities, defined-risk/collateral/max-loss conventions, denominator statuses including `rejected_undefined_risk`, assignment/expiration/settlement requirements, leakage controls, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, concentration, unresolved mechanics, undefined-risk exposure, and protected-holdout overlap. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, undefined-risk naked ratio spreads, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-flow-ratio-design` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_dispersion_proxy_hybrid_playbook.py`, exposed as `npm run options:research:preregistered-dispersion-proxy-hybrid-playbook`, owns the preregistration artifact for `index_constituent_dispersion_proxy_defined_risk_hybrid_v1`. The artifact is design-only and limited to defined-risk index-versus-constituent debit/credit hybrid pairs, with index legs `SPY`/`QQQ` and constituent legs from the existing proof/import universe. CVX must remain marked as requiring source-quality scope or zero-bid tradability handling before any future replay row can count. It freezes point-in-time dispersion/concentration/VIX inputs, pair universe, allowed variants, pair sizing, fixed DTE/strike/liquidity rules, exact OPRA/NBBO all-leg bid/ask requirements, side-aware pair entry/exit/expiry formulas, pair-level collateral/max-loss conventions, denominator statuses, assignment/expiration/settlement requirements, leakage controls, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, symbol/pair/month/date/winner dependence, unresolved mechanics, undefined-risk exposure, CVX scope, and protected-holdout overlap. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, undefined-risk pair structures, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-dispersion-design` kept the same saved ChatGPT conversation moving and selected one further read-only option-structure design before any stop exception. Durable decision: `scripts/build_regular_options_preregistered_pmcc_diagonal_playbook.py`, exposed as `npm run options:research:preregistered-pmcc-diagonal-playbook`, owns the preregistration artifact for `low_mid_vix_index_pmcc_diagonal_income_v1`. The artifact is design-only and limited to SPY/QQQ PMCC-style call diagonals with IWM/DIA as future extensions only. It freezes point-in-time trend/VIX inputs, long-call and short-call DTE/moneyness rules, max debit/width/liquidity thresholds, exact OPRA/NBBO entry/roll/exit leg requirements, side-aware entry/roll/exit/expiry formulas, short-call assignment/ex-dividend/expiration handling, roll ledger requirements, max-loss/collateral conventions, denominator statuses, leakage controls, and falsification gates for count, coverage, PF lower bound, stress PF, net USD P&L, roll-state/winner dependence, unresolved mechanics, naked short-call risk, and protected-holdout overlap. Status must remain `preregistered_design_only`: no implementation, replay, quote import, evidence mutation, protected-holdout use, scanner/strategy/stop/sizing/proof-bar changes, live validation, auto-track, broker orders, naked or undefined-risk short calls, or promotion is authorized by this design.

GPT-5.5 Pro Extended follow-up `profitabil-after-pmcc-design` kept the same saved ChatGPT conversation moving and selected a read-only implementation-readiness selector across the completed design portfolio. Durable decision: `scripts/build_regular_options_preregistered_playbook_readiness_selector.py`, exposed as `npm run options:research:preregistered-playbook-readiness-selector`, owns the selector artifact. The selector inventories the completed preregistered designs, consumes current readiness gates including the momentum bounded replay, preserves existing blockers across VRP, term-structure, skew, macro, flow, dispersion, PMCC, and momentum branches, and currently reports no blocker-free research implementation candidate. It is not a profit claim and does not authorize implementation, replay, quote import, evidence mutation, protected-holdout consumption, scanner release, live validation, auto-track, broker orders, stop/sizing/proof-bar changes, or promotion without explicit operator approval.

The operator approved that exact research-only implementation/replay harness scope on 2026-06-23, while explicitly instructing the loop not to treat that approval question as a recurring blocker. Durable decision: `scripts/build_regular_options_momentum_continuation_research_replay.py`, exposed as `npm run options:research:momentum-continuation-replay`, owns the approved harness for `breadth_confirmed_index_qqq_momentum_continuation_debit_spread_v1`. The approval covers only derived research artifacts and existing local data inspection for this kind of non-live, non-broker, non-mutating replay work. It still does not authorize live validation, auto-track, broker orders, quote import, evidence-store mutation, protected-holdout consumption, scanner release, stop/sizing/proof-bar changes, or promotion. Latest harness status is `implemented_research_replay_no_proof_qualified_rows`: `1,291` denominator rows, `0` proof-qualified rows, and diagnostic-only old-artifact economics of `net_pnl_usd=-58847.66` and PF `0.7543`. Current VIX is cleared; missing point-in-time breadth/SPY/QQQ momentum inputs and missing explicit side-aware exit bid/ask evidence are proof blockers, not reasons to count old spread marks as profit.

GPT-5.5 Pro follow-up `profitabil-after-momentum-replay-inline` selected one more read-only proof-blocker resolution step before parking momentum. Durable decision: `scripts/build_regular_options_momentum_continuation_proof_blocker_resolution.py`, exposed as `npm run options:research:momentum-continuation-proof-blocker-resolution`, owns that resolver. It may use existing local artifacts and read-only trusted OPRA/NBBO quote lookups to recompute side-aware call-debit-spread economics, but it may not import quotes, mutate evidence stores, append forward rows, change scanner/strategy/stops/sizing/proof bars, consume protected holdout, enable live/auto-track/broker, or promote. Latest result is `momentum_continuation_blocked_missing_local_proof_inputs`: `783` rows resolve side-aware quote pairs and have positive diagnostic recomputed economics (`net_pnl_usd=157441.2`, PF `2.2985`), but `0` rows are strict proof because preregistered breadth/SPY/QQQ momentum confirmations remain missing. Positive side-aware diagnostics cannot override missing preregistered point-in-time inputs or become forward proof.

GPT-5.5 Pro follow-up `profitabil-after-skew-replay-gate` then selected a bounded replay gate for the approved momentum branch. Durable decision: `scripts/build_regular_options_momentum_continuation_bounded_replay.py`, exposed as `npm run options:research:momentum-continuation-bounded-replay`, owns that gate and should consume the prior momentum replay/resolution instead of repeating them. Latest status is `blocked_momentum_continuation_bounded_replay`: strict exact rows remain `0`, current VIX is cleared, and breadth/SPY/QQQ momentum inputs plus quote/stat blockers remain. This branch is parked until a new point-in-time breadth/momentum input surface or explicitly approved data repair changes the blocker; historical side-aware diagnostic profit is not accepted profitability.

Durable decision: the volatility forward report and guarded append path fail closed unless `data/contracts/forward-cohort-preregistration.json` and `data/contracts/volatility-expansion-forward-paper-shadow-cohort-schema.json` both load. All review counts, PF, stressed PF, and continuation gates must come from strict post-freeze USD rows only; percent-only rows are diagnostics. Exact completed rows require trusted entry/exit quote source, timestamps, bid/ask provenance, policy exit condition, and `net_pnl_usd`. The appender may write only the exact cohort ledger path after validation, approval phrase, market-window confirmation, lock acquisition, and post-append verification; it remains forbidden from enabling live trading, auto-track, broker actions, scanner/strategy/stop/sizing edits, quote imports, historical repair, proof-bar changes, protected-holdout consumption, or promotion.

## 2026-06-21: Wire Bullish-Pullback Layer Stack Into Read-Only Paper-Shadow Harness Selection

Oracle follow-up `profitabil-next-slice` selected the B-prime closed-market bullish-pullback layer-stack-to-paper-shadow selector as the next useful no-market-window slice. The selector is implemented as `scripts/build_bullish_pullback_layer_shadow_selection.py`, exposed through `npm run options:plan:bullish-pullback-layer-shadow`, and consumed by the regular-options paper-shadow plan and market-window checklist.

Durable decision: the selector is read-only harness routing, not proof, promotion, or trading permission. It selects `layer_4_clean_exact` / `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timecombo55_50_75_mixed_v1` as the primary future paper-shadow harness, keeps `layer_5_count_expanded` reference-only because its unresolved candidates still block strict completeness, and carries the S/A/B core as a high-PF reference with provenance caveats. Any future row must still have exact entry, policy-defined exact exit, leg-level bid/ask audit, assignment/expiration review, trusted exact OPRA/NBBO provenance, and denominator/failure-row accounting. The selector may not mutate evidence stores, import quotes, create scanners, change strategy, stops, sizing, proof bars, promotion state, live validation, auto-track, broker behavior, or the volatility forward cohort ledger; the active goal loop remains `underpowered_forward_evidence` at `0` / `30` strict post-freeze rows.

## 2026-06-21: Block Bullish-Pullback Harness Until Leg-Level Execution Safety Is Proven

Oracle follow-up `profitabil-next-noattach` accepted the layer-shadow selector and selected the next closed-market slice: a read-only execution-safety preflight for the chosen `layer_4_clean_exact` harness. The implementation is `scripts/build_bullish_pullback_layer_execution_safety_audit.py`, exposed as `npm run options:audit:bullish-pullback-layer-execution-safety`, and consumed by the paper-shadow plan and market-window checklist.

Durable decision: the selected bullish-pullback layer is not operationally ready for future market-window paper-shadow collection unless leg-level bid/ask provenance is visible. The current selected source run has `129` selected rows and preserves the `layer_4_clean_exact` metrics, but the run only exposes spread marks and quote-basis labels: `0` rows with leg-level entry bid/ask, `0` rows with leg-level exit bid/ask, `0` rows with side-aware entry price, and `0` rows with side-aware exit price. Therefore the audit status is `blocked_execution_safety_preflight` and the checklist must carry that blocked precondition. Assignment/expiration classification from parsed OCC legs is useful review evidence, but it does not substitute for executable bid/ask proof. This decision does not authorize quote import, evidence-store mutation, live validation, broker orders, auto-track, scanner/strategy/stop/sizing/proof changes, protected-holdout consumption, volatility cohort append, or promotion.

Oracle follow-up `profitabil-next-after-preflight-real` selected a read-only existing-evidence resolver before any further loop pivot. Durable decision: resolving exact leg quotes from the existing trusted local `options_history.db` is allowed only as read-only audit evidence. The resolver must open SQLite in read-only mode, use trusted `thetadata_opra_nbbo_1m` rows, compute side-aware executable prices from leg bid/ask, and keep source-run spread marks diagnostic unless they match executable side-aware prices. The current resolver confirms all `129` selected rows have parseable OCC legs and trusted entry quotes, but only `126` rows have trusted exit quote pairs, `6` rows have zero/untradable leg quotes, and all `129` rows have side-aware price mismatches versus the historical source marks. The harness therefore remains `blocked_execution_safety_preflight`; the blocker is no longer merely absent source-run fields, it is executable side-aware proof failure on existing trusted evidence.

Oracle follow-up `profitabil-next-after-sideaware-block` selected a read-only executable-economics recompute over the resolver output. Durable decision: historical source-run spread marks are diagnostic only when they mismatch trusted side-aware leg bid/ask prices. The current recompute prices `120` tradable executable rows out of `129`, with `3` missing required quote rows, `6` zero/untradable rows, and `129` source-mark mismatch rows. The tradable-only executable slice is positive (`net_usd_total=45610.0`, PF `3.7414`, bootstrap PF lower bound `2.27`, leave-one-out PF lower bound `3.463`, avg-net lower bound `$235.07`), so `layer_4_clean_exact` is not rejected on recomputed economics. It is still `profitable_but_preflight_blocked` rather than accepted because the full denominator is not executable-clean and the historical source marks cannot be treated as operational fill evidence. This decision does not authorize quote import, evidence mutation, market-window rows, scanner/strategy/stop/sizing/proof changes, broker orders, auto-track, live validation, protected-holdout consumption, forward cohort append, or promotion.

Oracle follow-up `profitabil-next-after-economics` selected a read-only future capture protocol for bullish-pullback `layer_4_clean_exact`. Durable decision: the positive executable-economics result can justify preparing a future paper-shadow evidence path, but not collecting or appending rows yet. `scripts/build_bullish_pullback_layer4_forward_capture_protocol.py` owns the protocol, `scripts/validate_bullish_pullback_layer4_forward_candidate_rows.py` owns no-write candidate validation, and the generated approval packet is informational until a valid market-data window and separate operator approval. The protocol is ready only for future natural scanner selections on the selected layer/variant and allowed symbols, with full denominator logging, OCC leg identity, trusted OPRA/NBBO leg bid/ask entry/exit provenance, policy-defined exit condition, net USD P&L, assignment/expiration classification, and scanner policy provenance. The validator must keep `append_allowed=false` and `cohort_append_performed=false`; any future appender is a separate approval loop. Source marks, midpoint, EOD, display-only, stale, last-trade, manual, synthetic, lookahead, percent-only, missing quote, missing P&L, wrong-lane/layer/symbol, pre-freeze, duplicate, or zero/untradable-as-proof rows are rejected as proof.

Oracle follow-up `profitabil-next-after-protocol` selected a no-write market-window approval preflight before another loop or operator approval ask, then required the preflight to be visible in the operator surface before stopping. Durable decision: `scripts/build_regular_options_market_window_approval_preflight.py`, exposed as `npm run options:preflight:market-window-approval`, is a read-only fail-closed gate over current readbacks, market-window state, optional candidate JSONL validation, and explicit operator approval token. The market-window checklist safe command order must include the executable-economics audit and end with the preflight command, and the paper-shadow plan must load the preflight artifact as an operator-surface source. The preflight can report `blocked_market_closed`, `blocked_market_window_unknown`, `blocked_operator_approval_missing`, `blocked_stale_or_missing_readbacks`, `blocked_gateboard_or_no_chase`, `blocked_candidate_validation_failed`, `ready_for_operator_approval_no_append`, or `candidate_rows_valid_for_future_approval_no_append`, but it must always keep `append_allowed=false` and `cohort_append_performed=false`. The real latest status is `blocked_market_closed`; the valid fixture only reaches review-only candidate validity under simulated market-open plus the exact token. This preflight does not create an appender, cohort log, broker order, live validation, auto-track, quote import, evidence-store mutation, scanner/strategy/stop/sizing/proof change, protected-holdout consumption, or promotion.

## 2026-06-21: Classify Expired Suggested-Trade Review Rows As Historical Resolution Work

The Oracle-assisted profitability loop selected `execute_suggested_trade_review_plan` and suggested trade `id=138` as the first narrow blocker. Local read-only inspection showed the open suggested trade is `AAA260408C00462590`, filled `2026-03-31T09:45:00`, expiry `2026-04-08`, with no stored suggested-trade review. Because the option is already expired as of `2026-06-21`, treating it as next-market-window review work is misleading and not actionable.

Durable decision: the suggested-trade close-risk and suggested-trade review plan readbacks must carry contract symbol, expiry, and fill timestamp when available. Missing-review rows whose expiry is before the report `as_of_date` are classified as `resolve_expired_missing_suggested_trade_review` / `expired_missing_review_requires_historical_resolution`, with `market_window_required=false`. The required evidence is historical exact exit or expiry resolution, stored review snapshot, candidate-outcome ledger rerun, and monthly profitability audit rerun. This is read-only classification and operator routing only; it does not mutate `chat_history.db`, create trades, submit broker orders, auto-close suggested trades, synthesize P&L, change scanner policy, change stops or sizing, lower proof bars, consume protected holdout, or promote any lane.

## 2026-06-14: Adopt Minimal Senior-Dev Coding Style For Agent Work

Agents should prefer the smallest correct change in this repo. Before adding code, check whether the feature needs to exist, whether the standard library, platform/framework, or an installed dependency already solves it, and whether the change can be one line or one small function. New abstractions, dependencies, boilerplate, or extra files require clear justification, and complex requests should have scope challenged before implementation. Intentional shortcuts need a comment that states the ceiling and upgrade path.

This style preference does not weaken security, trust-boundary validation, data-loss prevention, accessibility, explicitly requested requirements, or tests for non-trivial logic.

## 2026-06-14: Add A Historical Walk-Forward Operator Workflow Over Robust Search And Peer Sleeves

The no-wait historical track needed one command that an operator can run and read without remembering the feature-store, robust-search, and all-planned sleeve artifact sequence. It also needed explicit risk readbacks and a fail-closed holdout boundary so a wrapper could not quietly turn historical research into live proof.

Durable decision: `scripts/build_regular_options_historical_walk_forward.py`, exposed as `npm run options:replay:regular-options-walk-forward`, owns the read-only regular-options historical walk-forward operator report at `docs/regular-options-historical-walk-forward.md` and ignored artifacts under `data/profitability-lab/regular-options-historical-walk-forward/`. The workflow refreshes the point-in-time feature-store report, runs the robust historical search evaluator, ingests the latest all-planned sleeve report, records `live_policy_change=false`, emits a ranked repair queue, and fails closed if robust candidates or all-planned `as_of_date` overlap the protected forward holdout, if holdout start/latest candidate entry metadata is missing, or if the all-planned refresh does not test every implemented variant. Robust-search split metrics now include cumulative P&L points, max drawdown points, loss streak, best trade, and worst trade. The latest workflow run is blocked rather than profitable-ready: `231` accepted exact rows, `0` / `3` robust candidates ready, all-planned sleeves loaded with `44` / `44` variants tested as of `2026-06-04`, `0` all-planned run failures, forward holdout guard `passed`, protected holdout starts `2026-06-05`, no holdout overlap, and `promotion_ready=false`. Batch `2147` imported the scoped `17` trusted exact-date repair rows for the four peer coverage-repair variants with `0` duplicates, `0` rejects, `0` lookahead-only rows, and `0` errors; rerun coverage is now `100.0%` on those variants, but none is promotion-ready. Schema `2` now ranks `19` repair rows, `3` high priority, with high-priority work back on robust-candidate source-quality/unpriced repair; the repaired peer variants need stress/risk repair, overlap review, or a new causal hypothesis rather than more quote-coverage import. Rows that would mutate evidence stores explicitly require separate approval and keep protected holdout unused. This workflow may nominate, reject, or refreeze future forward candidates only; it does not create trades, submit broker orders, mutate evidence stores, change scanner policy, change stops or sizing, lower proof bars, consume protected holdout, or turn historical rows into production proof.

## 2026-06-14: Add A Local Runtime Agent Control Plane Separate From Static Memory Navigation

The generated agent memory graph remains a static where-to-read artifact with `runtime_use=false`; treating it as a task ledger would conflate navigation metadata with live coordination state. The CEO/orchestrator flow also needs durable worker tasks, reports, decisions, blockers, and graph memories so larger sprints can recover after compaction or terminal handoff.

Durable decision: `docs/agent-control-plane.md` owns the local CEO/worker control-plane contract, and `scripts/agent_control.py` is the first implementation slice. The CLI writes ignored runtime state under `data/agent-control/` by default: a SQLite WAL ledger plus append-only `events.jsonl` mirror. The model is HydraDB-inspired but local: tenant/sub-tenant scope, separate `memory` and `knowledge` node kinds, typed `source -> relation -> target` graph triplets, task/message/report tables, blocker/evidence/decision/worker-run tables, and deterministic text/metadata query with graph-neighborhood expansion. The repo-wide seed layer is deterministic and local: `seed project` materializes checked startup/living docs, generated static memory-graph nodes, latest gateboard pathways, no-chase blockers, gateboard source artifacts, and visible repo text files from `git ls-files --cached --others --exclude-standard` into runtime graph nodes with content hashes, source-type metadata, tracked/untracked `git_state`, bounded body excerpts, metadata filtering, and prompt-ready query context. Seed refreshes prune prior seed-owned current-state static, gateboard pathway/blocker/source-artifact, and `repo_file_index` nodes before reseeding so removed files or cleared blockers do not remain current query results. `checkpoint write` records the current CEO objective, scope, status, constraints, next actions, files, and verification into `checkpoint:latest`, defaulting autonomy to `read_only_workers`; `checkpoint latest --prompt-only` reads that recovery state without reseeding. `bootstrap` is the default next-context-window handoff command: it runs the seed, returns a digest, and emits the latest checkpoint plus compact gateboard blocker context and suggested follow-up graph queries; `--prompt-only` prints the handoff text without JSON noise. Typed operating memory is the durable accepted layer: `task accept` writes back the latest submitted worker report as `source_type=operating_memory` nodes for accepted `worker_report`, `verification`, `artifact`, and `blocker` context linked to the raw report, task, and accepting decision; verification and artifact memories also get direct task links, while terminal `accepted`, `blocked`, and `cancelled` tasks reject late reports. `graph query` filters typed memory by `--memory-type`, active/inactive state, and freshness; `context pack` emits a pathway-focused prompt bundle that still includes all current fail-closed gateboard blockers; `memory audit` checks stale/expired active memory and supersession target/edge consistency; and `memory eval` verifies checkpoint, current gateboard, repo-file, context-pack, and audit recovery without assuming an open-risk blocker exists. CEO may spawn labelled visible worker terminals when the task is large enough, without per-terminal approval, but workers must still be bounded by pathway, allowed commands, prohibitions, report format, and stop conditions. The control plane is orchestration memory only; accepting a worker report is not proof or release approval, and the control plane does not create or approve trades, submit broker orders, mutate evidence stores, change scanner policy, change stops or sizing, lower proof bars, consume protected holdout, or promote lanes.

## 2026-06-14: Add A No-Wait Historical Robust-Search Track Without Weakening Forward Proof

The forward-cohort preregistration remains the only path to live-validation promotion under the fresh-evidence contract, but waiting for future rows is not the only useful profitability work. The active research track may use paid historical ThetaData OPRA/NBBO data to discover stronger lane candidates now, then freeze and forward-track only candidates that survive chronological out-of-sample checks.

Durable decision: extend the trusted intraday ThetaData history for the regular-options proof set before another broad variant loop. The current 13-symbol proof/import set is `SPY`, `QQQ`, `IWM`, `AAPL`, `GOOGL`, `UNH`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, `NEM`, and `DIA`. A dry-run for `2024-05-22` succeeded against ThetaTerminal v3, and batches `2130` through `2146` imported `2024-05-22` through `2025-05-14` as trusted `thetadata_opra_nbbo_1m` intraday rows: `5,805,236` imported, `0` duplicates, `0` rejects. The shared trusted intraday window for the 13-symbol set is now `505` dates from `2024-05-22` through `2026-06-04`, clearing the 504-date two-year target. The paid-data readiness audit remains not ready after batch `2147` because `CVX` executable quote coverage is `88.66%`, below the `90%` floor. Historical robust-search results may nominate or kill lanes faster, but they do not turn research/backfill rows into forward production proof, do not consume the protected forward holdout, and do not lower the existing promotion bars.

## 2026-06-14: Treat CVX Coverage Failure As Observed Zero-Bid Tradability

The CVX source-quality problem is not a ThetaData subscription blocker and not missing bid/ask data. `docs/regular-options-cvx-executable-coverage.md` shows `495,306` trusted intraday CVX rows across `505` quote dates, with executable quote coverage at `88.66%`; all `56,191` non-executable rows are zero-bid/positive-ask rows, with `0` missing bid/ask rows and `0` crossed quotes. The current multilane source report includes `3` selected CVX historical trades and `1` suppressed CVX duplicate, so affected CVX candidates must be handled by candidate-scoped source-quality exclusion or kill logic. Do not lower the `90%` executable quote floor, do not synthesize bids or midpoint fills, and do not call this a provider-data blocker.

Durable decision: `data/contracts/regular-options-source-quality-scope-policy.json` is the runtime contract for historical robust-search nomination scope. Its active `cvx_zero_bid_tradability_candidate_scope_v1` rule excludes matching `CVX` rows in `bullish_pullback_core` / `bullish_pullback_observation` from historical candidate metrics, and `docs/regular-options-source-quality-scope-policy.md` documents the boundary. This is a candidate-scope kill/exclusion rule only; it does not change scanner policy, broker behavior, forward cohort membership, proof bars, protected-holdout consumption, or the requirement for fresh forward exact realized P&L.

## 2026-06-14: Historical Robust Search Fails Closed Until Split, Regime, Ablation, And Quality Gates Pass

Historical search is useful only if it is treated as a nomination filter, not a profit claim. The repo now has a point-in-time feature-store readback and a split-aware robust-search evaluator for that boundary.

Durable decision: `scripts/build_regular_options_feature_store.py` owns the read-only point-in-time feature-store report at `docs/regular-options-feature-store.md` and ignored JSON/Markdown artifacts under `data/profitability-lab/regular-options-feature-store/`. Feature rows use trusted `option_quote_snapshots` joined to `import_batches` where `snapshot_kind='intraday'`, `source_label='thetadata_opra_nbbo_1m'`, and `data_trust='trusted'`; joins must require `tradable_after_time <= candidate_entry_time`. `scripts/build_regular_options_robust_search_evaluation.py` owns the read-only chronological train/validation/final-holdout report at `docs/regular-options-robust-search-evaluation.md` and ignored artifacts under `data/profitability-lab/regular-options-robust-search-evaluation/`. A historical candidate is not nomination-ready unless it has at least `100` total exact rows, at least `30` validation rows, at least `30` final-holdout rows, final-holdout bootstrap PF lower bound above `1.0`, selection-adjusted PF-LB context, regime robustness, a feature-store gate passing at least `504` shared trusted intraday dates, positive ablation versus baseline after costs, zero material winner-damage findings, and the source quality gate passed. The current result is blocked: `231` exact rows accepted after `3` CVX source-quality scope exclusions, `0` / `3` candidates ready, combined final holdout `28` rows with PF-LB `0.61`, regime robustness passed, feature-store gate passed at `505` / `504` shared dates, but final-holdout sample below `30`, final PF-LB below the selection-adjusted bar, paper-shadow/source-quality blockers, and lane-specific unpriced/zero-bid blockers remain. This does not change scanner policy, broker behavior, proof bars, protected-holdout consumption, or forward-promotion requirements.

## 2026-06-14: Cap Historical Goal Replays By As-Of Date

Historical experiment loops must not let later imported quote calendars or underlying history leak into an earlier as-of replay. `wfo_optimizer.run_historical_backtest` now accepts `as_of_date` and caps imported quote calendars, shared quote dates, SPY replay calendars, and per-symbol histories at that date before building replay results. The bounded bullish-pullback variant runners, all-planned sleeves runner, and goal experiment harness forward `--as-of-date` through to the WFO replay and record the date in output summaries. This is a lookahead-control guardrail only; it does not change scanner policy, proof bars, broker behavior, lane promotion, or protected-holdout rules.

## 2026-06-14: Freeze The Forward Cohort And Park Other Regular Lanes

Phase 1 hardening is complete, so the program now needs forward evidence rather than more retrospective tuning. The next answer must come from a frozen cohort with unchanged promotion bars, fresh exact realized P&L rows after freeze, and no protected-holdout consumption until the registered final evaluation.

Durable decision: `data/contracts/forward-cohort-preregistration.json`, generated by `scripts/generate_forward_cohort_preregistration.py`, is the active Phase 2 preregistration. The frozen regular lanes are `volatility_expansion_observation` and the clean `bullish_pullback_observation` carrier set (`IWM`, `AAPL`, `GOOGL`, `UNH`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, `NEM`). Freeze date is `2026-06-14`; eval date is `2026-07-28` (`30` market days). Promotion still requires the existing bars, plus at least `30` fresh forward exact realized P&L rows per lane after freeze, bootstrap 5% lower-bound PF above `1.0`, regime robustness, and zero winner-damage findings. Kill criteria are mechanical: PF below `1.0` over at least `30` fresh rows, or fewer than `10` fresh rows by eval date as an operational funnel kill requiring fix and refreeze. All other regular lanes are parked outside the frozen cohort with scans, chores, and promotion work disabled by readback until this cohort is evaluated or explicitly refrozen; the separate AI commodity lane remains outside this regular-lane freeze.

## 2026-06-14: Fail Closed On Missing Backend Bridge Token Outside Dev/Test

The FastAPI app exposes direct backend-only `/api/*` surfaces. Treating the Next-to-FastAPI bridge token as optional in normal startup made a missing deployment secret look like an intentional unauthenticated backend.

Durable decision: FastAPI startup requires `OPTIONS_BACKEND_API_TOKEN` unless `OPTIONS_BACKEND_ALLOW_UNAUTHENTICATED=1` is explicitly set for local dev/test. `npm run dev` and `npm run dev:python` are the sanctioned local unauthenticated paths and set the opt-out through `scripts/run_dev_python_backend.js`; non-dev operators should set the bridge token and let `src/lib/backend/transport.ts` forward it as `x-options-backend-token`. This does not merge backend bridge auth with local operator auth, and mutation-intent headers remain audit labels rather than authorization.

## 2026-06-14: Keep Backend Error Details In Logs, Not HTTP 500 Bodies

Raw exception messages in `500` responses can leak local paths, provider details, or implementation state, while Trading Desk mutations still need an auditable operator trail.

Durable decision: FastAPI backend exceptions are logged through stdlib JSON stderr logging and public `500` responses use a generic internal-error message. Request logs include method, path, status, duration, and mutation-intent header/value. Successful Trading Desk create, duplicate-create, review, and close outcomes append compact local audit refs to ignored `data/operator-audit/mutations.jsonl`; that ledger is local operator audit evidence only, not proof, not repository state, and not a production P&L source.

## 2026-06-14: Use Bootstrap Lower Bounds For Autoresearch Promotion Language

Point-estimate profit factor is not enough evidence after many overlapping sleeve variants have been searched. A branch can show PF above the discussion threshold while its trade-level downside resamples still include flat or losing outcomes.

Durable decision: `scripts/evaluate_regular_options_autoresearch.py` keeps the frozen gates, all-or-nothing `score`, and `progress_score` semantics unchanged, but promotion discussion must read the diagnostic bootstrap fields alongside the existing gates. The evaluator, experiment harness summaries, and ledger rows carry deterministic 10,000-draw trade-level net-P&L% bootstrap diagnostics: `pf_point`, `pf_lb_5pct`, `pf_ub_95pct`, `avg_net_lb_5pct`, `n_trades`, and `statistical_confidence`. A branch is `underpowered` when `pf_point >= 1.2` but `pf_lb_5pct < 1.0`, `confident_positive` only when `pf_lb_5pct > 1.0`, and `negative_or_flat` otherwise. These fields are stricter diagnostics only; they do not lower proof bars, change scanner policy, mutate replay artifacts, or promote research/backfill evidence to production proof.

## 2026-06-14: Freeze Per-Ticker Queue Decisions Below 30 Exact Trades

Ticker-level keep/remove calls at one or two trades are selection-luck readbacks, not stable policy evidence. The system may preserve legacy calls for auditability, but new queue emission needs a sample floor and shrinkage.

Durable decision: per-ticker disposition emission in `scripts/build_regular_options_symbol_sleeves.py` and `scripts/audit_bullish_pullback_ticker_evidence.py` must use `n_floor_disposition` and `queue_change_allowed_by_n_floor`. Any ticker with fewer than `30` exact trades is labeled `insufficient_n_frozen` and cannot emit a new keep/remove-style queue change, while legacy `status` and `decision` values remain visible. Per-ticker expectancy diagnostics must flow through `expectancy_calibration.py` shrinkage toward the lane mean before being used in new research readbacks.

## 2026-06-14: Protect Forward Holdout From Ordinary Autoresearch

Forward rows after the repaired intraday proof artifact are decision evidence, not reusable research fuel. Letting ordinary goal experiments consume that range would turn the final answer into another in-sample selection exercise.

Durable decision: `data/contracts/forward-holdout-contract.json`, generated by `scripts/generate_forward_holdout_contract.py` with doc `docs/forward-holdout-contract.md`, protects regular-options autoresearch candidate entry dates starting `2026-06-05`. `scripts/run_regular_options_goal_experiment.py` refuses any ordinary replay window that overlaps the protected range before running variant replay. A one-shot `--champion-final-eval` is allowed for exactly one variant and one inferred strategy family, requires ledger append, and records `holdout_consumption.consumed=true` so the same family cannot consume the holdout again. This does not change scanner policy, broker behavior, proof bars, or promotion gates.

## 2026-06-12: Treat Evidence Stores As Irreplaceable Product Data

The forward evidence stores can be replay-read, but not replay-created for missed calendar days. A single local disk failure or split-brain host could erase or confuse the evidence that decides whether the regular-options funnel is actually closing.

Durable decision: `data/contracts/evidence-host-policy.json` declares one authoritative evidence host, currently `KaesDevice`, for Postgres tracked positions, `chat_history.db`, `data/options-validation/forward_tracking_authoritative.db`, and `data/options-validation/options_history.db`. Non-authoritative hosts are read-only for those stores unless an explicit process-env override is set for a controlled recovery. Scheduled scan runs write host, commit SHA, branch, and run id provenance plus a heartbeat artifact, generated fresh forward-evidence readbacks write host/commit/run provenance in JSON and Markdown artifacts, and `scripts/log_scan_picks.py` fails closed before evidence writes when the current host is not authoritative. `scripts/backup_evidence_stores.py` owns rolling local evidence backups for `chat_history.db`, both `data/options-validation/forward_tracking*.db` ledgers, `data/options-validation/options_history.db`, and Postgres tracked positions under ignored `data/backups/` with `14`-day retention and optional weekly off-machine copy when `OPTIONS_BACKUP_WEEKLY_COPY_DIR` is configured. `scripts/build_project_operator_gateboard.py` and `scripts/build_monthly_all_lanes_profitability_audit.py` surface stale scheduled scans after more than `2` market days.

## 2026-06-12: Keep The FastAPI Backend Single-Process Unless Re-Architected

The backend still has module-level caches, strategy profiles, and repository singletons whose correctness assumes one process. Running multiple uvicorn workers would create divergent in-memory state and confusing evidence readbacks.

Durable decision: `python-backend/main.py` asserts at startup that configured worker count is `1` or absent. It checks `WEB_CONCURRENCY`, `UVICORN_WORKERS`, `OPTIONS_BACKEND_WORKERS`, and common CLI worker flags. Multi-worker startup requires an explicit `OPTIONS_ALLOW_MULTI_WORKER_BACKEND` override and should not be used without first removing or externalizing the single-process state assumptions.

## 2026-06-10: Treat Fresh Exact HOLD Reviews As Resolved Open-Risk Dispositions

Sprint 3 allowed open positions to be dispositioned as close, hold-with-thesis, or mark-as-research. Blocking every negative live-exact row even after a fresh executable exact HOLD review made the open-risk governor impossible to clear without violating the strategy's own stop/target/time-exit rules.

Durable decision: a negative live-exact tracked row does not block the open-risk governor when its latest review is fresh, executable, exact side-aware OPRA/NBBO evidence, recommendation is `HOLD`, `price_trigger_ok=true`, and no close trigger has fired. Stale, missing, non-executable, display-only, or SELL/close-ready rows still block. Buy/cover execution may use an executable ask when bid is zero, while sell-to-close still requires a positive bid, and debit-spread entry still requires a positive short-leg bid. Current readback: QQQ `id=537` is a resolved fresh executable HOLD, SBUX `id=104` is closed from exact side-aware exit evidence, `open_risk_governor_pass`, `open_risk_resolution_plan_clear`, and fresh evidence still waits on legitimate exact realized exit P&L for QQQ or another fresh candidate.

## 2026-06-09: Stop Treating No-Loss Samples As High Profit Factor

Profit-factor readbacks used several sentinels for no-loss samples, including `999.0` and gross-profit divided by tiny denominators. Those values looked like exceptional PF and could satisfy downstream readiness checks even when the sample had no losing trades and therefore no defined profit factor.

Durable decision: no-loss samples report `profit_factor=null` with an explicit `no_loss_sample=true` flag. Profit factor is computed on net USD P&L where available, not summed per-trade percentages, and no null/no-loss PF may be used as a ranking key or claim-readiness pass. This applies across the options-profit gate/flywheel, WFO reports, expectancy calibration, missed-pick audits, risk-budget sizing, chain-native exits, and metric-truth readbacks.

## 2026-06-09: Regular Strategy Direction After Sprint Audit

The sprint audit confirmed that the current broad regular-options shapes are losing in the evidence that matters: baseline missed-pick economics were `206` rows at PF `0.34` and average `-15.28%` in the June 9 audit and refreshed to PF `0.32` / average `-16.54%` after the June 10 open-risk review; the one-contract sizing replay is deeply negative, no lane is promoted, and the fresh executable realized-P&L cohort is still `0` rows. Filter tuning is not the live problem: regular bearish put chain-native relaxation kept current filters at PF `0.00` / average `-27.93%`, and the best relaxed branch reached only PF `0.62`. The only positive historical signal remains `volatility_expansion_observation`, but its positive slice is thin and still lacks a fresh exact realized exit cohort.

Durable decision: stop tuning `regular_bearish_put_primary`-style lanes, Lane A entry/memory/count variants, and broad bad-zero-ticker variants as the next profitability route. Put research weight on collecting fresh executable realized rows and growing the volatility-expansion sample only after exact exit evidence exists. Treat the 2026-05 drift as a regime-survival question, not a filter question, and re-decide after the fresh-evidence loop has at least `20` realized rows or a named market-window defect is cleared.

## 2026-06-09: Add Autoresearch Progress Score Without Relaxing Promotion Gates

The regular-options autoresearch judge had a frozen all-or-nothing `score`: below the full promotion bar, every experiment scored `0.00`, so the goal harness fell back to proxy-heavy ranking. That let raw count and quote-coverage backfill look like progress even when executable-P&L economics did not improve.

Durable decision: `scripts/evaluate_regular_options_autoresearch.py` is versioned to `regular-options-autoresearch-v2` with promotion gates and the all-or-nothing `score` unchanged. It now emits diagnostic `progress_score` from executable-P&L milestones only: promotable clean count after gates, conservative PF, 5%/side stress PF, and zero-bid exit rate. `progress_score` is triage-only and is not a promotion gate. `scripts/run_regular_options_goal_experiment.py` ranks experiment batches by `(score, progress_score)` and no longer ships retired Lane A default variants; operators must pass explicit pre-registered variants.

## 2026-06-09: Track Curated Latest Readbacks, Ignore Timestamped Run Snapshots

The repo already uses checked `data/forward-tracking/*_latest.*` artifacts as curated current-state readbacks that living docs reference. The safe convention is to keep those curated `latest` files trackable when they are named by living docs or generated report docs, while leaving timestamped run snapshots as local evidence churn unless a specific historical record is intentionally promoted into docs.

Durable decision: new monthly/readback builders may update checked `*_latest` JSON/Markdown artifacts when those artifacts are part of the current operator surface, but timestamped `data/forward-tracking/*_YYYYMMDDTHHMMSSZ.{json,md,csv}` snapshots stay untracked by default. Timestamped snapshots can still be archived deliberately, but doing so requires an explicit doc/index reference or a worklog note explaining why that dated artifact is a durable evidence record.

## 2026-06-08: Treat Structure-Specific Replay As Minute-Exit P&L Consumption, Not Promotion

The structure-specific harness previously showed the vertical-spread family but still had `0` true structure P&L rows. After the minute-exit replay consumed trusted ThetaData OPRA/NBBO exact-contract minute quotes, the safe profitability move was to let the structure harness consume those same side-aware rows by exact long/short contract pair, without changing scanner, broker, DB, stop, sizing, contract-selection, proof-bar, or promotion behavior.

Durable decision: `scripts/build_regular_options_structure_specific_harness.py` may count read-only `structure_pnl_rows` only when the source minute-exit replay row has complete trusted intraday OPRA/NBBO bid/ask quotes for the exact long and short contracts at entry and exit, plus side-aware entry debit, exit value, and gross P&L. Current readback is `structure_specific_harness_built_collecting`: `12` vertical-spread true structure P&L rows, decisions `hold_for_current_open_risk_review=1` and `reject_production_use_without_fill_or_position_link=11`, and remaining blocker `single_leg_or_other_multileg_samples_missing`. These rows move executable P&L truth, but they are not production proof, current open-risk resolution, exact realized P&L rows, broker fills, scanner permission, stop/sizing permission, lane promotion, or live activation.

## 2026-06-08: Treat Minute-Exit Replay As Read-Only Exact Quote Consumption, Not Stop Permission

The regular-options monthly command center previously kept minute-exit replay in `blocked_replay_gaps` after exact entry seeds existed but the minute quote coverage and replay consumer were missing. After the ThetaData v3 minute quote imports, the safe gate move was to consume those trusted OPRA/NBBO exact-contract minute quotes into side-aware read-only entry/exit replay, without changing stops, scanner behavior, sizing, broker state, proof bars, or lane promotion.

Durable decision: `scripts/build_regular_options_minute_exit_replay_readiness.py` owns the read-only minute-exit replay evidence at `docs/regular-options-minute-exit-replay-readiness.md` and `data/forward-tracking/regular_options_minute_exit_replay_readiness_latest.json`. It may compute fixed-minute side-aware replay P&L only from trusted intraday exact-contract OPRA/NBBO bid/ask quotes, using ask-side entry value and bid-side exit value for long legs, bid-side entry credit and ask-side exit cost for short legs, and explicit gross no-extra-fees/slippage assumptions. Current readback is `minute_exit_replay_coverage_ready`: `12` true minute-exit P&L rows, `1` position-linked row for QQQ `id=537`, full minute quote coverage, engine status `read_only_side_aware_engine_partial`, and decisions `hold_for_current_open_risk_review=1` plus `reject_production_use_without_fill_or_position_link=11`. `scripts/build_regular_options_minute_exit_quote_import_plan.py` must return `no_minute_exit_quote_seeds_to_plan` once source readiness has full coverage and true rows. This replay may remove the minute execution-realism gap, but it does not resolve current open risk, create broker or trading-row fills, create exact realized P&L rows, alter stop/sizing/scanner/contract-selection policy, lower proof bars, promote lanes, or turn unlinked diagnostic rows into production proof.

## 2026-06-08: Treat ThetaData Connection Refusal As Feed-Down, Not Evidence Exhaustion

The regular-options monthly command center carried execution-alternative quote import commands, but repeated loops treated `127.0.0.1:25503` connection refusal as if quote evidence could not be collected. That sent the loop toward archive churn even though the actual blocker was that ThetaTerminal was not running.

Durable decision: local ThetaData readiness is owned by `docs/thetadata-terminal-runbook.md`. Regular supervised-options quote-import loops must start/probe ThetaTerminal v3 at `http://127.0.0.1:25503` before declaring quote exhaustion or archiving current-source no-match branches. `WinError 10061`, connection refused, or no listener on `25503` is a feed-down state, not evidence. Use the importer dry-run as the readiness probe, not `/v3/system/status`. Do not echo or commit credential contents; use only the credential file path. On 2026-06-08, after starting ThetaTerminal v3, the execution-alternative quote import wrote the planned `8` command groups, generated `9,710` rows, imported `9,500`, and had `0` errors and `0` rejects. Rerunning coverage moved execution-alternative replay coverage to `execution_alternative_replay_coverage_ready` with `0` missing quote demands, `12` true top-spread replay P&L rows, and `12` true contract-replacement P&L rows; the quote import plan moved to `no_quote_demands_to_plan`. This may reduce evidence blockers and queue items, but it must not be counted as production proof, broker fill evidence, scanner permission, contract-selection permission, stop/sizing permission, or lane promotion.

## 2026-06-07: Treat Stale Candidate Archive As Branch Retirement, Not Candidate Reactivation

The monthly command center still carried `wait_for_fresh_match_or_archive_candidate` for `16` fresh-candidate ledger rows already dispositioned as `no_longer_matched`. Because chasing old selections is prohibited without a new executable scanner match, the safe gate-moving action was to record those rows as read-only archived stale branches without mutating candidate, scan, fill, or trading-row stores.

Durable decision: `scripts/build_regular_options_stale_candidate_archive.py` owns the read-only stale candidate archive at `docs/regular-options-stale-candidate-archive.md` and `data/forward-tracking/regular_options_stale_candidate_archive_latest.json`, exposed as `npm run options:audit:stale-candidate-archive`. Current readback is `stale_candidates_archived`: all `16` source wait/archive rows are archived as `archived_no_longer_matched_candidate`, with `0` archive exceptions, `0` production proof-ready rows, and `live_policy_change=false`; lane counts are `quality90_debit55_canary=2`, `swing=9`, `tracked_winner_observation=1`, `tracked_winner_primary=1`, and `volatility_expansion_observation=3`. The monthly command center removes the generic stale wait/archive action once this archive is complete, lowering the next-evidence queue to `14` actions while promotion blockers remain `28`. This archive may retire stale candidate branches from the operator queue, but it must not delete source rows, create trades, submit broker orders, mutate DB state, change scanner policy, change lane promotion, lower proof bars, count stale candidates as production proof, or reactivate no-longer-matched rows without a fresh executable exact OPRA/NBBO scanner match.

## 2026-06-07: Treat Lane Scan Hypothesis Repair As Predeclared Diagnostic Planning, Not Scanner Tuning

The lane outcome replay left a generic `build_or_repair_lane_scan_hypothesis_before_pnl_replay` action for `4` active regular supervised lanes that produced no signal candidates in the monthly window. Because threshold/filter/symbol/expiry/window tuning from tiny zero-signal samples is prohibited, the safe gate-moving step was to inventory only predeclared replacement candidates from existing symbol-sleeve evidence and identify lanes that still lack a causal hypothesis.

Durable decision: `scripts/build_regular_options_lane_scan_hypothesis_repair.py` owns the read-only lane scan hypothesis repair report at `docs/regular-options-lane-scan-hypothesis-repair.md` and `data/forward-tracking/regular_options_lane_scan_hypothesis_repair_latest.json`, exposed as `npm run options:plan:lane-scan-hypothesis-repair`. Current readback is `lane_scan_hypothesis_repair_built_collecting`: `4` no-signal lanes audited, `3` predeclared proof-only replacement candidates found across `2` lanes (`bearish_index_put_observation` and `range_breakout_observation`), `2` lanes still missing a predeclared causal replacement candidate (`bearish_defensive` and `quality90_debit55_canary`), `0` production proof-ready replacement candidates, `0` fresh exact scan retest rows, and `0` true lane outcome P&L rows. The monthly command center removes the generic no-signal build action once this artifact is present, lowering the next-evidence queue to `15` actions while promotion blockers remain `28`. This report may discover proof-only regular-options candidates and queue exact evidence collection or causal hypothesis drafting, but it must not create trades, submit broker orders, mutate DB state, change scanner policy, tune thresholds/symbols/expiries/windows from tiny samples, change contract selection, stops, sizing, lane promotion, or proof thresholds; and it must not count daily/EOD, midpoint, stale, last-trade, display, unresolved, migrated paper, or research/backfill rows as production proof.

## 2026-06-07: Treat Event Data Spine As Annotation Coverage, Not Event P&L Proof

The monthly command center still carried `build_event_data_spine` as the last layer-stack wired data gap after the structure harness was built. Because top open-risk/exact-exit work needed a fresh market window and quote imports still require ThetaData availability, the safe local gate move was to build the event annotation spine without inventing event labels, IV-crush economics, or post-event P&L.

Durable decision: `scripts/build_regular_options_event_data_spine.py` owns the read-only event data spine at `docs/regular-options-event-data-spine.md` and `data/forward-tracking/regular_options_event_data_spine_latest.json`, exposed as `npm run options:replay:event-data-spine`. Current readback is `event_data_spine_built_collecting`: `12` candidate-shown rows over `3` tickers and `4` playbooks; `10` proof-live exact entry rows; `1` paper-fill-recorded row; `0` event-annotated rows; `12` missing event annotations; `0` true event replay P&L rows; and `0` post-event vol-crush replay P&L rows. The all-20 layer stack treats layer 20 as `built_collecting`, and the monthly command center removes generic `build_event_data_spine`, lowering the queue to `16` actions while promotion blockers remain `28`. This spine may inventory event-annotation coverage and queue proof-only collection, but it must not create trades, submit broker orders, mutate DB state, change scanner policy, contract selection, stops, sizing, lane promotion, event thresholds, or proof bars; and it must not count daily/EOD, midpoint, stale, last-trade, display, migrated paper, or research/backfill evidence as production proof.

## 2026-06-07: Treat Structure-Specific Harness As Diagnostic Replay, Not Production Proof

The monthly command center still carried `build_structure_specific_harness` after fill-attempt logs already labeled the current executable candidates as `vertical_spread`. Because open-risk, exact-exit, suggested-trade, and quote-import work needed a fresh market or unavailable ThetaData window, the safe local gate move was to build the structure split without inventing P&L or promoting the vertical slice.

Durable decision: `scripts/build_regular_options_structure_specific_harness.py` owns the read-only structure-specific harness at `docs/regular-options-structure-specific-harness.md` and `data/forward-tracking/regular_options_structure_specific_harness_latest.json`, exposed as `npm run options:replay:structure-specific-harness`. Current readback is `structure_specific_harness_built_collecting`: `12` candidate-shown rows, all `vertical_spread`; `10` proof-live exact entry rows; `1` paper-fill-recorded row; `12` true structure-specific P&L rows after minute-exit replay consumption; and remaining blocker `single_leg_or_other_multileg_samples_missing`. The all-20 layer stack treats layer 19 as `built_collecting`, and the monthly command center removes generic `build_structure_specific_harness`. This harness may separate structure evidence and queue proof-only collection, but it must not create trades, submit broker orders, mutate DB state, change scanner policy, contract selection, stops, sizing, lane promotion, or proof thresholds; and it must not count daily/EOD, midpoint, stale, last-trade, display, migrated paper, or research/backfill evidence as production proof.

## 2026-06-07: Treat Suggested-Trade Review Plan As Review Coordination, Not Close Permission

The monthly command center still carried `refresh_suggested_trade_review` as a generic candidate-ledger bucket after open-risk and fill-attempt blockers were made row-specific. Because today is outside a fresh executable quote window, the safe gate-moving step was to convert the suggested-trade attention row into a precise read-only review plan without closing paper ideas, relying on missing marks, or mutating `chat_history.db`.

Durable decision: `scripts/build_regular_options_suggested_trade_review_plan.py` owns the read-only suggested-trade review plan at `docs/regular-options-suggested-trade-review-plan.md` and `data/forward-tracking/regular_options_suggested_trade_review_plan_latest.json`, exposed as `npm run options:plan:suggested-trade-review`. Current 2026-06-21 readback is `suggested_trade_review_plan_ready_for_historical_resolution`: `1` open suggested-trade attention row (`id=138`, ticker `AAA`, contract `AAA260408C00462590`, expiry `2026-04-08`), `1` missing review row, `0` executable close-ready rows, `0` market-window-required rows, `1` expired review-resolution row, and `live_policy_change=false`. The monthly command center uses this artifact to replace generic `refresh_suggested_trade_review` with `execute_suggested_trade_review_plan`. This plan may coordinate fresh explicit suggested-trade review or historical exact exit/expiry resolution for expired missing-review rows only; it must not create trades, submit broker orders, mutate suggested-trade DB state, auto-close from stale/display/missing review marks, change scanner policy, change stops or sizing, lower exact OPRA/NBBO proof bars, count suggested trades as production proof, or promote paper/research/backfill evidence.

## 2026-06-07: Treat Fill-Attempt Evidence Capture Plan As Logging Coordination, Not Fill Proof

The monthly command center still carried `capture_missing_fill_attempt_evidence` as a generic ledger bucket after open-risk, execution-alternative, and minute-exit blockers were made row-specific. A dry-run probe of the execution-alternative quote import path showed ThetaTerminal was not reachable at `127.0.0.1:25503`, so the safe buildable step was to turn the missing fill-attempt bucket into a precise row plan without inventing fill outcomes or touching trading rows.

Durable decision: `scripts/build_regular_options_fill_attempt_evidence_capture_plan.py` owns the read-only fill-attempt evidence capture plan at `docs/regular-options-fill-attempt-evidence-capture-plan.md` and `data/forward-tracking/regular_options_fill_attempt_evidence_capture_plan_latest.json`, exposed as `npm run options:plan:fill-attempt-evidence-capture`. Current readback is `fill_attempt_evidence_capture_plan_ready_blocked_for_fresh_selection`: `4` plan rows from `2026-06-05`, `4` missing durable fill-attempt rows, `0` ledger-stale already-logged rows, `4` market-window-required rows, lanes `short_term=1`, `swing=2`, and `volatility_expansion_observation=1`, tickers `QQQ=2` and `SPY=2`, and `live_policy_change=false`. The monthly command center uses this artifact to replace generic `capture_missing_fill_attempt_evidence` with `execute_fill_attempt_evidence_capture_plan`. This plan may coordinate fresh selection and durable fill-attempt logging only; it must not create trades, submit broker orders, mutate trading-row DB state, backfill broker fills, change scanner policy, change stops or sizing, lower exact OPRA/NBBO proof bars, or promote fill-attempt evidence.

## 2026-06-07: Treat Minute-Exit Quote Import Plan As Quote Coverage Coordination, Not Stop Or P&L Permission

The monthly command center still had `build_minute_exit_replay` as a generic blocker after minute-exit readiness found exact entry seeds. Because fresh open-risk and exact-exit actions require a market quote window, the safe buildable step was to turn those seeds into concrete trusted OPRA/NBBO minute quote-import commands without inventing stop results or minute P&L.

Durable decision: `scripts/build_regular_options_minute_exit_quote_import_plan.py` owns the read-only minute-exit quote import plan at `docs/regular-options-minute-exit-quote-import-plan.md` and `data/forward-tracking/regular_options_minute_exit_quote_import_plan_latest.json`, exposed as `npm run options:plan:minute-exit-quote-import`. Current readback is `minute_exit_quote_import_plan_ready_engine_blocked`: `24` parsed exact minute quote demands, `0` unparsed demands, `8` grouped ThetaData command groups, `12` source entry seeds, `1` position-linked seed, quote dates `2026-05-21`, `2026-05-29`, `2026-06-04`, and `2026-06-05`, underlyings `IWM`, `QQQ`, and `SPY`, and `0` true minute-exit P&L rows. The monthly command center uses this artifact to replace generic `build_minute_exit_replay` with `run_minute_exit_quote_import_plan_commands`. This plan may coordinate quote evidence collection only; it must not create trades, submit broker orders, mutate trading-row DB state, change scanner policy, change stops or sizing, synthesize minute-exit P&L, lower exact OPRA/NBBO proof bars, or promote quote-import evidence.

## 2026-06-07: Treat Open-Risk Resolution Plan As Review Coordination, Not Close Permission

The monthly command center showed open risk as the top blocker, but the queue repeated it across generic monthly and candidate-ledger actions. The safe next step was to convert the current open-risk readback into row-specific fresh executable review work without creating orders or resolving risk from stale/display-only marks.

Durable decision: `scripts/build_regular_options_open_risk_resolution_plan.py` owns the read-only open-risk resolution plan at `docs/regular-options-open-risk-resolution-plan.md` and `data/forward-tracking/regular_options_open_risk_resolution_plan_latest.json`, exposed as `npm run options:plan:open-risk-resolution`. Current readback is `open_risk_resolution_plan_clear`: `0` plan rows, live-exact negative QQQ `id=537` resolved as a fresh executable HOLD, SBUX `id=104` closed from exact side-aware exit evidence, `5` open regular rows, `5` negative rows, avg open P&L `-54.51%`, median `-57.66%`, and `live_policy_change=false`. The monthly command center drops the generic open-risk actions once this artifact is clear. This plan may coordinate fresh executable review work during quote windows, but it must not create trades, submit broker orders, auto-close display-only marks, mutate trading-row DB state, change scanner policy, change stops or sizing, lower exact OPRA/NBBO proof bars, or promote open-risk evidence.

## 2026-06-06: Treat Execution-Alternative Quote Import Plan As Import Coordination, Not Contract-Selection Permission

The execution-alternative coverage layer proved the blocker was missing trusted OPRA/NBBO quote coverage, not yet a replay-policy choice. The next safe step was to convert the `58` exact quote demands into grouped operator import/query commands without running a write import or changing scanner behavior.

Durable decision: `scripts/build_regular_options_execution_alternative_quote_import_plan.py` owns the read-only execution-alternative quote import plan at `docs/regular-options-execution-alternative-quote-import-plan.md` and `data/forward-tracking/regular_options_execution_alternative_quote_import_plan_latest.json`, exposed as `npm run options:plan:execution-alternative-quote-import`. Current readback is `execution_alternative_quote_import_plan_ready`: `58` parsed exact quote demands, `0` unparsed demands, `8` grouped ThetaData command groups, `33` entry demands, `25` exit demands, quote dates `2026-05-21`, `2026-05-29`, `2026-06-04`, and `2026-06-05`, and underlyings `IWM`, `QQQ`, and `SPY`. The monthly command center uses this artifact to replace generic `build_top_spread_alternative_replay` and `build_contract_replacement_replay` actions with `run_execution_alternative_quote_import_commands`. This plan may emit dry-run/write import command strings for quote evidence collection only, but it must not create trades, submit broker orders, mutate trading-row DB state, change scanner policy, change contract-selection policy, change stops or sizing, lower exact OPRA/NBBO proof bars, or promote replay rows to production proof.

## 2026-06-06: Treat Chain-Native Exit Outcome Replay And Archive As Branch Retirement, Not Policy Permission

The chain-native relaxation replay found selected diagnostic entry spreads after trusted quote import, but entry availability alone did not answer whether the branch was profitable. The next safe step was exact exit replay, followed by an archive if the relaxed branch failed on trusted P&L.

Durable decision: `scripts/build_regular_options_chain_native_exit_outcome_replay.py` owns the read-only exact-exit outcome replay at `docs/regular-options-chain-native-exit-outcome-replay.md` and `data/forward-tracking/regular_options_chain_native_exit_outcome_replay_latest.json`, exposed as `npm run options:replay:chain-native-exit-outcomes`. `scripts/build_regular_options_chain_native_relaxation_archive.py` owns the read-only archive at `docs/regular-options-chain-native-relaxation-archive.md` and `data/forward-tracking/regular_options_chain_native_relaxation_archive_latest.json`, exposed as `npm run options:audit:chain-native-relaxation-archive`. Current readback is `chain_native_exit_outcome_replay_exact_pnl_available_diagnostic_only` plus `negative_chain_native_branches_archived`: all `28` selected chain-native scenario rows have exact trusted exit P&L, `0` exit quote demands remain, current filters are PF `0.00` / avg `-27.93%`, the best relaxed scenario is `widen_dte_window_only` at PF `0.62` / avg `-9.26%`, and all `7` exact-negative diagnostic branches (`1` current and `6` relaxed) are archived. The monthly command center removes the chain-native quote import, exact-exit, negative-branch archive, and stale later-holdout actions once these artifacts are present. These reports do not relax live contract-selection policy; create trades; submit broker orders; mutate DB state; change scanner policy, stops, sizing, lane promotion, or proof thresholds; or promote negative diagnostic replay to paper or live proof.

## 2026-06-07: Treat Repeated Exact-Date No-Match Contracts As Current-Source Exhausted, Not Proof

The repair burndown keeps exhausted provider no-match rows out of the active import loop, but the monthly loop still needed a concrete archive count for contract/date targets that cannot currently produce exact-contract executable evidence. Because the ThetaData quote-import dry run refused `127.0.0.1:25503`, the safe local action was to archive exactly one top exhausted target from existing repair memory.

Durable decision: `scripts/build_regular_options_exhausted_contract_archive.py` owns the read-only exhausted contract archive at `docs/regular-options-exhausted-contract-archive.md` and `data/profitability-lab/regular-options-exhausted-contract-archive/latest.json`, exposed as `npm run options:audit:exhausted-contract-archive`. Current readback is `exhausted_contract_target_archived`: the archive carries forward prior exhausted targets and appends one new target per run from its previous `latest.json`; `GOOGL260102C00355000` and `GOOGL260102C00360000` on `2025-12-22`, `GOOGL260102C00365000` on `2025-12-23`, `GOOGL260213C00350000` on `2026-02-12`, `GOOGL260306C00365000` on `2026-02-27`, and `GOOGL260306C00360000` on `2026-03-02` in `tracked_winner_chain_native_qqq_time80_intraday`, plus `GOOGL260102C00355000` and `GOOGL260102C00360000` on `2025-12-22` in `tracked_winner_cheap_debit_continuity_v1`, are archived as `archived_current_source_exhausted_contract_date` after repeated exact-date no-match attempts, each with `0` exact rows and `0` total rows. The latest archive run was preceded by the ThetaData importer readiness probe, which generated `530` dry-run rows with `0` errors, so the branch retirement was not based on feed-down. This archive may retire selected contract/dates from current-source repair loops, but it must not create trades, submit broker orders, mutate trading rows, change scanner policy, change contract selection, stops, sizing, lane promotion, or proof thresholds; and no-match rows must not count as production proof.

## 2026-06-06: Treat Chain-Native Filter Relaxation Replay As Diagnostic Quote-Coverage Triage, Not Policy Relaxation

The exact-candidate repair narrowed one monthly no-outcome lane to `regular_bearish_put_primary` on `2026-05-22`, but the next question was whether current chain-native entry filters were blocking viable spreads or whether trusted entry contract quote coverage was missing. The safe next step was a frozen, read-only diagnostic replay over predeclared relaxation scenarios.

Durable decision: `scripts/build_regular_options_chain_native_filter_relaxation_replay.py` owns the read-only chain-native filter relaxation replay at `docs/regular-options-chain-native-filter-relaxation-replay.md` and `data/forward-tracking/regular_options_chain_native_filter_relaxation_replay_latest.json`, exposed as `npm run options:replay:chain-native-filter-relaxation`. Current readback is `chain_native_filter_relaxation_replay_candidates_found_diagnostic_only`: after the trusted `2026-05-22` META/DIS/SBUX/COIN put quote import, the frozen `regular_bearish_put_primary` target has `4` signal candidates, `7` scenarios, `28` scenario rows, `4` current selected entry spreads, `24` relaxed selected entry spreads, and `0` remaining entry quote demands. The follow-on exact-exit replay and archive now retire this relaxation branch as exact-priced negative, so the monthly command center no longer carries chain-native quote import, exact-exit, branch-archive, or stale later-holdout actions. This replay may diagnose whether a lane is blocked by quote coverage versus entry filters, but it must not relax live contract-selection policy; create trades; submit broker orders; mutate DB state; change scanner policy, stops, sizing, lane promotion, or proof thresholds; or synthesize P&L from relaxed entry candidates.

## 2026-06-06: Treat Lane Outcome Replay As Coverage Readback, Not Synthetic P&L

The monthly profitability flywheel showed `5` active regular supervised lanes in `needs_replay_engine`, but that label was still too coarse to know whether each lane lacked scan signals, exact chain-native candidates, selected rows, or only priced outcome rows. The next safe step was a read-only lane outcome coverage pass over the all-lanes zero-pick audit and missed-pick outcome evidence.

Durable decision: `scripts/build_regular_options_lane_outcome_replay.py` owns the read-only regular-options lane outcome replay at `docs/regular-options-lane-outcome-replay.md` and `data/forward-tracking/regular_options_lane_outcome_replay_latest.json`, exposed as `npm run options:replay:lane-outcomes`. Current readback is `lane_outcome_replay_built_collecting`: `13` active regular lanes audited, `8` lanes with exact priced monthly outcomes, and `5` missing outcome lanes. Four lanes (`bearish_defensive`, `bearish_index_put_observation`, `quality90_debit55_canary`, and `range_breakout_observation`) had no signal candidates in the monthly window, while `regular_bearish_put_primary` had signal candidates but `0` exact chain-native spread candidates. The monthly command center removes the generic `build_lane_outcome_replay` action once this artifact is present and replaces it with lane-specific `build_or_repair_lane_scan_hypothesis_before_pnl_replay` and `repair_chain_native_exact_candidate_selection` actions. This replay may classify lane outcome coverage and queue exact-candidate or scan-hypothesis repairs, but it must not synthesize P&L for missing lanes; create trades; submit broker orders; mutate DB state; change scanner policy, contract selection, stops, sizing, lane promotion, or proof thresholds; or promote paper/research/backfill evidence.

## 2026-06-06: Treat Exact-Candidate Selection Repair As Targeting, Not Policy Relaxation

The lane outcome replay narrowed one `needs_replay_engine` branch to a specific failure mode: `regular_bearish_put_primary` produced signals but no exact chain-native spread candidates. The safe next step was to turn that into date/ticker/reason targets before testing any diagnostic chain-native filter relaxation.

Durable decision: `scripts/build_regular_options_exact_candidate_selection_repair.py` owns the read-only exact-candidate selection repair report at `docs/regular-options-exact-candidate-selection-repair.md` and `data/forward-tracking/regular_options_exact_candidate_selection_repair_latest.json`, exposed as `npm run options:replay:exact-candidate-repair`. Current readback is `exact_candidate_selection_repair_targets_ready`: `1` target lane/date, `4` signals, `0` exact candidates, signal tickers META/COIN/SBUX/DIS, and `no_chain_native_spread_passed_current_filters=4` for `regular_bearish_put_primary` on `2026-05-22`. The monthly command center removes `repair_chain_native_exact_candidate_selection` once this artifact is present and feeds the target into the chain-native filter relaxation replay, which now shows entry spread candidates are available after quote import but are still non-promotable until exact exit P&L replay and later-date/fresh-paper holdout exist. This report may target read-only relaxation replay, but it must not change scanner policy, contract-selection policy, lane promotion, stops, sizing, broker behavior, DB state, proof thresholds, or synthesize P&L for signal-only rows.

## 2026-06-06: Treat Execution-Alternative Coverage As Quote-Triage, Not Contract-Selection Permission

The monthly profitability flywheel showed that the execution-alternative readiness queue had `12` logged top-spread and contract-replacement seeds, but the command center still could not tell whether the blocker was missing replay code or missing trusted quotes. The next safe step was a read-only exact OPRA/NBBO coverage pass over those logged contract pairs.

Durable decision: `scripts/build_regular_options_execution_alternative_replay_coverage.py` owns the read-only execution-alternative quote-coverage and side-aware replay availability report, generated at `docs/regular-options-execution-alternative-replay-coverage.md` and `data/forward-tracking/regular_options_execution_alternative_replay_coverage_latest.json`, and exposed as `npm run options:replay:execution-alternative-coverage`. Current readback is `blocked_partial_quote_coverage_no_true_replay_pnl`: `12` top-spread candidates, `12` contract-replacement candidates, top/replacement entry quote coverage `missing`, exit quote coverage `partial`, `0` true top-spread P&L rows, `0` true contract-replacement P&L rows, missing quote reasons of `12` entry-long, `12` entry-short, `10` exit-long, and `11` exit-short trusted OPRA/NBBO quotes, and a read-only quote-demand manifest with `58` unique missing quote targets (`33` entry, `25` exit). It feeds the all-20 profitability layer stack and monthly command center, moving top-spread and contract-replacement layers from `built_readiness_blocked` to `built_replay_coverage_blocked` while giving the next import/query pass exact contract, date, and ET-minute targets. It may emit side-aware P&L only when trusted intraday OPRA/NBBO entry and exit bid/ask quotes exist for both long and short legs; it must not synthesize P&L from midpoint, daily/EOD, stale, display, or manual marks; change contract selection; create trades; submit broker orders; mutate DB state; change scanner policy; change stops or sizing; lower proof bars; or promote replay rows to production proof.

## 2026-06-06: Treat Execution-Alternative Replay Readiness As Evidence Plumbing, Not Contract-Selection Permission

The monthly profitability flywheel showed that fill-attempt logs already preserve selected spreads, top alternatives, quote timestamps, and fill-degradation fields, but the contract-replacement/top-spread lanes were still generic replay gaps. The next safe step was to turn those logged alternatives into a concrete seed queue without inventing alternative P&L.

Durable decision: `scripts/build_regular_options_execution_alternative_replay_readiness.py` owns the read-only execution-alternative readiness queue, generated at `docs/regular-options-execution-alternative-replay-readiness.md` and `data/forward-tracking/regular_options_execution_alternative_replay_readiness_latest.json`, and exposed as `npm run options:replay:execution-alternatives`. Current readback has `12` top-spread replay seeds, `12` contract-replacement seeds, `0` true top-spread/contract-replacement P&L rows, missing alternative exit quote coverage, missing liquidity-first replay engine, and missing contract-replacement replay engine. It feeds the all-20 profitability layer stack and monthly command center, moving top-spread and contract-replacement layers from generic wired replay gaps to `built_readiness_blocked`. It may identify selected/alternative contract seed pairs for future exact OPRA/NBBO replay, but it must not synthesize P&L from midpoint, daily/EOD, stale, display, or manual marks; change contract selection; create trades; submit broker orders; mutate DB state; change scanner policy; change stops or sizing; lower proof bars; or promote readiness rows to production proof.

## 2026-06-06: Carry Quote Evidence Classes Through Research Readbacks

The historical-data audit found that the active quote store can contain multiple evidence qualities at once: trusted intraday OPRA/NBBO, trusted daily/EOD, research EOD, synthetic/test rows, and unknown sources. Source path alone is therefore not enough; every audit/report surface that could be used for lane, data, or profitability decisions needs both a quote-source class and a row-use class.

Durable decision: `scripts/quote_evidence_readback.py` is the shared report helper for quote evidence readbacks outside the Trading Desk API. Read-only audit reports that inventory `options_history.db`, missed selected-pick outcomes, missed-pick failure modes, missed-pick filter matrices, all-lanes zero-pick coverage, paid-data readiness, or Lane Lab planning must carry `quote_evidence_class` from the proof contract and must separately label research/backfill row policy with `production_proof=false`. Trusted intraday OPRA/NBBO quote evidence can be production-proof-source eligible, but historical missed-pick rows, zero-pick audit rows, lane-lab rows, migrated paper rows, daily/EOD rows, and counterfactual filter rows remain research/backfill or legacy/planning readbacks unless the row itself clears the live scanner production-proof gates. `scripts/audit_options_data_store.py` refuses the root `options_history.db` placeholder and audits the active `data/options-validation/options_history.db` read-only. This decision does not create trades, mutate stores, lower proof bars, or turn research/backfill evidence into production proof.

## 2026-06-06: Use The Monthly All-Lanes Profitability Audit As The Iteration Command Center

The June 6 profitability debate showed that the next practical monthly loop is not another live policy tweak. The repo already has many profitability, filter, promotion, execution, risk, and repair readbacks, but they were not gathered into one monthly P&L-style operator surface.

Durable decision: `scripts/build_monthly_all_lanes_profitability_audit.py` owns the monthly all-lanes regular-options profitability command center, with generated artifacts at `docs/monthly-all-lanes-profitability-audit.md` and `data/forward-tracking/monthly_all_lanes_profitability_audit_latest.json`. Use `npm run options:audit:monthly-profitability` after the gateboard and all-20 profitability layer stack when answering "where are we profitable?", "what lanes are more negative?", or "what should we iterate next monthly?" Current status is `profitability_iteration_ready_blocked_for_promotion`: baseline missed-pick PF `0.32`, avg `-16.54%`, recent month `2026-05` `paper_only_recent_break`, execution realism `ready`, open-risk governor `open_risk_governor_pass`, `0` exact realized P&L rows, `0` paper-candidate rules, `10` rejected/overfit rules archived, `0` unarchived rejected rules, `24` promotion blockers, and oracle ceiling unavailable until trusted MFE/MAE replay exists. The audit classifies all active regular supervised lanes into read-only dispositions from the allowed set `profitable_candidate`, `paper_shadow`, `retest`, `needs_replay_engine`, `quarantine`, and `archive`; latest counts are `1` paper-shadow lane, `4` quarantine lanes, `3` retest lanes, `5` needs-replay-engine lanes, and `0` profitable-candidate lanes. The lane quarantine archive records all `4` quarantine lanes, leaves `0` unarchived quarantine lanes, the risk-budget sizing replay is `sizing_replay_built_collect_fresh_exact_evidence` with `paper_shadow_only` as the best research scenario (`+$971.30`, PF `1.83`), the lane outcome replay moves the generic no-economics lane blocker into `built_collecting` with `8` priced outcome lanes, `4` no-signal lanes, and `1` signal-without-exact-candidates lane, exact-candidate selection repair moves that branch into a frozen target, chain-native exit outcome replay proves the relaxation branch negative, and chain-native relaxation archive records all `7` exact-negative diagnostic scenarios as archived dead ends. The next-evidence queue now has `10` actions after open-risk cleared and stale/quote-import/replay build actions were replaced with precise evidence work. This audit may rank lanes, reject overfit filters, expose replay gaps, classify lane dispositions, replay research sizing, classify lane outcome coverage, and queue next evidence actions, but it must not create trades, submit broker orders, mutate DB state, change scanner policy, change stops, change sizing, lower exact OPRA/NBBO proof bars, or promote paper/research/backfill rows into production proof.

## 2026-06-06: Archive Quarantined Lanes Instead Of Reprocessing Bad Branches

The monthly profitability flywheel classified four regular supervised lanes as `quarantine` after their broad missed-pick economics stayed negative and promotion gates remained blocked. Leaving `record_lane_quarantine_disposition` in the monthly queue would keep sending operator time back to already-decided bad branches instead of open risk, exact evidence, paper-shadow proof, retests, or replay engines.

Durable decision: `scripts/build_regular_options_lane_quarantine_archive.py` owns a read-only lane quarantine archive at `docs/regular-options-lane-quarantine-archive.md` and `data/forward-tracking/regular_options_lane_quarantine_archive_latest.json`, exposed as `npm run options:audit:lane-quarantine-archive`. Current readback is `lane_quarantines_archived`: `4` / `4` quarantine lanes archived (`bullish_momentum`, `bullish_pullback_observation`, `short_term`, and `swing`), `0` unarchived quarantine lanes, and `live_policy_change=false`. The monthly command center still displays quarantined lanes for evidence, but removes `record_lane_quarantine_disposition` from the next-evidence queue once the archive proves the bad lane branches are recorded. This archive does not delete lane rows, disable lanes, change scanner policy, change lane promotion, create trades, submit broker orders, mutate DB state, lower proof bars, or promote quarantined lanes.

## 2026-06-06: Treat Risk-Budget Sizing Replay As Research Evidence, Not Size Permission

The monthly profitability flywheel still had `risk_budget_sizing_replay_missing`, which meant the command center could say broad one-contract economics were bad but could not show what P&L would have looked like under simple no-quarantine or paper-shadow-only research budgets. That made sizing an uninspected blocker rather than a measurable one.

Durable decision: `scripts/build_regular_options_risk_budget_sizing_replay.py` owns a read-only sizing replay at `docs/regular-options-risk-budget-sizing-replay.md` and `data/forward-tracking/regular_options_risk_budget_sizing_replay_latest.json`, exposed as `npm run options:replay:risk-budget-sizing`. Current readback is `sizing_replay_built_collect_fresh_exact_evidence`: the baseline one-contract untracked set has `206` rows, net `-$18,755.00`, and PF `0.32`; `quarantine_zero_weight` improves to `-$1,387.15` but stays negative; `paper_shadow_only` is `+$971.30` at PF `1.83`; and `tiered_shadow_full_retest_quarter` is `+$381.69` at PF `1.17`. The all-20 layer stack treats this layer as `built_collecting`, not ready: the rows are historical research/backfill rather than production sizing proof, fresh exact realized sizing evidence is required, and any sizing change needs a separate promotion gate. This replay does not change live size tiers, create trades, submit broker orders, mutate DB state, change scanner policy, lower proof bars, or promote research/backfill sizing evidence.

## 2026-06-06: Archive Rejected Overfit Rules Instead Of Relitigating Them

The monthly profitability flywheel showed that all ten candidate filter scenarios were rejected for overfit, thin holdout, failed later-date validation, non-entry-time features, or winner damage. Leaving `archive_overfit_rule` in the monthly queue after that decision made the operator loop keep reprocessing a branch that had already failed.

Durable decision: `scripts/build_regular_options_overfit_rule_archive.py` owns a read-only rejected-rule archive at `docs/regular-options-overfit-rule-archive.md` and `data/forward-tracking/regular_options_overfit_rule_archive_latest.json`, exposed as `npm run options:audit:overfit-rule-archive`. Current readback is `overfit_rules_archived`: `10` / `10` rejected rules archived, `0` unarchived rejected rules, `0` paper-candidate rules, and `live_policy_change=false`. The monthly command center still displays archived rejected rules for evidence, but removes `archive_overfit_rule` from the next-evidence queue once the archive proves the rejected rules are retired. This archive does not delete filter-matrix scenarios, change scanner policy, change lane promotion, create trades, submit broker orders, mutate DB state, lower proof bars, or promote rejected rules.

## 2026-06-06: Treat The All-20 Profitability Stack As A Readback Control Plane

The June 6 layered profitability debate produced twenty next iteration layers, but those layers have different maturity states: some are built operator readbacks, some are collecting fresh exact paper evidence, and some deliberately expose replay or event-data gaps.

Durable decision: `scripts/build_regular_options_profitability_layer_stack.py` owns the all-20 regular-options profitability iteration stack, with generated artifacts at `docs/regular-options-profitability-layer-stack.md` and `data/forward-tracking/regular_options_profitability_layer_stack_latest.json`. Use `npm run options:profitability-layer-stack` after the gateboard when answering "what next for profitability?" Current status is `all_20_layers_wired_live_blocked_collect_evidence`: `20` / `20` layers wired, `4` ready, `5` collecting, `11` blocked, implementation counts of `5` built, `4` built-blocked, `6` built-collecting, `1` built-readiness blocked, `2` built-replay-coverage blocked, `1` wired replay gap, and `1` wired data gap; the anti-overfit layer records `10` archived rejected rules and `0` unarchived rejected rules, and the risk-budget sizing layer is now built-collecting while open risk and fresh exact sizing evidence remain blockers. This stack may rank and explain the next work, but it must not create trades, submit broker orders, change scanner policy, change stops, change sizing, mutate DB state, lower exact OPRA/NBBO proof bars, or promote paper/research/backfill rows into production proof.

## 2026-06-06: Treat Minute-Exit Replay Readiness As Evidence Plumbing, Not Stop Permission

The first monthly profitability goal-loop run could not safely resolve open-risk or collect fresh exact exit rows because it was Saturday, June 6, 2026, and those actions require a fresh executable quote window. The next buildable command-center blocker was `build_minute_exit_replay`, because daily close-check stop grids are not minute-level stop/exit proof.

Durable decision: `scripts/build_regular_options_minute_exit_replay_readiness.py` owns the read-only minute-exit replay readiness queue, generated at `docs/regular-options-minute-exit-replay-readiness.md` and `data/forward-tracking/regular_options_minute_exit_replay_readiness_latest.json`, and exposed as `npm run options:replay:minute-exit-readiness`. Current readback has `12` exact OPRA entry seeds, `1` position-linked seed, `0` true minute-exit P&L rows, missing minute quote coverage, and missing minute exit replay engine. It may identify contract/time/entry-price seeds for a future exact OPRA/NBBO minute replay, but it must not synthesize P&L from daily, midpoint, stale, display, or manual marks; change stops; create trades; submit broker orders; mutate DB state; change scanner policy; change sizing; lower proof bars; or promote readiness rows to production proof.

## 2026-06-06: Scanner-Origin Creation Must Pass Fresh Promotion Gates

The June 6 subagent audit selected the first sprint as a no-live-leak entry boundary: the live scanner, scheduled scan runner, suggested-trade create path, tracked-position create path, and optional broker-paper handoff must all share the same promotion-gated semantics.

Durable decision: scanner-origin creates and scheduled auto-track are not allowed to rely only on archived lineage, market-open state, portfolio caps, guardrails, `creation_eligible`, or exact-contract proof labels. They must also pass a fresh usable lane profitability report and a fresh usable lane promotion-state report. Missing, stale, malformed, paper-probation, diagnostic-only, or otherwise blocked lane gate artifacts fail closed as creation blockers. Scheduled scan wrappers must enable portfolio-cap enforcement and `OPTIONS_ENFORCE_LANE_PROFITABILITY_GATE=1`; backend scanner-origin create validation must rerun current guardrails and then consult the lane profitability and promotion-state artifacts before building a proof-required payload. The current readback remains intentionally blocked: `0` live-validation lanes and `0` auto-track lanes. This decision does not lower proof bars, submit live broker orders, auto-close existing positions, or promote paper/research/backfill evidence to production proof.

## 2026-06-06: Use Open-Risk And Paper-Evidence Governors Before Chasing Entries

The remaining June 6 sprint work found the next fail-open risk: even with lane promotion gates, unresolved live-exact open risk and legacy pre-promotion rows could be misread as permission to chase the volatility lane.

Durable decision: regular scanner-origin entries must also pass the open-risk governor emitted by `scripts/audit_regular_open_position_risk.py` and consumed through `scripts/regular_open_risk_governor.py`. A stale, missing, malformed, or blocked open-risk report is a hard creation blocker for scheduled auto-track and backend scanner-origin tracked/suggested creates. Paper/probation lanes use `pending_paper_exact_evidence` to collect fresh exact OPRA/NBBO paper evidence, not `pending_live_validation`; paper exact evidence statuses cannot submit broker orders or create scanner-origin rows. `scripts/build_regular_options_fresh_evidence_loop.py` marks legacy pre-promotion rows explicitly, and `scripts/lane_promotion_state.py` excludes those legacy rows from promotion-ready fresh-paper counts. `scripts/build_volatility_probation_reconciliation.py` owns the volatility-specific reconciliation: current readback is `paper_probation_blocked`, with `6` legacy pre-promotion volatility rows, `0` current paper exact pending rows, open-risk governor `open_risk_governor_pass`, and QQQ `id=537` still negative but resolved by fresh executable HOLD. The gateboard no-chase manifest is the operator-facing summary. This decision does not auto-close open positions, alter stops, lower proof bars, or convert legacy/research/backfill rows into production proof.

## 2026-06-05: Use The Six-Pathway Operating Map And Gateboard For Project Orientation

The June 5 follow-up discussion showed that even after data repair and promotion gating, the project was too hard to hold in one mental model. Data quality, candidate lifecycle, proof evidence, profitability, lane promotion, and operator visibility were all valid concerns, but they were scattered across many artifacts.

Durable decision: project orientation is organized through the generated six-pathway model in `docs/project-operating-map.md` and `data/contracts/project-pathway-registry.json`, owned by `scripts/generate_project_pathway_registry.py`. The pathways are Data, Candidate, Evidence, Profitability, Promotion, and Operator. The current state readback is `docs/project-operator-gateboard.md` and `data/forward-tracking/project_operator_gateboard_latest.json`, built by `scripts/build_project_operator_gateboard.py`. Use `npm run options:gateboard` before answering whether the current blocker is bad data, missing candidates, insufficient proof, negative economics, promotion safety, or operator readiness. Current gateboard status is `safe_blocked_no_live_release`: Data and Candidate pass, while Evidence, Profitability, Promotion, and Operator remain blocked. These artifacts are navigation/readback surfaces only; they do not create trades, submit broker orders, change scanner policy, change lane promotion policy, lower proof bars, change stop policy, or turn paper/research/backfill rows into production proof.

## 2026-06-05: Centralize Candidate Lifecycle Statuses And Dispositions

The June 5 follow-up audit found a concrete lifecycle drift bug: `paper_validation_only_lane_promotion_state` was mapped to `paper_only` in one helper but was missing from the validation disposition report allowlist, so a valid blocker could disappear from the operator readback.

Durable decision: regular-options pending-candidate statuses and validation outcomes are centralized in `scripts/candidate_lifecycle.py`, with generated artifacts at `data/contracts/candidate-lifecycle-contract.json`, `docs/candidate-lifecycle-contract.md`, and `src/lib/generated/candidateLifecycleContract.ts`. Profitability gates, promotion-state gates, all-lanes queueing, pending validation, validation disposition reporting, and the fresh-evidence loop must consume the shared status/outcome sets instead of hardcoding local allowlists. Adding a new paper-only, diagnostic, pending, or validation-attempted status requires updating the lifecycle contract and its generated artifacts, and `npm run verify:docs` checks them for drift. This contract is vocabulary/governance only; it does not create trades, change broker behavior, lower proof bars, change lane promotion policy, or turn research/backfill rows into production proof.

## 2026-06-05: Use A Universal Lane Promotion State Before Live Validation

The June 5 missed-pick repair showed that fixing data integrity and lane profitability gates was necessary but not sufficient: a historically profitable pocket can still be too thin, too recent, too exposed, or too dependent on unresolved open risk to be treated as a live-validation equal.

Durable decision: regular supervised lanes move through `diagnostic -> paper_probation -> live_validation -> auto_track`, and `scripts/lane_promotion_state.py` owns the shared generated state artifact. A lane may enter paper/probation only after the lane profitability report is clean and the lane itself is profitable enough; a lane may enter live validation only after entry-time-only rules, sufficient later-date/walk-forward depth, sufficient fresh exact realized paper rows, no unresolved live-exact negative open-risk rows, no lane-specific recent-cohort circuit breaker, and duplicate-spread suppression are all clean. Auto-track remains reserved for a later explicit release review after live-validation gates pass. The daily all-lanes queue, pending validator, and scanner validation path must consult the promotion state after lane profitability passes and fail closed if the state report is missing or stale. Current readback: `13` diagnostic lanes, `1` paper/probation lane (`volatility_expansion_observation`), `0` live-validation lanes, `0` auto-track lanes, `1` current live-exact negative open row, and `open_risk_governor_pass` because that row is resolved by fresh executable HOLD. This artifact is a routing/safety gate; it does not create trades, change broker behavior, lower proof bars, change stop policy, or turn research/backfill rows into production proof.

## 2026-06-05: Make Repository Data Integrity A Strict Project Gate

The June 5 data audit found that the OPRA/NBBO quote blocker for the missed-pick audit was repaired, but the Trading Desk repositories still had integrity risk: one closed SQLite suggested-trade row lacked stored exit/P&L fields, closed historical research/backfill tracked rows still had missing realized P&L when no exact exit leg quote existed, and the repository constraint CLI could look clean when `.env.local` was not loaded and Postgres was skipped.

Durable decision: `npm run options:audit:data-integrity` is the strict Trading Desk repository data-integrity gate. The audit must load local env before resolving `DATABASE_URL`, fail strict on closed production or unclassified tracked rows with missing realized P&L, fail strict on closed suggested trades with missing stored exit/P&L, and report research/backfill missing-realized rows only as quarantined diagnostics when the missing value is caused by absent trusted exact exit-leg OPRA/NBBO evidence. Research/backfill diagnostics are not production proof, broker fills, live exposure, or daily/weekly realized P&L for scanner caps. `supervised_scan.load_open_position_context()` must ignore explicitly identified research/backfill rows in live exposure and realized-loss math while reporting the ignored counts. Repairs may populate missing historical P&L only from trusted executable exact-contract spread quotes; do not synthesize from midpoint, last trade, stale, daily/EOD, or display marks. Exact `bid=0, ask>0` OPRA/NBBO rows are valid loss evidence for historical close/mark paths and must be retained side-aware rather than treated as absent; they still do not count as clean live entry/liquidity coverage. Current strict readback is `pass_or_skipped`: SQLite suggested trades `pass`, Postgres tracked positions `pass`, hard violations `0`, diagnostics `0`.

## 2026-06-05: Treat Passed Lane Gates As Paper Probation Until Fresh Profit Evidence Matures

The May 22 through June 5 audit proved that clean data alone did not make the all-lanes selected set tradable: the untracked set is currently PF `0.32` and avg `-16.54%`. The follow-up filter matrix showed that the current lane gate plus volatility self-guardrails isolates a profitable historical pocket (`10` rows, PF `69.14`, avg `+34.87%`, later-date split pass), but it also blocks `63` historical winners and has too little fresh forward evidence to justify live promotion.

Durable decision: a passed lane profitability gate is a paper/probation state, not live-validation or auto-track permission. `candidate_gate_decision(..., probation_paper_only=True)` routes otherwise passed candidates to `paper_validation_only_lane_profitability_probation`, and scanner validation reruns use that mode when `OPTIONS_ENFORCE_LANE_PROFITABILITY_GATE=1`. Gate health now fails closed not only on missing/malformed/future/stale reports, but also on unpriced audit rows, incomplete tracked P&L, or missing lane rows. Active lane self-guardrails fail closed when required debit metrics are missing. Exact duplicate spreads selected by multiple lanes are suppressed to a single deterministic risk owner, with extra candidates recorded as `paper_validation_only_duplicate_exact_spread`. Debit, DTE, ticker-cluster, and extended ticker filters remain diagnostic/paper-shadow candidates until fresh entry-time-only OOS evidence clears the promotion bar; the extended ticker exclusion is explicitly overfit-warning territory.

## 2026-06-05: Portfolio Cap Breaches Are Auto-Track Blockers

The June 5 live exact QQQ auto-track showed that lane profitability alone was not enough: the source pick already carried breached exposure/drawdown/cost caps, but those breaches were stored as cautions, leaving `creation_blockers=[]` and allowing a live exact tracked row.

Durable decision: when portfolio caps are enforced, breached caps are hard scanner guardrail blockers for auto-track and scanner-origin creation. Same-ticker/exact-spread exposure, playbook daily cap, sector/regime cap, daily/weekly realized loss cap, open executable drawdown cap, position cost-risk cap, portfolio cost-risk cap, max concurrent positions, and correlated-index cap must set `guardrail_decision=blocked`, which makes `creation_eligible=false` through the existing creation-blocker path. Near-cap notes and sizing reductions may remain cautions, and caps-off diagnostic scans remain available only as diagnostics. The June 5 volatility lane also uses starter execution caps of `6.0%` max fill degradation versus midpoint and `2.0%` max worst-leg bid/ask spread. This does not auto-close existing positions, change stop policy, lower proof bars, or turn historical/research evidence into production proof.

## 2026-06-05: Require Lane Profitability Before Live Validation Or Auto-Track

The May 22 through June 5 missed-pick outcome audit is now the profitability gate for regular all-lanes selected candidates. It marks missed candidates with trusted intraday exact-contract OPRA/NBBO evidence through `scripts/audit_missed_regular_picks_outcomes.py`, writes `data/forward-tracking/missed_regular_picks_outcome_latest.json`, and exposes lane-level pass/fail plus self-guardrails. The failure-mode companion audit `scripts/analyze_missed_regular_picks_failure_modes.py` writes `data/forward-tracking/missed_regular_picks_failure_modes_latest.json` and `docs/missed-regular-picks-failure-modes.md` so lane blocks, ticker/debit/DTE clusters, and earn-back rules are rerunnable instead of debated from memory.

Durable decision: a regular lane is not treated as an equal live-validation or auto-track lane until its current exact-contract outcome audit passes minimum sample, positive average net P&L, and profit-factor gates. Losing or undersampled lanes remain diagnostic-only even when their scanner guardrails return `clear`. A profitable lane enters paper/probation first and may still block its own bad ticker clusters, debit-width buckets, and any lane-report execution self-guardrails before validation; the current passed lane is `volatility_expansion_observation`, but its SPY/IWM clusters and debit above `45%` of width are blocked. A diagnostic lane earns back only after at least `30` exact marked rows, `0` unpriced rows, PF `>= 1.2`, positive average net P&L, entry-time-only rules, and at least `10` later-date/out-of-sample rows; production discussion targets PF near `1.5`, fee/slippage stress survival, no unblocked negative ticker/debit cluster, and fresh forward paper rows. Current failure-mode readback is clean-data/unprofitable-strategy: `210` / `210` priced, tracked P&L complete, untracked PF `0.32`, avg `-16.54%`, with XLK/SPY/TSLA/IWM damage clusters and debit `>=45%` of width plus DTE `>=36` as diagnostic filter candidates requiring retest. `scripts/ensure_daily_all_lanes_audit_ran.py` must load a fresh gate before queueing candidates and route clear candidates to diagnostics or paper/probation if the gate report is missing, malformed, future-dated, missing `generated_at_utc`, stale beyond the freshness window, has unpriced rows, has incomplete tracked P&L, or lacks lane rows. `scripts/validate_pending_scan_candidates.py` must recheck the fresh gate before any auto-track validation rerun, write paper-only rows for blocked or probation candidates, and fail closed when the report is unusable. `supervised_scan.py` enforces the same fresh gate during validation reruns through `OPTIONS_ENFORCE_LANE_PROFITABILITY_GATE=1`. This gate does not create broker fills, lower OPRA/NBBO proof bars, change stop policy, or turn historical/research mark evidence into production proof.

## 2026-06-05: Use Trusted Intraday OPRA/NBBO As The Regular Profit-Loop Truth Lane

The regular options profit loop now uses `historical_imported` trusted intraday ThetaData OPRA/NBBO evidence as its active proof lane. `.env.local` must point `HISTORICAL_OPTIONS_DB_PATH` at `data/options-validation/options_history.db`, and readiness audits must load local env so stale scratch DB overrides are visible in their readbacks.

Durable decision: regular profit-loop health, live scan policy, smoke fixtures, proof fingerprints, replay validation, and daily truth refresh should use the active intraday lane, not `historical_imported_daily`. The daily artifact path remains covered for archived/backward-compatible tests, but it must not be the active regular proof claim because daily/EOD evidence is below the repo's regular-options proof bar. Profit-loop forward holdout should use the regular `quality90_debit55_canary` proof-control scan playbook for system-health candidate flow, while replay calibration can still use `broad`.

## 2026-06-05: Keep PR CI Deterministic, With Strict Proof Gates As Readbacks

`.github/workflows/ci.yml` blocks PRs on dependency integrity, generated docs/contracts, lint, typecheck, frontend node suites, full Python tests, day-trading/Polymarket sidecar tests, smoke, and build. The proof-readbacks job also runs `npm run verify:proof-readbacks` so current proof-readiness state remains visible in CI.

Durable decision: `npm run verify:e2e:local` is the passable local CI-equivalent command. Strict proof-readiness commands remain explicit diagnostics: `npm run verify:e2e:strict`, `npm run verify:accuracy:no-write`, and `npm run verify:profit-loop:dry`. Keep those strict gates visible but non-blocking for regular-options PR CI while their owned evidence blockers are documented and active. The separate AI commodity `alpaca_opra_daily_snapshot` shared-date blocker remains active; the regular profit-loop truth-lane and forward-holdout blockers were repaired on 2026-06-05. Promote a strict gate to required CI only after its blocker is repaired or for PRs whose explicit scope is repairing that gate.

## 2026-06-05: Keep Alpaca Broker Execution Paper-Only, Opt-In, And Proof-Gated

`python-backend/alpaca_paper_trading.py` is the only broker-submission adapter for the current Trading Desk. It submits Alpaca paper orders only when `OPTIONS_ALPACA_PAPER_TRADING_ENABLED=1` (or the legacy `ALPACA_PAPER_TRADING_ENABLED=1`) is set, Alpaca paper credentials are present, and the order target is the official `https://paper-api.alpaca.markets/v2` endpoint unless an explicit custom-base override is configured for test use.

Durable decision: Alpaca execution is paper-broker execution, not live production proof. Scanner-origin creates must still pass the existing lineage, guardrail, exact-contract, proof-eligible, and duplicate-open-contract gates before a paper order is submitted, and broker execution is capped at exactly `1` contract per order. Paper fill metadata may update tracked-position entry pricing and is written into `source_pick_snapshot.alpaca_paper_order`, while raw submit/fill/link events append to `data/forward-tracking/alpaca_paper_order_events.jsonl`. `alpaca_paper_fill` and `alpaca_paper_limit_order` remain paper execution bases and must not be counted as trusted OPRA/NBBO production-proof fills.

## 2026-06-05: Keep Paper-Gate Readiness Scorecard And Memory Graph Non-Runtime

The active options operating scorecard is generated by `scripts/build_regular_profitability_operating_scorecard.py` at `docs/regular-options-operating-scorecard.md`, and the agent memory graph is generated by `scripts/generate_agent_memory_graph.py` at `docs/agent-memory-graph.md` and `data/contracts/agent-memory-graph.json`.

Durable decision: paper-gate readiness counts in the scorecard, the candidate outcome ledger, and the paper-gate path in the agent memory graph are readback/navigation surfaces only. They may summarize paper-shortlist, fresh-evidence-loop, current-policy circuit-breaker, profit-capture queue, repair-attempt, repair burn-down, open-risk governor, and suggested-trade review artifacts so humans and agents can answer what is eligible, blocked, paper-only, next-evidence-required, or live-prohibited. They must not create or approve trades, change scanner policy, change broker behavior, change stop policy, change auth/session behavior, change DB schema, lower proof bars, or turn paper-gate readbacks into recommendations.

## 2026-06-04: Keep Profitability Bridges Paper-Only And Evidence-Gated

The regular-options profit-capture queue is generated by `scripts/build_regular_options_profit_capture_queue.py`, fill-attempt evidence is logged by `scripts/log_scan_picks.py`, and the multi-lane quality gate is generated by `scripts/run_regular_options_multilane_portfolio.py`.

Durable decision: profitable historical evidence is a paper/repair routing signal, not live promotion. Tier A clean exact rows may enter the paper shortlist bridge only when a fresh executable quote-window scanner row matches the same Tier A lane signature; Tier B remains watch/repair, Tier C remains historical-signature-only, blocked rows remain blocked, and quarantine rows remain do-not-chase. Fill-discipline snapshots, leg bid/ask fields, spread mids, and top alternatives are evidence for paper review and repair forensics only. Lane A count success must stay blocked from production readiness when side-aware zero-bid replay shows conservative combined PF below `1.30`, unresolved zero-bid exits, or zero-bid exit rate above `2%`. Do not use these readbacks to change scanner policy, broker action, stop policy, proof bars, route/auth behavior, DB schema, AI commodity proof state, or paused sidecar lanes.

## 2026-06-04: Surface Paper Gate Operator Workflow As Read-Only Visibility

The Trading Desk operator workflow is documented in `docs/regular-options-operator-workflow.md`. `python-backend/main.py` overlays `paper_gate_operator_workflow` on the read-only `/api/options-profit/status` payload from the paper-shortlist, fresh-evidence-loop, pending-validation, and current-policy circuit-breaker artifacts. `OperatorSessionPanel.tsx` opens the local operator session through `POST /api/operator/session`, and `PaperGateOperatorPanel.tsx` renders bridge status, blockers, matched Tier A lanes, pending outcomes, and fill-discipline explanations.

Durable decision: the operator workflow is visibility and local-session ergonomics only. It may make missing artifacts, no-fill rows, skipped auto-track rows, paper-only circuit-breaker routes, and paper-review candidates readable in the scanner evidence drawer, but it must not create trades, change broker behavior, change scanner policy, change proof gates, change DB schema, store the operator token client-side, or present paper-gate rows as recommendations. Missing paper-gate artifacts fail visible as `paper_gate_artifacts_missing`, not as proof readiness.

## 2026-06-04: Route Broken Recent Cohorts To Paper Validation Only

The current-policy recent-cohort circuit breaker is generated by `scripts/build_current_policy_circuit_breaker.py`, documented in `docs/current-policy-circuit-breaker.md`, and consumed by `scripts/validate_pending_scan_candidates.py` before a pending lane rerun can enter the auto-track validation path.

Durable decision: while cohort health reports `paper_only_recent_week_break`, pending `short_term` and `bullish_pullback_observation` candidates receive a `paper_only` validation disposition through the circuit breaker instead of being rerun with auto-track enabled. Recovery requires the recent cohort to stop reporting a paper-only break, at least `20` fresh current-policy rows, at least `5` champion-matched candidate-blocked rows, trusted exact realized P&L in point-in-time replay, a paper-monitor pass, and no winner damage. Passing recovery gates creates a review candidate, not an automatic live promotion. The breaker does not delete lanes, change broker behavior, change stop policy, change auth/DB/schema, lower proof bars, or make the short-term fill-degradation rule global.

## 2026-06-04: Keep Regular-Options Repair Attempts And Burn-Down Readback-Only

Regular-options exact-repair memory is documented in `docs/regular-options-repair-attempts.md`, generated by `scripts/build_regular_options_repair_attempt_readback.py`, consumed by `scripts/build_regular_options_profit_capture_queue.py` as repair actionability only, and converted into the exact repair burn-down at `docs/regular-options-repair-burndown.md` by `scripts/build_regular_options_repair_burndown.py`.

Durable decision: repair-attempt rows and the repair burn-down are operator/agent memory, not proof graduation. `imported_pending_replay` means rerun the source replay before any promotion; `exact_date_rows_found` in dry-run is a candidate only; `lookahead_only_rows_found` is diagnostic and must not repair the missing exact proof date; `exact_date_no_match` exhausts the current source/date until new evidence exists. The burn-down may rank unexhausted exact-date work and surface source-replay-required rows, but it must not use lookahead-only or exhausted current-source rows as active import targets. Do not use repair-attempt memory or burn-down status to change scanner policy, broker action, stop policy, proof bars, route/auth behavior, DB schema, AI commodity proof state, or paused sidecar lanes.

## 2026-06-04: Keep Living Docs Hygiene Manifest-Driven And Non-Runtime

Living-doc ownership and generated-artifact hygiene are documented in `docs/living-docs-hygiene.md` and checked by `scripts/check_living_docs_hygiene.py`, which is wired into `npm run verify:docs`.

Durable decision: the hygiene checker is a docs orientation and drift guard only. Keep it manifest-driven and scoped to living owner docs plus known generated readability artifacts. Do not turn it into broad Markdown lint, stale-data governance for every research report, runtime behavior, route behavior, auth, proof, scanner, replay, DB, schema, or frontend policy. Do not require every dated report or generated research artifact to be linked from `docs/index.md`, and do not duplicate generated inventories in handwritten docs.

## 2026-06-04: Keep The Trading Desk Schema Bridge Generated But Non-Runtime

The generated Trading Desk schema bridge is documented in `docs/trading-desk-schema-bridge.md` and stored in `data/contracts/trading-desk-api-schema-bridge.json`. `scripts/generate_trading_desk_schema_bridge.py` builds it from `python-backend/trading_desk_api_models.py`, `src/lib/trading-desk/apiContracts.ts`, and `src/lib/trading-desk/storeOwnership.ts`, and `npm run verify:docs` checks it for drift.

Durable decision: the bridge is documentation and drift-check metadata only. It may expose Pydantic JSON Schemas for the intentionally loose Trading Desk mutation body adapters and top-level response envelopes, and it may map those schemas to manual TypeScript request/response names. It must not become FastAPI `response_model`, automatic `422` request validation, OpenAPI, runtime JSON Schema/Zod/AJV validation, generated TypeScript, payload reshaping, auth/header/cookie behavior, proof/scanner semantics, DB schema work, or a Strategy Lab/generic-route/backend-only/AI-commodity/crypto/Polymarket/day-trading schema sweep.

## 2026-06-04: Keep Generic Route Lifecycle Headers Descriptive

Generic mounted Next route lifecycle headers are documented in `docs/route-lifecycle-contracts.md` and represented in `src/lib/route-lifecycle/routeContracts.ts`. `jsonWithRouteLifecycle()` applies `x-options-route-*` headers to scan, predictions, risk/status, local operator session, sectors, and tool dispatch routes. `scripts/generate_route_parity.py` reads the registry so `docs/route-parity.md` stays generated from source.

Durable decision: these headers are descriptive readability and route-inventory signals only. Do not use them as auth, request validation, response-envelope validation, proof evidence, scanner creation safety, cache policy, or generated schema plumbing. Keep Trading Desk routes on `x-trading-desk-*`, Strategy Lab routes on `x-strategy-lab-*`, preserve response JSON and existing cookie/timing header behavior, and keep backend-only FastAPI routes plus crypto, Polymarket, day-trading, and AI commodity proof artifacts outside this generic browser-route registry unless a later decision reopens them.

## 2026-06-04: Validate Only Trading Desk Response Envelopes At The Next Boundary

Trading Desk response-envelope validation is documented in `docs/typescript-api-contracts.md` and implemented by `src/lib/trading-desk/apiResponseValidation.ts`. The six Next Trading Desk route handlers return through `jsonWithValidatedTradingDeskStore()`, which checks shallow backend response envelopes before returning them to the browser.

Durable decision: runtime validation is limited to critical Trading Desk response envelope shape. Accept unavailable `{ error: string }` sentinels, require tracked `position` / `positions` envelopes and tracked mutation `position_event_persistence`, require suggested `trade` / `trades` envelopes, reject suggested top-level `position_event_persistence`, and shallow-check row arrays plus page metadata. Do not deep-validate `TrackedPosition`, `ScanPick`, proof, quote, P&L, scanner lineage, replay, profile, tool, AI commodity, crypto, Polymarket, or day-trading payloads here. Do not add request-body validation, Zod, OpenAPI, JSON Schema, Pydantic-to-TypeScript generation, payload reshaping, or FastAPI response models in this point.

## 2026-06-04: Keep TypeScript API Contracts Manual And Trading-Desk Scoped

Trading Desk browser/Next/backend helper request and response shapes are documented in `docs/typescript-api-contracts.md` and represented in `src/lib/trading-desk/apiContracts.ts`. The contract module names the tracked-position and suggested-trade list, create, review, and close envelopes consumed by `src/lib/backend/positions.ts`, the six Next mutation routes, `PredictionsView.tsx`, and `useTradingDeskRecords.ts`.

Durable decision: TypeScript API contracts are compile-time readability and drift guards, not runtime validation or generated schema plumbing. Do not add OpenAPI, JSON Schema, Pydantic-to-TypeScript generation, Zod validators, route payload reshaping, scanner/proof semantics, or all-route contract sweeps in this point. Generic transport errors, tool args, search params, dynamic replay reports, and proof/report blobs may still use open record shapes where their contents are intentionally flexible. Suggested-trade request/response envelopes stay distinct from tracked-position envelopes; suggested mutation responses must not include `position_event_persistence`.

## 2026-06-04: Keep Trading Desk Pydantic Models Narrow And Route-Owned

Trading Desk mutation body shape is documented in `docs/trading-desk-api-models.md` and represented in `python-backend/trading_desk_api_models.py`. The manifest covers only `POST /api/positions`, `POST /api/positions/review`, `POST /api/positions/{id}/close`, `POST /api/suggested-trades`, `POST /api/suggested-trades/review`, and `POST /api/suggested-trades/{id}/close`.

Durable decision: Pydantic is an internal adapter and drift guard for this point, not the public FastAPI contract. Keep handlers typed as `dict[str, Any]`, parse inside existing route `try` blocks, and translate model validation into route-owned `400` errors. Do not add `response_model=`, automatic FastAPI `422` responses, deep row schemas, scan/replay/proof schemas, TypeScript/OpenAPI generation, or route payload reshaping here. Existing helper parsers still own numeric, boolean, datetime, position-id, scanner-origin, and proof semantics; suggested-trade envelopes still omit `position_event_persistence`.

## 2026-06-04: Keep Local DB Hardening Read-Only And Non-Repairing

Local SQLite safety is documented in `docs/local-db-hardening.md` and represented in `python-backend/local_db_hardening.py`. The manifest classifies `chat_history.db` as the active mutable suggested-trade SQLite store, `data/tracked_positions.db` as explicit test/legacy storage only, options-history/forward-ledger/market-cache/AI-commodity stores as outside Trading Desk repository hardening, and SQLite sidecars/backups as ignored local files. `scripts/audit_local_databases.py` opens existing covered DBs with SQLite `mode=ro`, runs integrity/table checks, and reports sidecars/backups without initializing schema.

Durable decision: local DB hardening is an audit and handling contract, not a repair or migration mechanism. Do not add a tracked-position SQLite fallback, change `chat_history.db` journal mode, run `VACUUM` / `REINDEX` / checkpoint / table rebuilds, delete backups, initialize missing DBs from the audit, or mutate proof/scanner/replay semantics. Optional migration-ledger absence on older local DB files is a warning until the app bootstraps current repository code; broken required tables, failed `quick_check`, or foreign-key violations are health issues for a separate explicit repair plan.

## 2026-06-04: Keep Suggested-Trade Parity Descriptive And Paper-Only

Tracked/suggested record parity is documented in `docs/trading-desk-record-parity.md` and represented in `python-backend/repository_parity.py`. The parity map names the shared read/create/review/close lifecycle, common position-shaped repository surface, common display row keys, common `latest_review` shape, and shared scanner-origin creation-safety validation.

Durable decision: parity is descriptive and for readability. Tracked positions remain `postgres_tracked_positions` / `tracked_position` production rows; suggested trades remain `sqlite_suggested_trades` / `suggested_trade` local paper/hypothetical workflow state. Do not merge stores, rename public envelopes, add top-level `proof_*` or `source_scan_*` fields to suggested trades, add `position_event_persistence` to suggested-trade mutation responses, require tracked-only repository capabilities on suggested trades, or count suggested trades in production proof or options-profit truth. The current TypeScript `SuggestedTrade = TrackedPosition` alias is a display-row alias only, not a semantic merge.

## 2026-06-04: Keep Repository Indexes Read-Path Owned And Evidence-Gated

Trading Desk repository indexes are documented in `docs/repository-indexes.md` and represented in `python-backend/repository_indexes.py`. The manifest names existing Postgres tracked-position, SQLite suggested-trade, and explicit test/legacy SQLite tracked-position indexes, the read paths they support, and deferred composite candidates. `scripts/audit_repository_indexes.py` provides a read-only index metadata audit.

Durable decision: indexes are performance/read-path support, not proof semantics, scanner safety, route behavior, or constraints. Do not add broad Postgres index DDL in startup schema code without measured row counts or query-plan evidence and a maintenance plan; `CREATE INDEX CONCURRENTLY` does not fit the current transaction-style `init_schema()` path. Unique indexes require both index and constraint decisions. Composite status/fill/id, tracked-review id-tiebreaker, and closed-at/profit indexes remain deferred candidates until evidence justifies them. AI commodity artifacts and the options-history truth store remain outside this Trading Desk repository index contract.

## 2026-06-04: Keep Repository Constraints Layered And Audit-First

Trading Desk repository constraints are documented in `docs/repository-constraints.md` and represented in `python-backend/repository_constraints.py`. The constraint map separates DB-enforced relational safety, API/service validators, proof-contract-owned semantics, and deferred DB checks. `scripts/audit_repository_constraints.py` provides a read-only audit for primitive row-shape violations and orphan reviews.

Durable decision: do not encode production proof, scanner creation safety, replay math, or lifecycle evidence as SQL constraints. Keep broad status/direction/numeric `CHECK` constraints, unique open-contract constraints, JSON-shape checks, and broad closed-row coherence `CHECK`s deferred until the audit proves existing rows are clean or intentionally exempt. The read-only audit may still enforce hard data-integrity facts outside DB DDL: closed suggested trades and production/unclassified tracked rows must not be missing realized P&L, while explicitly identified research/backfill rows with absent exact exit-leg quotes stay diagnostic and quarantined. For Point 12, the only DB enforcement change is enabling SQLite `PRAGMA foreign_keys=ON` for suggested-trade repository connections so the already-declared review foreign key protects future writes. Missing Postgres still fails closed; no SQLite tracked-position fallback, broad data backfill, table rebuild, or Alembic migration is introduced.

## 2026-06-04: Keep Repository Migrations Forward-Only And Ledgered

Trading Desk repository schema evolution is documented in `docs/repository-migrations.md` and represented in code by the versioned manifest and ledger helpers in `python-backend/repository_migrations.py`. Point 11 records the current inline repository `init_schema()` DDL as checksum-guarded baselines for Postgres tracked positions, SQLite suggested trades, and explicit test/legacy SQLite tracked positions.

Durable decision: repository migrations are forward-only, additive, and idempotent unless a later decision explicitly approves destructive schema work. Do not introduce Alembic in this codebase until the repository layer needs a broader framework. Do not edit recorded baselines to smuggle schema changes; add a new migration and focused tests. Migrations must not redefine proof/evidence semantics, scanner creation safety, auth, route payloads, lifecycle evidence, replay math, repository method contracts, AI commodity artifacts, or the options-history truth store. Missing or failing `DATABASE_URL` must still fail closed through the unavailable tracked-position sentinel rather than migrating or falling back to SQLite.

## 2026-06-04: Keep Repository Interfaces Structural And Store Ownership Explicit

Trading Desk repository ownership is documented in `docs/repository-contract.md` and represented in code by structural Protocols in `python-backend/repository_contracts.py`. Tracked positions are Postgres-owned through `DATABASE_URL`; suggested trades are SQLite-owned through `chat_history.db`; memory and SQLite tracked-position repositories are explicit test or legacy-tool doubles.

Durable decision: repository interfaces are readability and drift guards, not new persistence behavior. Do not add a silent tracked-position SQLite fallback, force optional tracked-position capabilities onto suggested trades, change route payloads, change proof semantics, or alter DB schemas/migrations/constraints/indexes in the interface layer. Keep concrete repositories duck-typed and late-bound-friendly; optional capabilities such as `list_compact_positions` and `profit_status_snapshot` remain optional.

## 2026-06-04: Keep Replay/Profit Readbacks In A Late-Bound Service

Replay/profit route readbacks are assembled by `python-backend/replay_profit_service.py`, with ownership boundaries explained in `docs/replay-profit-contract.md`. FastAPI `python-backend/main.py` remains the HTTP adapter and cache primitive owner for now, while replay engines, metric truth, profitability forensics, scanner policy generation, proof predicates, and profit-cycle state stay in their existing domain modules.

Durable decision: replay/profit application services must be decorator-free and late-bound through `BackendRouteContext`. They may assemble cached readbacks for backtest reports, metric truth, experiments, forensics, stability, live policy, exit audit, comparison, and summary, but they must not import canonical `main.py`, change replay/profit thresholds, own proof definitions, create rows, mutate DB state, or reshape public API payloads. Split `wfo_optimizer.py` internals only under a separate tested plan.

## 2026-06-04: Keep Scanner Pipeline Stages Contract-Owned And Descriptive

The scanner pipeline stage map is owned by `data/contracts/scanner-creation-safety-contract.json`, explained in `docs/scanner-creation-safety-contract.md`, and mirrored by `supervised_scan.SCANNER_PIPELINE_STAGES` for runtime-adjacent discoverability.

Durable decision: scanner stages describe existing ownership boundaries from playbook resolution through raw candidates, scan drops, policy, guardrails, managed selection, payload assembly, forward lineage capture, proof classification, and creation or validation disposition. They are for agent readability, contract testing, and future refactor planning; they do not add public API response fields or relax scanner creation safety. Any future behavioral split must keep the stage contract, proof contract, and route/storage docs in parity before changing row creation semantics.

## 2026-06-04: Keep Application Services Decorator-Free And Late-Bound

Backend application services are workflow/orchestration modules between FastAPI route adapters and domain/repository modules. `python-backend/proof_summary_service.py` is the first service in this layer: it assembles `/api/proof-summary` from measurement-gate state, claim-readiness state, forward-evidence counts, and tracked-position proof counts while leaving proof predicates in `python-backend/proof_contract.py` and row/review semantics in `python-backend/positions_service.py`.

Durable decision: application services must not define FastAPI decorators, import canonical `main.py`, own repository globals, or redefine proof/scanner/auth rules. Services that need mutable backend dependencies use `BackendRouteContext` or explicit late-bound providers so tests and synthetic backend module loads can patch dependencies after app creation. Routes own HTTP parsing/errors, services own workflow assembly, domain modules own semantics, and repositories own persistence.

## 2026-06-04: Keep Extracted FastAPI Routers Late-Bound

`python-backend/main.py` remains the FastAPI composition root: app construction, middleware, repository globals, report caches, and router mounting stay there. Extracted route modules such as `python-backend/profile_routes.py`, `python-backend/predictions_routes.py`, and `python-backend/tools_routes.py` own their route decorators and handler bodies.

Durable decision: extracted routers must not import canonical `main.py` or capture mutable backend globals at registration time. Tests and local fixtures load `main.py` under synthetic module names and patch globals after app creation, so router handlers that need backend globals must use `BackendRouteContext(globals())` or explicit late-bound providers. Keep literal full `/api/...` paths in top-level `python-backend/*_routes.py` files so `scripts/generate_route_parity.py` continues to see the generated route inventory without prefix inference.

## 2026-06-04: Make Evidence Recording Failures Observable

Tracked-position lifecycle mutations must not silently lose their forward-evidence side effects. `POST /api/positions`, `POST /api/positions/review`, and `POST /api/positions/{id}/close` may continue to fail open after the primary row mutation commits, but their responses must include `position_event_persistence` with `status=recorded|failed|skipped`, operation, position ids, ledger session metadata, and error details when lifecycle evidence recording fails.

Durable decision: forward-evidence recording health is operational observability, not proof promotion. A successful position row with failed lifecycle-event persistence remains a successful row mutation, but the evidence gap must be visible in the response and through `GET /api/backtest/forward-evidence`. Do not return a late 500 after the row already committed, and do not relax proof eligibility, scanner creation safety, or OPRA/NBBO evidence requirements to compensate for missing lifecycle events.

## 2026-06-04: Make Scanner Creation Safety A Versioned Contract

Scanner-origin creation, scheduled auto-track, and pending candidate validation semantics are owned by `data/contracts/scanner-creation-safety-contract.json` and explained for humans and agents in `docs/scanner-creation-safety-contract.md`.

Durable decision: scanner-origin tracked-position and suggested-trade creates must verify archived source lineage, require the source pick to be caps-enforced and `creation_eligible=true` with no blockers, rerun current guardrails with caps enabled, require the current rerun pick to remain caps-enforced and `creation_eligible=true` with no blockers, and then require proof-eligible exact-contract evidence. Scheduled auto-track must require `OPTIONS_SCAN_AUTO_TRACK`, `market_open_at_run=true`, an auto-track playbook, `exposure_snapshot.available=true`, `exposure_snapshot.portfolio_caps_enforced=true`, and source creation metadata before building a payload. Unknown market state, unavailable exposure state, caps-off scans, blocked/current-ineligible reruns, fallback execution labels, and proof-ineligible rows are diagnostics or validation dispositions, not creation events.

## 2026-06-04: Make The Proof/Evidence Contract A Versioned Semantic Owner

Trading Desk proof/evidence semantics are owned by `data/contracts/proof-evidence-contract.json` and explained for humans and agents in `docs/proof-evidence-contract.md`. Backend proof classification remains authoritative through `python-backend/proof_contract.py` and `python-backend/positions_service.py`; frontend display code must consume the shared proof classes, tokens, and display groups rather than inventing local evidence categories.

Durable decision: production proof remains only `proof_class=live_scan_exact_contract` with `proof_eligible=true`, exact contract fields, executable fresh scan entry, OPRA/NBBO source, and verified forward-scan lineage. Creation-time classification, stored-row metrics, and frontend truth-grade display must re-check those fields rather than trusting the class string alone; closed proof-grade rows, including `closed_at` rows with stale/missing status, also require a trusted executable exit and calculable realized P&L. Raw exact-looking fields, manual broker exact rows, manual closes, research/backfill, historical paper, lifecycle-only rows, missing/stale quote freshness, stale `proof_eligible` flags without the live exact proof class, and top-level or source-snapshot backfill/migration identity fields such as `backfill_audit_id`, `position_migration_id`, and `position_migrated_at_utc` are visible evidence classes but not production proof. Future backend compact rows may emit `evidence_group` and `proof_contract_version` directly, but until then frontend inference is a defensive display layer over the shared contract.

## 2026-06-04: Separate Local Operator Auth From Bridge Auth And Mutation Intent

Browser-facing state-changing and tool routes must have a real local operator boundary before body parsing. `src/lib/operator-auth.ts` owns that boundary through server-only `OPTIONS_LOCAL_OPERATOR_TOKEN`, the `x-options-operator-token` header, `Authorization: Bearer ...`, and the HttpOnly `options_local_operator_session` cookie opened by `POST /api/operator/session`.

Durable decision: `OPTIONS_BACKEND_API_TOKEN` remains a separate Next-to-FastAPI bridge token and is not user/operator auth. Trading Desk and Strategy Lab mutation-intent headers remain audit/intent labels and must run after local operator auth. A correct `x-trading-desk-mutation` or `x-strategy-lab-mutation` header without local operator auth must fail authorization. Any new browser-facing `POST`, `PUT`, `PATCH`, or `DELETE` route must either call `requireLocalOperator(req)` or be documented and tested as an explicit auth/session exception.

## 2026-06-04: Keep Regular-Options Proof And Candidate Disposition Strict

Pending selected candidates must have an auditable validation disposition after market-hours validation. `scripts/validate_pending_scan_candidates.py` writes `data/forward-tracking/pending_scan_candidate_validation_latest.json` with one outcome per validation-attempted candidate: `created`, `duplicate`, `blocked`, `no_longer_matched`, `paper_only`, or `proof_ineligible`. Scheduled auto-track and scanner-origin creates require source `creation_eligible=true`; absence of blockers is not enough.

Durable decision: production-proof counts require live scanner exact-contract proof, not raw exact-looking fields or manual exact closes. `/api/proof-summary` keeps raw exact closed rows separate from proof-grade exact closed rows, and the proof-grade/legacy `exact_contract_closed_count` excludes `manual_broker_exact_contract`, manual closes, research/backfill, historical paper, lifecycle-only, and stale `proof_eligible` rows without `proof_class=live_scan_exact_contract`. Manual broker/exact rows remain visible as manual evidence but are not production proof. `short_term_fill_degradation_ge_15` remains lane-scoped and paper-only; the point-in-time scanner candidate replay plus fresh paper monitor gates must pass before any scanner promotion. Live `90%` stop policy stays unchanged until minute-level exact OPRA/NBBO replay clears the exit-policy bar.

## 2026-06-04: Auto-Track All Regular Scanner Lanes By Default

Every regular supervised options playbook defaults to `position_tracking_mode=auto_track`. That includes peer/scout/control playbooks such as Tracked Winner, Regular Bearish Put, range breakout, volatility expansion, speculative, and `quality90_debit55_canary`; the quality90 canary remains `proof_scope=regular_supervised_control` so control rows cannot inflate production-proof stats. AI Commodity remains a separate proof-first strategy lane with tracking disabled in the browser/scanner lane family.

Durable decision: tracking eligibility is broad, but row creation is still strict. Scheduled or scanner-origin tracking requires the auto-track kill switch, market-open status, portfolio caps enabled, verified archived forward-scan lineage, source `creation_eligible=true`, a current guardrail rerun, and exact executable OPRA/NBBO proof eligibility. A regular lane should not be paper-review-only merely because it is a scout, control, or non-Bullish-Pullback lane; if it produces a fresh executable eligible row, it should be queued/validated and auto-tracked. Blocked, stale, unpriced, paper-only, or proof-ineligible rows remain diagnostics and must be reported through candidate disposition rather than silently disappearing.

## 2026-06-03: Make Scanner Creation Safety Lane-Wide And Metadata-Driven

Scanner validation, auto-track, and position creation must be governed by playbook metadata across the regular supervised options lane family, not by a hardcoded Bullish Pullback exception. Every regular playbook defaults to fresh live validation; AI Commodity remains a separate proof-first strategy lane. Auto-track is allowed for regular playbooks whose metadata resolves to `position_tracking_mode=auto_track`; control/scout proof labels do not make otherwise eligible regular rows paper-review-only.

Durable decision: production scans default to portfolio caps enabled in browser, API, and scheduled batch paths. Caps-off scans are diagnostic unless explicitly allowed, and scheduled auto-track refuses rows without a caps-enforced exposure snapshot. Scanner-origin `POST /api/positions` and `POST /api/suggested-trades` now require verified archived forward-scan lineage, a caps-enforced source scan, source `creation_eligible=true`, a current guardrail rerun, and proof-eligible exact-contract evidence. Lineage matching includes evidence/source/caps/creation fields so a caller cannot relabel fallback-delayed or caps-off rows as proof-grade. Historical/research rows can still be stored only through explicit `manual_paper` or `manual_broker` creation modes and remain proof-ineligible when lineage or OPRA/NBBO proof is missing. Portfolio caps now include open executable drawdown from fresh executable reviews; stale/unpriced/display-only review marks are counted separately and do not become executable P&L. Existing-position exposure, max concurrent positions, same-ticker/exact-spread exposure, daily/weekly loss, open drawdown, portfolio cost-risk, sector/regime caps, and correlated-index caps are hard blockers when caps are enforced; near-cap exposure notes and sizing reductions may remain cautions. Proof, lane-scope, execution-label, liquidity, replay-promoted entry-quality, lane-profitability, and stale-gate failures are also hard blockers. Side-aware zero-bid replay rows persist entry/exit quote evidence and stable hashes for replay auditability.

## 2026-06-02: Run The Daily No-Pick Audit Across All Supervised Lanes

Daily no-pick explanations must not rely on one scheduled auto-track route alone. All configured regular-options lanes are peer lanes, and the daily audit/readback must run read-only across every configured supervised playbook, include AI Commodity as its own separate strategy lane, and cover each playbook's full configured ticker scope.

Durable decision: `scripts/ensure_daily_all_lanes_audit_ran.py` owns this safety net. It requires an `all_supervised_guardrail_starvation` artifact for the market day, verifies that all `SCAN_PLAYBOOKS` completed, checks that commodity strategy playbooks and all configured ticker scopes were included, and repairs the artifact by running `scripts/audit_regular_guardrail_starvation.py --include-commodity` when needed. Clear rows from regular auto-track lanes are written to `data/forward-tracking/pending_scan_candidates.jsonl` as `pending_live_validation`, not dropped and not immediately created as positions. `scripts/validate_pending_scan_candidates.py` owns the market-hours follow-up: it reruns pending regular auto-track lanes with portfolio caps enabled, and only the live scan/proof path may create positions after fresh executable OPRA/NBBO evidence and verified forward-scan lineage. `scripts/run_scan_picks.bat` now forces the all-lanes audit after the scheduled live scan route, then runs pending-candidate validation; `scripts/run_scan_picks_safety_net.bat` verifies the same path. Manual commands are `npm run options:audit:all-lanes` and `npm run options:validate:pending-candidates`. Blocked, stale, unpriced, paper-only, and AI Commodity rows stay diagnostics unless their separate proof paths explicitly promote them.

## 2026-06-02: Build A Profit Capture Queue Without Lowering Proof Bars

The regular options profitability problem now has a visibility layer before any scanner-policy change. `scripts/build_regular_options_profit_capture_queue.py` reads the per-symbol sleeve matrix, current-policy historical picks, and fresh guardrail-starvation audit, then writes `data/profitability-lab/regular-options-profit-capture-queue/latest.json` plus `docs/regular-options-profit-capture-queue.md`. The repaired latest queue has `97` research/paper capture rows: `15` Tier A clean exact capture rows, `82` Tier B profitable watch/repair rows, `16` high-priority evidence repairs, `6` fresh scan signature matches, `4` blocked-but-interesting rows, and `173` quarantine/do-not-chase rows. It restores visibility into profitable sleeves such as GOOGL watch/repair, NEM/AAPL/LLY clean exact rows, and current-policy SPY/QQQ swing signatures while keeping guardrail reasons visible.

Durable decision: the queue is a research/paper capture and proof-hardening layer, not a scanner promotion, broker recommendation, live stop change, or proof-bar reduction. Tier A requires trusted intraday OPRA/NBBO exact-contract evidence, zero unresolved rows, adequate sample, high quote coverage, positive PF/average P&L, and no clean disqualifier. Tier B is watch/repair evidence and must show unresolved/coverage/sample warnings. Tier C fresh scan matches are historical-signature matches only. The queue now carries explicit paper/research routing: Tier A is `paper_review_candidate`, Tier B is `watch_repair_only`, clear fresh signatures are `historical_signature_only`, blocked rows are `blocked_guardrail_only`, and quarantine rows are `do_not_chase`. Blocked candidates remain blocked until a separate replay/paper proof process justifies a scoped policy change. Do not chase GOOGL from the repaired queue; NEM can be considered only as a paper-review candidate unless a fresh executable scan matches it.

## 2026-06-02: Treat 80% Historical Stops As Research, Not A Live Stop Change

The current-policy exact-contract daily close-check stop grid found a small non-destructive `80%` stop candidate on the tracked current-policy slice, but it is not strong enough to promote live. The tracked replay covered `112` Postgres current-policy realized rows with `0` unresolved rows across all `17` tickers. Baseline was avg `+53.54%`, median `+50.60%`, `29` negatives, and `8` rows `<= -90%`. A daily close-check `80%` stop improved avg to `+53.69%`, kept negatives at `29`, reduced `<= -90%` to `7`, triggered `9` rows, and flipped `0` winners. Because the tracked slice is only Apr/May 2026, the report also includes a separate annual replay-backed exact cohort from the regular multi-lane stack: `234` rows, entry window `2025-08-14` to `2026-03-24`, exit window `2025-09-09` to `2026-04-27`, and `0` unresolved rows across all `37` tickers. That annual cohort is audit evidence, not inserted tracked rows or broker/live fills, and every tested stop from `50%` through `90%` reduced average P&L while flipping winners.

Durable decision: keep the live `90%` stop cap for now. Treat `80%` as a research candidate that must pass minute-by-minute OPRA/NBBO stop replay and paper validation before promotion. Initial entry-avoidance testing for `short_term` concentration, fill degradation `>=15%`, quality score `<60`, and repeat ticker clusters is governed by the paper-monitor decision below.

## 2026-06-02: Paper-Monitor The Short-Term Fill-Degradation Entry Filter Before Promotion

The current-policy entry-filter lab found `short_term_fill_degradation_ge_15` is the only useful historical candidate from the first loss-cohort pass. It blocks `9` current-policy historical rows, avoids `5` rows `<= -50%`, avoids `3` rows `<= -90%`, loses `2` winners, and improves the kept historical cohort from avg `+53.54%` to `+61.01%`. Broader fill-degradation, quality-score, repeat-ticker, and tighter threshold filters removed too many winners or had insufficient coverage.

Durable decision: do not promote this into live scanner guardrails from historical paper evidence. Run it as a paper-only forward monitor through `scripts/monitor_current_policy_entry_filter_paper.py`. Promotion requires at least `20` fresh current-policy rows, at least `5` fresh champion-matched candidate-blocked rows, trusted executable realized P&L, positive kept-cohort read, and monitor gates passing without winner damage. Until then, the operating scorecard should say to keep the filter paper-only.

## 2026-06-02: Validate Entry Filters Across All Regular Lanes, But Keep Them Lane-Scoped

The all-regular-lanes walk-forward validator found the frozen `short_term_fill_degradation_ge_15` rule is useful but not promotable. It is a `historical_pass_candidate` on all `112` current-policy realized rows and on the `2026-05` holdout, but the `2026-04` calibration slice is `winner_damage_too_high`. The same fill-degradation `>=15%` idea fails as a broad all-lane guardrail: it matches `19` rows, avoids `6` deep losses, but loses `10` winners and is `winner_damage_too_high`. Lane readbacks confirm the rule is only plausible in `short_term`; `swing` has `no_deep_loss_reduction`, `bullish_momentum` has `winner_damage_too_high`, and Bullish Pullback has `no_coverage`.

Durable decision: all-lane validation is required before scanner promotion, but the current result argues against a global fill-degradation rule. Keep the frozen rule short-term-only and paper-only. The next historical-data step is broader point-in-time scanner candidate replay, not another threshold sweep on the same realized rows.

## 2026-06-01: Use Recent Cohort Health As The Current-Policy Paper-Only Kill Switch

Current-policy replay can be positive in aggregate while the newest cohort is not showable. The Trading Desk should therefore separate `showcase_month` from `recent_month` / `recent_week` before presenting current-policy historical P&L as an active algorithm state.

`scripts/build_current_policy_cohort_health.py` and `docs/current-policy-cohort-health.md` now own that readback. Latest cohort health keeps the April edge visible: `2026-04` has `70` priced current-policy rows at avg `+81.17%`, median `+71.82%`, and `8.6%` negative rate. It also blocks the current claim: `2026-05` has `42` priced rows at avg `+7.49%`, median `-4.6%`, and `54.8%` negative rate, while `2026-W21` has `3` priced rows at avg `-82.06%`, median `-83.61%`, and `100.0%` negative rate. The current overall state is `paper_only_recent_week_break`.

Durable decision: the April cohort can be used as historical evidence of a discovered edge, but current-policy picks should remain paper-only until a fresh recent recovery cohort revalidates them. The UI may show current-policy aggregate P&L, but it must also show showcase month, recent month, recent median, and cohort state so a negative recent regime cannot be hidden by older winners.

## 2026-06-01: Default Closed Trades To Current-Policy Replay, Not Raw Historical P&L

The Trading Desk Closed Trades default is now `Current policy`: rows in the promoted regular repair lanes (`short_term`, `swing`, `bullish_momentum`, and Bullish Pullback) that clear today's promoted entry guardrails and have trusted executable realized P&L. This is the operator-facing answer to "what would the current algorithm still take?" rather than a raw archive of every historical paper/backfill result.

`Realized P&L` remains available as the raw historical-learning view, and `Truth-grade` remains the stricter production-proof view. Historical paper/research rows must not be deleted or rewritten to make the algorithm look better; they are relabeled through `would_take_today`, `blocked_by_current_policy`, `unknown_missing_evidence`, or `out_of_scope_lane` in `scripts/build_current_policy_historical_picks_audit.py` and `docs/current-policy-historical-picks-audit.md`.

Superseding dashboard decision on 2026-06-22: Closed Trades must default to `Truth-grade`, not `Current policy`, and the standalone historical current-policy replay panel must not be rendered in the Trading Desk. The replay artifact remains useful for research, but presenting old closed rows that pass today's guardrails as "current picks" or default operator performance is misleading. Use `Truth-grade` for dashboard-first production proof, `Realized P&L` for broader executable historical review, and keep `docs/current-policy-historical-picks-audit.md` off the primary operator surface.

The latest replay audited `488` closed rows and `400` current-policy scope rows. The raw realized scope averaged `+4.87%` with `51.8%` negative rate, while the current-policy slice had `112` priced rows at avg `+53.54%`, median `+50.6%`, and `25.9%` negative rate. The learned-away bucket had `274` rows / `243` priced at avg `-17.56%` and `63.8%` negative rate. Treat those numbers as product-side policy replay over historical paper, not live-production proof.

## 2026-06-01: Treat Per-Symbol Sleeves As Evidence Cards, Not Ticker-Specific Strategies

Every tracked regular stock/ETF can have a per-symbol sleeve card under relevant existing lane logic, but those cards are an audit and queue-decision layer rather than permission to data-mine a separate strategy per ticker. The durable hierarchy is lane family -> shared strategy logic -> per-symbol sleeve -> status (`keep`, `watch`, `quarantine`, `rejected`, `needs-paper`, or `not_applicable`).

Use `scripts/build_regular_options_symbol_sleeves.py` before making ticker-specific claims. Its generated JSON under `data/profitability-lab/regular-options-symbol-sleeves/` and report `docs/regular-options-symbol-sleeves.md` must keep `status` separate from `evidence_class`, distinguish exact trusted intraday OPRA/NBBO proof from daily/research/backfill/display-mark evidence, and keep executable exit P&L separate from paper/mark P&L. Queue removals or quarantines from the matrix are recommendations unless scanner/playbook config is explicitly changed in a separate scoped task.

## 2026-06-01: Superseded - Default Closed Trades To Realized P&L, Keep Truth-Grade As Strict Proof

This decision is superseded by the current-policy replay default above. `Realized P&L` remains a first-class filter and still means a closed row with executable entry, trusted executable exit, and calculable realized P&L. It may include historical paper and research/backfill rows when their exits are backed by trusted exact-contract spread evidence.

`Truth-grade` remains available as a stricter production-proof filter. It still excludes historical paper, research/backfill, lifecycle-only, proof-ineligible, and legacy-unclassified rows, even when those rows have trusted realized P&L. Use Truth-grade for live-production accuracy claims; use Realized P&L for operator review and historical learning.

## 2026-05-31: Realize Historical Backfill P&L At The First Executable Suggested Close

Historical research/backfill tracked positions must use the first executable historical suggested-close point for realized P&L, not a later lifecycle placeholder close. The close must be backed by trusted exact-contract OPRA/NBBO spread evidence for the selected legs. Stop, target, profit-harvest/giveback, and time-exit checks may close a row during historical repair only when the pessimistic executable spread price can be computed from exact bid/ask rows.

Rows with no executable exit quote stay visibly missing rather than being filled with midpoint, last-trade, stale, daily/EOD, or synthetic values. The unresolved rows are data-quality work items: either import trusted exact exit-leg quotes for the specific OCC contract/date, or keep them out of realized production proof.

## 2026-05-31: Surface Trading Desk Lane Families, Not Source Variants

The Trading Desk should show operator-facing lane families rather than combining lane names with evidence-source or migration provenance. The regular `bullish_pullback_observation` lane is displayed as `Bullish Pullback`; legacy lane-label variants and source details such as `Historical Backfill` or `Live Exact` should not split the lane list or repeat in the Lane column.

Durable decision: use a clickable lane filter row near the top of tracked trades so open and closed positions can be filtered by lane family. Keep source/provenance details available in evidence-specific fields where needed, but do not use them as lane names.

## 2026-05-31: Separate Regular-Options Count Feasibility From Clean Promotion

The operator-facing phrase `200 good trades` is ambiguous and should not be used as a status label. The current regular stock-options multi-lane runner proves count feasibility: `234` trusted intraday exact trades after strict entry-date + ticker + direction dedupe, with gap `0` to the `200` count target. That count is not promotable-clean proof because the quality gate remains `quality_pending`, primarily due to Lane A coverage, rolling/OOS, paper-shadow, and conservative zero-bid exit economics.

Durable decision: use these terms precisely. `234` means multi-lane count-feasible scout evidence. `157` means the clean core-plus-clean-reference replacement baseline before Lane A. `130` means the bullish-pullback count-expanded core, and also the latest autoresearch fallback after the bad-zero-ticker/debit Lane A repair became uncountable. `0` in the multi-lane report means no count gap to `200`; `0` in the autoresearch evaluator means zero promotable-clean trades and score zero. `200` is a threshold whose meaning depends on the artifact: count feasibility in the multi-lane runner, clean promotion in the frozen evaluator. `300` is a later research-count challenge, not the current target and not cleanly achieved.

Use `scripts/run_regular_options_multilane_portfolio.py` before count claims, `scripts/evaluate_regular_options_autoresearch.py` before clean-promotion claims, and `scripts/run_regular_options_all_planned_sleeves.py` before claiming planned sleeves were tested. Historical bullish-pullback-only decisions saying `not 200+` remain true only inside that narrower lane scope and must not be used to contradict the broader regular multi-lane `234` count result.

## 2026-05-31: Make The Browser UI A Trading Desk, Not A Research Wall

The main browser experience should open as an operational options workbench. The primary surface is now the `Trading Desk`, with `Open`, `Closed`, and `Tracked Stocks` views; the live scanner and hypothetical `Paper`/paper ideas stay behind the archive toggle. The secondary surface is `Strategy Lab`, which opens replay-first as `Replay Review` and keeps slider-heavy policy editing behind `Policy Editor`.

Durable decision: do not restore contradictory first-viewport signals such as `Crypto Pilot`, long sidebar workflow essays, duplicate Open/Closed controls, walls of equal-weight metrics, account-size assumptions, lane-quality / guardrail-readback card decks, or raw quote/review/exit-reason enum columns on the primary tracked-trade view. Keep proof state, quote coverage, guardrails, warnings, and live scan picks accessible, but collapse them behind row-level evidence, scanner/archive surfaces, or intentional diagnostics when they are not the immediate action. The UI should prioritize a friend-readable open-trade board, plain-language signals such as `Hold` / `Close now`, per-trade live P&L percentages, closed-trade truth views with plain `Closed` status, and the all-tracked-stock rollup while preserving backend/API contracts.

## 2026-05-31: Keep Agent Behavior Global And Repo Memory Local

Global agent behavior rules, including the Truthfulness And Challenge Rules, live in `C:\Users\kalec\AGENTS.md` and `C:\Users\kalec\CLAUDE.md`. `options-chatbot` now has a repo-level `AGENTS.md` for repo-specific startup, active lane scope, evidence rules, and documentation placement.

Durable decision: do not copy the global truthfulness/challenge rules into every repo report or research note. Keep global behavior global; keep options-chatbot facts in the repo memory files and living docs. Generated Markdown under `data/` and `research_runs/` remains artifact evidence, not the source of truth when it disagrees with code or living docs.

## 2026-05-31: Use Profit-First 90% Stops For Live Tracked Positions

The tracked-position review engine should optimize for the strategy's expected profit, not for conservative account-risk optics. For the current paper/live-shadow use case, the configured `stop_loss_pct=90` is intentional: rows down more than `50%` can remain `Hold` if the tested strategy's profit profile expects that much option drawdown before a mechanical stop.

Durable decision: live tracked-position review now uses a `90%` effective stop cap/default instead of `50%`. A configured stop at or below `90%` is honored; stops wider than `90%` are capped to `90%` for live review. Stop and target triggers still require an executable bid/ask-derived exit quote, so display-only last-price marks remain informational and must not auto-close positions. If an executable review produces a zero exit price, paper tracking should close at `0.0` rather than leaving a total-loss option open indefinitely.

Saved position-review metrics must include `review_policy_version` and `max_live_review_stop_loss_pct` so future audits can distinguish current 90% profit-first reviews from stale historical review text generated under older stop caps.

The positions UI must not call the state-changing review endpoint during passive page load or background polling. Opening the app and reading positions should be read-only; users must explicitly refresh/review before the UI POSTs to `/api/positions/review` or `/api/suggested-trades/review`.

## 2026-05-31: Run Zero-Pick Backfill Audits Through Every Runnable Lane

There is no primary supervised options lane for historical selection audits. The zero-pick/backfill audit surface is `scripts/audit_zero_pick_days_all_lanes.py`, which iterates every supervised scan playbook, records whether each lane used a direct replay playbook or a calibration adapter, and reports lane failures explicitly instead of silently falling back to another lane's signal rules.

The older `scripts/audit_zero_pick_days_current_main_lane.py` remains available only for explicit single-lane diagnosis. Its replay builder now uses the requested lane's replay/calibration metadata and no longer injects bullish-pullback signal assumptions into non-pullback lanes. Future backfill claims must say whether they are all-lanes or explicitly single-lane; a single-lane result must not be described as "the" backfill.

After the explicit operator correction that would-have-picked rows should be tracked for learning, the 2026-05-31 all-lanes audit was applied. It wrote `data/forward-tracking/all_lanes_zero_pick_current_algo_audit_latest.json` with `14` / `14` lanes completed, `1,496` signal candidates, `1,228` exact historical candidates, and `425` tracked research/backfill picks. `scripts/migrate_main_lane_backfills_to_positions.py` now supports `--playbook-id all`, and the all-lanes migration report is `data/forward-tracking/all_lanes_zero_pick_position_migration_v1_latest.json`: `425` historical paper positions created, with all matching scan/fill log rows linked back to tracked position IDs. After the June 5 integrity repair, the current Postgres store reads `400` closed and `25` open all-lanes rows, plus `37` closed and `23` open prior single-lane rows; live scanner exposure ignores these explicitly identified research/backfill rows.

These rows should be visible and tracked because data improves the system. They remain research/backfill paper positions, not live broker fills or live-production proof.

## 2026-05-31: Treat Zero-Pick Current-Algorithm Backfills As Research Tracking Only

The zero-pick-day audit for the main stock lane is now reproducible through `scripts/audit_zero_pick_days_current_main_lane.py`. It reconstructs the current `bullish_pullback_observation` signal by historical date, resolves exact historical ThetaData OPRA/NBBO call verticals when available, and writes additive tracking rows only when the exact long/short contracts, strikes, expirations, and entry debit can be recovered.

The 2026-05-31 application appended `60` historical backfilled picks to `data/forward-tracking/scan_picks.jsonl` and matching `backfilled_historical_track` rows to `data/forward-tracking/fill_attempts.jsonl`. It also recorded `18` `research_backfill` sessions in `data/options-validation/forward_tracking.db`. These rows answer "what would the current algorithm have picked on no-pick days?" but they are not live-production proof, not broker fills, and not authoritative forward evidence.

Durable decision: missed historical candidates may be tracked for research and review only when exact historical contracts are available. Do not relabel these backfills as `live_production`, and do not create live/open tracked positions from them without a separate explicit paper-position migration.

## 2026-05-31: Migrate Zero-Pick Backfills Into Paper Positions Only On Explicit Request

After the explicit paper-position request, `scripts/migrate_main_lane_backfills_to_positions.py` migrated the `60` zero-pick audit rows into the Postgres `tracked_positions` store used by the UI. The migration is idempotent by `backfill_signature`, keeps `backfill_audit_id=main_lane_zero_pick_current_algo_v1` in the source snapshot, and updates the scan/fill logs with the created tracked position IDs.

The migration reconciles lifecycle state as of `2026-05-31`: `27` positions remain open, while `33` are closed by historical time-exit. None closed primarily by expiry because the organic time-exit schedule arrived first. Of the closed rows, `21` have trusted historical spread-exit pricing and P&L; `12` are lifecycle-only closed rows because one or both exit-leg quotes were unavailable, so no exit P&L is assigned.

Durable decision: these are historical paper tracked positions, not broker fills, not live-production proof, and not authoritative forward evidence. Future zero-pick audits may be migrated into paper positions only after explicit operator approval, and any lifecycle-only closed rows must stay visibly unpriced rather than receiving synthetic zero-value losses.

## 2026-05-31: Test All Implemented Planned Regular Sleeves Before Ranking

The regular stock-options search should not rank planned sleeves by intuition and stop. It now has an all-planned-sleeves runner at `scripts/run_regular_options_all_planned_sleeves.py`, with artifacts under `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/`, that executes every currently implemented planned regular stock-options variant end-to-end, computes standalone metrics, robustness, and strict-new contribution versus the `157`-trade core-plus-clean-reference stack, and records planned lane-lab specs that are blocked or not yet testable.

The merged 2026-05-31 audit tested `24` implemented variants with `0` run failures and recorded `30` planned specs as blocked/not yet testable. No currently implemented planned sleeve both closes the `43`-trade gap to `200` and clears quality. The largest count additions fail economics or coverage: tracked-winner QQQ adds `109` strict-new exact rows but is only PF `1.04` with `54.2%` coverage and stress PF `0.68`; tracked-winner no-XLK adds `83` but has only `49.7%` coverage and stress PF `1.10`; volatility-expansion calls and high-beta momentum each add `44` strict-new rows but are unprofitable. Profitable overlap sleeves such as move-bucket refill, REIT/industrial refill, and defensive refill remain useful diagnostics but do not close the clean gap because they overlap heavily or fail coverage.

The follow-up repair/unlock pass expanded the runner to `30` implemented variants. It added current trusted ThetaData readiness, cleared the stale `IWM` thin-data blocker, implemented a `relative_strength_pullback` replay hook, and tested tracked-winner, cheap-debit continuity, fast-volatility, narrowed-bearish, and relative-strength variants. The first near-gap read showed `tracked_winner_chain_native_googl_nvda_time65_all_sleeves` at `49` strict-new exact trades, PF `1.98`, avg `+18.27%`, and stress PF `1.39`, but quote coverage was only `68.1%` with `23` unpriced candidates. A targeted side-aware replay batch later priced the tracked-winner missing exits and rejected the apparent repair path: no-SPY fell to conservative combined PF `0.62`, and GOOGL/NVDA fell to conservative combined PF `0.67`. These are adverse zero-bid/exit economics, not harmless coverage misses to patch into the clean count. The next pass expanded the implemented surface to `33` variants by making `iwm_small_cap_risk` runnable through the existing per-ticker sleeve plus risk-on/risk-off WFO probes; the current IWM shapes are diagnostic only, not promotable. The best IWM WFO call shape has `18` exact trades, PF `1.25`, full coverage, `7` strict-new rows, and stress PF `0.87`; the existing per-ticker IWM sleeve is PF `2.47` but only `11` exact trades at `73.3%` coverage; the put shape is unprofitable. A first liquidity-first tracked-winner contract-hygiene probe then expanded the surface to `34` variants, but it failed the proof gates despite count: `102` strict-new exact trades, PF `0.81`, avg `-5.22%`, coverage `58.0%`, and stress PF `0.53`.

Durable decision: treat the all-planned runner as the audit surface for "have we tested the sleeves?" claims. Currently implemented planned sleeves are tested; remaining lane-lab specs are not dismissed, but they must be converted into runnable tests by importing missing underlyings or adding the required paper-log, spread, event, breadth, intraday, portfolio, exit, or structure instrumentation. Side-aware zero-bid replay is a proof gate before any gap-closing spread sleeve can be called repairable. Do not spend the next profitability cycle on GOOGL/NVDA survivability unless a new causal selection rule first changes the zero-bid economics. Historical WFO chain-native spread replays now persist diagnostic-only selected-spread, top-alternative, and fill-degradation fields; use them to design liquidity-first causal rules, but never as proof-gate relaxations. The first liquidity-first tracked-winner hygiene shape is rejected; do not tune that failed shape as a count hack. Sector ETF trusted data is now complete, but the first regular sector ETF shapes are also rejected, so the next implementation focus should be a materially different causal liquidity/exit rule under the frozen evaluator.

Sector ETF import decision: the planner is allowed to treat Theta v3's retired `/v2/system/status` `410 Gone` response as "terminal reachable" when local v3 quote requests work, but this is not proof readiness. Theta v3 sector import commands must use the documented hourly interval spelling `1h`, not legacy `60`, and must keep the trusted OPRA/NBBO proof gates unchanged. The 2026-06-01 sector imports now cover `252` shared trusted dates for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` from `2025-05-22` through `2026-05-22`, and the planner reports `ready_for_sector_replay`. Targeted `10:10` entry and min-DTE-0 `15:45` exit backfills are allowed when they resolve exact-contract evidence for a focused replay, but they do not relax proof gates.

Regular sector replay decision: the first focused XLE/XLF/KRE/SMH/TLT and sector-rotation all-planned variants are rejected, not promotable scouts. Batch `20260601T015109Z` excluded GLD/commodity-lane sleeve implementation, kept the work inside the regular lane, and found no candidates for TLT, XLE, XLF, or KRE. SMH priced `17` of `20` candidates at PF `0.49`, avg `-15.45%`, coverage `85.0%`, and stress PF `0.32`; the regular sector ETF stack priced `16` of `23` candidates at PF `0.27`, avg `-32.36%`, coverage `69.6%`, and stress PF `0.19`. Do not tune these simple sector shapes as count hacks.

## 2026-05-31: Freeze A Regular-Options Autoresearch Evaluator Before Goal Loops

Karpathy-style `/goal` loops for the regular stock-options stack must run against a frozen evaluator instead of optimizing raw trade count. The evaluator is `scripts/evaluate_regular_options_autoresearch.py`, the operator prompt is `docs/autoresearch/regular-options-goal.md`, and the append-only experiment ledger is `data/profitability-lab/regular-options-autoresearch/ledger.jsonl`.

The hard historical `promotable_clean` gates are: trusted intraday OPRA/NBBO exact evidence only, strict date+ticker+direction dedupe, clean count `>=200`, PF `>=1.50`, avg PnL `>0`, effective quote coverage `>=97.5%`, unresolved candidates `0`, 5%/side stress PF `>=1.25`, rolling/OOS pass, side-aware conservative zero-bid replay whenever Lane A is counted, Lane A conservative PF `>=1.30`, and zero-bid exit rate `<=2%`. Production readiness additionally requires paper-shadow status `passed`. The evaluator itself, its gates, and the ledger schema are not editable during a `/goal` strategy experiment.

The first baseline row was recorded on 2026-05-31 with `score: 0.00`, `research_score: 177.74`, status `scout_or_blocked`, clean count `0`, scout count `234`, PF `2.16`, avg `+26.76%`, effective side-aware coverage `96.71%`, unresolved `14`, stress PF `1.53`, zero-bid exit rate `41.99%`, and Lane A conservative PF `0.85`. Therefore the next goal is not to chase `300`; it is to repair Lane A zero-bid survivability under the frozen conservative evaluator and preserve as many clean trades as possible.

The first Lane A goal loop added `scripts/run_regular_options_goal_experiment.py` as the experiment harness and tested entry survivability, debit/width, DTE, early-exit, and bad-zero-ticker repair probes. The simple entry filters improved effective coverage but did not fix Lane A conservative economics: the best entry-filter variant had coverage `99.09%`, unresolved `3`, and Lane A conservative PF `0.88`. The broad bad-zero-ticker/debit repair proved that Lane A conservative PF can be brought to the gate (`1.30` to `1.36`), but only by shrinking the lane to `72` to `81` exact trades, below the `100` exact-trade portfolio-candidate threshold. The 2026-06-01 causal memory loop tested selected-contract short-expiry/strike exit-failure memory and ticker symbol-health memory. The best branch, `lane_a_goal_stop200_time75_symbol_health90_backfill`, improved effective coverage to `99.06%` and unresolved candidates to `3`, but remained blocked with clean count `0`, scout count `191`, zero-bid exit rate `43.24%`, and Lane A conservative PF `0.92`. Existing clean bullish-pullback replacement sleeves also do not close the gap: core plus clean reference reaches `157` strict-deduped trades, and core plus clean reference plus all high-PF/coverage cluster artifacts reaches only `158` because the trades overlap heavily. Current trusted-intraday tracked-winner reruns failed as a replacement; the time65 QQQ rerun reached `109` exact trades but only PF `1.04`.

Durable decision: do not spend the next loop on more small Lane A entry-liquidity or memory variants. Lane A remains useful as a diagnostic and count challenge, but the next `200` clean-trade attempt should be a genuinely non-overlapping regular stock-options sleeve or materially different exit/liquidity rule with a target of at least `43` strict-new clean trades over the `157` core-plus-clean-reference stack. Goal experiment runs must keep evaluator artifacts experiment-scoped by default; writing global autoresearch `latest.json` is an explicit operator action via `--write-global-latest`, not the default behavior.

## 2026-05-30: Use The Regular Multi-Lane Runner For The 200-Trade Question

The regular stock-options trade-count question now has a dedicated portfolio readback: `scripts/run_regular_options_multilane_portfolio.py`, with artifacts under `data/profitability-lab/regular-options-multilane/` and report `docs/regular-options-multilane-2026-05-30.md`. The runner normalizes available regular stock-lane replay artifacts, separates proof-grade trusted intraday OPRA/NBBO exact rows from daily/EOD or scout evidence, dedupes portfolio candidates by entry date, ticker, and direction, and reports the combined gap to `200`.

The corrected current result clears the target: `234` trusted intraday exact trades after strict dedupe, PF `2.16`, avg `+26.76%`, and a `0`-trade gap to `200`. The count comes from the current `sleeve_pf59_coverage_a_refill_v1` core plus the latest `lane_a_chain_native_ret20_4_stop200_time75_rerun4_v1` exact replay. The earlier `130`-only conclusion was too narrow because it omitted this regular stock-lane artifact.

The durable policy is to use this multi-lane runner before making trade-count claims and to treat `200` as count feasibility, not production readiness. The runner now carries a `200` quality gate: the count status is `passed`, but overall status is `quality_pending` because Lane A still has `137` unpriced candidates, `53.1%` quote coverage, and rolling-OOS watch status after exact and lookahead fill passes. Lane A priced-only economics are strong (`155` exact trades, PF `3.59`, avg `+42.34%`, 5%/side stress PF `2.65`), but a 2026-05-31 side-aware zero-bid replay found the `111` classified missing short legs were exact `bid=0, ask>0` rows rejected by the then-current importer/classifier `bid > 0` rule, not truly absent contracts. Conservative long-bid/short-ask replay priced `126` of `127` missing-exit candidates, with `118` priced rows using at least one zero-bid exit quote; those rows alone were PF `0.11` and avg `-66.59%`, and combined Lane A falls to `281` priced trades at PF `0.85`, avg `-6.51%`, and `96.2%` coverage. Therefore the coverage blocker remains valid, but the blocker should be framed as zero-bid short-leg exit risk and side-aware replay debt rather than pure provider no-match absence.

A 2026-05-31 contrarian scan found that `300` exact-priced trades are plausibly reachable from existing trusted intraday artifacts only under a research/count interpretation, not a production-quality one. The strongest omitted count family was `tracked_winner_chain_native_qqq_time80_research`; older artifacts added up to `120` strict-new exact trades over the current `234` stack, but only with roughly `50%` to `54%` quote coverage and 100+ unpriced candidates. A current trusted-intraday rerun is now visible in the runner as `tracked_winner_chain_native_qqq_time80_intraday`: it adds `102` strict-new exact trades, but fails promotion at PF `1.36`, avg `+9.52%`, `51.8%` coverage, and `95` unpriced candidates, with `77` latest missing leg/date items classified as provider no-match exact contracts. Clean/high-coverage existing artifacts do not solve `300`: clean coverage>=97.5 and unpriced=0 artifacts union to only `134` strict keys, and the clean bullish-pullback reference adds only `17` strict-new rows. Therefore, do not say all regular lanes are exhaustively tested or that `300` is structurally impossible; keep explicit research-count challenges visible in the runner while keeping low-coverage artifacts out of production readiness. Daily/EOD artifacts, unresolved candidates, weak bearish/range/volatility probes, and duplicate presentations of the same date/ticker/direction trade do not improve the production proof count.

## 2026-05-29: Freeze The Bullish-Pullback Layer Stack And Do Not Promote Defensive Refill

The next few bullish-pullback profitability layers are now frozen in `data/profitability-lab/bullish-pullback-observation/layer-stack/latest.json` and summarized in `docs/bullish-pullback-layer-stack-2026-05-29.md`, generated by `scripts/build_bullish_pullback_layer_stack.py`. The stack preserves the exact-evidence layers that are usable for paper-shadow work: S/A/B confidence core (`108` exact, PF `4.86`), high-PF cluster (`113` exact, PF `4.34`), high-coverage quoted cluster (`115` exact, PF `2.57`), clean cluster component (`95` exact, PF `3.36`), clean exact mixed branch (`129` exact, PF `2.20`, `100%` coverage), count-expanded branch (`130` exact, PF `2.04`, `97.7%` coverage), high-PF 130-trade reference (`130` exact, PF `2.53`), and S-only timecluster watch (`95` exact, PF `3.71`).

The defensive refill scout is not promoted after a fresh exact-fill attempt. `sleeve_next_defensive_refill_v1` reran as `data/options-validation/runs/20260529_180218_sleeve_next_defensive_refill_v1_intraday.json` after `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260530T000025Z.json`; ThetaData returned `497` normalized rows but `0` new trusted rows because they were duplicates. The post-import replay stayed at `130` exact trades, PF `2.15`, `94.2%` coverage, and `8` unpriced candidates. Its WMT/PM refill tier is weak (`22` exact trades, PF `1.36`, avg `+7.66%`), and the remaining `10` missing legs classify as provider no-match exact OCC contracts with `0` executable exact rows on the missing dates.

The durable decision is to wire and monitor the frozen layer stack rather than continue broad refill tuning. Refill branches that improve headline count but fail coverage, stress, rolling-OOS, or incremental-tier profitability remain research-only. This remains paper-shadow research, not live-capital approval.

## 2026-05-29: Stop Bullish-Pullback Next-Layer Expansion At The Current Evidence Ceiling

The `bullish_pullback_observation` next-layer count expansion was tested as separate frozen hypotheses instead of by loosening the current lane. The tested lanes were ETF/index (`QQQ`, `DIA`, `XLK`), high-beta (`NVDA`, `AMZN`, `TSLA`), defensive/refill (`WMT`, `PM`), REIT/industrial (`PLD`, `CAT`), and a combined move-bucket refill. New variant definitions live in `scripts/run_bullish_pullback_sleeves.py`, and the latest result round is `data/profitability-lab/bullish-pullback-observation/sleeves/sleeve_round_20260529T052213Z.json`.

ThetaData v3 was started on `127.0.0.1:25503`, then exact missing-contract imports and broader targeted imports were run before replay/classifier/robustness reruns. The move-bucket exact import `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260529T042508Z.json` imported `3,676` trusted rows, targeted UNH/CAT/PM/WMT/JNJ bulk imports added over `5.7M` trusted rows, and expanded-count import `20260529T050747Z.json` imported `6,743` trusted rows. Required second exact-fill cycles were duplicate-only. Post-import readiness is `ready_for_exact_replay` with `12,981,701` trusted intraday ThetaData OPRA/NBBO rows and `252` shared quote dates across all `59` required underlyings.

No move-bucket branch is promoted as the next profitability layer. `sleeve_next_move_bucket_refill_v1` improved to `135` exact trades after import, but quote coverage is only `91.2%`, 5%/side stress PF is `1.49`, and `13` candidates remain unpriced. The highest-count tested branch, `sleeve_pf59_s_themeA_no_ticker_bans_v1`, reached `165` exact trades but failed at PF `1.49`, 5%/side stress PF `1.12`, and rolling-OOS watch status. ETF/index adds too few trades; high-beta exact replays are decisively unprofitable; WMT remains weak as a count filler; PM, PLD, and CAT are promising only as small scouts. The best clean 100%-covered branch remains `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timecombo55_50_75_mixed_v1`: `129` exact trusted trades, PF `2.20`, avg `+28.97%`, rolling test PF `2.51`, 5%/side stress PF `1.67`, and no unresolved candidates. The best 130-trade PF branch remains `sleeve_winner_cluster_exit_balanced_quoted_v1`: `130` exact trades, PF `2.53`, avg `+33.20%`, rolling test PF `2.77`, and 5%/side stress PF `1.91`, but it is still blocked by `3` unresolved candidates.

The durable policy is to preserve the current keep/move/remove buckets and stop broad count expansion until a genuinely different causal signal or longer-history exact validation changes the evidence. In this bullish-pullback-only scope, the current exact annual ceiling is roughly `129` to `130` robust trusted trades, with non-promoted scouts up to `165` exact trades that fail reliability gates, not `200+`. Remaining unpriced candidates classify as provider no-match exact OCC contracts with same-expiry trusted chain rows present and `0` exact executable rows found after import attempts. This remains paper-shadow research only, not live-capital approval, and is superseded for broader regular-lane count questions by the regular multi-lane runner.

## 2026-05-28: Use Per-Ticker Audit To Separate Current Picks From Scout Lanes

The `bullish_pullback_observation` lane now has a per-ticker audit at `docs/bullish-pullback-ticker-audit-2026-05-29.md` and `data/profitability-lab/bullish-pullback-observation/ticker-audit/latest.json`, generated by `scripts/audit_bullish_pullback_ticker_evidence.py`. It uses exact trusted ThetaData intraday OPRA/NBBO evidence only and reconciles subagent slice reviews across the full `59`-symbol active universe.

The current bullish-pullback tradable queue is limited to `10` keep symbols: `IWM`, `AAPL`, `GOOGL`, `UNH`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, and `NEM`. These are supported by S/A/B confidence evidence totaling `108` exact quoted trades at PF `4.86` and avg `+53.22%`. The count-expanded all-59 candidate remains `130` exact trades at PF `2.04`, leaving a `70` to `92` exact-trade gap to the desired `200+` annual cadence.

Positive or strategically important symbols that are weak under the current bullish-pullback hypothesis are moved to separate frozen hypotheses rather than forced into current picks: `QQQ`, `DIA`, `XLK`, `NVDA`, `AMZN`, `TSLA`, `WMT`, `PM`, `CAT`, and `PLD`. `JPM`, `BAC`, `C`, `ABBV`, `SLB`, `RTX`, `FCX`, `COIN`, and `PLTR` are removed from the current bullish-pullback tradable queue. The remaining `30` symbols stay research/data-needed until exact evidence proves a repeatable pattern.

This decision answers the bullish-pullback-only trade-count concern directly: the lane is not failing because the stock universe is uninteresting; it is failing to reach `200+` exact annual trades because many tracked symbols do not show repeatable bullish-pullback evidence in the current one-year exact-contract sample. The next profitability layer should be separate ETF/index, high-beta momentum/volatility, defensive-income/refill, REIT/rate-sensitive, and industrial/commodity hypotheses, not looser current-lane gates. For the broader regular stock-options count question, use the regular multi-lane runner instead.

## 2026-05-28: Add Confidence Tiers And Count-Expanded All-59 Paper-Shadow Candidate

The `bullish_pullback_observation` lane now has a confidence-tier report at `data/profitability-lab/bullish-pullback-observation/confidence/latest.json`, generated by `scripts/analyze_bullish_pullback_confidence_tiers.py`. It scores exact quoted time-exit trades from frozen sleeve artifacts and reports S/A/B/C/Blocked evidence across the full `59`-symbol active universe. Current S/A/B contains `108` exact quoted trades across `10` symbols at PF `4.86`, avg `+53.22%`; S alone is `72` trades at PF `5.98`, avg `+59.68%`. C and Blocked are retained as scout evidence only because they are not profitable as groups.

The count-expanded all-59 candidate is `sleeve_pf59_coverage_a_refill_v1`; after the 2026-05-29 v3 rerun the current artifact is `data/options-validation/runs/20260528_224313_sleeve_pf59_coverage_a_refill_v1_intraday.json`: `133` candidates, `130` exact quoted trades, `3` unresolved provider no-match candidates, `97.7%` quote coverage, PF `2.04`, avg `+24.54%`, and all priced exits are bid/ask `time_exit` fills. Its robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_pf59_coverage_a_refill_v1.json` passes the available `50/20` rolling window with `34` exact test trades at PF `2.35`, avg `+33.33%`, no unpriced test candidates, and 5%/side stress PF `1.53`; robustness status remains `blocked` because the `3` full-sample candidates are still unresolved.

Broad forced all-59 refill variants are not promoted. They can raise exact trade count, but the incremental refill tiers lowered PF, coverage, or rolling-OOS reliability: after v3 imports `sleeve_pf59_s_ab_timecluster_v1` reached `146` exact trades at PF `2.23` with only `89.6%` coverage and rolling-OOS watch status, `sleeve_pf59_s_themeA_no_ticker_bans_v1` reached `165` exact trades but fell to PF `1.49`, and `sleeve_pf59_coverage_clean_v1` reached `146` exact trades but stayed below gate at PF `1.66`. The durable policy is to take more trades from proven sleeves and confidence tiers, not to force every tracked stock to generate a trade when its exact-contract evidence is weak.

This is a paper-shadow/research decision, not live-capital approval. Missing-contract classification for the count-expanded branch found `3` provider no-match exact-contract gaps in WMT/JNJ with same-expiry chains present. The refreshed ThetaData v3 imports wrote `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260529T042128Z.json` and `20260529T042332Z.json`; both returned `0` normalized rows for those exact contracts, so the remaining WMT/JNJ gaps are not unresolved local import work.

## 2026-05-28: Promote Ticker-Cluster Fixed-Exit Sleeves For Profitability-First Paper/Live-Shadow

The no-CAT/no-PM hard-prior fixed-time family is extended by ticker-cluster exit schedules. The high-PF quoted branch is `sleeve_winner_cluster_exit_50_55_60_no_pld_xlk_v1` at `data/options-validation/runs/20260528_013544_sleeve_winner_cluster_exit_50_55_60_no_pld_xlk_v1_intraday.json`: `120` candidates, `113` exact quoted trades, `7` unresolved provider no-match candidates, `94.2%` quote coverage, PF `4.34`, avg `+49.78%`, median `+54.20%`, and all priced exits are bid/ask `time_exit` fills. Its robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_cluster_exit_50_55_60_no_pld_xlk_v1.json` passes the available `50/20` rolling window, stays positive under 5%/side stress at PF `3.23`, after top-5 winner removal at PF `3.60`, after worst-ticker removal at PF `3.72`, and after worst-month removal at PF `3.10`.

The higher-coverage quoted branch is `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_v1` at `data/options-validation/runs/20260528_014057_sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_v1_intraday.json`: `116` candidates, `115` exact quoted trades, `1` unresolved JNJ provider no-match candidate, `99.1%` quote coverage, PF `2.57`, avg `+34.29%`, and all priced exits are bid/ask `time_exit` fills. Its robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_v1.json` passes the available `50/20` rolling window with `36` exact test trades at PF `2.59`, avg `+41.86%`, and no unpriced test candidates; it stays positive under 5%/side stress at PF `1.95`, after top-5 winner removal at PF `2.08`, after worst-ticker removal at PF `2.08`, and after worst-month removal at PF `2.06`.

The clean quoted proof subset is `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_jnj_v1` at `data/options-validation/runs/20260528_014353_sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_jnj_v1_intraday.json`: `95` candidates, `95` exact quoted trades, `0` unresolved candidates, `100.0%` quote coverage, PF `3.36`, avg `+44.07%`, and all exits are bid/ask `time_exit` fills. It is useful as a clean component sleeve but is not the main paper family because it is below the `100`-trade target and has no full `50/20` rolling window.

This remains profitability-first paper/live-shadow approval only, not live-capital or strict proof-complete approval. A bounded ThetaData exact-fill pass for the cluster branches wrote `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T074150Z.json`, imported `0` new rows, and left the remaining misses classified as provider no-match exact-contract gaps with same-expiry chains present. The implementation also adds `spread_time_exit_basis="calendar_elapsed"` support for future fixed-exit tests, but the integrated calendar branch did not beat the cluster variants.

## 2026-05-28: Promote Fixed-Time No-CAT/No-PM Sleeves For Profitability-First Paper/Live-Shadow

The hard-prior no-CAT/no-PM branch is superseded by fixed-time variants that reduce noisy interim exit quote dependence while preserving exact-contract pricing. The current strict quoted-exit lead is `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly55_v1` at `data/options-validation/runs/20260528_005918_sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly55_v1_intraday.json`: `119` candidates, `112` exact priced trades, `7` unresolved candidates, `94.1%` quote coverage, PF `3.04`, avg `+37.41%`, median `+36.76%`, win rate `67.0%`, and every priced exit is a bid/ask `time_exit`.

The latest robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly55_v1.json` passes the available `50/20` rolling window with `30` exact test trades at PF `2.24`, avg `+28.58%`, and no unpriced test candidates. It stays positive under 5%/side slippage stress at PF `2.26`, after top-5 winner removal at PF `2.44`, and after worst-ticker removal with `NEM` removed at `97` trades and PF `2.50`.

The higher-coverage quoted fallback is `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly50_v1`: `116` exact trades, `3` unresolved candidates, `97.5%` quote coverage, PF `2.59`, and rolling test PF `2.33`. The fully priced sibling is `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly75_v1`: `118` exact trades, `0` unresolved candidates, PF `2.16`, and rolling test PF `1.70`, but it has `49` expiration-settlement exits and must not be presented as a pure quoted-exit proof.

This is a profitability-first paper/live-shadow decision, not live-capital approval. Remaining caveats are limited OOS windows, unresolved provider no-match candidates in the quoted sleeves, month/ticker concentration, and expiration/assignment handling for the `timeonly75` sibling. The implementation now forces time-only spread exits to check expiry-date quotes before intrinsic settlement, covered by `tests/test_chain_native_continuity.py`.

## 2026-05-28: Superseded Hard-Prior No-CAT/No-PM Winner Clean-Plus-Liquid Lead

The earlier `bullish_pullback_observation` profitability-first lead was `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_v1`. It derives its symbols from the active universe, keeps `CMCSA` inactive, excludes `CAT` and `PM`, requires one prior exact quote day within a 30-day lookback on selected legs, and uses exact-contract ThetaData intraday OPRA/NBBO rows for P&L. Its replay has `104` exact priced trades at PF `4.09`, avg `+39.46%`, 5%/side stress PF `2.94`, top-5 winner removal PF `3.53`, and one rolling OOS window with `21` exact test trades at PF `4.22` and no unpriced test candidates.

This is a profitability-first research/paper-shadow lead, not strict proof-complete promotion or live-capital approval. It still has `15` unpriced selected candidates at `87.4%` quote coverage, and the remaining misses classify as provider no-match exact-contract gaps after bounded exact-fill loops. The latest fill pass normalized `10` rows but imported `0` new rows because they were duplicates. A skeptical audit accepted the no-CAT/no-PM family for conditional paper/live-shadow only, with the largest residual risks being unresolved provider no-match exits, concentration in NEM/GOOGL/top months, thin rolling OOS, and too few dense/promotable exact trades for strict promotion.

The raw-profit sibling `sleeve_winner_clean_plus_liquid_no_cat_v1` remains worth tracking because it has `104` exact trades, PF `4.38`, avg `+40.58%`, and rolling OOS PF `6.48`, but its quote coverage is lower at `85.2%`. The stricter tradability sibling has `100` exact trades and PF `4.61`, but one unpriced OOS test candidate, so it is a watch-list candidate rather than the lead. Future work should focus on causal contract-level replacement and backfill for provider no-match risk rather than broad ticker bans or additional alpha curve-fitting.

## 2026-05-28: Superseded Profit-Theme Sleeve Lead

The earlier leading `bullish_pullback_observation` family was `sleeve_profit_theme_stack_v1`, not the pooled broad baseline. The sleeve stack combines the profitable energy, defensive, liquid-core, materials/metals, and index-core subgroups with tier-aware backfill and allocation caps. Its exact-contract replay has `122` priced exact trades at PF `1.75`, avg `+16.54%`, and 5%/side stress PF `1.25`.

This is a profitability research lead, not strict historical promotion proof. It still has unresolved selected candidates caused mostly by short-leg exit quote no-matches. Future work should focus on contract-selection and execution-survivability replacement rules that reduce missing short-leg exits while preserving the `100+` exact trade count, rather than adding broad alpha filters first.

## 2026-05-27: Do Not Promote The OOS-Positive Plus3 Near-Miss Until Dense And Unpriced Gates Clear

`lane_a_shortbid05_excl_oos_unpriced_plus3_backfill15` is not promoted to paper/live-shadow even though its exact-contract full-sample, rolling OOS, winner-removal, symbol/month holdout, and slippage-stress economics are positive. The lane still has unresolved selected candidates and only `12` promotable dense exact trades, below the `25` dense/promotable threshold.

Future promotion requires a causal pre-entry skip/backfill policy or audited data-quality classification that prevents unresolved selected candidates from supporting the lane, plus enough dense exact-contract trades to justify live-shadow. Excluding symbols after observing unpriced OOS failures is a research diagnostic unless the exclusion rule is frozen as a forward policy and retested.

## 2026-05-27: Keep Bullish Pullback In Research Until Exact OOS Gates Clear

The full active-universe ThetaData intraday OPRA/NBBO replay for `bullish_pullback_observation` is not promoted to paper or live-shadow. Positive full-sample exact-contract profitability is insufficient when unresolved candidates, weak rolling OOS gates, or sub-100 exact executable trade counts remain.

Profitability claims for this lane must use exact-contract trades only. Nearest-listed rows are research-only, and unresolved/unpriced replay rows must retain their concrete `unpriced_reason` in reporting so provider no-match, missing exit quote, and no-chain-native-spread failures are not conflated with nearest-listed substitutions.

## 2026-05-25: Keep Agent Worktrees Clean And Push Big Fix Sets

Large verified agent changes should not remain only as local dirty state. When an agent or automation produces a broad audit fix set, cross-subsystem changes, new required tests/scripts/docs, or release-blocking fixes, it should branch with the `codex/` prefix, commit, push, and report the branch or PR when publishing is allowed.

Small local fixes may stay unstaged when the user asked only for investigation or local patching, but every run must report required untracked files and generated artifacts. See `docs/agent-worktree-hygiene.md` for the working contract.

## 2026-05-25: Narrow Active Work To Regular Options And AI Commodity Options

Active project work is limited to the regular supervised options lane family and the separate AI commodity / commodity-infrastructure options strategy lane for the foreseeable future.

The crypto options lane, Polymarket lane, and day-trading lane are out of scope for implementation, performance work, research cycles, documentation work, and automation work unless the user explicitly asks to archive, remove, repair, or reopen one of those lanes. The day-trading lane is paused rather than deleted.

Future agents should avoid optimizing or debugging `crypto_options/*`, `src/lib/polymarket/*`, `src/lib/day-trading/*`, and related lane-specific scripts/tests/docs unless that work is required by shared infrastructure for the active regular options or AI commodity options lanes.

## 2026-05-23: Superseded - Treat Bullish Pullback As A Scheduled Fallback, Not The Main Lane

This decision is superseded by the 2026-06-02 all-lanes audit decision and the 2026-06-03 lane-parity correction. `bullish_pullback_observation`, surfaced as Bullish Pullback, remains a managed broad liquid-universe bullish call-vertical lane and may be used as a scheduled scan fallback when no playbook is supplied. That routing fallback is not a product-priority statement. All configured regular-options lanes are peer lanes and need lane-specific profitability, risk, execution, and proof validation.

`tracked_winner_primary` remains a selectable peer lane; its promotion status depends on the same proof bars as every other regular-options lane. `quality90_debit55_canary` remains the proof/control yardstick and requires executable OPRA paper candidates.

No lane should be treated as promoted for sizing beyond the coded caps until closed forward Alpaca OPRA results show positive expectancy and pass the configured proof gates.

## 2026-05-22: Keep Alpaca OPRA As Preferred Proof, Add ThetaData NBBO Import Readiness

The commodity lane remains locked to Alpaca SIP/OPRA forward captures for the preferred proof path because the current repo artifacts isolate `alpaca_opra_daily_snapshot` as the accepted proof source. The latest living-state docs show `3` of `100` required shared dates captured; this decision records the policy, while `docs/current-state.md` and `docs/NEXT_STEPS.md` carry the current count.

Historical Alpaca option bars and trades remain research-only because they do not preserve executable bid/ask BBO. Latest quote/snapshot surfaces count only when captured forward into the exact store.

ThetaData v3 historical option quotes are a valid OPRA-backed acquisition candidate if a licensed Standard/Pro terminal is available. A new importer was added to normalize ThetaData v3 NBBO rows into the existing historical option snapshot schema, but those rows must be isolated by source label and audited before any proof claim.

An authenticated Alpaca access probe on 2026-05-22 confirmed that the configured credentials can read historical option bars and trades and current/latest OPRA quote surfaces, but the suspected historical option quotes route `GET /v1beta1/options/quotes` returns 404. Until Alpaca exposes account-accessible historical bid/ask quotes, Alpaca can only accelerate the exact proof path through forward-captured latest OPRA snapshots.
