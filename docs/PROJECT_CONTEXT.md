# Project Context

This repository is a mixed Next.js and FastAPI options research system. The active regular-options product is a supervised lane family for scanning, replay diagnostics, paper ideas, and tracked-position review. The browser UI is organized as a `Trading Desk` for open/closed positions, Alpaca paper-tracked positions, and an all-tracked-stock rollup, with live scan picks available behind the archive toggle, plus a replay-first `Strategy Lab` for validation and policy editing. FastAPI `python-backend/main.py` remains the app composition root, while profile, predictions, and tools routes are extracted into late-bound route modules so LLM agents can read route ownership without losing test-time monkeypatch behavior. Decorator-free application services now include `python-backend/proof_summary_service.py` for `/api/proof-summary`, `python-backend/replay_profit_service.py` for replay/profit readback assembly, and `python-backend/alpaca_paper_trading.py` for opt-in Alpaca paper order submission from proof-gated scanner-origin creates; proof, replay, scanner policy, broker-paper, and profit-cycle semantics stay in the domain modules named by `docs/replay-profit-contract.md` and `docs/DECISIONS.md`. The Trading Desk now treats open tracked positions, Alpaca paper-tracked rows, and open paper ideas as operator review surfaces, while closed/history-heavy rows load on demand through paged read routes. Closed Trades defaults to `Truth-grade`, the strict production-proof filter for live-production accuracy claims. `Realized P&L` remains available as a broader executable historical-learning slice, but historical current-policy guardrail replay is no longer surfaced in the operator dashboard because it can be mistaken for current recommendations or forward-audit performance.

The proof/evidence contract for those views is versioned at `data/contracts/proof-evidence-contract.json` and explained in `docs/proof-evidence-contract.md`. Backend proof predicates remain authoritative; frontend evidence groups flow from generated `src/lib/generated/proofEvidenceContract.ts` through `src/lib/trading-desk/proofContract.ts` as display/readability wrappers around the same proof classes, quote evidence classes, research/backfill markers, top-level and source-snapshot backfill/migration identity fields, and exit-basis tokens. Compact Trading Desk rows emit read-time `evidence_group` and `quote_evidence_class` diagnostics from the same contract; those labels are not persisted authority and stale frontend labels fail closed by contract version. Read-only audit and research reports use `scripts/quote_evidence_readback.py` for the same quote-class vocabulary, while separately labeling research/backfill row policy and production-proof falsehood. The generated `docs/proof-invariant-table.md`, sourced from `data/contracts/proof-invariant-cases.json`, is the shared backend/frontend regression matrix for raw exact, production proof, Truth-grade, and realized-P&L boundaries.

The local agent-control memory graph is an active orchestration surface, not a trading authority. `scripts/agent_control.py` owns the runtime SQLite graph under ignored `data/agent-control/`; future agents should recover context with `npm run memory:bootstrap`, build focused handoffs with `npm run memory:context`, audit automation with `npm run memory:operator-dashboard`, sync allowlisted generated profitability readbacks with `npm run memory:profit-learning-sync`, audit that sync with `npm run memory:profit-learning-audit`, and inspect research-only provenance with `npm run memory:research-priorities`. Memory graph v2 stamps accepted memory with `authority_scope=orchestration_only`, uses `retrieval_documents` FTS/BM25 plus source-quality explanations for graph queries, writes context manifests for audit, and records zero-candidate/profit-learning episodes and hypotheses as research provenance only. None of these memory surfaces authorize cohort append, evidence mutation, quote import, scanner/strategy changes, proof-bar changes, live validation, auto-track, broker action, stop/sizing changes, promotion, protected-holdout use, or treating historical rows as forward proof.

AI commodity / commodity-infrastructure options is a separate proof-first strategy lane under `data/ai-commodity-infra/` and `scripts/run_ai_commodity_opra_progress.py`. The generated isolation owner is `docs/ai-commodity-isolation.md`, backed by `data/contracts/ai-commodity-isolation.json`. Its preferred final proof path is Alpaca SIP/OPRA bid/ask snapshot replay using `alpaca_opra_daily_snapshot` rows in `data/options-validation/options_history.db`.

The lane must not claim profitability from underlying bars, option OHLC bars, last trades, stale snapshots, indicative feeds, midpoint-only fills, tiny samples, or in-sample-only sweeps. Final promotion requires point-in-time bid/ask or NBBO replay with realistic costs and validation splits.

Regular-options metric readbacks must now use the same proof posture. WFO simulation charges slippage and per-contract fees, expiry settlement uses expiry-day prices, blank/unknown evidence classes quarantine instead of fail-opening to live evidence, profit factor is nullable for no-loss samples, and PF claims use net USD P&L where available. The fresh forward evidence funnel still has `0` exact realized P&L rows and `0` promotion-ready rows; the current named-gate defect report is `docs/fresh-executable-evidence-defect-report-2026-06-09.md`. QQQ `id=537` now has a fresh executable exact HOLD review and SBUX `id=104` is closed from executable exact side-aware exit evidence, so the remaining fresh-evidence gate is legitimate exact realized exit P&L for QQQ `id=537` or another fresh candidate.

The regular supervised scanner safety contract is now lane-wide and metadata-driven. Regular playbooks default to fresh live validation and `position_tracking_mode=auto_track`; AI Commodity remains separate with scanner/tracked-position tracking disabled. Production scans default to portfolio caps on; caps-off scans are diagnostic unless explicitly allowed. Scanner-origin position and suggested-trade creation requires verified archived forward-scan lineage, a caps-enforced source scan, source `creation_eligible=true`, a current guardrail rerun, and exact-contract proof eligibility. When caps are enforced, existing positions, same-ticker/exact-spread exposure, max concurrent positions, cost-risk, drawdown, daily/weekly loss, sector/regime caps, and correlated-index exposure are hard blockers for auto-track and scanner-origin creation; near-cap notes and sizing reductions may remain cautions. Historical/research rows must use explicit manual modes and remain separated from production proof; control/scout proof labels do not make fresh executable regular rows paper-review-only.
The scanner creation safety contract is versioned at `data/contracts/scanner-creation-safety-contract.json` and explained in `docs/scanner-creation-safety-contract.md`. Scheduled auto-track requires explicit market-open state, an available caps-enforced exposure snapshot, auto-track playbook metadata, source `creation_eligible=true`, no creation blockers, proof eligibility, fresh profitability/promotion artifacts, and a passing regular open-risk governor; unknown market, exposure, lane, proof, or open-risk state is not a creation event. Manual Alpaca paper execution uses the same scanner-origin creation gate, submits exactly `1` contract through the Alpaca paper endpoint when explicitly enabled, and records broker-paper metadata separately from OPRA/NBBO production-proof evidence.
The candidate lifecycle status contract is generated by `scripts/candidate_lifecycle.py` at `data/contracts/candidate-lifecycle-contract.json`, `docs/candidate-lifecycle-contract.md`, and `src/lib/generated/candidateLifecycleContract.ts`. Queue builders, profitability/promotion gates, pending validation, disposition reporting, and fresh-evidence readbacks use this shared status/outcome vocabulary so paper-only, diagnostic, pending, and validation-attempted rows cannot disappear because a new status was added in only one module. Paper/probation lanes use `pending_paper_exact_evidence` for exact evidence collection; they do not enter `pending_live_validation`.
The Phase 2 regular-options forward cohort is preregistered at `data/contracts/forward-cohort-preregistration.json`, with generated doc `docs/forward-cohort-preregistration.md`. Freeze date is `2026-06-14` and eval date is `2026-07-28`. The frozen lanes are `volatility_expansion_observation` and the clean `bullish_pullback_observation` carrier set (`IWM`, `AAPL`, `GOOGL`, `UNH`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, `NEM`). `scripts/lane_promotion_state.py`, daily all-lanes/starvation checks, audit completeness guards, scheduled scan logging, and pending validation read this contract so every other regular lane is parked outside the cohort with scans and chores disabled until evaluation or an explicit refreeze. The contract does not lower existing proof bars, consume the protected holdout, submit orders, or convert research/backfill rows into production proof.
Phase 2 candidate staging is owned by `scripts/build_phase2_regular_options_forward_paper_shadow_candidate_rows.py` and exposed as `npm run options:stage:phase2-forward-paper-shadow-candidates`. It reads existing scan-pick artifacts only; it does not run the scanner, import quotes, append the cohort log, enable live validation, auto-track, or broker orders. Real append eligibility requires `candidate_source_mode=real_market_window_scan_picks`, `fixture_mode=false`, `market_window_status=open`, source artifact path/hash, captured timestamp, frozen lane/symbol/policy-hash checks, and exact evidence fields required by denominator status. When `--selection-date` is omitted, the stager defaults from the current `America/New_York` market date rather than the UTC calendar date, matching the collector and throughput audit. When scan-pick rows lack explicit `selection_date` / `scan_date`, timezone-aware selection timestamps are also converted to the New York market date before stale-row checks. The stager now requires an actual trusted executable entry quote source and explicit quote timestamp from the scan pick or quote snapshot; it no longer defaults missing source labels to `opra_nbbo`. Fixture/test/synthetic rows are allowed only for structural tests and are rejected by the validator/appender before append. Scanner candidates and scheduled scan logs preserve explicit entry quote source/timestamps and both `scan_run_id` and `scanner_run_id`; the stager also accepts legacy `scan_run_id` as scanner provenance when the newer aliases are absent, so future same-day Phase 2 rows can carry executable entry provenance into candidate staging without synthesizing source labels.
The passive Phase 2 capture runner is `scripts/run_phase2_regular_options_forward_paper_shadow_capture.py`, exposed as `npm run options:capture:phase2-forward-paper-shadow`. It wraps the stager/validator/guarded appender, reads existing scan picks only, defaults to no append, and cannot call scanner, live, auto-track, broker, order, or Trading Desk creation paths. The current real readback has `0` staged rows and no cohort log. The VRP credit-spread structure harness is `scripts/build_regular_options_vrp_credit_spread_structure_harness.py`, exposed as `npm run options:research:vrp-credit-spread-structure-harness`; it is read-only and now consumes the ready point-in-time VIX bucket. The refreshed VRP playbook has frozen candidate geometry, and the read-only quote-surface proof shows existing trusted ThetaData rows are still insufficient for full-window preregistered SPY/QQQ/IWM/DIA coverage, so the structure harness and bounded replay gate remain blocked only on `missing_index_credit_spread_quote_surface`.
The passive frozen-cohort scan sweep is `scripts/run_forward_cohort_scan_sweep.py`, exposed as `npm run options:scan:forward-cohort-sweep`. It resolves the frozen playbooks from `data/contracts/forward-cohort-preregistration.json`, runs/checks each lane separately, keeps portfolio and profitability gates enabled, and forces `OPTIONS_SCAN_AUTO_TRACK=0` so strict forward-audit collection cannot create tracked positions. Closed-window strict-forward readbacks use a `default_selection_date` so weekend and pre-open scheduler refreshes review the latest completed market day rather than inventing missing scheduled sessions for a non-tradable date. The current throughput audit confirms both June 26 frozen-lane scheduled sessions exist with `0` picks and reports `scheduled_phase2_drop_stage_summary.status=candidate_starvation_from_scan_filters`, led by `momentum`, `history_or_liquidity`, and `tech_score` drops. Future scheduled scans persist symbol-level scan-drop reasons into the forward ledger for this audit surface; the existing June 26 sessions predate that reason persistence, so the aggregate status is `candidate_starvation_evidence_status=stage_counts_only_waiting_for_symbol_drop_reasons`. The strict-forward readiness refresh, 30-row goal loop, and bounded collector summaries now propagate candidate-starvation evidence plus scan-task health status/blockers for higher-level agents. Forward capture is blocked by natural candidate starvation under current gates, not by stale VIX, a skipped volatility lane, a hidden scan-task-health failure, or a false weekend session gap.
The strict-forward auto-window collector is `scripts/run_regular_options_strict_forward_30_auto_window_collector.py`, exposed as `npm run options:goal-loop:strict-forward-30-auto-window`. It is the repeated scheduler-safe wrapper around the bounded collector and remains no-append by default. During open windows it now pauses collection when a current candidate JSONL exists and its path/hash matches the latest no-append capture batch, preserving that pending review artifact instead of allowing a later no-pick scan/capture pass to delete it. The pause applies even when a scheduled scan batch invokes the wrapper with `--skip-scan-sweep` after a separate sweep. This guard does not append, authorize live validation, enable auto-track, submit broker orders, import quotes, mutate evidence stores, lower proof bars, or count historical rows as forward proof.
The strict-forward scheduled wrappers refresh scheduler health, scan-task health, candidate review, exit-evidence planning, exit-completion staging, lifecycle, and completion monitoring after collector runs. The completion monitor requires both the dedicated collector scheduler health and the scan-task health artifact to be loaded and fresh relative to the collector before it can make a completion claim, so a ready collector task cannot hide broken 11:00/11:30 scan feeders.
The 59-symbol ThetaData OPRA/NBBO source-repair resume gate is `scripts/run_regular_options_59_symbol_thetadata_opra_import_resume.py`, exposed as `npm run options:source-repair:59-symbol-thetadata-opra-import-resume`. The package script `options:source-repair:59-symbol-thetadata-opra-import` also accepts `--resume-missing-only --provider-recheck` and writes resume artifacts. Latest tokened resume state is no longer the old provider-down refusal: local ThetaTerminal is reachable through the v3 service and the resume dry-run preflight is ready, but the non-dry resume wrapper still reports `blocked_59_symbol_import_repair` / `bulk_import_execution_not_started_by_preflight_wrapper`, so `import_attempted=false`, `imported_rows=0`, shared trusted dates remain `260`, gaps remain `11,565`, protected-holdout overlap rows `0`, and outside-universe import rows `0`. A direct OPRA quote dry-run against the currently loaded terminal returned `403 Forbidden` while the terminal banner showed `Options: FREE`; treat the current blocker as scoped import execution/entitlement-source state, not stale connection refusal.
The direct VIX source is materialized. `scripts/import_regular_options_direct_vix_source.py`, exposed as `npm run options:source-import:direct-vix`, imported official Cboe VIX daily history into generated point-in-time source rows and wrote the frozen bucket policy without mutating `options_history.db`. `scripts/build_regular_options_point_in_time_vix_bucket.py`, exposed as `npm run options:research:point-in-time-vix-bucket`, now reports `point_in_time_vix_bucket_ready` with `505` / `505` feature-store dates covered and no VIX blockers.
The macro-event calendar source repair packet is `scripts/build_regular_options_macro_event_calendar_source_repair_packet.py`, exposed as `npm run options:source-plan:macro-event-calendar`, and the tokened importer is `scripts/import_regular_options_macro_event_calendar.py`, exposed as `npm run options:source-import:macro-event-calendar`. No trusted macro-event source CSV has been supplied yet, so the point-in-time macro-event validator still blocks on missing local calendar rows.
The flow-extreme volume/OI source repair packet is `scripts/build_regular_options_flow_extreme_source_repair_packet.py`, exposed as `npm run options:source-plan:flow-extreme-volume-oi`, and the tokened importer is `scripts/import_regular_options_flow_extreme_volume_oi.py`, exposed as `npm run options:source-import:flow-extreme-volume-oi`. No trusted SPY/QQQ daily option volume/open-interest source CSV has been supplied yet, so flow-extreme remains blocked on missing point-in-time flow rows.
The underlying daily source path has three distinct boundaries: `scripts/build_regular_options_underlying_daily_source_repair_packet.py` defines the source schema, `scripts/build_regular_options_underlying_daily_source_acquisition_packet.py` / `npm run options:source-acquisition:underlying-daily-history` is the read-only staging preflight, and `scripts/import_regular_options_underlying_daily_history.py` / `npm run options:source-import:underlying-daily-history` is the tokened materializer. Current status is `underlying_daily_history_source_import_materialized`: an Alpaca SIP adjusted daily source CSV was staged under `data/import-staging/underlying_daily/point_in_time_underlying_daily_ohlcv_adjusted_v1.csv`, tokened import wrote `6,422` point-in-time generated source rows, and `scripts/build_regular_options_point_in_time_market_regime_inputs.py` now reports `point_in_time_market_regime_inputs_ready` with `494` / `494` dates and `24` / `24` months covered. This clears the daily OHLCV and base market-regime source-file blockers only; it does not clear opening-bucket/intraday underlying surfaces, candidate-generation diagnostics, quote-surface blockers, replay, profitability proof, live validation, broker permission, or promotion.
The term-structure calendar/diagonal structure harness is `scripts/build_regular_options_term_structure_calendar_structure_harness.py`, exposed as `npm run options:research:term-structure-calendar-harness`. It is read-only and now resolves the branch's structure mechanics, frozen geometry contract, strict-new dedupe contract, denominator statuses, front-leg assignment/expiration handling, roll/expiry policy, net USD P&L, protected-holdout guard, and proof-boundary labeling. The remaining term blockers before any bounded replay can be honest are `missing_point_in_time_term_structure_inputs` and `missing_index_calendar_quote_surface`.
The term-structure calendar/diagonal bounded replay gate is `scripts/build_regular_options_term_structure_calendar_bounded_replay.py`, exposed as `npm run options:research:term-structure-calendar-bounded-replay`. It consumes the preregistered playbook and structure harness, writes only generated research readbacks, and currently does not run replay because the same two real term-structure source/quote blockers remain unresolved.
The skew broken-wing put-fly structure harness is `scripts/build_regular_options_skew_broken_wing_put_fly_structure_harness.py`, exposed as `npm run options:research:skew-broken-wing-structure-harness`. It is read-only and resolves BWB side-aware pricing, payoff-grid max-loss, geometry, assignment/expiration, strict-new identity, denominator, holdout, and proof-boundary mechanics. The refreshed readback consumes the ready point-in-time VIX bucket; the current remaining blockers before any bounded replay can be honest are `missing_point_in_time_downside_skew_inputs` and `missing_index_broken_wing_quote_surface`.
The skew broken-wing put-fly bounded replay gate is `scripts/build_regular_options_skew_broken_wing_put_fly_bounded_replay.py`, exposed as `npm run options:research:skew-broken-wing-bounded-replay`. It currently does not run replay because the skew structure harness is blocked by those remaining non-VIX input surfaces; it writes only generated research readbacks and keeps accepted profitability false.
The macro-event long-strangle readiness path now has separate read-only input validators. `scripts/build_regular_options_macro_event_calendar.py`, exposed as `npm run options:research:macro-event-calendar`, validates point-in-time scheduled-event source rows and currently blocks on missing local calendar rows. `scripts/build_regular_options_point_in_time_vix_bucket.py`, exposed as `npm run options:research:point-in-time-vix-bucket`, validates point-in-time VIX bucket rows and is now ready. `scripts/build_regular_options_macro_event_long_strangle_replay_readiness.py` consumes both artifacts and now blocks on `macro_event_calendar_source_missing`, not VIX.
The quote-surface opening-range reversal replay is `scripts/build_regular_options_quote_surface_opening_range_reversal_replay.py`, exposed as `npm run options:research:quote-surface-opening-range-reversal-replay`. It is read-only and opens `data/options-validation/options_history.db` in SQLite read-only mode. `scripts/import_regular_options_alpaca_underlying_minute_price_surface.py`, exposed as `npm run options:source-import:alpaca-underlying-minute-price-surface`, materialized Alpaca SIP underlying minute source rows at `data/profitability-lab/regular-options-alpaca-underlying-minute-price-surface/source_rows.jsonl` without mutating `options_history.db`. The refreshed replay consumes that source and no longer blocks on `blocked_missing_quote_surface_underlying_price`: it emits `1,976` denominator rows, `74` exact historical candidate rows, full-window net P&L `-2134.4`, PF `0.0914`, and latest-four strict executable rows `0/30`. Current blockers are latest-four row count, PF lower bound, and concentration/stat economics; this branch is falsified/parked, not profitable or forward proof.
The quote-derived synthetic-forward opening-bucket source surface is `scripts/build_regular_options_quote_derived_synthetic_forward_surface.py`, exposed as `npm run options:research:quote-derived-synthetic-forward-surface`. It is read-only and attempts to derive research-only opening bucket inputs from same-minute OPRA/NBBO call-put pairs, but the current real readback is `blocked_quote_derived_synthetic_forward_surface`: `1,976` SPY/QQQ/IWM/DIA symbol-date rows and `7,904` requested buckets were checked for 09:35/10:35/10:40/15:50 ET, `0` buckets were ready, coverage was `0.0%`, train/latest-four month coverage was `0/20` and `0/4`, and no replay command was emitted. Do not rerun this source surface or the opening-range replay unless a trusted opening-bucket/underlying source, different approved source surface, or explicit data repair changes the missing same-minute call-put-pair blocker.
The existing repository point-in-time input/source surface atlas is `scripts/build_regular_options_existing_input_surface_atlas.py`, exposed as `npm run options:research:existing-input-surface-atlas`. It is read-only and inventories local DB/artifact input families before any further replay. The refreshed readbacks consume the materialized VIX bucket and Alpaca SIP underlying daily source rows as clean current inputs, with base point-in-time market-regime rows ready. The remaining broad input/source blockers are opening-range/intraday underlying surfaces, branch-specific trend/regime or breadth/momentum surfaces not satisfied by the base artifact, IV proxy, volume/OI/flow, macro/earnings event, dispersion/concentration, candidate-generation diagnostics, and fresh-forward collection.
`docs/regular-options-profitability-blocker-inventory.md` is the current whole-surface blocker inventory. It records VIX as cleared and keeps the overall profitability goal open because forward proof, candidate generation, trusted point-in-time input sources, quote surfaces, replay engines, provider/source availability, approval boundaries, and market-window forward capture still block the path to `30` strict completed rows.
The strict-forward operator queue is generated by `scripts/build_regular_options_strict_forward_operator_queue.py`, exposed as `npm run options:plan:strict-forward-operator-queue`, and written to `docs/regular-options-strict-forward-operator-queue.md` plus `data/forward-tracking/regular_options_strict_forward_operator_queue_latest.json`. It is the current profitability-loop visibility artifact after the blocker-removal pass: strict forward proof remains `0/30`, profitability readiness is false, fresh capture is blocked by operator approval plus a valid market window, and stale cleanup branches are parked unless a new source/artifact state changes them. It does not create trades, import quotes, mutate evidence stores, append cohort rows, consume holdout, change scanner/strategy/stops/sizing/proof bars, enable live validation, enable auto-track, submit broker orders, or promote lanes.
The strict-forward market-window readiness refresh is generated by `scripts/build_regular_options_strict_forward_market_window_readiness_refresh.py`, exposed as `npm run options:plan:strict-forward-market-window-readiness-refresh`, and written to `docs/regular-options-strict-forward-market-window-readiness-refresh.md` plus `data/forward-tracking/regular_options_strict_forward_market_window_readiness_refresh_latest.json`. The latest no-write refresh loaded current queue, gateboard, trade qualification, bullish-pullback layer, paper-shadow, monthly profitability, market-window preflight, throughput, and scan-task-health readbacks; status is `market_window_blocked_no_candidate_jsonl` with `0/30` strict rows, `candidate_jsonl_exists=false`, `append_allowed=false`, `operator_approval_required=true`, market-window status `market_closed`, and `scan_task_health_status=scan_tasks_ready_for_next_market_window`. It fails closed as `scan_task_health_blocked` if the scheduled scan feeders are missing or blocked. Stale upstream readback blockers are cleared: trade qualification is `blocked_no_live_release`, paper-shadow evidence planning is `paper_shadow_evidence_collecting`, market-window approval preflight is `blocked_market_closed`, and robust-edge discovery is `paper_shadow_only`.
The forward candidate throughput audit is generated by `scripts/build_regular_options_forward_candidate_throughput_audit.py`, exposed as `npm run options:audit:forward-candidate-throughput`, and written to `docs/regular-options-forward-candidate-throughput-audit.md` plus `data/forward-tracking/regular_options_forward_candidate_throughput_audit_latest.json`. When `--selection-date` is omitted it follows the strict-forward closed-window market-date policy: open and after-close market-day refreshes use the current New York market date, while weekend and pre-open refreshes use the latest completed market day. Row-level timestamp fallbacks use New York market dates for timezone-aware timestamps, so after-hours and weekend refreshes do not create false missing-session or stale-row blockers. The latest audit over `scan_picks.jsonl` found `550` scan-pick rows, `1` post-freeze Phase 2 row, both frozen-lane scheduled sessions present for `2026-06-26`, `0` target-date Phase 2 rows, `0` staged candidate rows, and stager status `no_phase2_natural_selections`. It now surfaces scheduled-session eligibility blockers and scan-funnel drop counts; latest scheduled drops are led by `momentum=50`, `history_or_liquidity=8`, `tech_score=2`, `direction_score=1`, `ev_floor=1`, and `option_liquidity=1`. The next action is to wait for a valid market window with real Phase 2 scan picks.
The strict-forward 30-row coordinator is `scripts/run_regular_options_strict_forward_30_goal_loop.py`, exposed as `npm run options:goal-loop:strict-forward-30`. It coordinates the passive frozen-cohort sweep, Phase 2 capture runner, throughput audit, and strict-forward readiness refresh, and now emits the next market-window trade date plus the exact safe no-append collection command. Its top-level readback carries candidate-starvation evidence and scan-task health status/blockers from the readiness layer. The command defaults to no append and never enables live validation, auto-track, broker orders, quote import, proof-bar changes, historical-row counting, or unguarded evidence mutation. A scanner sweep runs only when `--run-scan-sweep` is supplied, and append still requires the existing guarded capture/appender flags and token.
The strict-forward 30-row market-window collector is `scripts/run_regular_options_strict_forward_30_market_window_collector.py`, exposed as `npm run options:goal-loop:strict-forward-30-collector`, and written to `docs/regular-options-strict-forward-30-market-window-collector.md` plus `data/forward-tracking/regular_options_strict_forward_30_market_window_collector_latest.json`. It repeats the strict-forward coordinator only during an explicitly confirmed open market window, is bounded by `--max-attempts`, defaults to no append, and stops on candidate review, guarded append, scan failure, safety violation, or `30/30` completion. Closed-window refreshes do not run the scanner or append path. Repeated open-window attempts now get fresh per-attempt timestamps unless an explicit test timestamp is supplied, so market-window artifacts do not silently overwrite each other during a long collector run. The collector top-level and attempt summaries carry the latest scan-task health status/blockers. The coordinator now defaults missing selection dates to the current ET market date rather than the UTC calendar date, and writes the fresh throughput audit before the readiness refresh so nested readbacks do not carry stale scheduled-session status.
The scheduler-friendly strict-forward wrapper is `scripts/run_regular_options_strict_forward_30_auto_window_collector.py`, exposed as `npm run options:goal-loop:strict-forward-30-auto-window`, and written to `docs/regular-options-strict-forward-30-auto-window-collector.md` plus `data/forward-tracking/regular_options_strict_forward_30_auto_window_collector_latest.json`. It checks the US equity regular market window from current UTC time: outside the window it invokes the collector without scan or append, and during an open window it invokes the bounded collector with scan sweep enabled, no append, and default `max_attempts=1` unless the caller supplies a larger bounded attempt count. This gives repeated automation a safe entrypoint without treating historical rows as forward proof or enabling live, broker, auto-track, quote-import, proof-bar-change, or unguarded mutation paths. Latest refresh at `2026-06-27T05:10:28Z` is `auto_window_collector_waiting_for_open_market_window`: strict rows are still `0/30`, no candidate JSONL exists, and the next safe no-append collection window is `2026-06-29`.
The Windows scheduled collection surface now has three tasks. `\OptionsStrictForward30Collector` runs `scripts/run_strict_forward_30_auto_window_collector.bat` on weekdays starting at 07:35 local and repeats every 30 minutes for 6.5 hours; the batch wrapper forces `OPTIONS_SCAN_AUTO_TRACK=0`, keeps portfolio/profitability gates enabled, invokes the auto-window collector with `--max-attempts 3 --sleep-seconds 300 --json` and no append, then refreshes scheduler health, the read-only candidate-review packet, exit-evidence plan, exit-completion stager, lifecycle audit, and completion monitor. `\OptionsScanPicks` runs `scripts/run_scan_picks.bat` on weekdays at 11:00 local, and `\OptionsScanPicksSafetyNet` runs `scripts/run_scan_picks_safety_net.bat` on weekdays at 11:30 local. Both scan batch files force `scripts/run_forward_cohort_scan_sweep.py` first, then call `scripts/run_regular_options_strict_forward_30_auto_window_collector.py --skip-scan-sweep --json`, scheduler health, the candidate-review packet, exit-evidence plan, exit-completion stager, lifecycle audit, and completion monitor so strict-forward staging, fresh scheduler evidence, review state, append-only exit-completion state, lifecycle state, and completion proof refresh follow the scheduled sweep without appending rows. The 11:30 safety-net task is intentionally a second passive frozen-cohort market-window sample, not just a duplicate read of the 11:00 session. `scripts/build_regular_options_strict_forward_scan_task_health.py`, exposed as `npm run options:goal-loop:strict-forward-scan-task-health`, verifies those two scan tasks and batch contents without running scans or mutating evidence.
The strict-forward scheduler health readback is `scripts/build_regular_options_strict_forward_30_scheduler_health.py`, exposed as `npm run options:goal-loop:strict-forward-30-scheduler-health`, and written to `docs/regular-options-strict-forward-30-scheduler-health.md` plus `data/forward-tracking/regular_options_strict_forward_30_scheduler_health_latest.json`. It queries Windows Task Scheduler and fails closed if `\OptionsStrictForward30Collector` is missing, disabled, mispointed, not ready/running, or not on the expected repeat cadence. It also inspects `scripts/run_strict_forward_30_auto_window_collector.bat` and blocks if the wrapper drops the no-autotrack gates, collector, scheduler-health, candidate-review, exit-evidence-plan, exit-stager, lifecycle, or completion-monitor steps, changes their order, or adds an append/autotrack token. Latest status is `scheduler_ready_for_next_market_window` with no blockers and batch status `batch_chain_ready`.
The strict-forward candidate review handoff is `scripts/build_regular_options_strict_forward_30_candidate_review_packet.py`, exposed as `npm run options:goal-loop:strict-forward-30-candidate-review`, and written to `docs/regular-options-strict-forward-30-candidate-review-packet.md` plus `data/forward-tracking/regular_options_strict_forward_30_candidate_review_packet_latest.json`. It consolidates capture, collector, scheduler-health, scan-task health, and candidate JSONL validation into a no-append operator packet, and now fails closed if scheduler health or scan-task health is missing, malformed, blocked, or older than the collector report being reviewed. It also fails closed if the capture artifact is stale relative to the collector unless the collector carries a fresh nested capture report from the same pass. A successful upstream guarded append is now treated as `candidate_review_guarded_append_observed_waiting_for_exits` instead of as a review-packet safety violation, but the packet itself still cannot append. Candidate review requires any present candidate JSONL path and SHA-256 to match the fresh capture batch before append review can proceed, preventing stale or swapped candidate files from being reviewed as current; the Markdown readback renders scheduler freshness, scan-task freshness, and candidate batch provenance so these guards are visible outside JSON. Latest status is `candidate_review_waiting_for_real_candidate_jsonl` with scheduler freshness `scheduler_health_fresh_for_candidate_review`, scan-task freshness `scan_task_health_fresh_for_candidate_review`, capture freshness `collector_nested_capture_fresh_for_candidate_review`, and candidate provenance `candidate_batch_not_present`; if future scheduled collection writes candidate rows, the packet validates them and renders guarded validation/append commands with an explicit operator-token placeholder while keeping append/live/broker/auto-track/import/proof-change flags false.
The strict-forward 30-row completion monitor is `scripts/build_regular_options_strict_forward_30_completion_monitor.py`, exposed as `npm run options:goal-loop:strict-forward-30-completion-monitor`, and written to `docs/regular-options-strict-forward-30-completion-monitor.md` plus `data/forward-tracking/regular_options_strict_forward_30_completion_monitor_latest.json`. It recomputes completion directly from the Phase 2 forward paper-shadow cohort report instead of trusting collector side effects, embeds that Phase 2 report as `source=computed_inline`, and keeps the non-written proposed latest Phase 2 report path as metadata rather than a loaded source path. It fails closed if collector, scheduler-health, candidate-review, exit-evidence-plan, exit-stager, lifecycle, or candidate-review capture-freshness dependencies are missing, malformed, blocked, or stale relative to the collector report under review. The underlying Phase 2 forward report now uses New York market-date fallback for timezone-aware `selection_timestamp_utc` values when explicit `selection_date` is absent during append validation, acceptance counting, and concentration grouping, and consumes fresh robust-edge discovery where the current candidate is `paper_shadow_candidate` rather than a stale missing-readback placeholder. Upstream guarded append state no longer trips the monitor safety gate; live validation, auto-track, broker, quote import, proof-bar, and historical-proof flags still fail closed. Latest status is `completion_monitor_waiting_for_first_cohort_row` with dependency freshness `completion_monitor_dependencies_fresh`: strict rows are `0/30`, accepted profitability is `false`, the Phase 2 cohort log is missing, scheduler health is ready, candidate review is waiting for a real JSONL, exit-evidence plan is waiting for open rows, exit stager is waiting for open rows, lifecycle audit is waiting for the first entry row, and no safety violations are present. It is read-only and cannot append, enable live validation, auto-track, broker orders, quote import, proof-bar changes, or historical rows as forward proof.
The strict-forward 30-row lifecycle audit is `scripts/build_regular_options_strict_forward_30_lifecycle_audit.py`, exposed as `npm run options:goal-loop:strict-forward-30-lifecycle-audit`, and written to `docs/regular-options-strict-forward-30-lifecycle-audit.md` plus `data/forward-tracking/regular_options_strict_forward_30_lifecycle_audit_latest.json`. It reads the Phase 2 cohort log and makes the append-only entry-to-exit lifecycle explicit: open entry rows may be completed by later exact-exit rows with a unique row ID and reused selection ID, but exact-exit rows still require candidate validation and guarded append. Latest status is `lifecycle_waiting_for_first_entry_row`; the cohort log is missing and strict rows remain `0/30`.
The strict-forward 30-row exit-evidence handoff is `scripts/build_regular_options_strict_forward_30_exit_evidence_plan.py`, exposed as `npm run options:goal-loop:strict-forward-30-exit-evidence-plan`, and written to `docs/regular-options-strict-forward-30-exit-evidence-plan.md` plus `data/forward-tracking/regular_options_strict_forward_30_exit_evidence_plan_latest.json`. It is read-only: when Phase 2 open rows exist, it lists each open `selection_id`, required trusted exact-exit JSONL fields, the target exit-evidence path, and follow-on stager/guarded append commands without writing evidence, importing quotes, forcing exits, or appending. The strict-forward 30-row exit-completion stager is `scripts/build_regular_options_strict_forward_30_exit_completion_stager.py`, exposed as `npm run options:goal-loop:strict-forward-30-exit-completion-stager`, and written to `docs/regular-options-strict-forward-30-exit-completion-stager.md` plus `data/forward-tracking/regular_options_strict_forward_30_exit_completion_stager_latest.json`. It reads existing open Phase 2 cohort rows plus `data/forward-tracking/phase2_regular_options_forward_paper_shadow_exit_evidence.jsonl` and writes exact-exit candidate rows only when trusted executable exit source/timestamp, spread bid/ask, policy exit condition, and `net_pnl_usd` validate; untrusted exit sources reject as `untrusted_exit_quote_source`. Phase 2 candidate append validation now also rejects exact-entry, exact-exit, and open policy-waiting rows without trusted executable entry quote provenance as `exact_entry_missing_entry_quote_provenance`. Latest status is `exit_completion_waiting_for_open_forward_rows`; no candidate rows were staged and no append occurred.
The Phase 2 forward paper-shadow market-window capture audit is `docs/regular-options-phase2-forward-paper-shadow-market-window-capture.md`, backed by `data/profitability-lab/regular-options-phase2-forward-paper-shadow-market-window-capture/latest.json`. It consumes the existing passive capture and goal-loop commands only. Current status is `no_phase2_natural_selections_no_append`: `0` candidate rows staged, no candidate JSONL, no validation/append, missing cohort log, and `0/30` strict completed forward rows. Forward append remains permitted only through the existing guarded validator/appender when real same-day Phase 2 rows pass `append_allowed=true`.
The local OPRA/NBBO structure capability matrix is `scripts/build_regular_options_local_quote_structure_capability_matrix.py`, exposed as `npm run options:research:local-quote-structure-capability-matrix`. It is read-only and inventories constructible structures from existing trusted bid/ask rows at fixed 10:40/14:30 entry buckets and 15:50 exit buckets for the 13-symbol proof set. Current status is `local_quote_surface_only_structures_exhausted_under_current_data`: `12,864` daily structure rows and `1,333` representatives were emitted, but `0` structures were replay-feasible. Dense recent single-leg/vertical/butterfly/condor/ratio rows exist, yet train-month coverage is only `2` to `6` months depending on structure; straddles and iron structures also fail full-window/latest-four depth. No next replay candidate was emitted, and all profitability, forward-proof, live, broker, import, mutation, holdout, proof-bar, and promotion flags remain false.
The all-local quote-minute structure capability atlas is `scripts/build_regular_options_all_local_quote_minute_structure_capability_atlas.py`, exposed as `npm run options:research:all-local-quote-minute-structure-capability-atlas`. It is read-only and broadens the fixed matrix to all trusted local underlyings and all available 5-minute quote buckets from 09:35 through 15:55 ET, ranked by quote coverage only. Current status is `all_local_quote_surface_replayability_exhausted_under_current_data`: `66` trusted underlyings, `2,520` bucket inventory rows, `40` selected detailed surfaces, `324,576` daily status rows, and `191,641` representatives were emitted, but `0` surfaces cleared replay feasibility and `next_replay_candidate=null`. The strongest all-local surfaces have dense latest-four depth, but train-month coverage tops out at `15` for selected `IWM` coverage and is commonly `9`, below the required `20`. Do not rerun quote-surface-only replayability work unless trusted source/data repair changes train coverage or the already named source-surface blockers.

The active Oracle-assisted profitability loop is anchored by `scripts/run_options_goal_loop.py`, `data/contracts/options_goal_loop_policy.json`, and `docs/research-decisions/options_goal_loop_latest.md`, with GPT-5.5 handoff now formalized by `scripts/build_options_oracle_profit_loop_packet.py`, `data/forward-tracking/options_oracle_profit_loop_packet_latest.json`, and `docs/research-decisions/options_oracle_profit_loop_packet_latest.md`. Continue Oracle via the saved ChatGPT conversation when possible (`--followup <latest-session-id> --browser-bundle-files --browser-archive never`) so GPT-5.5 Pro keeps loop context while receiving current files. Use `npm run options:oracle-loop:packet` before each GPT-5.5 handoff; the packet now targets at least `30` profitable strict completed rows in the latest approximately four months / post-freeze forward-style audit window, forces a `verdict=continue|stop_exception`, and makes stopping an earned exception rather than a casual branch. Before any `stop_exception`, GPT-5.5 must explicitly evaluate fresh forward collection, scoped source repair/replay, new historical data surfaces or longer lookbacks, new causal playbooks, and new option structures beyond the current directional-spread surface, then return exactly one Codex task with files, commands, acceptance criteria, failure criteria, and expected artifacts. `scripts/build_regular_options_causal_falsification_slice.py`, exposed as `npm run options:research:causal-falsification`, now feeds the packet after the same-session GPT-5.5 browser response stalled on its selected read-only causal falsification branch; latest status is `existing_surface_falsified_new_causal_branch_still_possible`, with raw count aggregation, tracked-winner count retuning, clean index/IWM refill as primary gap closer, and existing momentum-compatible artifact aggregation stopped, leaving only `new_preregistered_causal_playbook` for GPT-5.5 to select or stop. GPT-5.5 follow-up `profitabil-causal-next` selected the design-only playbook `breadth_confirmed_index_qqq_momentum_continuation_debit_spread_v1`, now generated by `scripts/build_regular_options_preregistered_momentum_continuation_playbook.py` and exposed as `npm run options:research:preregistered-momentum-continuation-playbook`; status is `preregistered_design_only`, not implementation or replay permission. GPT-5.5 Pro Extended then selected the design-only volatility-risk-premium credit-spread concept `low_mid_vix_index_put_credit_spread_vrp_v1`, generated by `scripts/build_regular_options_preregistered_vrp_credit_spread_playbook.py` and exposed as `npm run options:research:preregistered-vrp-credit-spread-playbook`; status is also `preregistered_design_only`, with no implementation or replay permission. GPT-5.5 Pro Extended follow-up `profitabil-after-vrp-design` selected the read-only replay-readiness audit generated by `scripts/build_regular_options_vrp_credit_spread_replay_readiness.py`, exposed as `npm run options:research:vrp-credit-spread-replay-readiness`; latest status is `blocked_vrp_credit_spread_replay_readiness` because the repo lacks exact two-leg credit-spread side-aware entry/exit pricing, full denominator mapping, assignment/expiration classification, credit-spread margin/max-loss convention, exact SPY/QQQ/IWM/DIA credit-spread quote-surface proof, and protected-holdout guard, while the point-in-time VIX bucket is now ready. GPT-5.5 Pro Extended follow-up `profitabil-after-vrp-readiness` then selected the design-only term-structure calendar/diagonal concept `low_mid_vix_index_calendar_term_structure_dislocation_v1`, generated by `scripts/build_regular_options_preregistered_term_structure_calendar_playbook.py` and exposed as `npm run options:research:preregistered-term-structure-calendar-playbook`; status is `preregistered_design_only`, with no implementation or replay permission. GPT-5.5 Pro Extended follow-up `profitabil-after-term-calendar` then selected the read-only replay-readiness audit generated by `scripts/build_regular_options_term_structure_calendar_replay_readiness.py`, exposed as `npm run options:research:term-structure-calendar-replay-readiness`; latest status is `blocked_term_structure_calendar_replay_readiness` because the repo lacks dedicated calendar/diagonal side-aware entry pricing, exit/expiry engine, full denominator mapping, front-leg assignment/expiration classifier, frozen roll/expiry policy, point-in-time term-structure inputs, exact SPY/QQQ multi-expiry quote-surface proof, and strict-new dedupe. GPT-5.5 Pro Extended follow-up `profitabil-after-term-readiness` then selected the design-only skew/broken-wing concept `low_mid_vix_index_skew_broken_wing_put_fly_v1`, generated by `scripts/build_regular_options_preregistered_skew_broken_wing_playbook.py` and exposed as `npm run options:research:preregistered-skew-broken-wing-playbook`; status is `preregistered_design_only`, with no implementation or replay permission. GPT-5.5 Pro Extended follow-up `profitabil-after-skew-design` then selected the design-only macro-event long straddle/strangle concept `low_mid_vix_macro_event_long_strangle_v1`, generated by `scripts/build_regular_options_preregistered_macro_event_long_strangle_playbook.py` and exposed as `npm run options:research:preregistered-macro-event-long-strangle-playbook`; status is `preregistered_design_only`, with no event calendar implementation, replay, or live permission. The Oracle packet now includes this artifact before the next same-session handoff. The loop currently focuses on `volatility_expansion_observation` and reports `underpowered_forward_evidence`: `0` / `30` post-freeze strict exact completed rows, no strict USD PF lower bound, `promotion_ready=false`, and no live, auto-track, or broker permission. Its strict acceptance snapshot requires the forward preregistration and cohort schema contracts, reports missing/malformed cohort logs as named blockers, uses strict USD rows for review counts/PF/gates, and rejects pre-freeze rows, wrong-lane or non-preregistered-symbol rows, non-executable or lookahead marks, missing quote provenance, missing net USD P&L, duplicate row IDs, unknown denominator statuses, missing schema fields, and scanner hash drift before any paper-validation review state. The closed-market Oracle follow-up `profitabil-next-slice` added `scripts/build_bullish_pullback_layer_shadow_selection.py`, exposed as `npm run options:plan:bullish-pullback-layer-shadow`, to wire the frozen bullish-pullback layer stack into paper-shadow plan/checklist harness selection while preserving volatility forward acceptance at `0` / `30` and all live, broker, auto-track, quote-import, and evidence-mutation flags false. The follow-up execution-safety audit now resolves existing trusted leg quotes read-only from `options_history.db`: all `129` selected rows have parseable OCC legs and trusted entry quotes, but only `126` trusted exit quote pairs, `6` zero/untradable rows, and `129` side-aware executable price mismatches versus the historical spread marks. The executable-economics recompute prices `120` tradable rows at PF `3.7414` with bootstrap PF lower bound `2.27`, but the harness remains `profitable_but_preflight_blocked` because `3` rows are missing required quotes, `6` are zero/untradable, and source marks are diagnostic only. `scripts/build_bullish_pullback_layer4_forward_capture_protocol.py` and `scripts/validate_bullish_pullback_layer4_forward_candidate_rows.py` now prepare a read-only future capture protocol for that layer; status is `protocol_ready_waiting_for_market_window_and_operator_approval`, with no appender, no cohort log creation, and `append_allowed=false`. `scripts/build_regular_options_market_window_approval_preflight.py`, exposed as `npm run options:preflight:market-window-approval`, is the current no-write gate before any future layer4 candidate approval review; the market-window checklist safe command order includes the executable-economics audit and ends with this preflight, while the paper-shadow plan loads its artifact as an operator-surface source. Latest real preflight status is `blocked_market_closed`, with all readbacks fresh, optional candidate validation read-only, and append/live/broker/auto-track/mutation/promotion flags false.
The no-wait historical robust-search track is separate from forward promotion. `scripts/build_regular_options_feature_store.py` generates `docs/regular-options-feature-store.md` from trusted ThetaData intraday OPRA/NBBO quote rows in `data/options-validation/options_history.db`, using `as_of_utc` as `event_time`, `published_time`, and `tradable_after_time`; candidate joins must require `feature.tradable_after_time <= candidate_entry_time`. `data/contracts/regular-options-source-quality-scope-policy.json` is the manually maintained historical-nomination scope policy, currently excluding CVX `bullish_pullback_core` rows after the CVX executable-coverage diagnostic proved observed zero-bid tradability rather than missing provider data. `scripts/build_regular_options_robust_search_evaluation.py` generates `docs/regular-options-robust-search-evaluation.md` from the regular-options multilane selected exact rows, the source-quality scope policy, the regime report, the feature-store report, a local baseline report when present, and autoresearch ledger search-effort counts. `scripts/build_regular_options_historical_walk_forward.py`, exposed as `npm run options:replay:regular-options-walk-forward`, is the operator workflow wrapper: it refreshes feature-store and robust-search readbacks, ingests the all-planned peer sleeve report, adds drawdown/risk metrics, emits a ranked repair queue, and fails closed on protected-holdout overlap, missing holdout/candidate-date metadata, or incomplete all-planned variant coverage without creating trades or changing policy. `scripts/build_regular_options_historical_depth_selected_trades.py`, exposed as `npm run options:build:historical-depth-selected-trades`, is the read-only selected-trade calendar-depth readback for the requested `2024-06` through `2026-05` window; the latest result is `blocked_historical_depth_selected_trades` because only `8` selected-entry months and `231` selected rows are proven, while `16` requested months remain unproven and protected-holdout overlap is false. `scripts/build_regular_options_point_in_time_selected_trade_depth.py` and `scripts/build_regular_options_point_in_time_candidate_generation.py`, exposed as `npm run options:build:point-in-time-selected-trade-depth` and `npm run options:build:point-in-time-candidate-generation`, are the newer read-only source-reproduction and candidate-generation proof reports. They reproduce the current `231` selected rows but prove only `8` source diagnostic months (`2025-08` through `2026-03`); explicit zero-selection calendar months can count only when candidate generation is proven for the contributing source artifacts. `scripts/build_regular_options_historical_simulated_forward_audit.py`, exposed as `npm run options:audit:historical-simulated-forward`, answers the explicit historical-depth question: the quote store has `505` shared trusted intraday dates through `2026-06-04`, but the current selected-trade source has only `8` entry months and `231` accepted exact rows after source-quality scope, so the requested `20` train months plus latest `4` simulated-forward audit months cannot be honestly claimed from the current source. Its available latest-four-month audit (`2025-12` through `2026-03`) has point PF `1.4249` and avg P&L `+12.21%` across `113` rows, but bootstrap PF lower bound is `0.99`, so it remains underpowered. Two-year source replay attempts confirm the current blocker: default `252` trusted imported calendar-date replays expand diagnostics only to `9` months, and `--min-imported-calendar-dates 505` fails because the current `59`-symbol replay universe has only `260` selected shared imported quote dates as of `2026-06-04`. Oracle follow-up `profitabil-after-260-date-blocker` concluded no meaningful closed-market profitability-improving implementation remains for this historical-depth branch under current prohibitions; remaining paths require separate operator approval for quote import/evidence repair, a preregistered narrower universe/source audit, or future market-window forward evidence. GPT-5.5 Pro later selected `scripts/build_regular_options_countable_throughput_frontier.py`, exposed as `npm run options:research:countable-throughput-frontier`, as the stricter all-planned frontier stop-verdict surface: latest status is `current_historical_surface_exhausted_under_current_prohibitions`, with `44` variants evaluated, `11` raw-count candidates, `0` countable throughput candidates, `33` below strict-new count, `2` execution-quality blocks, and `9` negative/flat rejections. Latest workflow readback after scoped quote repair is still `historical_walkforward_ran_candidates_blocked`: feature store built with `505` shared quote dates and `12,149,436` trusted intraday rows, robust search blocked with `231` accepted exact rows and `0` / `3` ready candidates, all-planned sleeves loaded with `44` / `44` variants tested as of `2026-06-04`, `0` all-planned run failures, forward holdout guard `passed`, protected holdout start `2026-06-05`, no holdout overlap, and `promotion_ready=false`. Batch `2147` imported `17` trusted exact-date repair rows for the four coverage-repair variants with `0` duplicates, `0` rejects, `0` lookahead-only rows, and `0` errors. The repair cleared coverage blockers on those variants but did not make a lane ready: `sleeve_next_index_refill_v1` is `profitable_but_overlaps`, `sleeve_next_reit_industrial_refill_v1` is `repair_stress_before_counting`, and `sleeve_next_defensive_refill_v1` plus `sleeve_next_move_bucket_refill_v1` are `weak_positive_or_marginal`. Workflow schema `2` now ranks `19` repair rows, `3` high priority, with high-priority work back on robust-candidate source-quality/unpriced repair. Latest robust-search readback is `historical_candidates_blocked`: combined final holdout `28` rows with bootstrap PF lower bound `0.61`, and blockers for final-holdout sample below `30`, final PF-LB below the selection-adjusted bar, paper-shadow/source-quality blockers, and lane-specific unpriced/zero-bid blockers. This track may nominate, reject, or refreeze future forward candidates only; it is not fresh forward proof, live-validation eligibility, protected-holdout consumption, evidence-store mutation approval, or a proof-bar change.
The 13-symbol candidate-generation surface audit is owned by `scripts/build_regular_options_13_symbol_candidate_generation_surface_audit.py`, exposed as `npm run options:research:13-symbol-candidate-generation-surface-audit`. The no-write/as-of/universe-filter runner support is proven by `scripts/run_regular_options_13_symbol_no_write_candidate_generation.py`, exposed as `npm run options:research:13-symbol-no-write-candidate-generation`. The reusable frozen daily entrypoint is `scripts/regular_options_frozen_candidate_generation_entrypoint.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-entrypoint`; latest status is `blocked_frozen_13_symbol_candidate_generation_entrypoint` with `6,916` frozen lane/symbol/date rows, all blocked by `missing_daily_candidate_generation_diagnostics`, `0/24` covered candidate-generation months, and `0` selected candidates. The frozen source-surface materializer is `scripts/build_regular_options_13_symbol_frozen_candidate_generation_source_surface.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-source-surface`; it now consumes the frozen entrypoint and remains blocked with `0/24` candidate-generation months and `0` selected rows. The historical simulated-forward audit now defaults to this frozen source chain and remains blocked with `0` selected rows, `0` train months, `0` audit months, and `0/30` exact audit trades. This parks the current historical-depth branch unless a separately scoped daily candidate-generation source/data-surface repair changes the blocker. It is not accepted profitability and does not authorize quote import, evidence mutation, scanner changes, live validation, auto-track, broker actions, holdout consumption, proof-bar changes, or promotion.
The frozen 13-symbol candidate-generation engine/daily diagnostics materializer is `scripts/build_regular_options_13_symbol_frozen_candidate_generation_engine.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-engine`. Latest status is `blocked_frozen_13_symbol_candidate_generation_engine`, decision `blocked_frozen_candidate_generation_entrypoint_incomplete`: it consumes the reusable frozen entrypoint, but all `6,916` daily diagnostics rows are blocked, with candidate-generation months `0/24`, train months `0`, audit months `0`, latest-audit exact trades `0`, and selected candidates `0`. Do not repeat this 13-symbol branch unless a real daily frozen candidate-generation source changes this blocker.
The dispersion-proxy hybrid input materializer is `scripts/build_regular_options_point_in_time_dispersion_concentration_proxy.py`, exposed as `npm run options:research:point-in-time-dispersion-concentration-proxy`. `scripts/import_regular_options_dispersion_concentration_proxy_source.py`, exposed as `npm run options:source-import:dispersion-concentration-proxy`, materializes `alpaca_sip_underlying_daily_dispersion_concentration_proxy_v1` rows from the already-imported Alpaca SIP underlying daily source rows under an exact approval token and no-replay boundary. Latest source import status is `dispersion_concentration_proxy_source_import_materialized`: `6,422` generated source rows, `494` / `494` requested dates, `24` / `24` months, and no blockers. Latest proxy status is `point_in_time_dispersion_concentration_proxy_available`, and `scripts/build_regular_options_dispersion_proxy_hybrid_replay_readiness.py` now reports `dispersion_proxy_hybrid_replay_readiness_ready` with `blockers=[]`. This is source/input readiness only; no replay, quote import, evidence-store mutation, live validation, auto-track, broker action, holdout consumption, proof-bar change, promotion, or profitability claim is authorized.
The flow-extreme ratio/backspread input materializer is `scripts/build_regular_options_point_in_time_flow_extreme_input.py`, exposed as `npm run options:research:point-in-time-flow-extreme-input`. The attempted trusted volume/open-interest source-row generator is `scripts/build_regular_options_flow_extreme_volume_oi_source_rows.py`, exposed as `npm run options:research:flow-extreme-volume-oi-source-rows`; latest status is `blocked_flow_extreme_volume_oi_source_rows` because trusted `thetadata_opra_nbbo_1m` intraday rows have `0` usable volume/open-interest aggregates, no `source_rows.jsonl` was written, and coverage is `0/24` months. Latest materializer status remains `blocked_point_in_time_flow_extreme_input`: no trusted local flow source rows exist, covered months are `0/24`, date coverage is `0.0%`, and blockers are `missing_point_in_time_flow_extreme_source`, `missing_required_flow_fields`, `insufficient_month_coverage`, and `insufficient_date_coverage`. The refreshed flow-extreme replay-readiness audit no longer has VIX, pricing, denominator, or strict-new dedupe blockers; it is waiting on real point-in-time flow input.

The project operating model is generated by `scripts/generate_project_pathway_registry.py` at `docs/project-operating-map.md` and `data/contracts/project-pathway-registry.json`. It organizes current work into six pathways: Data, Candidate, Evidence, Profitability, Promotion, and Operator. The current readback is `docs/project-operator-gateboard.md`, built by `scripts/build_project_operator_gateboard.py`; latest gateboard status is `safe_blocked_no_live_release`, with Data and Candidate paths passing while Evidence, Profitability, Promotion, and Operator paths remain blocked. The no-chase manifest is active because broad missed-pick economics are negative (`206` untracked rows, PF `0.32`, avg `-16.54%`), there are `0` live-validation lanes, no fresh evidence is promotion-ready, no paper shortlist row is eligible, and suggested-trade review has an attention row. The open-risk governor currently passes with `0` live-exact negative open rows, but live validation remains fail-closed because the evidence, profitability, promotion, and operator paths are still blocked. These artifacts are navigation/readback surfaces only. They do not create trades, change scanner policy, change lane promotion, lower proof bars, or turn paper/research/backfill rows into production proof.

Runtime agent orchestration is separate from the generated static navigation graph. `docs/agent-memory-graph.md` and `data/contracts/agent-memory-graph.json` remain `runtime_use=false` where-to-read artifacts. `docs/agent-control-plane.md` owns the CEO/worker runtime memory graph, and `scripts/agent_control.py` writes ignored local state under `data/agent-control/` by default: a SQLite WAL task/message/graph ledger, append-only `events.jsonl`, retrieval documents, context manifests, dream run audits, and research provenance records. The control plane borrows HydraDB-style boundaries: scoped memories versus shared knowledge, tenant/sub-tenant partitioning, typed graph triplets, and text/metadata query with graph-neighborhood expansion. Memory graph v2 stamps accepted memory, dream promotions, retrieval docs, manifests, dashboard output, and research provenance with orchestration-only policy metadata and rejects wording that tries to authorize live trading, broker action, evidence mutation, scanner/strategy/proof-bar changes, promotion, stop/sizing changes, append readiness, or treating historical rows as forward proof. Graph writes populate a local SQLite `retrieval_documents` FTS/BM25 index with source-quality and authority explanations; context packs write manifests under `data/agent-control/context-packs/`; `memory:operator-dashboard` audits lifecycle, retrieval, outbox, dream automation, startup/context manifests, and deterministic eval; and `memory:research-priorities` exposes research-only zero-candidate/provenance priorities. The deterministic repo seed command copies checked startup/living docs, static memory-graph nodes, latest gateboard pathways, no-chase blockers, gateboard source artifacts, and the visible repo text workspace from `git ls-files --cached --others --exclude-standard` into local runtime graph nodes with metadata filters, tracked/untracked `git_state`, content hashes, bounded excerpts, and prompt-ready context output. Seed refreshes prune prior seed-owned current-state static, gateboard pathway/blocker/source-artifact, and `repo_file_index` nodes before reseeding so current queries do not retain removed files or cleared blockers. The `checkpoint` command records the current CEO objective, scope, status, constraints, next actions, files, and verification into `checkpoint:latest` for session recovery, defaulting autonomy to `read_only_workers` unless a broader mode is explicitly passed. The `bootstrap` command is the normal next-context-window entrypoint: it runs the seed, returns a digest, and emits the latest checkpoint plus compact gateboard blocker context, with `--prompt-only` available for paste-ready handoff text. Accepted worker reports now write back to typed operating memory (`worker_report`, `verification`, `artifact`, and accepted `blocker` nodes) linked to the raw report, task, and accepting decision; verification and artifact memories also link directly back to the task for future retrieval, raw submitted reports remain task artifacts until accepted, and terminal tasks reject late reports. `context pack` keeps accepted operating memory pathway-scoped while always including current fail-closed gateboard blockers, `memory audit` checks stale/expired memory plus both sides of supersession consistency, `memory eval` verifies current gateboard recovery without requiring an open-risk blocker, and `graph query` can filter typed memory by active/inactive and freshness. It is orchestration/retrieval/research-selection memory only; it does not mutate evidence stores, create broker orders, change scanner policy, lower proof bars, promote lanes, append cohort rows, or replace living docs and current gateboard readbacks.

Evidence-store survivability is documented in `docs/evidence-operations.md`. `data/contracts/evidence-host-policy.json` declares `KaesDevice` as the current authoritative evidence host for Postgres tracked positions, `chat_history.db`, `data/options-validation/forward_tracking_authoritative.db`, and `data/options-validation/options_history.db`; non-authoritative hosts are read-only for those stores unless an explicit process-env override is set. `scripts/log_scan_picks.py` writes scheduled-scan host/commit provenance and fails closed before evidence writes on a non-authoritative host; generated fresh forward-evidence readbacks also record host/commit/run provenance in their JSON and Markdown artifacts. `npm run evidence:backup` writes ignored rolling backups under `data/backups/` for `chat_history.db`, both forward ledgers under `data/options-validation/forward_tracking*.db`, `data/options-validation/options_history.db`, and Postgres tracked positions, while `npm run evidence:backup:weekly` copies the completed bundle off-machine when `OPTIONS_BACKUP_WEEKLY_COPY_DIR` is configured. `npm run scan:heartbeat` checks the local scheduled-scan heartbeat, and `npm run daily-ops` chains the daily readback chores through heartbeat health and gateboard refresh.

The monthly profitability iteration surface is `docs/monthly-all-lanes-profitability-audit.md`, generated by `scripts/build_monthly_all_lanes_profitability_audit.py` and exposed as `npm run options:audit:monthly-profitability`. It combines the missed-pick outcome/failure/filter artifacts, current-policy monthly drift, entry-filter validation, point-in-time replay, paper monitor, candidate ledger, all-20 layer stack, open-risk governor, multilane portfolio, lane-promotion state, and fill-attempt logs into one read-only operator queue. Latest readback is `profitability_iteration_ready_blocked_for_promotion`: baseline PF `0.32`, avg `-16.54%`, recent month `2026-05` `paper_only_recent_break`, execution realism `ready`, risk/portfolio `blocked`, open-risk `open_risk_governor_pass`, `24` promotion blockers, `10` proof-live exact fill-attempt rows, `6` fresh exact entries, `0` exact realized P&L rows, `12` true minute-exit P&L rows, `12` true top-spread replay P&L rows, `12` true contract-replacement replay P&L rows, and `12` true read-only structure-specific P&L rows. The command center classifies current active regular supervised lanes into read-only profitability dispositions: `1` `paper_shadow`, `4` `quarantine`, `3` `retest`, `0` `needs_replay_engine`, and `0` `profitable_candidate`; all `4` quarantine lanes are archived as read-only evidence. Structure-specific harness and event data spine are `built_collecting`, lane scan hypothesis repair is `built_collecting`, execution-alternative replay coverage is ready after ThetaData import, minute-exit replay coverage is ready after exact quote consumption, and stale candidate archive is `built` with all `16` no-longer-matched candidates archived as read-only stale branches and `0` production proof-ready rows. The monthly queue drops generic `build_structure_specific_harness`, `build_event_data_spine`, `build_or_repair_lane_scan_hypothesis_before_pnl_replay`, `wait_for_fresh_match_or_archive_candidate`, the execution-alternative quote-import action, and the minute-exit quote-import action, leaving `10` actions. Top items are now `collect_exact_exit_evidence`, `collect_paper_shadow_exact_evidence`, and historical resolution for expired suggested-trade review rows such as `id=138`. These reports do not create trades, submit broker orders, auto-close display-only or missing-review marks, mutate DB state, change scanner policy, tune thresholds/symbols/expiries/windows, change contract selection, change stops or sizing, synthesize unsupported event/lane P&L, lower proof bars, reactivate stale candidates without fresh executable exact evidence, or promote paper/research/backfill evidence.
The current-regime new-lane incubation surface is `docs/regular-options-current-regime-lane-incubator.md`, generated by `scripts/build_regular_options_current_regime_lane_incubator.py` and exposed as `npm run options:research:current-regime-lane-incubator`. It was added after Oracle clarified that the historical-depth stop was branch-scoped, not a claim that no options lanes can be researched. Latest status is `current_regime_lane_incubator_ready_for_operator_review`: `6` preregistered concepts, `0` accepted profitable lanes, and all live/auto-track/broker/import/mutation/proof-bar/promotion flags false. The only design-ready concept is `regime_momentum_continuation_debit_spread`. The first read-only edge-throughput test for that concept is `docs/regular-options-current-regime-momentum-edge.md`, generated by `scripts/build_regular_options_current_regime_momentum_edge.py` and exposed as `npm run options:research:current-regime-momentum-edge`. Latest status is `raw_count_available_but_not_countable_profitable_edge`: current momentum-compatible artifacts can exceed `200` raw combined rows, but `0` candidates are countable profitable momentum-edge candidates after strict-new de-duplication, quote coverage, stress, unpriced-row, and point-PF blockers. The next loop should ask Oracle for a new causal read-only playbook or falsification path rather than aggregating raw overlapping counts.

Trading Desk repository ownership is documented in `docs/repository-contract.md`, with structural Protocols in `python-backend/repository_contracts.py`. Tracked/suggested route and row-shape parity is documented in `docs/trading-desk-record-parity.md`, with the manifest in `python-backend/repository_parity.py`; shared display/review shape does not merge Postgres tracked-position proof rows with SQLite paper suggested trades. Trading Desk mutation body modeling is documented in `docs/trading-desk-api-models.md`, with narrow Pydantic adapters and envelope drift guards in `python-backend/trading_desk_api_models.py`; FastAPI handlers still use `dict[str, Any]` bodies and existing route parsers so public 400 behavior, proof rules, scanner safety, and response JSON remain unchanged. Trading Desk TypeScript request/response contracts are documented in `docs/typescript-api-contracts.md`, with manual compile-time envelopes in `src/lib/trading-desk/apiContracts.ts` and shallow runtime response-envelope validation in `src/lib/trading-desk/apiResponseValidation.ts`; this replaces opaque Trading Desk helper records and fails closed on malformed backend envelopes without adding request validators, OpenAPI, JSON Schema runtime validation, or generated TypeScript. The generated Trading Desk schema bridge is documented in `docs/trading-desk-schema-bridge.md` and stored at `data/contracts/trading-desk-api-schema-bridge.json`; it maps route contracts, manual TypeScript names, and narrow Pydantic adapter JSON Schemas for readability and drift checks only, with `runtime_use=false`. Generic Next route lifecycle headers are documented in `docs/route-lifecycle-contracts.md`, with `src/lib/route-lifecycle/routeContracts.ts` naming descriptive route/store/lifecycle headers for scan, predictions, risk/status, local operator session, sectors, and tools without changing auth, payloads, proof, or scanner behavior. FastAPI startup requires `OPTIONS_BACKEND_API_TOKEN` unless `OPTIONS_BACKEND_ALLOW_UNAUTHENTICATED=1` is explicitly set for local dev/test; `npm run dev` and `npm run dev:python` set that opt-out through `scripts/run_dev_python_backend.js`. FastAPI request/exception logging is stdlib JSON to stderr through `python-backend/logging_setup.py`; request middleware records method, path, status, duration, and mutation-intent header/value, while successful Trading Desk create/review/close mutation outcomes append compact local audit refs to ignored `data/operator-audit/mutations.jsonl`. Local SQLite safety is documented in `docs/local-db-hardening.md`, with DB role manifest in `python-backend/local_db_hardening.py` and read-only audit in `scripts/audit_local_databases.py`; it classifies `chat_history.db` as active suggested-trade SQLite, `data/tracked_positions.db` as test/legacy only, `data/operator-audit/mutations.jsonl` as ignored append-only JSONL local operator audit evidence, and options-history, forward ledgers, market cache, and AI commodity stores as outside Trading Desk repository hardening. Repository schema evolution is documented in `docs/repository-migrations.md`, with versioned manifest and ledger helpers in `python-backend/repository_migrations.py`. Repository constraint ownership is documented in `docs/repository-constraints.md`, with DB-enforced, API-enforced, proof-contract-owned, and deferred invariants represented in `python-backend/repository_constraints.py`; `npm run options:audit:data-integrity` loads local env and is the strict project gate for closed suggested-trade P&L, production/unclassified tracked-position P&L, and quarantined research/backfill diagnostics. Latest strict readback is clean: SQLite suggested trades `pass`, Postgres tracked positions `pass`, hard violations `0`, and diagnostics `0`. Repository index ownership is documented in `docs/repository-indexes.md`, with current read-path indexes and deferred candidates represented in `python-backend/repository_indexes.py`. Tracked positions are Postgres-owned through `DATABASE_URL`, suggested trades are SQLite-owned through `chat_history.db`, and memory/SQLite tracked-position repositories are explicit test or legacy-tool doubles rather than production fallbacks. Live scanner exposure and realized-loss cap math ignore explicitly identified research/backfill tracked rows while reporting ignored counts, so historical paper inventory cannot contaminate auto-track caps.

ThetaData terminal update: local ThetaTerminal v3 readiness for regular supervised-options quote imports is documented in `docs/thetadata-terminal-runbook.md`. The current local repo endpoint is `http://127.0.0.1:25503`; connection refusal or `WinError 10061` is feed-down, not evidence exhaustion. Start/probe ThetaTerminal v3 and rerun the importer dry-run before archiving current-source no-match branches.

Oracle loop 59-symbol source-repair update: `scripts/run_regular_options_59_symbol_thetadata_opra_import_repair.py`, exposed as `npm run options:source-repair:59-symbol-thetadata-opra-import`, owns the approved scoped 59-symbol ThetaData OPRA/NBBO source-repair preflight for `2024-05-22` through `2026-06-04`; `scripts/run_regular_options_59_symbol_thetadata_opra_import_resume.py`, exposed as `npm run options:source-repair:59-symbol-thetadata-opra-import-resume`, owns the current resume recheck. The old `blocked_thetaterminal_source_unavailable` connection-refused branch is superseded. Current local ThetaTerminal v3 is reachable and the dry-run resume preflight is ready, but the non-dry resume wrapper reports `blocked_59_symbol_import_repair` / `bulk_import_execution_not_started_by_preflight_wrapper`, with `import_attempted=false`, `imported_rows=0`, and `quotes_imported=false`. A direct OPRA quote dry-run returned `403 Forbidden` while the loaded terminal banner showed `Options: FREE`. Treat this as scoped import execution/entitlement-source state, not accepted profitability, not forward proof, and not an earned Oracle-loop stop.

Execution-alternative quote demand update: `scripts/build_regular_options_execution_alternative_replay_coverage.py` emits a read-only `quote_demands` manifest for the top-spread and contract-replacement blockers. `scripts/build_regular_options_execution_alternative_quote_import_plan.py` converts that manifest into a read-only grouped import/query plan at `docs/regular-options-execution-alternative-quote-import-plan.md`. After the 2026-06-08 ThetaData v3 import, latest coverage is `execution_alternative_replay_coverage_ready`: `0` missing quote demands, selected/top/replacement entry and exit coverage `full`, `12` true top-spread replay P&L rows, and `12` true contract-replacement replay P&L rows. The latest quote-import plan is `no_quote_demands_to_plan`. These artifacts still make no contract-selection, scanner-policy, broker, DB, stop, sizing, proof-bar, or promotion change.

Minute-exit quote demand update: `scripts/build_regular_options_minute_exit_replay_readiness.py` now consumes trusted intraday OPRA/NBBO exact-contract minute quotes from `data/options-validation/options_history.db` into read-only fixed-minute, side-aware entry/exit P&L. Latest readiness is `minute_exit_replay_coverage_ready`: `12` exact OPRA entry seeds, `1` position-linked seed, minute quote coverage `full`, engine status `read_only_side_aware_engine_partial`, `12` true minute-exit P&L rows, and decisions `hold_for_current_open_risk_review=1` plus `reject_production_use_without_fill_or_position_link=11`. `scripts/build_regular_options_minute_exit_quote_import_plan.py` now reports `no_minute_exit_quote_seeds_to_plan` with `0` command groups because source readiness already has minute P&L available. This remains read-only replay evidence; it is not current open-risk resolution, broker fill proof, stop-policy permission, scanner permission, sizing permission, promotion, or production proof for rows without fill/position linkage and exact realized exits.

Open-risk resolution update: `scripts/build_regular_options_open_risk_resolution_plan.py` owns the read-only row plan at `docs/regular-options-open-risk-resolution-plan.md`, exposed as `npm run options:plan:open-risk-resolution`. Current readback is `open_risk_resolution_plan_clear`: `0` plan rows, `0` display-only SELL rows, live-exact negative QQQ `id=537` resolved as a fresh executable HOLD, `5` open regular rows, `5` negative rows, avg open P&L `-54.51%`, median `-57.66%`, and `live_policy_change=false`. The monthly command center no longer carries generic open-risk resolution actions; it does not create trades, submit broker orders, auto-close display-only marks, mutate DB state, change scanner policy, stops, sizing, proof bars, or lane promotion.

Suggested-trade review update: `scripts/build_regular_options_suggested_trade_review_plan.py` owns the read-only row plan at `docs/regular-options-suggested-trade-review-plan.md`, exposed as `npm run options:plan:suggested-trade-review`. Current readback is `suggested_trade_review_plan_ready_for_historical_resolution` with operator status `ready_for_historical_suggested_trade_resolution`: `1` plan row for suggested trade `id=138` / ticker `AAA` / contract `AAA260408C00462590`, expiry `2026-04-08`, `1` missing review row, `0` executable close-ready rows, `0` market-window-required rows, `1` expired review-resolution row, and `live_policy_change=false`. The monthly command center uses it to replace the generic ledger `refresh_suggested_trade_review` bucket with `execute_suggested_trade_review_plan`; resolving this row requires historical exact exit or expiry evidence, a stored review snapshot, candidate-ledger rerun, and monthly audit rerun. It does not create trades, submit broker orders, mutate suggested-trade DB state, auto-close from stale/display/missing review marks, change scanner policy, stops, sizing, proof bars, or lane promotion.

Fill-attempt evidence capture update: `scripts/build_regular_options_fill_attempt_evidence_capture_plan.py` owns the read-only row plan at `docs/regular-options-fill-attempt-evidence-capture-plan.md`, exposed as `npm run options:plan:fill-attempt-evidence-capture`. Current readback is `fill_attempt_evidence_capture_plan_ready_blocked_for_fresh_selection`: `4` plan rows from `2026-06-05`, all missing durable fill-attempt evidence, with lanes `short_term=1`, `swing=2`, `volatility_expansion_observation=1`, tickers `QQQ=2` and `SPY=2`, `497` source fill-attempt rows, and `live_policy_change=false`. The monthly command center uses it to replace the generic ledger `capture_missing_fill_attempt_evidence` bucket with `execute_fill_attempt_evidence_capture_plan`; it does not create trades, submit broker orders, mutate trading-row DB state, backfill broker fills, change scanner policy, stops, sizing, proof bars, or lane promotion.

Structure-specific harness update: `scripts/build_regular_options_structure_specific_harness.py` owns the read-only structure split at `docs/regular-options-structure-specific-harness.md`, exposed as `npm run options:replay:structure-specific-harness`. Current readback is `structure_specific_harness_built_collecting`: `12` candidate-shown rows, all `vertical_spread`; `10` proof-live exact entry rows; `1` paper-fill-recorded row; `12` true structure-specific P&L rows consumed from trusted ThetaData OPRA/NBBO minute-exit replay; and remaining blocker `single_leg_or_other_multileg_samples_missing`. Decisions are `hold_for_current_open_risk_review=1` and `reject_production_use_without_fill_or_position_link=11`. The monthly command center uses it to remove `build_structure_specific_harness`; it does not count daily/EOD, midpoint, stale, last-trade, display, migrated paper, or research/backfill evidence as production proof, and it does not resolve open risk, create exact realized P&L rows, or promote the vertical slice.

Event data spine update: `scripts/build_regular_options_event_data_spine.py` owns the read-only event annotation spine at `docs/regular-options-event-data-spine.md`, exposed as `npm run options:replay:event-data-spine`. Current readback is `event_data_spine_built_collecting`: `12` candidate-shown rows over `3` tickers and `4` playbooks; `10` proof-live exact entry rows; `1` paper-fill-recorded row; `0` event-annotated rows; `12` missing event annotations; `0` true event replay P&L rows; and `0` post-event vol-crush replay P&L rows. The monthly command center uses it to remove `build_event_data_spine`; it does not count daily/EOD, midpoint, stale, last-trade, display, migrated paper, or research/backfill evidence as production proof.

For the regular `bullish_pullback_observation` lane, historical proof currently uses trusted ThetaData intraday OPRA/NBBO rows only. Imported replay lookup helpers support a `trusted_only` mode, and the research runner supports entry-time execution-aware ranked backfill, tier-aware sleeve selection, allocation caps, selected-contract execution memory, fixed-time spread exit monitoring, calendar-basis spread time exits, and by-sleeve diagnostics. The current profitability-first paper/live-shadow family is the no-CAT/no-PM hard-prior fixed-exit sleeve family. The strongest 100+ exact-trade branches are ticker-cluster fixed-exit variants: `sleeve_winner_cluster_exit_50_55_60_no_pld_xlk_v1` is the high-PF branch with unresolved provider no-matches, and `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_v1` is the higher-coverage quoted branch with one unresolved provider no-match. `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_jnj_v1` is the clean quoted proof subset with `95` exact trades and no unresolved candidates, but it is below the `100`-trade target. The count-expanded all-59 paper-shadow branch is `sleeve_pf59_coverage_a_refill_v1` with `130` exact quoted trades at PF `2.04` and `97.7%` quote coverage, but it remains strict-proof blocked by `3` unresolved WMT/JNJ provider no-match candidates. Confidence-tier reporting lives under `data/profitability-lab/bullish-pullback-observation/confidence/` and should be used to separate tradable S/A/B evidence from scout-only broad-symbol evidence. The per-ticker audit under `data/profitability-lab/bullish-pullback-observation/ticker-audit/` is the current source for keep/move/research/remove decisions across the full `59`-symbol universe.

The regular profit-loop automation uses the same trusted intraday source. `HISTORICAL_OPTIONS_DB_PATH` resolves through `.env.local` to `data/options-validation/options_history.db`, readiness audits print that effective path, live scan policy and profit-loop proof fingerprints use `historical_imported`, and active truth refresh rebuilds the intraday artifact instead of the archived daily artifact. The 2026-06-05 strict non-dry canary passed end to end after rebuilding `data/options-validation/runs/20260605_022559_broad_intraday.json`, reading `20,048,707` trusted intraday rows through `2026-06-04`, recording forward holdout candidate flow through the regular `quality90_debit55_canary` proof-control lane, and finding no open profit-loop issues. The stale `20.7%` broad intraday quote-coverage blocker was repaired as an operational data-pipeline issue on 2026-06-14 by importing missing trusted ThetaData OPRA/NBBO exit-quote rows and rerunning the production-equivalent broad artifact. The active artifact is now `data/options-validation/runs/20260613_213820_broad_intraday.json`: `104` / `121` candidates priced, `quote_coverage_pct=86.0`, and the exact-fill planner returns `0` remaining missing-exit quote targets. The remaining `17` unpriced rows are replay-selection blockers (`13` `missing_long_entry_quote`, `4` `invalid_spread_width`), not missing imported exit quotes. Broad remains exploratory-only and non-promotable; the exact-contract subset is still negative (`11` trades, PF `0.23`, avg `-35.56%`), so this repair is not a profitability promotion.

The regular all-lanes candidate pipeline now requires lane profitability before a selected candidate can be treated as live-validation or auto-track eligible. `scripts/audit_missed_regular_picks_outcomes.py` prices the May 22 through June 5 missed selected set with trusted intraday exact-contract marks and writes `data/forward-tracking/missed_regular_picks_outcome_latest.json` plus `docs/missed-regular-picks-outcome-audit.md`; `scripts/analyze_missed_regular_picks_failure_modes.py` then writes `data/forward-tracking/missed_regular_picks_failure_modes_latest.json` plus `docs/missed-regular-picks-failure-modes.md`; `scripts/analyze_missed_regular_picks_filter_matrix.py` writes `data/forward-tracking/missed_regular_picks_filter_matrix_latest.json` plus `docs/missed-regular-picks-filter-matrix.md` so candidate filters can be compared before scanner policy changes. Latest readback: `210` rows were fully priced through `2026-06-04` using `quote_evidence_class=trusted_intraday_opra_nbbo`, while the row policy remains `evidence_group=research_backfill` and `production_claim=false`; this is not a broker-fill or production-proof surface. The set includes `4` already-tracked rows with stored P&L and `206` untracked rows. The failure-mode data status is `clean_for_failure_analysis`, not a remaining data blocker. The untracked rows had `72` winners, `134` losers, avg net `-16.54%`, median `-12.64%`, PF `0.32`, `-$18,755.00` one-spread net dollars, `41` rows at or below `-50%`, and `15` rows at or below `-80%`; the verdict is `data_clean_strategy_unprofitable`. Only `volatility_expansion_observation` currently passes the profitability gate, but passed-gate rows route to paper/probation rather than live validation while fresh forward evidence matures; the lane still self-blocks SPY/IWM clusters and debit above `45%` of width, and the scanner caps it at `6.0%` max fill degradation versus midpoint plus `2.0%` max worst-leg bid/ask spread while it remains a starter lane. The filter matrix shows lane gate plus self-guardrails keeps `10` untracked rows at PF `69.14`, avg `+34.87%`, and later-date split pass, but loses `63` historical winners, so it is a paper-shadow/probation signal rather than production permission. The largest diagnostic damage clusters are XLK, SPY, TSLA, and IWM, while debit `>=45%` of width and DTE `>=36` are retest-required pre-entry guardrail candidates, not immediate live policy changes. Diagnostic lanes need at least `30` exact marked rows, `0` unpriced rows, PF `>=1.2`, positive average net P&L, entry-time-only rules, and later-date/OOS evidence before probation. `scripts/lane_promotion_state.py` now owns the shared promotion-state artifact at `data/forward-tracking/lane_promotion_state_latest.json` and `docs/lane-promotion-state.md`; latest readback has `2` diagnostic lanes, `1` paper/probation lane, `11` parked lanes, `0` live-validation lanes, `0` auto-track lanes, `0` current live-exact negative open rows, and `open_risk_governor_pass`, so no regular lane is live-validation eligible today. The volatility lane's current candidate status is `pending_paper_exact_evidence`, and its June 4/5 rows are pre-freeze diagnostics rather than post-`2026-06-14` acceptance evidence. The daily all-lanes safety net loads a fresh gate, fresh promotion state, and current open-risk governor before queueing or creating, routing otherwise clear candidates to diagnostics or paper/probation if reports are missing, malformed, future-dated, missing `generated_at_utc`, stale, unpriced, incomplete, lacking walk-forward/fresh-paper evidence, blocked by unresolved live-exact risk, or under a lane-specific circuit breaker; exact duplicate spreads across lanes are suppressed to one risk owner with paper-only duplicate dispositions. Pending validation rechecks the same gates before scanner reruns, writes paper-only rows for blocked or probation candidates, and fails closed if either report is unusable. Scanner validation reruns enforce them with `OPTIONS_ENFORCE_LANE_PROFITABILITY_GATE=1`. This is a routing gate for paper/live-validation safety, not broker execution evidence or production proof.

The 2026-05-29 next-layer profitability iteration tested separate frozen ETF/index, high-beta, defensive/refill, REIT, and industrial scout hypotheses under the same trusted ThetaData intraday OPRA/NBBO proof rule. ThetaData v3 was started on `127.0.0.1:25503`, exact and targeted backfills were imported, affected replays were rerun, and readiness remains `ready_for_exact_replay` across all `59` required underlyings. The best new move-bucket scout, `sleeve_next_move_bucket_refill_v1`, reached only `135` exact trades at PF `2.03` with `91.2%` quote coverage, `13` unresolved candidates, and 5%/side stress PF `1.49`, so it does not materially improve the `130`-trade count-expanded branch. The highest-count expanded branch reached `165` exact trades but failed at PF `1.49`, stress PF `1.12`, low coverage, and rolling-OOS watch status. The best clean 100%-covered branch remains `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timecombo55_50_75_mixed_v1` with `129` exact trades, PF `2.20`, rolling test PF `2.51`, and 5%/side stress PF `1.67`. That 2026-05-29 "not 200" conclusion was scoped to the tested next-layer bullish-pullback expansion artifacts, not every regular stock-lane artifact in the repo.

The current frozen bullish-pullback layer stack is generated by `scripts/build_bullish_pullback_layer_stack.py` at `data/profitability-lab/bullish-pullback-observation/layer-stack/latest.json` and documented in `docs/bullish-pullback-layer-stack-2026-05-29.md`. A post-stack defensive refill scout rerun attempted exact ThetaData import for `sleeve_next_defensive_refill_v1`; it imported `0` new trusted rows, stayed at `130` exact trades with only `94.2%` coverage, and is not promoted because the incremental WMT/PM tier is weak and remaining misses are provider no-match exact contracts.

The active options operating scorecard is `scripts/build_regular_profitability_operating_scorecard.py`, with latest artifacts under `data/profitability-lab/regular-options-operating-scorecard/` and dashboard `docs/regular-options-operating-scorecard.md`. It separates product-side Trading Desk profitability from proof-grade autoresearch readiness, live safety, route/cache performance, paper-gate readiness, profit-capture queue, and the separate AI commodity OPRA strategy lane. Current paper-gate evidence remains blocked: fresh exact candidates are visible, but exact realized P&L rows and promotion-ready rows are `0`, QQQ `id=537` still needs a legitimate exact realized exit, and suggested-trade `id=138` still needs explicit review. The all-20 profitability layer stack is generated by `scripts/build_regular_options_profitability_layer_stack.py` at `docs/regular-options-profitability-layer-stack.md`; current status is `all_20_layers_wired_research_blocked`, with `20` / `20` layers wired, `7` ready, `7` collecting, `6` blocked, implementation counts of `5` built, `4` built-blocked, `8` built-collecting, `2` built-replay-coverage blocked, and `1` built-replay-coverage ready. The stack is a read-only iteration control plane for profitability work, not a live-release, scanner-policy, broker, stop, sizing, or proof-bar change.


The exact-candidate selection repair is generated by `scripts/build_regular_options_exact_candidate_selection_repair.py` at `docs/regular-options-exact-candidate-selection-repair.md`. Current read is `exact_candidate_selection_repair_targets_ready`: the only target is `regular_bearish_put_primary` on `2026-05-22`, with `4` signals, `0` exact candidates, signal tickers META/COIN/SBUX/DIS, and `no_chain_native_spread_passed_current_filters=4`. It replaces the monthly `repair_chain_native_exact_candidate_selection` queue item with diagnostic chain-native relaxation work without changing scanner policy, contract selection, lane promotion, broker behavior, DB state, or proof bars.

The chain-native filter relaxation replay is generated by `scripts/build_regular_options_chain_native_filter_relaxation_replay.py` at `docs/regular-options-chain-native-filter-relaxation-replay.md`. Current read is `chain_native_filter_relaxation_replay_candidates_found_diagnostic_only`: `1` target lane/date, `4` signals, `7` predeclared relaxation scenarios, `28` scenario rows, `4` current selected entry spreads, `24` relaxed selected entry spreads, and `0` remaining entry quote demands after the trusted `2026-05-22` META/DIS/SBUX/COIN put quote import. The follow-on exit replay and archive retired this branch as exact-priced negative; it does not relax live contract-selection policy or synthesize P&L.

The chain-native exit outcome replay is generated by `scripts/build_regular_options_chain_native_exit_outcome_replay.py` at `docs/regular-options-chain-native-exit-outcome-replay.md`, and the archive is generated by `scripts/build_regular_options_chain_native_relaxation_archive.py` at `docs/regular-options-chain-native-relaxation-archive.md`. Current read is `chain_native_exit_outcome_replay_exact_pnl_available_diagnostic_only` plus `negative_chain_native_branches_archived`: all `28` selected chain-native scenario rows have exact trusted exit P&L, `0` exit quote demands remain, current filters are PF `0.00` / avg `-27.93%`, the best relaxed scenario is still negative at PF `0.62` / avg `-9.26%`, and all `7` exact-negative diagnostic branches (`1` current and `6` relaxed) are archived as dead ends. These artifacts remove the chain-native exact-exit, archive, and stale holdout actions from the monthly queue without changing scanner policy, contract selection, lane promotion, broker behavior, DB state, or proof bars.

The exhausted contract archive is generated by `scripts/build_regular_options_exhausted_contract_archive.py` at `docs/regular-options-exhausted-contract-archive.md`. Current read is `exhausted_contract_target_archived`: `8` exact contract/date repair targets are archived as current-source exhausted after repeated exact-date no-match attempts, with `1` newly archived in the latest run and `38` eligible exhausted targets remaining. Archived targets are the GOOGL short-leg exit quote gaps `GOOGL260102C00355000` and `GOOGL260102C00360000` on `2025-12-22`, `GOOGL260102C00365000` on `2025-12-23`, `GOOGL260213C00350000` on `2026-02-12`, `GOOGL260306C00365000` on `2026-02-27`, and `GOOGL260306C00360000` on `2026-03-02` from `tracked_winner_chain_native_qqq_time80_intraday`, plus `GOOGL260102C00355000` and `GOOGL260102C00360000` on `2025-12-22` from `tracked_winner_cheap_debit_continuity_v1`; they remain retryable only with a new exact source or materially new evidence, and no no-match row counts as production proof.

The regular stock-options multi-lane portfolio runner is `scripts/run_regular_options_multilane_portfolio.py`, with latest artifacts under `data/profitability-lab/regular-options-multilane/` and report `docs/regular-options-multilane-2026-05-30.md`. It counts trusted intraday OPRA/NBBO exact rows only, dedupes the proof stack by entry date, ticker, and direction, and now reports a `200` quality gate that separates count success from production readiness. The current readback clears the `200` count target with `234` strict-deduped exact trades, PF `2.16`, and avg `+26.76%`, by combining `sleeve_pf59_coverage_a_refill_v1` with the latest `lane_a_chain_native_ret20_4_stop200_time75_rerun4_v1` artifact. This proves the count is not structurally impossible, but the quality gate remains `quality_pending`: Lane A has `155` exact trades, `137` unpriced candidates, `53.1%` quote coverage, and strong priced-only economics. A side-aware zero-bid replay found that conservative pricing resolves `126` of `127` Lane A missing-exit candidates, but those rows alone are PF `0.11` and avg `-66.59%`; combined Lane A falls to `281` priced trades, `96.2%` coverage, PF `0.85`, and avg `-6.51%`. The quality gate now surfaces Lane A zero-bid status as `blocked` because conservative combined PF is below `1.30`, conservative combined unpriced exits are `11`, and zero-bid exit rate is `41.99%`, above the `2%` gate.

The regular per-symbol sleeve matrix is generated by `scripts/build_regular_options_symbol_sleeves.py`, with latest artifacts under `data/profitability-lab/regular-options-symbol-sleeves/` and report `docs/regular-options-symbol-sleeves.md`. It is an audit/reporting layer under existing lane logic, not permission to create ticker-specific strategy forks. The latest readback found `60` tracked symbols and `343` symbol-lane rows, with statuses `keep=25`, `watch=59`, `quarantine=91`, `rejected=82`, and `needs-paper=86`. Bullish Pullback's legacy carrier symbols remain `AAPL`, `COP`, `CVX`, `GOOGL`, `IWM`, `JNJ`, `LLY`, `NEM`, `UNH`, and `XOM`, and legacy remove recommendations remain `ABBV`, `BAC`, `C`, `COIN`, `FCX`, `JPM`, `PLTR`, `RTX`, and `SLB`, but per-ticker queue-change emission now uses the additive `n_floor_disposition` and `queue_change_allowed_by_n_floor` fields: rows below `30` exact trades are labeled `insufficient_n_frozen` while legacy `status`/`decision` stays visible for migration. Per-symbol expectancy readbacks now also include shrinkage toward the lane mean via `expectancy_calibration.py`. The matrix currently shows no high-beta symbol-lane row clearing the real-crusher sample/coverage bar; thin positives are watch/noisy rather than proof of option-spread edge, and refreshed zero-bid/unresolved rows are quarantined rather than promoted.
The bullish-pullback layer-shadow selector is generated by `scripts/build_bullish_pullback_layer_shadow_selection.py`, with latest artifacts at `docs/bullish-pullback-layer-shadow-selection.md` and `data/forward-tracking/bullish_pullback_layer_shadow_selection_latest.json`. It selects `layer_4_clean_exact` as the primary future paper-shadow harness, keeps `layer_5_count_expanded` as reference-only because of unresolved unpriced candidates, and carries the S/A/B high-PF core reference into market-window checklist requirements. This is harness routing only; it requires exact entry/exit, leg-level bid/ask audit, assignment/expiration review, and denominator/failure-row handling before any future paper-shadow row can count. The execution-safety preflight is generated by `scripts/build_bullish_pullback_layer_execution_safety_audit.py`, with latest artifacts at `docs/regular-options-bullish-pullback-layer-execution-safety-audit.md` and `data/forward-tracking/bullish_pullback_layer_execution_safety_audit_latest.json`. The current real audit is `blocked_execution_safety_preflight`: all `129` selected historical rows parse to vertical-spread OCC legs, all have trusted entry quote pairs, `126` have trusted exit quote pairs, `6` are zero/untradable, and all `129` mismatch historical source marks. The executable-economics report is generated by `scripts/build_bullish_pullback_layer_executable_economics.py`, with latest artifacts at `docs/regular-options-bullish-pullback-layer-executable-economics.md` and `data/forward-tracking/bullish_pullback_layer_executable_economics_latest.json`; it finds `120` tradable executable rows with `net_usd_total=45610.0`, PF `3.7414`, bootstrap PF lower bound `2.27`, and status `executable_economics_recomputed_profitable_but_preflight_blocked`. The forward capture protocol is generated by `scripts/build_bullish_pullback_layer4_forward_capture_protocol.py`, with latest artifacts at `docs/regular-options-bullish-pullback-layer4-forward-capture-protocol.md` and `data/forward-tracking/bullish_pullback_layer4_forward_capture_protocol_latest.json`; it is ready for future market-window/operator-approval discussion only, and the validator remains read-only with no append path.

The regular profit-capture queue is generated by `scripts/build_regular_options_profit_capture_queue.py`, with latest artifacts under `data/profitability-lab/regular-options-profit-capture-queue/` and report `docs/regular-options-profit-capture-queue.md`. It combines symbol sleeves, current-policy historical symbol/lane buckets, the fresh regular guardrail-starvation audit, and optional repair-attempt readback from `data/profitability-lab/regular-options-repair-attempts/latest.json`. Tier A rows are clean exact capture evidence only; Tier B rows are profitable watch/repair evidence, including current-policy paper rows such as SPY/QQQ swing; Tier B medium/high repairs now include Tier A promotion gaps, missing quote-date/contract summaries, and repair actionability; Tier C rows are fresh scan signature matches; blocked rows remain blocked with guardrail reasons; quarantine rows are first-class do-not-chase evidence. Latest readback: `15` Tier A rows, `82` Tier B rows, `16` high-priority repair rows, `6` clear fresh SPY/QQQ lane-signature matches, `9` blocked-but-interesting rows, and `173` quarantine rows. Repair actionability currently counts `15` `no_repair_needed_clean_exact`, `3` `needs_status_or_forward_validation_after_repair`, `13` `current_source_exhausted`, `23` `lookahead_only_not_exact_proof`, and `43` `not_applicable` low-priority Tier B rows. The exact repair burn-down is generated by `scripts/build_regular_options_repair_burndown.py`, with report `docs/regular-options-repair-burndown.md`; use it before any provider import because it separates active exact targets from source-replay-required rows, lookahead-only diagnostics, and exhausted current-source loops. Selection readiness is paper/research routing only: `paper_review_candidate` for Tier A, `watch_repair_only` for Tier B, `historical_signature_only` for clear fresh matches, and `do_not_chase` for quarantine. Tier A rows now carry a `paper_shortlist_bridge`; that bridge requires a fresh executable quote-window scanner lane match before paper-shortlist eligibility. The paper-shortlist release gate is generated by `scripts/build_regular_options_paper_shortlist.py`, with report `docs/regular-options-paper-shortlist.md`; strict mode fails closed on a missing source queue or ready rows that violate invariants, and invariant-bad rows are excluded from the eligible candidate list. Current read has `0` eligible paper-review candidates, `0` invariant violations, all `15` fresh matches `not_bridge_eligible`, and `live_policy_change=false`. The post-repair verdict is no live picks from this artifact alone, no GOOGL chase, and NEM only as a paper-review candidate unless a fresh executable scan matches it. No live scanner, broker, or stop policy changes are implied by this queue or burn-down.

The current-policy historical picks audit is generated by `scripts/build_current_policy_historical_picks_audit.py`, with latest artifacts under `data/forward-tracking/` and report `docs/current-policy-historical-picks-audit.md`. It replays closed Trading Desk history through today's promoted repair-lane entry guardrails without deleting or rewriting historical paper rows. The latest readback audited `488` closed rows, found `400` current-policy scope rows, and split them into `112` `would_take_today` rows, `274` `blocked_by_current_policy` rows, `14` `unknown_missing_evidence` rows, and `88` out-of-scope-lane rows. The raw realized scope averaged `+4.87%` with `51.8%` negative rate; the current-policy slice averaged `+53.54%` with `25.9%` negative rate; learned-away rows averaged `-17.56%` with `63.8%` negative rate. This is now retained as a research artifact only, not a dashboard panel or default Closed Trades view. Treat it as product-side policy replay and historical-learning cleanup, not production-proof profitability.

The current-policy cohort health report is generated by `scripts/build_current_policy_cohort_health.py`, with latest artifacts under `data/forward-tracking/` and report `docs/current-policy-cohort-health.md`. It answers why the early profitable April picks and the recent negative screen can both be true: the current-policy replay is strong in aggregate, but the recent cohort has broken. Latest readback: overall current-policy rows are `112` priced at avg `+53.54%` and median `+50.6%`; showcase month `2026-04` has `70` priced rows at avg `+81.17%`, median `+71.82%`, and `8.6%` negative rate; recent month `2026-05` has `42` priced rows at avg `+7.49%`, median `-4.6%`, and `54.8%` negative rate; latest week `2026-W21` has `3` priced rows at avg `-82.06%` and `100.0%` negative rate. Current overall cohort state is `paper_only_recent_week_break`: April-like historical cohorts can be shown as discovered edge, but recent current-policy picks should be paper-only until a fresh recovery cohort revalidates them.

The Trading Desk exit-policy replay is generated by `scripts/replay_trading_desk_exit_policies.py`, with latest artifacts under `data/forward-tracking/` and report `docs/trading-desk-exit-policy-replay-2026-05-31.md`. The 2026-06-02 stop-grid refresh keeps live stop policy unchanged: the `107` stored executable-review rows have baseline avg `+39.06%`, median `+43.72%`, `23` negatives, and `2` rows `<= -90%`; `stop_60`, `stop_70`, `stop_80`, and `stop_90` reduce the `<= -90%` bucket to `1` at best, but the positive-delta stop shapes still increase negatives to `24` and flip `2` stored winners to losses. Do not lower the live `90%` stop cap from this replay alone. The separate current-policy stop grid below partially addresses historical rows without stored review timelines, but it is still daily close-check evidence rather than minute-level intraday stop evidence.

The current-policy historical stop grid is generated by `scripts/replay_current_policy_historical_stop_grid.py`, with latest artifacts under `data/forward-tracking/` and report `docs/current-policy-historical-stop-grid.md`. It is a read-only exact-contract OPRA/NBBO daily close-check replay, not a minute-by-minute intraday stop simulation. Latest readback replayed all `112` Postgres current-policy realized rows with `0` unresolved rows across all `17` tickers. Because that tracked slice is only Apr/May 2026, the same report now also carries a separate annual replay-backed exact cohort from the regular multi-lane stack: `234` rows, entry window `2025-08-14` to `2026-03-24`, exit window `2025-09-09` to `2026-04-27`, and `0` unresolved rows across all `37` tickers. That annual cohort is shaped for stop-grid audit evidence only; it is not inserted into the tracked-position store and is not broker/live tracked fills. The tracked baseline is avg `+53.54%`, median `+50.60%`, `29` negatives, and `8` rows `<= -90%`; `stop_80` is the only non-destructive daily close-check candidate on that slice. The annual replay-backed cohort is less favorable to tighter stops: all tested `50%` through `90%` stops reduce average P&L and flip winners, including `10` winner flips at `stop_90`. Keep live stops unchanged until minute-level OPRA/NBBO stop evidence and entry-filter validation clear the promotion bar.

The current-policy entry-filter lab is generated by `scripts/analyze_current_policy_entry_filters.py`, with latest artifacts under `data/forward-tracking/` and report `docs/current-policy-entry-filter-lab.md`. It found one paper-only candidate worth monitoring: `short_term_fill_degradation_ge_15` blocks `9` historical current-policy rows, avoids `5` rows `<= -50%`, avoids `3` rows `<= -90%`, loses `2` winners, lifts the kept cohort to avg `+61.01%` and median `+53.33%`, and keeps the daily close-check `stop_80` overlay at avg `+61.17%`. The all-regular-lanes walk-forward read is generated by `scripts/validate_current_policy_entry_filter_walkforward.py`, with report `docs/current-policy-entry-filter-walkforward.md`; it classifies the frozen rule as `mixed_walkforward_watch_not_promoted`. The frozen short-term filter is positive over all `112` rows and the `2026-05` holdout, but the `2026-04` calibration slice is `winner_damage_too_high`, and the diagnostic broad all-lane fill-degradation `>=15%` rule is rejected with `10` lost winners. Keep the rule lane-scoped and paper-only. The point-in-time scanner candidate replay is generated by `scripts/replay_short_term_filter_point_in_time.py`, with report `docs/current-policy-entry-filter-point-in-time.md`; latest readback is `paper_only_collecting` with `5` scanner candidate rows, `0` champion-matched rows, `0` exact-priced rows, and `5` unpriced/non-executable rows. The forward monitor is generated by `scripts/monitor_current_policy_entry_filter_paper.py`, with report `docs/current-policy-entry-filter-paper-monitor.md`; it is currently `collecting` with `0` fresh rows since `2026-06-02`. The recent-cohort circuit breaker is generated by `scripts/build_current_policy_circuit_breaker.py`, with report `docs/current-policy-circuit-breaker.md`; current readback routes `short_term` and `bullish_pullback_observation` pending candidates to `paper_validation_only` while cohort health is `paper_only_recent_week_break`, with `0` lane deletions and `live_policy_change=false`. `scripts/validate_pending_scan_candidates.py` consumes that breaker before auto-track validation and writes a paper-only validation disposition for affected lanes. Any promotion discussion requires passing point-in-time replay, at least `20` fresh current-policy rows, at least `5` fresh champion-matched blocked rows, executable realized P&L from trusted exact-contract evidence, and no winner damage.

The frozen regular-options autoresearch evaluator is `scripts/evaluate_regular_options_autoresearch.py`, with `/goal` prompt `docs/autoresearch/regular-options-goal.md`, experiment harness `scripts/run_regular_options_goal_experiment.py`, and ledger `data/profitability-lab/regular-options-autoresearch/ledger.jsonl`. Do not read `data/profitability-lab/regular-options-autoresearch/latest.json` as the immutable baseline: it is the latest global evaluator result, while baseline/current-stack truth is reproduced from the ledger or a no-write evaluator run. As of 2026-05-31, the goal experiment harness writes evaluator artifacts inside each experiment batch by default; overwriting the global evaluator latest requires explicit `--write-global-latest`. As of 2026-06-09, evaluator v2 keeps promotion gates and all-or-nothing `score` unchanged, adds diagnostic `progress_score` over executable-P&L milestones, ranks batches by `(score, progress_score)`, and removes retired Lane A default variants from the harness. As of 2026-06-12, the evaluator also emits deterministic trade-level bootstrap diagnostics over net P&L% for the combined stack and each selected branch: `pf_point`, `pf_lb_5pct`, `pf_ub_95pct`, `avg_net_lb_5pct`, `n_trades`, and `statistical_confidence`. It also emits advisory search-effort accounting from the autoresearch ledger: `variants_searched` per `strategy_family` and `selection_adjusted_bar = 1.0 + 0.05 * log2(max(variants_searched, 1))`, rounded to two decimals, for PF-LB discussion context. Bootstrap and search-effort fields are diagnostic only and do not change `score`, `progress_score`, scanner policy, proof bars, promotion state, or existing gates. As of 2026-06-14, `data/contracts/forward-holdout-contract.json` and generated doc `docs/forward-holdout-contract.md` protect the forward range starting `2026-06-05`: ordinary goal experiments whose implied replay window overlaps that range are refused before replay, and `--champion-final-eval` can consume it only once per strategy family with the consumption recorded in the autoresearch ledger. The baseline ledger row judged the `234` stack as `score: 0.00`, clean count `0`, scout count `234`, effective side-aware coverage `96.71%`, unresolved `14`, zero-bid exit rate `41.99%`, and Lane A conservative PF `0.85`. The latest experiment-scoped batch, `data/profitability-lab/regular-options-autoresearch/experiments/20260601T020317Z/summary.json`, tested causal Lane A memory variants and did not overwrite global latest. The best symbol-health memory branch improved effective coverage to `99.06%` and unresolved candidates to `3`, but still had clean count `0`, scout count `191`, zero-bid exit rate `43.24%`, and Lane A conservative PF `0.92`. Earlier goal loops showed the same pattern: simple entry-liquidity filters left Lane A conservative PF below `0.90`, while the bad-zero-ticker/debit repair reached Lane A conservative PF `1.30` to `1.36` only by shrinking to `72` to `81` exact trades, below the portfolio-candidate threshold. A separate contrarian artifact scan found a raw `300+` exact-priced scenario is possible only by adding low-coverage tracked-winner intraday research; current trusted-intraday tracked-winner reruns are visible as rejected scouts and do not replace Lane A. Clean/high-coverage bullish-pullback artifacts top out at `157` to `158` strict-deduped trades without Lane A because they overlap heavily. Future `/goal` loops should optimize against the frozen judge and prioritize genuinely non-overlapping regular stock-options sleeves or a materially different exit/liquidity rule, not raw trade count or more Lane A entry/memory tuning.

The all-planned regular stock-options audit runner is `scripts/run_regular_options_all_planned_sleeves.py`, with latest artifact `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/latest.json`. It tests every currently implemented planned sleeve end-to-end, runs side-aware zero-bid replay for profitable gap-closing candidates before ranking them, and records planned lane-lab specs that are blocked by missing data, missing paper logs, or missing instrumentation. The latest full refresh loaded by the walk-forward workflow tested `44` implemented variants as of `2026-06-04` with `0` run failures and still found no clean gap-closer over the `157` core-plus-clean-reference stack. After scoped exact quote repair, current worth counts are `1` `profitable_but_overlaps`, `1` `repair_stress_before_counting`, `6` `weak_positive_or_marginal`, `6` `thin_sample`, `19` `not_worth_current_shape`, and `11` `no_current_candidates`; the previously coverage-blocked peer variants moved to overlap, stress, or marginal economics blockers rather than becoming countable clean gap closers. IWM remains diagnostic: the refreshed per-ticker sleeve is profitable but thin/low-coverage, while stricter IWM WFO probes stay non-promotable. The tracked-winner family remains watch or rejected despite some priced positives because coverage and zero-bid/side-aware economics remain adverse. Historical WFO replay rows persist diagnostic-only `selected_spread`, `top_spread_alternatives`, and fill-degradation fields for chain-native spreads; these are observability fields only and do not relax trusted exact bid/ask, coverage, robustness, or zero-bid proof gates. The remaining lane-lab rows are not considered disproven; they need data imports, paper logs, or instrumentation before replay metrics can exist.

Sector ETF expansion is no longer data-depth blocked. The trusted intraday local store has `252` ThetaData OPRA/NBBO quote dates for `IWM`, and `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` now each have `252` trusted shared quote dates from `2025-05-22` through `2026-05-22`. The 2026-05-31/2026-06-01 Theta v3 imports created batches `1832` through `1920`, totaling `2,651,299` trusted intraday sector ETF rows with zero rejects; the later targeted `10:10` entry and `15:45` min-DTE-0 exit backfills intentionally produced duplicates where the broader `1h` import already covered rows. `scripts/plan_regular_sector_etf_imports.py --json` now returns `ready_for_sector_replay`. The first regular-sector replay pass added focused XLE, XLF, KRE, SMH, TLT, and sector-rotation variants while deliberately excluding GLD/commodity-lane sleeve implementation. That partial batch, `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260601T015109Z/summary.json`, rejected the simple shapes: TLT/XLE/XLF/KRE produced no current candidates, SMH priced `17` of `20` candidates at PF `0.49`, avg `-15.45%`, coverage `85.0%`, and 5%/side stress PF `0.32`, and the regular sector ETF stack priced `16` of `23` candidates at PF `0.27`, avg `-32.36%`, coverage `69.6%`, and stress PF `0.19`. These are not clean count candidates, and the frozen evaluator remains unchanged.

Zero-pick historical selection audits now run through all supervised scan lanes with `scripts/audit_zero_pick_days_all_lanes.py`. No lane has priority in these audits: each scan playbook must either run through its own replay/calibration adapter or report a lane-specific failure. The prior `main_lane_zero_pick_current_algo_v1` output is a `bullish_pullback_observation` single-lane artifact, not an all-lanes backfill. The 2026-06-05 missed-pick repair rerun wrote `data/forward-tracking/all_lanes_zero_pick_current_algo_audit_20260605T073037Z.json` and updated `data/forward-tracking/all_lanes_zero_pick_current_algo_audit_latest.json` in report-only mode, excluding the separate AI commodity lane: `13` / `13` regular lanes completed, `405` signal candidates, `389` exact historical candidates, `206` would-track selections, `apply=false`, no `no_exact_option_quotes_for_date` blockers, and quote-store coverage `9` / `10` market days with only pre-session `2026-06-05` missing. The 2026-05-31 all-lanes applied run remains the latest applied migration source with `14` / `14` lanes completed, `1,496` signal candidates, `1,228` exact historical candidates, and `425` tracked research/backfill picks. The all-lanes paper migration report is `data/forward-tracking/all_lanes_zero_pick_position_migration_v1_latest.json`: `425` historical paper positions created, with all scan/fill log rows linked to tracked position IDs. Historical backfill realized P&L is repaired through `scripts/repair_historical_backfill_realized_pnl.py` and must reflect the first executable historical suggested close backed by trusted exact OPRA/NBBO spread quotes; rows without exact exit-leg evidence remain visibly unpriced. After the June 5 zero-bid/data-integrity repair, the strict repository audit has `0` hard violations and `0` diagnostics. These rows are intentionally visible for learning, while remaining research/backfill paper evidence rather than live broker fills, production proof, or live exposure.

Latest local AI commodity readback:

- generated: `2026-06-03T20:22:59Z`
- exact shared quote dates: `3` / `100`
- proof source: `alpaca_opra_daily_snapshot`
- scan/proof universe: `24` aligned symbols
- live scan candidates: `0`
- latest fresh scan: `python scripts/run_ai_commodity_opra_progress.py --skip-capture` on `2026-06-01`; no material progress, `0` candidates, quote freshness `fresh_or_not_age_limited`, and raw drop reasons still recorded for `24` symbols
- latest guarded capture attempt: `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29` on `2026-06-03`, latest generated artifact `2026-06-03T20:22:59Z`
- latest capture result: failed/no material progress again; capture status was `no_rows_captured`, all `24` target symbols remained missing for `2026-05-29`, shared quote dates stayed `3`, `exact_capture_progress_outcome.status` was `exact_capture_progress_failed_or_not_observed`, local exact store already matched the proof window, local refresh cannot advance history depth, `snapshot_updated_since_is_backfill_capability=false`, and missed capture dates since the latest shared date are `2026-05-26`, `2026-05-27`, and `2026-05-28`
- next guarded event: still reports `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29` as ready, but this target has repeatedly failed with no rows captured and the latest next evidence action is `repair_full_scan_universe_capture_and_proof_alignment`; inspect/repair the full-universe capture path before looping it again
- full replay unlock projection at one capture per market day: `2026-10-15`

Alpaca account probing shows historical option bars and trades are available, but historical option quotes returned `404`, so they do not currently accelerate the exact bid/ask proof path. OnclickMedia EOD bid/ask data is research-grade only, not OPRA-certified final proof.

## Active Lane Scope

The generated boundary owner is `docs/legacy-lane-boundaries.md`, with machine-readable checks in `data/contracts/legacy-lane-boundaries.json`. Use it when deciding whether a lane is active browser work, separate non-browser proof work, active legacy analytics, or paused/out-of-scope sidecar code.

Until this document is updated or the user explicitly reopens a lane, project work should stay focused on:

- Regular supervised options lane family: live scan, replay diagnostics, forward evidence, paper ideas, and tracked-position review across peer regular-options lanes.
- AI commodity / commodity-infrastructure options: separate proof-first strategy lane for OPRA/SIP/NBBO validation and related automation.

The following lanes are out of scope for active work:

- Crypto options lane under `crypto_options/*`.
- Polymarket lane under `src/lib/polymarket/*` and related scripts.
- Day-trading lane under `src/lib/day-trading/*`, day-trading tests, and legacy day-trading docs. This lane is paused, not deleted.

Do not spend implementation, performance, research, documentation, or automation effort on out-of-scope lanes unless the user explicitly asks to archive, remove, repair, or reopen one of them. Shared infrastructure changes may touch adjacent files only when required for the regular options lane family or the separate AI commodity strategy lane.

Relevant commands:

```bash
npm run build
npm run verify
uv run --locked python -m unittest discover -s tests -p "test_*.py" -v
python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest
python scripts/audit_paid_data_readiness.py --json
npm run accuracy:report
npm run verify:accuracy:no-write
```

Agent runtime memory is local, active, and fail-closed. `scripts/agent_control.py`, exposed as `npm run agent:control`, owns the options runtime graph under ignored `data/agent-control/`, including checkpoints, accepted worker reports, session transcript provenance, focused context packs, dream proposals, and token-gated profit-learning provenance. For large, resumed, CEO-style, audit, or worker/subagent work, agents should recover context with `npm run memory:bootstrap`, build focused handoffs with `npm run memory:context -- --goal "<goal>" --pathway operator --prompt-only`, write back reviewed worker reports with `npm run memory:writeback -- <task-id> --summary "..."`, run/audit automated dreaming with `npm run memory:dream-run` and `npm run memory:dream-audit`, sync/audit profitability readback provenance with `npm run memory:profit-learning-sync` and `npm run memory:profit-learning-audit`, audit/repair authority metadata with `npm run memory:audit` and `npm run memory:repair-authority`, and inspect dream state with `npm run memory:review-dreams`. The registered Windows task `\OptionsMemoryDreaming` runs the same low-risk automated dream loop nightly. All operating memory is `authority_scope=orchestration_only` and does not authorize trading or evidence mutation. The computer-wide graph at `C:\Users\kalec\projects-memory` is an index/provenance layer only; it does not override this repo's living docs, generated readbacks, gateboard state, exact evidence artifacts, or code.

Oracle loop post-event update: GPT-5.5 Pro Extended follow-up `profitabil-after-macro-event-design` selected the design-only post-event IV-crush iron-condor/iron-butterfly concept `post_event_iv_crush_index_iron_condor_v1`, generated by `scripts/build_regular_options_preregistered_post_event_iv_crush_iron_condor_playbook.py` and exposed as `npm run options:research:preregistered-post-event-iv-crush-iron-condor-playbook`. The artifact is `preregistered_design_only`; it did not implement an event calendar, scanner, replay engine, quote import, evidence mutation, live validation, broker flow, or promotion. It exists only to give the same-session Oracle loop the next falsifiable option-structure branch.
The post-event IV-crush readiness audit is now `scripts/build_regular_options_post_event_iv_crush_replay_readiness.py`, exposed as `npm run options:research:post-event-iv-crush-replay-readiness`. Current status is `blocked_post_event_iv_crush_replay_readiness`: preregistration, VIX, formula/denominator, strict-new, holdout, assignment/expiration, and max-loss/margin boundaries are present, but macro-event calendar source/category coverage, IV/event-premium proxy, and four-leg quote-surface coverage remain blocked. The Oracle packet validates this artifact before handoff. This is not replay, profitability proof, quote import, evidence mutation, live/broker/auto-track permission, holdout use, proof-bar change, or promotion.

Oracle loop flow-extreme update: GPT-5.5 Pro Extended follow-up `profitabil-after-ivcrush-condor-design` selected the design-only flow-extreme mean-reversion ratio/backspread concept `index_flow_extreme_mean_reversion_ratio_backspread_v1`, generated by `scripts/build_regular_options_preregistered_flow_extreme_ratio_backspread_playbook.py` and exposed as `npm run options:research:preregistered-flow-extreme-ratio-backspread-playbook`. The artifact is `preregistered_design_only`; it did not implement scanner logic, replay, quote import, evidence mutation, live validation, broker flow, undefined-risk spreads, or promotion. It exists only to give the same-session Oracle loop the next falsifiable option-structure branch.
Oracle loop flow-extreme readiness update: `scripts/build_regular_options_flow_extreme_ratio_backspread_replay_readiness.py`, exposed as `npm run options:research:flow-extreme-ratio-backspread-replay-readiness`, now owns the read-only readiness audit for that concept and consumes the point-in-time flow-extreme input artifact, the volume/open-interest source-row artifact, the multi-leg side-aware pricing capability artifact, the base clean stack identity ledger artifact, and the flow-extreme denominator/dedupe bridge artifact. Latest status is `blocked_flow_extreme_ratio_backspread_replay_readiness`, and `scripts/build_options_oracle_profit_loop_packet.py` validates the latest readiness artifact as loaded, fresh, expected-report, expected-concept, expected-structure, and safety-flag clean before passing it to GPT-5.5 Pro. `scripts/build_regular_options_flow_extreme_volume_oi_source_rows.py`, exposed as `npm run options:research:flow-extreme-volume-oi-source-rows`, fails closed with `0` usable trusted volume/OI aggregates and writes no source rows. `scripts/build_regular_options_multi_leg_side_aware_pricing_capability.py`, exposed as `npm run options:research:multi-leg-side-aware-pricing-capability`, clears `missing_side_aware_ratio_backspread_pricing` with a trusted local bid/ask fixture that is explicitly not proof eligible. `scripts/build_regular_options_base_clean_stack_identity_ledger.py`, exposed as `npm run options:research:base-clean-stack-identity-ledger`, emits a ready `157`-row clean-base identity ledger with `157` unique identities, no duplicate identities, no missing identity rows, no future/outcome identity dependencies, and no protected-holdout overlap. `scripts/build_regular_options_flow_extreme_denominator_dedupe_bridge.py`, exposed as `npm run options:research:flow-extreme-denominator-dedupe-bridge`, now clears both `missing_full_denominator_mapping` and `missing_strict_new_dedupe`. Current VIX is cleared; the branch remains parked on the blocked flow-input/source-row artifacts only. It is not replay, not profitability proof, not live/broker/auto-track permission, and not promotion.

Oracle loop 13-symbol runner-support update: `scripts/run_regular_options_13_symbol_no_write_candidate_generation.py`, exposed as `npm run options:research:13-symbol-no-write-candidate-generation`, now emits the read-only no-write/as-of/universe-filter runner-support artifact for the frozen 13-symbol surface. `scripts/build_regular_options_13_symbol_candidate_generation_surface_audit.py` validates that artifact and now reports runner status `read_only_no_write_runner_available` instead of the old generic missing-runner blocker. The frozen source-surface materializer now proves `0/24` candidate-generation months and `0` selected rows from the current broad `59`-symbol source, refuses post-hoc filtering as proof, and names blockers `missing_frozen_13_symbol_candidate_generation_engine`, `source_artifact_universe_not_13_symbol`, `outside_universe_source_rows_present`, `missing_daily_candidate_generation_diagnostics`, and `candidate_generation_months_0_below_requested_24`. This is not replay, profitability proof, live/broker/auto-track permission, quote import, evidence mutation, protected-holdout use, scanner/strategy/proof-bar change, or promotion.

Oracle loop 13-symbol denominator-v2 update: GPT-5.5 Pro selected `scripts/build_regular_options_13_symbol_frozen_candidate_generation_denominator_v2.py`, exposed as `npm run options:research:13-symbol-frozen-candidate-generation-denominator-v2`, to materialize daily candidate/no-pick/blocker rows from the existing no-write runner and frozen source-surface artifacts. Latest status is `blocked_13_symbol_frozen_candidate_generation_denominator_v2`: `494` / `494` market-date denominator rows are blocked, the frozen source surface still has `0/24` candidate-generation months, latest-four strict-new candidate rows after opportunity dedupe are `0`, and the smallest next blocker is `missing_frozen_13_symbol_candidate_generation_engine`. This confirms the blocker is a missing frozen candidate-generation engine/source artifact, not quote depth, dashboard display, or PMCC readiness.

Oracle loop dispersion-proxy update: GPT-5.5 Pro Extended follow-up `profitabil-after-flow-ratio-design` selected the design-only index-versus-constituent dispersion-proxy hybrid concept `index_constituent_dispersion_proxy_defined_risk_hybrid_v1`, generated by `scripts/build_regular_options_preregistered_dispersion_proxy_hybrid_playbook.py` and exposed as `npm run options:research:preregistered-dispersion-proxy-hybrid-playbook`. The artifact is `preregistered_design_only`; it did not implement scanner logic, replay, quote import, evidence mutation, live validation, broker flow, undefined-risk pair structures, or promotion. It exists only to give the same-session Oracle loop the next falsifiable option-structure branch.
Oracle loop dispersion readiness update: `scripts/build_regular_options_dispersion_proxy_hybrid_replay_readiness.py`, exposed as `npm run options:research:dispersion-proxy-hybrid-replay-readiness`, now consumes the preregistered dispersion playbook as evidence for its frozen pair construction, all-leg side-aware pricing, pair max-loss/collateral, denominator, and strict-new dedupe contracts, plus the Alpaca-backed point-in-time dispersion/concentration proxy source rows. Current status is `dispersion_proxy_hybrid_replay_readiness_ready` with `blockers=[]`. This is not replay, profitability proof, quote import, evidence mutation, live/broker/auto-track permission, holdout use, proof-bar change, or promotion.

Oracle loop PMCC update: GPT-5.5 Pro Extended follow-up `profitabil-after-dispersion-design` selected the design-only PMCC-style diagonal concept `low_mid_vix_index_pmcc_diagonal_income_v1`, generated by `scripts/build_regular_options_preregistered_pmcc_diagonal_playbook.py` and exposed as `npm run options:research:preregistered-pmcc-diagonal-playbook`. The artifact is `preregistered_design_only`; it did not implement scanner logic, replay, quote import, evidence mutation, live validation, broker flow, naked short calls, or promotion. It exists only to give the same-session Oracle loop the next falsifiable option-structure branch.
Oracle loop PMCC readiness update: GPT-5.5 Pro follow-up `continue-the-same-regular-options` selected the read-only replay-readiness audit generated by `scripts/build_regular_options_pmcc_diagonal_replay_readiness.py`, exposed as `npm run options:research:pmcc-diagonal-replay-readiness`. Current status is `blocked_pmcc_diagonal_replay_readiness`: the preregistered design, formula/denominator/strict-new/holdout boundaries, and VIX bucket are valid, but the repo lacks point-in-time trend/regime inputs and read-only trusted quote inspection found no long-DTE SPY/QQQ call rows for a PMCC diagonal surface. The Oracle packet now validates this artifact before handoff. This is not replay, profitability proof, live/broker/auto-track permission, quote import, evidence mutation, holdout use, proof-bar change, or promotion.

Oracle loop selector update: GPT-5.5 Pro Extended follow-up `profitabil-after-pmcc-design` selected the read-only preregistered-playbook readiness selector, generated by `scripts/build_regular_options_preregistered_playbook_readiness_selector.py` and exposed as `npm run options:research:preregistered-playbook-readiness-selector`. The selector inventories all completed design-only playbooks, consumes current readiness gates including momentum bounded replay, preserves known readiness blockers, keeps strict forward proof at `0/30`, and currently reports `candidate_selected_for_research_only_implementation_approval` with `dispersion_proxy_hybrid` selected as the blocker-free research-only implementation/replay candidate. It does not implement or replay any candidate and does not authorize live validation, auto-track, broker orders, quote import, evidence mutation, protected-holdout use, scanner release, proof-bar changes, or promotion.
The momentum bounded replay gate now treats the selector as preregistered inventory provenance only, not as a top-candidate control gate. This avoids a circular dependency where the selector consumes bounded replay readiness while bounded replay demands that the selector choose momentum. Current momentum blockers are real point-in-time input, quote/P&L/stat, duplicate, and strict-row blockers, not selector-control state.

Oracle loop packet ranking update: `scripts/build_options_oracle_profit_loop_packet.py`, exposed as `npm run options:oracle-loop:packet`, now ranks VRP and term-structure through their current structure harness plus bounded replay artifacts instead of older replay-readiness blockers. The packet's current VRP blockers are only `missing_index_credit_spread_quote_surface`; current term-structure blockers are only `missing_index_calendar_quote_surface` and `missing_point_in_time_term_structure_inputs`. Older engine/readiness blockers remain provenance in their own artifacts, not current packet blocker arrays.

Oracle loop momentum-replay update: the operator approved the selector's exact research-only implementation/replay harness question for `breadth_confirmed_index_qqq_momentum_continuation_debit_spread_v1`, without approving live validation, auto-track, broker orders, quote import, evidence mutation, protected-holdout consumption, scanner release, stop/sizing/proof-bar changes, or promotion. `scripts/build_regular_options_momentum_continuation_research_replay.py`, exposed as `npm run options:research:momentum-continuation-replay`, now owns that approved harness. Latest status is `implemented_research_replay_no_proof_qualified_rows`: `1,291` denominator rows, `0` proof-qualified rows, and diagnostic-only old-artifact economics of `net_pnl_usd=-58847.66`, PF `0.7543`, and win rate `52.34%`. The old marks are not proof because required point-in-time breadth inputs and explicit side-aware exit bid/ask evidence are missing; older missing-VIX language is stale after direct VIX materialization.

Oracle loop momentum-resolution update: GPT-5.5 Pro follow-up `profitabil-after-momentum-replay-inline` selected a read-only proof-blocker resolution audit for the approved momentum branch. `scripts/build_regular_options_momentum_continuation_proof_blocker_resolution.py`, exposed as `npm run options:research:momentum-continuation-proof-blocker-resolution`, resolves existing trusted OPRA/NBBO side-aware quote pairs without importing or mutating data. Latest status is `momentum_continuation_blocked_missing_local_proof_inputs`: `783` rows have side-aware quote-pair diagnostics and recompute to net `$157,441.20`, PF `2.2985`, and win rate `64.11%`, but strict proof remains `0` because point-in-time breadth confirmations are missing for all `1,291` denominator rows. This is historical research only, not forward proof, live permission, or promotion.
The momentum-continuation bounded replay gate is `scripts/build_regular_options_momentum_continuation_bounded_replay.py`, exposed as `npm run options:research:momentum-continuation-bounded-replay`. It consumes the prior replay and proof-blocker resolution as a stop/replay-gate artifact, not a new scanner or quote import. Its older readback is stale with respect to VIX; current remaining replay blockers are led by missing point-in-time breadth confirmation, with `0` strict exact rows, `0` point-in-time input rows resolved, and `783` side-aware diagnostic rows. Do not repeat this momentum replay branch unless a new point-in-time breadth input surface or explicit approved data repair changes the blocker.

Oracle loop macro-event readiness update: GPT-5.5 Pro selected the read-only replay-readiness audit for `low_mid_vix_macro_event_long_strangle_v1`. `scripts/build_regular_options_macro_event_long_strangle_replay_readiness.py`, exposed as `npm run options:research:macro-event-long-strangle-replay-readiness`, now owns that artifact. Current status is `blocked_macro_event_long_strangle_replay_readiness`: valid preregistration, protected-holdout guard, VIX bucket, side-aware long-premium formulas, and denominator/strict-new schemas are present, but the macro-event calendar source rows are missing. The Oracle packet includes this readiness result before the next same-session handoff. This is not replay, profitability proof, quote import, evidence mutation, live validation, auto-track, broker permission, scanner release, holdout consumption, proof-bar change, or promotion.
The read-only macro-event calendar validator is `scripts/build_regular_options_macro_event_calendar.py`, exposed as `npm run options:research:macro-event-calendar`. Latest status is `blocked_macro_event_calendar_source_missing`: no trusted local source rows exist at `data/profitability-lab/regular-options-macro-event-calendar/source_rows.jsonl`, so the artifact refuses to invent scheduled event dates. The refreshed macro-event readiness audit now consumes that artifact and reports `macro_event_calendar_source_missing`, not VIX.

## Agent Worktree Hygiene

Agents should read `docs/agent-worktree-hygiene.md` before broad edits, recurring automation work, or any audit expected to touch multiple files. Big verified changes should be pushed to a `codex/` branch or PR when publishing is allowed, instead of being left only as a large dirty worktree. Required untracked files must be reported and included before release.
