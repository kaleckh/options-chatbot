# Worklog

## 2026-06-04

- Completed Profitability Paper Gate Operator Workflow Sprint 4. `/api/options-profit/status` now overlays `paper_gate_operator_workflow` from the paper-shortlist, fresh-evidence-loop, pending-validation, and current-policy circuit-breaker artifacts; missing/unreadable artifacts show as `paper_gate_artifacts_missing` rather than proof readiness, and invariant-bad paper-shortlist artifacts fail closed as `paper_gate_invariant_violations`. Added `OperatorSessionPanel.tsx` so the archive-gated scanner can open the local HttpOnly operator session through `POST /api/operator/session` without storing the token, and added `PaperGateOperatorPanel.tsx` inside `Evidence & guardrails` to show bridge status, blockers, matched Tier A lanes, pending outcomes, no-fill/skipped auto-track explanations, and paper-only circuit-breaker routes. Added `docs/regular-options-operator-workflow.md`, TypeScript payload types, focused backend route coverage, a delayed circuit-breaker disposition regression test, an invariant-bad paper-gate regression test, and scanner readability tests. Six-subagent done debate cleared with `6` / `6` no-blocker verdicts; the only proof-boundary follow-up was patched before completion. Verification passed: focused options-profit status endpoint tests, `uv run --locked python -m unittest tests.test_current_policy_circuit_breaker -v`, `node --test tests\ui\operator-auth.test.js tests\ui\route-lifecycle.test.js tests\trading-desk\scanner-tab-readability.test.js`, `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, `npm run verify:docs`, `npm run verify:profitability-paper-gates`, `uv run --locked python -m py_compile python-backend\main.py scripts\pending_audit_candidates.py`, `git diff --check`, and desktop/mobile browser QA of Archive > Live Scan > Evidence & guardrails with local operator unlock.
- Completed Profitability Paper Gate Operator Workflow Sprint 3. Added `scripts/build_current_policy_circuit_breaker.py`, `tests/test_current_policy_circuit_breaker.py`, generated `docs/current-policy-circuit-breaker.md`, and `data/forward-tracking/current_policy_circuit_breaker_latest.*` so recent broken current-policy cohorts become an explicit paper-validation route instead of an implicit recommendation. Current readback is `overall_status=paper_only_recent_week_break`, `route_status=paper_validation_only`, `paper_validation_only_lane_count=2` for `short_term` and `bullish_pullback_observation`, `lane_deletion=false`, and `live_policy_change=false`; recovery still fails on `recent_cohort_recovered`, `fresh_current_policy_rows`, `fresh_champion_matched_rows`, `trusted_exact_realized_pnl_rows`, `point_in_time_replay_pass`, and `paper_monitor_pass`. `scripts/validate_pending_scan_candidates.py` now consumes the breaker before auto-track validation and writes a `paper_only` validation disposition for held lanes. Six-subagent review found and closed three real fail-open edges: recovered cohort labels with failed forward gates, missing breaker artifacts, and malformed empty `lane_routes` artifacts. Final debate cleared with `6` / `6` no-blocker verdicts. Verification: `uv run --locked python -m unittest tests.test_current_policy_circuit_breaker tests.test_daily_all_lanes_audit -v`, `npm run verify:profitability-paper-gates`, `npm run verify:docs`, and `git diff --check` passed.
- Completed Profitability Paper Gate Operator Workflow Sprint 2. Added `scripts/build_regular_options_fresh_evidence_loop.py`, `tests/test_regular_options_fresh_evidence_loop.py`, `docs/regular-options-fresh-evidence-loop.md`, and generated readbacks under `data/forward-tracking/regular_options_fresh_evidence_loop_*` to reconcile pending validation candidates, fill-attempt snapshots, paper/tracked linkage, exact OPRA/NBBO exit evidence, and realized P&L states without changing scanner, broker, auth, DB, stop, or proof behavior. Current readback is conservative: `20` validation-attempted candidates, `15` no-longer-matched, `5` proof-ineligible, `0` linked positions, `0` exact realized P&L rows, `0` stale rows, `0` non-executable rows, `0` promotion-discussion-ready rows, and `live_policy_change=false`. The six-subagent done debate initially found one proof-boundary blocker, then cleared with `6` / `6` no-blocker verdicts after the patch removed permissive exit-basis matching, rejected contaminated `spread_bid_ask*` / `expired_auto_close` exit evidence, preserved `0.0` realized P&L, and clarified that entry evidence is quote/limit evidence rather than broker fill evidence. Verification: `npm run verify:profitability-paper-gates`, `npm run verify:docs`, and `git diff --check` passed.
- Completed Profitability Paper Gate Operator Workflow Sprint 1. Added `scripts/build_regular_options_paper_shortlist.py` to turn the profit-capture queue into a generated paper-review release gate at `docs/regular-options-paper-shortlist.md` and `data/profitability-lab/regular-options-paper-shortlist/latest.json`. Current readback is `eligible_count=0`, `invariant_violation_count=0`, `release_gate_status=no_paper_shortlist_candidates`, and `live_policy_change=false`. The gate is paper-only: it does not change scanner promotion, broker behavior, stop policy, auth, DB schema, or proof bars.
- Hardened the Sprint 1 gate after six-subagent review found a real fail-open edge. `--strict-gate` now fails closed when the source queue is missing or when ready rows violate invariants, and invariant-bad ready rows are reported as violations without appearing in `eligible_paper_review_candidates`. Added focused tests for the empty current release gate, valid ready rows, invariant-bad rows, missing-source strict failure, and strict invariant failure; added bridge/evidence negative tests for Tier B, symbol-only, blocked, stale, midpoint-only, EOD, fallback, and manual evidence. Verification: `uv run --locked python -m unittest tests.test_regular_options_paper_shortlist tests.test_regular_options_profit_capture_queue -v`, `npm run verify:profitability-paper-gates`, `npm run verify:docs`, and `git diff --check` passed. Final six-subagent done debate cleared with `6` / `6` no-blocker verdicts (`2` done, `4` done with minor follow-ups).
- Added the Profitability Paper Gate Operator Workflow as a durable sprint backlog and reusable `/goal` prompt. `docs/autoresearch/profitability-paper-gate-goal.md` now defines six ordered sprints: paper-gate release pack, fresh exact evidence loop, recent-cohort circuit breaker, operator workflow, exact repair queue burn-down, and scorecard/agent-memory updates. Each sprint's done gate requires implementation, focused verification, living-doc/report updates, and six independent subagent reviews with at least `4` / `6` agreement and no severe blocker. `docs/NEXT_STEPS.md` now carries the sprint backlog, `docs/index.md` links the prompt, and `scripts/generate_agent_memory_graph.py` now exposes an "If Touching Profitability Paper Gates" path for future agents. No scanner policy, broker behavior, proof bar, route/auth runtime behavior, DB schema, AI commodity lane, or paused sidecar lane changed.
- Added the first paper-only profitability protection slice from the subagent debate. `scripts/build_regular_options_profit_capture_queue.py` now adds a Tier A fresh-match paper bridge: clean exact Tier A evidence still routes to paper review only, and the bridge requires a fresh executable quote-window scanner match against the same Tier A lane signature before a row can become a paper-shortlist candidate. Current regenerated queue remains `97` rows with `15` Tier A, `82` Tier B, `15` fresh scan signature matches, `9` blocked-but-interesting rows, and `173` quarantine rows, while the bridge has `0` eligible Tier A fresh matches and all `15` fresh matches are `not_bridge_eligible`. `scripts/log_scan_picks.py` now records fill-discipline snapshots, leg bid/ask/mids, spread mid/entry debit, fill degradation, top alternatives, and quote freshness on paper fill-attempt rows without changing scanner gates or broker behavior. `scripts/run_regular_options_multilane_portfolio.py` now makes Lane A side-aware zero-bid economics an explicit quality blocker; current read still passes count at `234` exact trades but stays `quality_pending` because conservative Lane A zero-bid PF is `0.85`, conservative combined unpriced exits are `11`, and zero-bid exit rate is `41.99%`. Verification: focused unittest suite for the queue, fill logs, multi-lane gate, and all-planned sleeves passed; no-write queue, multi-lane, point-in-time, and paper-monitor readbacks passed; generated reports were refreshed.
- Added repair-attempt memory/readback for the regular-options proof repair loop without changing scanner, broker, stop-policy, auth, or proof bars. `scripts/regular_options_repair_targets.py` now owns shared OCC parsing, target filtering, de-dupe, lookahead expansion, and repair-attempt keys for the importer and classifier. `scripts/import_missing_replay_quotes_from_thetadata.py` now records per-base-target exact-date rows, lookahead rows, available dates, first later date, and proof repair status (`exact_date_repair_candidate`, `lookahead_only_not_exact_proof`, `current_source_exhausted`, or `exact_date_imported_pending_replay`). `scripts/build_regular_options_repair_attempt_readback.py` generates `docs/regular-options-repair-attempts.md`; current baseline scanned `156` old summaries and found `0` keyed latest attempts because those summaries predate the ledger. `scripts/build_regular_options_profit_capture_queue.py` now consumes the readback and adds `repair_actionability`; latest queue remains `97` rows, `15` Tier A, `82` Tier B, `16` high-priority repairs, `15` fresh signatures, `9` blocked-but-interesting, and `173` quarantine rows, with actionability counts `15` no repair needed, `39` needs status/forward validation after repair, and `43` not applicable low-priority Tier B rows. Verification: focused repair/importer/classifier/readback/queue unittest suite, repair-attempt readback no-write, profit-capture queue no-write, `npm run verify:docs`, and `git diff --check` passed.
- Added scoped exact-repair filters after the first WMT high-priority check showed the source artifact had `34` unpriced targets and would expand to `137` broad requests without targeting. `scripts/import_missing_replay_quotes_from_thetadata.py` and `scripts/classify_missing_replay_contracts.py` now both support `--ticker`, `--contract-symbol`, and `--quote-date`, and both echo `target_filters` in JSON output. Added classifier filter tests and importer filter tests. Scoped WMT readback on `data/options-validation/runs/20260602_110317_relative_strength_pullback_ex_clean_universe_v1_intraday.json` produced `3` base targets, `12` lookahead requests, and classifier status `provider_no_match_exact_contract_with_same_expiry_chain` for all three WMT contracts. A targeted dry-run found `3` normalized rows for `WMT260402C00138000` on lookahead date `2026-03-27`, while the exact missing dates/contracts remained no-match; no CSV, summary, DB import, source replay, queue rebuild, live scanner action, broker action, or proof promotion was written. Verification: focused helper tests, in-memory compile check, symbol-sleeve / profit-capture queue / scorecard no-write readbacks, operator-risk no-write audits, pending-candidate dry-run, proof/scanner regressions, `npm run trading-desk:test`, and `npm run ui:test` passed.
- Hardened `scripts/import_missing_replay_quotes_from_thetadata.py` for the next profit-capture repair loop. The importer now emits a de-duplicated `repair_manifest` with base targets, expanded request targets, source occurrences, option type, and max-request truncation state; `--plan-only --json` prints the manifest without ThetaData requests or writes; `--dry-run --json` fetches and normalizes rows without writing summaries, CSVs, or DB imports. Added focused unittest coverage for both modes and reran the high-priority verification set: importer tests, importer compile, UNH plan-only manifest readback (`2` base targets, `9` expanded request targets, `2` source occurrences), remaining-gap classification, symbol-sleeve / profit-capture queue / scorecard no-write readbacks, proof/scanner regressions, Trading Desk API/UI tests, operator-risk no-write audits, pending-candidate dry-run validation, docs generation checks, AI commodity isolation check, and AI commodity next-execution readback. `npm run verify:accuracy:no-write` intentionally failed its strict gate because AI commodity profitability is still unverified (`3` / `100` exact shared dates, replay locked, `0` live candidates, automation inactive); this preserves the proof bar rather than weakening it.
- Ran the second targeted profit-capture exact repair pass on UNH `bullish_pullback_observation`. The focused import from `data/options-validation/runs/20260602_102557_sleeve_ticker_unh_intraday.json` added `561` new trusted ThetaData OPRA/NBBO rows, then the latest rerun moved one gap and a follow-up import added `1461` more trusted rows. The focused sleeve still stayed `8` / `10` priced at PF `2.08`; latest classifier readback leaves exact provider no-matches for `UNH251128C00410000` on `2025-11-10` and `UNH251205C00390000` on `2025-11-20`. Rebuilt the symbol-sleeve, profit-capture queue, and operating scorecard readbacks; the queue stayed `97` rows, `15` Tier A, `82` Tier B, and `16` high-priority repairs. Subagents then debated the next test list and converged on repair-target freshness, proof-bar regressions, live validation, operator-risk audits, generated/readability checks, and AI commodity isolation as the next checks before more import loops.
- Ran the first targeted profit-capture exact repair pass from the new Evidence Repair Queue. Imported exact ThetaData OPRA/NBBO candidates and reran the focused per-ticker sleeves for NEM, LLY, and AAPL without changing scanner guardrails or proof rules. NEM imported `807` trusted rows for `NEM251107C00093000` on later dates, but the required `2025-10-27` quote remained no-match and the rerun stayed `15` / `16` priced at PF `12.46`. LLY imported `1164` trusted later-date rows for `LLY260109C01155000`, but the required `2025-12-10` quote remained no-match and the rerun stayed `9` / `10` priced at PF `2.89`. AAPL imported `111` trusted rows for the first missing dates, then the rerun advanced to new missing dates; a follow-up exact import found `0` rows through expiry, so the sleeve stayed `11` / `13` priced at PF `273.54`. Result: no Tier B row graduated, and the proof boundary correctly blocked promotion where required exact positive-bid/ask rows are absent.
- Added the first profitability-pass repair readback without changing scanner or broker policy. `scripts/build_regular_options_profit_capture_queue.py` now adds Tier A promotion-gap diagnostics to capture rows and attaches missing quote-date / exact-contract summaries for medium/high Tier B evidence repairs by reading each row's source replay artifacts. `docs/regular-options-profit-capture-queue.md` now renders a dedicated Evidence Repair Queue table with missing leg counts, dates, contracts, and source artifacts so the next repair loop can target exact OPRA/NBBO gaps instead of coarse "high priority" labels. Regenerated the queue and active operating scorecard; current queue remains paper/research only at `97` rows, `15` Tier A, `82` Tier B, `16` high-priority repairs, `15` fresh signature matches, `9` blocked-but-interesting rows, and `173` quarantine rows. No live scanner policy, guardrails, proof gates, stop policy, or broker-action behavior changed.
- Added Point 44 final remediation closure pack. `scripts/generate_final_remediation_closure_pack.py` now emits `data/contracts/final-remediation-closure-pack.json` and `docs/final-remediation-closure-pack.md` from the remediation loop map, generated artifact governance, agent memory graph, living-doc links, package docs wiring, and active lane-boundary guard artifacts. The closure pack marks the 44-point architecture/readability remediation loop closed only when all points are completed, closure artifacts are governed, generated guard artifacts have no validation errors, discoverability links are present, and regular-options/AI-commodity/paused-lane scope boundaries remain intact. Wired the artifacts into `npm run verify:docs`, generated-artifact governance, the docs index, living-doc hygiene, architecture docs, the agent memory graph, and the remediation loop map. No route/auth/payload behavior, proof/scanner/replay semantics, DB schema, frontend runtime behavior, product profitability claim, broker execution readiness claim, AI commodity proof completion claim, network freshness check, DB access, or paused sidecar lane reopening changed.
- Added Point 43 generated artifact governance and stale-artifact trust boundaries. `scripts/generated_artifact_manifest.py` now owns the shared checked generated-artifact manifest consumed by living-doc hygiene and `scripts/generate_generated_artifact_governance.py`, which emits `data/contracts/generated-artifact-governance.json` and `docs/generated-artifact-governance.md`. The generated governance map classifies owner commands, generator checks, runtime posture, trust roles, source inputs, stale handling, hand-edit policy, excluded volatile artifact classes, and the single runtime-consumed generated artifact `src/lib/generated/proofEvidenceContract.ts`. Wired the artifacts into `npm run verify:docs`, living-doc hygiene, the docs index, architecture docs, the agent memory graph, and the remediation loop map. No route/auth/payload behavior, proof/scanner/replay semantics, DB schema, frontend runtime behavior, generated TS content, volatile research output governance, hashes, mtimes, or network freshness checks changed.
- Added Point 42 backend route ownership map. `scripts/generate_backend_route_ownership_map.py` now emits `data/contracts/backend-route-ownership-map.json` and `docs/backend-route-ownership-map.md` from static FastAPI decorator discovery plus `data/contracts/route-mutation-inventory.json`, covering direct `main.py` adapters, extracted profile/predictions/tools routers, backend-only routes, service delegation, module roles, router mounts, Next-only exclusions, and owner docs. Wired the artifacts into `npm run verify:docs`, living-doc hygiene, the docs index, API/storage docs, architecture docs, the agent memory graph, and the remediation loop map so Point 42 is completed and Point 43 is the next planned target. No FastAPI route handlers, decorators, auth behavior, payloads, proof/scanner/replay semantics, DB schema, frontend behavior, or OpenAPI/runtime introspection changed.
- Added Point 41 generated remediation loop map / LLM handoff ledger. `scripts/generate_remediation_loop_map.py` now emits `data/contracts/remediation-loop-map.json` and `docs/remediation-loop-map.md` from an explicit checked manifest covering all 44 remediation points, Points 1-41 completion status, Points 42-44 planned targets, owner docs/artifacts, verification anchors, non-goals, and worklog evidence. Wired the generated artifacts into `npm run verify:docs`, living-doc hygiene, the docs index, the architecture best-practices rubric, the reusable remediation goal prompt, and the agent memory graph so future LLM handoffs can recover the loop without chat context. No route handlers, auth semantics, proof predicates, scanner policy, replay math, DB schema, frontend runtime behavior, backend route ownership inventory, stale-artifact governance, or final closure verification changed.
- Added Point 40 AI commodity isolation governance. `scripts/generate_ai_commodity_isolation.py` now emits `data/contracts/ai-commodity-isolation.json` and `docs/ai-commodity-isolation.md` from a checked manifest covering the separate non-browser AI commodity proof lane, Alpaca OPRA-only proof-source claims, disabled scanner tracking, no dedicated browser/API/tool route, storage-map separation, and latest progress-readback honesty. Wired the generated artifacts into `npm run verify:docs`, living-doc hygiene, the docs index, scanner creation safety docs, architecture docs, and the legacy lane-boundary generator so Point 39 now points to the completed detail owner. No AI commodity proof logic, runner flow, network/capture behavior, scanner thresholds, route payloads, auth, DB schema, Trading Desk behavior, or frontend behavior changed.
- Added Point 39 legacy lane boundary governance. `scripts/generate_legacy_lane_boundaries.py` now emits `data/contracts/legacy-lane-boundaries.json` and `docs/legacy-lane-boundaries.md` from one checked manifest covering the active regular supervised browser product, active legacy Trading Desk analytics, the separate AI commodity proof lane, and paused/out-of-scope day-trading, crypto-options, and Polymarket sidecars. The checker asserts empty day-trading route scaffolding has no active route handlers, active browser surfaces do not import paused-lane modules, route mutation inventory does not mount paused lanes, and living docs link the boundary owner. `tests/test_legacy_lane_boundaries.py` covers freshness, required statuses, path/doc references, route/import guards, and nav/route-lifecycle exclusions; `npm run verify:docs` now checks the generated boundary artifacts. Updated living docs and generated-artifact hygiene references without editing paused lane implementations, package sidecar scripts, route payloads, auth, proof/scanner/replay behavior, DB schema, frontend behavior, or AI commodity isolation details.
- Added Point 38 encoding/mojibake cleanup. Replaced the corrupted EV note marker in `options_chatbot.py` with ASCII `Error:` text and simplified the touched EV, risk-settings, and health-check separator comments to ASCII. Added `tests/test_text_encoding_hygiene.py`, which scans active source, docs, tests, and checked contract files for Unicode replacement characters and common UTF-8 mojibake signatures without banning valid non-ASCII everywhere or scanning bulky generated run outputs. No route payloads, proof/scanner/replay behavior, DB schema, auth behavior, frontend behavior, or generated artifacts changed.
- Added Point 37 golden proof-count/replay readback tests. `data/contracts/proof-replay-golden-readbacks.json` now pins test-only aggregate expectations over the proof invariant rows: case counts, raw-exact versus proof-grade closed counts, grouped Trading Desk tracked/proof summaries, options-profit realized metrics, options-profit status overlay counts, profit-cycle candidate metrics, and deterministic replay-service summary/no-result/cache-key readbacks. `tests/test_golden_proof_replay_readbacks.py` asserts those literal golden values through `proof_contract.py`, `proof_summary_service.py`, `python-backend/main.py` grouped/status helpers, `options_profit_gate.py`, `options_profit_flywheel.py`, and `replay_profit_service.py` without running WFO replay, network, live DBs, or AI commodity loops. Updated proof/replay docs and the docs index to make the fixture discoverable as `runtime_use=false` regression/readability data, not runtime policy. No proof predicate, scanner policy, replay math, route payload, DB schema, auth, frontend behavior, or generated TypeScript changed.
- Added Point 36 suggested/tracked parity runtime tests. `tests/test_trading_desk_record_parity.py` now creates the same canonical Trading Desk row through `MemoryTrackedPositionsRepository` and `SQLiteSuggestedTradesRepository`, verifies both expose every shared `COMMON_POSITION_ROW_FIELDS` key, proves suggested trades keep tracked-only `source_scan_*` / `proof_*` fields out of their top-level rows while preserving scanner context only inside `source_pick_snapshot`, then saves the same review and closes both rows to assert common `latest_review`, exit, realized P&L, fee, and synthetic `SELL` shapes stay aligned. `docs/trading-desk-record-parity.md` now includes an executable row-shape test map for future agents. No route payloads, auth behavior, proof/profit counting, scanner policy, DB schema, or frontend behavior changed.
- Added Point 35 DB migration/constraint safety tests. Repository migration tests now pin the current baseline checksums, verify ordered per-store migration versions, prove unknown stores do not create migration ledgers, assert SQLite ledger primary keys reject duplicate migration rows, and cover checksum drift for SQLite tracked plus fake Postgres. Repository constraint tests now prove the migration-ledger DB-enforced constraint is represented for every migration store, verify suggested and legacy tracked SQLite review foreign keys through repository connections, exercise clean/missing/dirty SQLite constraint-audit outcomes, and prove dirty audit rows are reported without repair. `python-backend/repository_constraints.py` now explicitly records SQLite tracked and suggested migration-ledger primary-key constraints, and the repository migration/constraint docs have behavior matrices for future agents. No route payloads, auth, proof/scanner behavior, repository schema DDL, frontend behavior, or data cleanup changed.
- Added Point 34 scanner lineage mutation coverage. `tests/options_algorithm_fixtures.py` now has a proof-ready scanner-origin pick and matching archived forward-event helper; tracked-position API tests prove matching scanner lineage can create a proof-eligible row and that mutations to lineage IDs, contract identity, execution fields, source labels, and creation flags reject with `source_scan_lineage_unverified` without persisting rows. Suggested-trade API tests mirror the shared validator for high-risk tamper cases while preserving the suggested-trade paper boundary. `docs/scanner-creation-safety-contract.md` and `docs/api-and-storage.md` now point future agents to the lineage mutation guard and test anchors. No route payloads, auth, scanner policy, proof thresholds, DB schema, frontend behavior, or generated artifacts changed.
- Added Point 33 proof invariant table tests. `data/contracts/proof-invariant-cases.json` now holds a test-only, `runtime_use=false` matrix for clean live exact rows, open live entry proof, raw exact, manual exact, research/backfill, migrated historical paper, stale proof flags, missing lineage, non-OPRA source, missing/stale freshness, midpoint entry, lifecycle-only exits, and mark exits. `tests/test_proof_invariant_cases.py` loads the table to assert backend proof predicates, `/api/proof-summary` raw-exact versus proof-grade counts, and options-profit gate/flywheel proof-grade metrics; `tests/trading-desk/proof-invariants.test.js` loads the same cases to assert frontend evidence group, production-proof, realized-P&L, and Truth-grade behavior. `scripts/generate_proof_invariant_table.py` now generates checked `docs/proof-invariant-table.md`, and the agent memory graph points proof/evidence work to the manifest and generated table. Closed frontend production-proof counts now require the Truth-grade closed predicate so a row may retain `live_exact` entry display while mark/lifecycle/untrusted exits stay out of closed production-proof summaries. No route payloads, auth, scanner policy, DB schema, backend proof predicates, or public API behavior changed.
- Added Point 32 auth rejection/allowance coverage. `tests/ui/operator-auth.test.js` now proves generic mutating proxy routes (`/api/scan`, `/api/predictions/grade`, and `/api/tools/[name]`) reject missing or wrong local operator auth before body parsing or bridge calls and allow valid local operator auth through to the mocked bridge once with `x-options-route-*` lifecycle headers. It also directly tests `POST /api/operator/session` unconfigured/invalid-token rejection, valid-token HttpOnly SameSite=Strict session-cookie flags, and an inventory-backed auth-test anchor map for every mutating mounted browser route. Updated `docs/api-and-storage.md` with an auth verification map and cleared the completed session-test item from `docs/NEXT_STEPS.md` without changing route handlers, auth semantics, payloads, proof/scanner/replay behavior, DB behavior, schemas, or frontend behavior.
- Added Point 31 no-unclassified-mutation route guard. `scripts/generate_route_parity.py` now fails route parity validation when a mutating mounted Next route has local auth but no explicit route lifecycle/store/record contract, references an unknown route contract, or uses a contract with mismatched method/path; Next-only operator session routes remain exempt from FastAPI mirroring and local-operator auth but not from classification. Backend-only mutating FastAPI routes now fail if they fall back to generic `backend_write` instead of a named lifecycle override. Added focused `tests/test_route_parity_generator.py` coverage for authenticated-but-unclassified mutators, unknown/mismatched contracts, backend-only lifecycle fallback, and current JSON inventory mutation classification without changing route handlers, auth behavior, payloads, headers, proof/scanner/replay semantics, DB behavior, schemas, or frontend behavior.
- Added Point 30 living-docs hygiene guard. `docs/living-docs-hygiene.md` now owns living-doc responsibilities, evidence-record/source-of-truth boundaries, generated artifact command/check ownership, and docs-hygiene non-goals. `scripts/check_living_docs_hygiene.py` checks required living docs, generated-artifact links, `generated_by` / `runtime_use=false` boundaries, generated banners, package-script wiring, placeholder markers, dated worklog sections, `NEXT_STEPS` update markers, and durable-decision language; it is wired into `npm run verify:docs` and covered by `tests/test_living_docs_hygiene.py`. Updated README, living docs, decisions, architecture orientation, and the agent memory graph without changing runtime behavior, routes, auth, proof/scanner/replay semantics, DB behavior, schemas, payloads, or frontend behavior.
- Added Point 29 generated storage ownership map. `scripts/generate_storage_ownership_map.py` now emits deterministic `data/contracts/storage-ownership-map.json` and `docs/storage-ownership-map.md` from the route/mutation inventory plus repository migration, constraint, index, local DB, and tracked/suggested parity manifests. The artifacts are marked `runtime_use=false`, classify active repository stores, test/legacy stores, route artifacts, local DB roles, support caches, session cookies, virtual backend dispatch, and separate/out-of-scope lanes, and are wired into `npm run verify:docs` without opening databases, running audits, changing routes, auth, payloads, schemas, proof/scanner behavior, or DB behavior. Updated living-doc links and the agent memory graph to point to the storage map.
- Added Point 28 generated route/mutation JSON inventory. `scripts/generate_route_parity.py` now emits `data/contracts/route-mutation-inventory.json` alongside `docs/route-parity.md` from the same Next route, FastAPI decorator, client-fetch, and route-contract registry source signals. The JSON artifact is deterministic, marked `runtime_use=false`, checked by the existing route parity `--check` path and `npm run verify:docs`, and exposes mounted browser routes, backend-only routes, client fetches, auth boundaries, mutation-intent labels, lifecycle, store, record-class, and owner metadata without changing route handlers, auth, payloads, proof/scanner behavior, DB behavior, OpenAPI, schema validation, or generated TypeScript. Updated living-doc links and the agent memory graph to point to the JSON sibling.
- Added Point 27 generated agent memory graph. `scripts/generate_agent_memory_graph.py` now emits deterministic `data/contracts/agent-memory-graph.json` and `docs/agent-memory-graph.md` from an explicit checked manifest of owner docs, code, contracts, generated artifacts, edges, and practical playbooks. The graph is marked `runtime_use=false`, is wired into `npm run verify:docs`, and explicitly does not replace route inventory, route mutation inventory, storage ownership maps, proof/source contracts, or stale-artifact governance. Linked it from `docs/index.md`, `docs/architecture-best-practices.md`, and `docs/architecture-overview.md`, and added `tests/test_agent_memory_graph.py` to guard paths, edges, playbooks, non-goals, and generated freshness without changing runtime behavior.
- Added Point 26 architecture best-practices target doc. `docs/architecture-best-practices.md` now defines the target architecture/readability rubric for future remediation loops, including active scope, architecture principles, boundary acceptance bars, readability rules, verification expectations, docs ownership, completion checklist, and explicit non-goals for route inventory, storage maps, mutation inventory, memory graph artifacts, and stale-artifact governance. Linked it from `docs/index.md`, `docs/architecture-overview.md`, `docs/architecture-audit.md`, and `docs/autoresearch/code-audit-remediation-goal.md`, and added `tests/test_architecture_best_practices_doc.py` to guard the anchor without changing code behavior.
- Added Point 25 `FinTable` mobile contract coverage. The remaining archive/legacy prediction analytics tables in `src/components/predictions/legacy-tabs.tsx` now declare explicit mobile title, subtitle, and priority-column contracts, with optional hidden columns only where desktop-only detail would crowd mobile cards. `tests/ui/fin-table.test.js` now scans production `src/**/*.tsx` and fails any `FinTable` usage missing `mobileTitleCol`, `mobileSubtitleCol`, or `mobilePriorityCols`, while keeping `FinTable` itself permissive for tests and low-risk reuse. No table row data, labels, route behavior, proof/scanner behavior, auth, DB, or response JSON changed.
- Added Point 24 generated frontend proof/evidence policy artifact. `scripts/generate_proof_evidence_contract.py` now emits `src/lib/generated/proofEvidenceContract.ts` from `data/contracts/proof-evidence-contract.json`, supports `--check`, and is included in `npm run verify:docs`. `src/lib/trading-desk/proofContract.ts` now imports the generated artifact instead of the JSON directly, preserving the existing frontend facade consumed by `positionEvidence.ts`. Added generation drift tests and kept backend proof predicates, proof semantics, route behavior, auth, scanner safety, DB behavior, and response JSON unchanged.
- Added Point 23 typed tab and route ID catalogs. Main app tab IDs and header/sidebar copy now live in `src/lib/navigation/tabs.ts`; `AppShell`, `Sidebar`, and `Header` use `MainAppTabId`. Trading Desk active and visible tab IDs now live in `src/components/predictions/tradingDeskTabs.ts`, preserving `closed-trades` as a visible tab that resolves to active `positions` plus closed view. Trading Desk, Strategy Lab, and generic route contract IDs are now exported readonly arrays in their existing registry modules, and their union types derive from those arrays. Added static catalog tests without changing tab order, labels, keyboard behavior, fetch URLs, route handlers, headers, auth, proof/scanner behavior, DB behavior, or generated docs.
- Added Point 22 `ScannerTab.tsx` readability split. The scanner evidence/truth/guardrail details now live in `src/components/predictions/ScannerEvidencePanel.tsx`, and the selected-pick record UI now lives in `src/components/predictions/ScannerPickRecordForm.tsx`. `ScannerTab.tsx` dropped from `729` to `297` lines while keeping scanner toolbar controls, row construction, `FinTable`, explicit mobile table columns, create submit callbacks, route payloads, proof/scanner behavior, auth, DB, and API behavior unchanged. Added `tests/trading-desk/scanner-tab-readability.test.js`.
- Added Point 21 `PredictionsView.tsx` close-flow extraction. The tracked/suggested close dialog state and close POST submitters now live in `src/components/predictions/useTradingDeskCloseDialogs.ts`, the modal UI lives in `src/components/predictions/CloseTradeModal.tsx`, and shared exit-price parsing lives in `src/components/predictions/tradingDeskCloseForm.ts`. `PredictionsView.tsx` dropped from `1128` to `843` lines and now wires hook-provided modal props while keeping scanner trade creation, duplicate checks, tab orchestration, route payloads, mutation-intent headers, proof/scanner behavior, and UI copy unchanged. Added `tests/trading-desk/predictions-view-readability.test.js`, and updated static Trading Desk source tests to include the new close-flow hook.
- Added Point 20 generated Trading Desk schema bridge. `scripts/generate_trading_desk_schema_bridge.py` now emits `data/contracts/trading-desk-api-schema-bridge.json` and generated `docs/trading-desk-schema-bridge.md` from the existing Trading Desk store ownership registry, manual TypeScript API contract registry, and narrow Pydantic API model manifest. The bridge maps the eight Trading Desk route contracts to TypeScript request/response names, marks read routes as TypeScript-only, adds Pydantic JSON Schema refs for the six mutation body adapters and top-level response envelopes, and sets `runtime_use=false`. It is checked by `npm run verify:docs` and does not add OpenAPI, runtime JSON Schema/Zod/AJV validation, generated TypeScript, FastAPI `response_model`, automatic `422` validation, payload reshaping, auth changes, proof/scanner behavior, or DB changes. Added Python and JS schema-bridge drift tests.
- Added Point 19 generic route lifecycle headers for mounted non-domain Next route groups. `src/lib/route-lifecycle/routeContracts.ts` now names scan, predictions, risk/status, local operator session, sectors, and tool dispatch contracts, and `jsonWithRouteLifecycle()` applies `x-options-route-*` response headers while preserving response bodies, local operator auth, Trading Desk headers, Strategy Lab headers, options-profit timing headers, and operator session cookie behavior. `scripts/generate_route_parity.py` now reads the generic registry and recognizes `jsonWithValidatedTradingDeskStore()` plus `jsonWithRouteLifecycle()`, so `docs/route-parity.md` lists route/store/lifecycle owners for these routes. Added `docs/route-lifecycle-contracts.md` and `tests/ui/route-lifecycle.test.js`.
- Added Point 18 Trading Desk response-envelope validation at the Next boundary. `src/lib/trading-desk/apiResponseValidation.ts` now shallow-validates tracked-position and suggested-trade read/create/review/close envelopes, accepting `{ error: string }` storage-unavailable sentinels, requiring tracked lifecycle `position_event_persistence` on successful tracked mutations, and rejecting tracked-only lifecycle persistence on suggested responses. The six Trading Desk Next routes now use `jsonWithValidatedTradingDeskStore()`, which returns a 502 with route contract/path/reason and Trading Desk headers when the backend envelope is malformed. No request-body validation, generated schema, deep row/proof/P&L validation, or payload reshaping was added. Added runtime validation tests in `tests/trading-desk/api-response-validation.test.js` and a mocked invalid-route response check.
- Added Point 17 TypeScript API contract ownership for the Trading Desk boundary. `src/lib/trading-desk/apiContracts.ts` now names tracked-position and suggested-trade list/create/review/close request and response envelopes, including tracked-only `position_event_persistence` and suggested-only `trade` / `trades` envelopes. `src/lib/backend/positions.ts`, the six Next mutation routes, `PredictionsView.tsx`, and `useTradingDeskRecords.ts` now use those named contracts instead of opaque Trading Desk `Record<string, unknown>` or inline response shapes. `src/app/api/_utils.ts` and backend transport helpers were made generic for compile-time typing only; runtime parsing, status codes, auth, mutation intent, proof/scanner semantics, and JSON payloads are unchanged. Added `docs/typescript-api-contracts.md` and static contract coverage in `tests/trading-desk/api-contracts.test.js`.
- Added Point 16 Trading Desk API model ownership. `python-backend/trading_desk_api_models.py` now names the six active tracked-position and suggested-trade mutation bodies with narrow Pydantic adapters plus tracked/suggested top-level envelope drift guards. `python-backend/main.py` parses those adapters inside the existing route error paths while keeping `dict[str, Any]` endpoint signatures, existing helper parser semantics, route-owned `400` behavior, and unchanged response JSON. Added `docs/trading-desk-api-models.md` plus living-doc links and tests for route manifest coverage, raw-value preservation, malformed `scan_pick` rejection, suggested default-contract preservation, zero-exit handling, and tracked/suggested envelope separation.
- Added Point 15 local DB hardening ownership. `python-backend/local_db_hardening.py` now classifies local SQLite roles for `chat_history.db`, explicit legacy/test `data/tracked_positions.db`, out-of-scope truth/forward/market/AI commodity stores, and ignored sidecar/backup files. `scripts/audit_local_databases.py` opens covered SQLite DBs read-only, runs `quick_check`, `foreign_key_check`, required-table checks, and sidecar/backup inventory without creating, repairing, vacuuming, checkpointing, or deleting files. Added `docs/local-db-hardening.md` plus living-doc links and tests for manifest classification, read-only missing/initialized/corrupt-shape audit behavior, out-of-scope classification, and `.gitignore` DB patterns.
- Added Point 14 suggested-trade parity ownership. `python-backend/repository_parity.py` now records the shared tracked/suggested lifecycle, repository method surface, row/review keys, route envelope pairs, and intentional differences; `docs/trading-desk-record-parity.md` explains that suggested trades are local paper/hypothetical workflow state, not production proof rows or a tracked-position fallback. Living docs now link the parity owner, and tests guard that SQLite suggested trades keep the common row/review columns while omitting tracked-only `proof_*` / `source_scan_*` top-level fields and `position_event_persistence` mutation responses.
- Switched regular supervised scanner tracking to default-on for every regular playbook. `supervised_scan.py` now resolves all non-AI playbooks to `position_tracking_mode=auto_track`; AI Commodity remains disabled for the browser/scanner tracked-position family, and `quality90_debit55_canary` stays `proof_scope=regular_supervised_control` so canary rows remain control-labeled even when trackable. Updated the pending-candidate tests and living docs to make the new rule explicit: scout/control/non-Bullish-Pullback regular rows should queue and auto-track when they have fresh executable proof eligibility, while blocked/stale/unpriced/paper-only/proof-ineligible rows get dispositions instead of disappearing. Verification: metadata probe confirmed all regular playbooks are `auto_track`, AI Commodity is `disabled`, and quality90 is `regular_supervised_control`; `uv run --locked python -m py_compile supervised_scan.py scripts\log_scan_picks.py scripts\pending_audit_candidates.py scripts\validate_pending_scan_candidates.py`; `uv run --locked python -m unittest tests.test_playbook_discovery tests.test_log_scan_picks tests.test_daily_all_lanes_audit -v` (`42` tests); `npm run verify:typecheck`; and `npm run verify:docs`. A follow-up `npm run options:audit:all-lanes` attempt exceeded the 3-minute local timeout, but it did refresh the read-only artifacts before timeout: `14` / `14` supervised playbooks, `13` returned diagnostics, `6` clear rows queued as `pending_live_validation` across Swing, Volatility Expansion, and Quality90 canary, and `7` blocked rows. `uv run --locked python scripts\validate_pending_scan_candidates.py --dry-run --date 2026-06-04` then correctly skipped validation because the market was not open.
- Implemented the debate-vetted regular-options next steps without changing live scanner exits or promoting the short-term filter. Pending candidate validation now writes `pending_scan_candidate_validation_latest.json` with per-candidate outcomes, scheduled/scanner creation requires source `creation_eligible=true`, `/api/proof-summary` splits raw exact-contract closed rows from proof-grade live-scan exact rows, manual exact evidence is visible but not production proof, review executability now rejects manual/midpoint/mark bases, and `scripts/replay_short_term_filter_point_in_time.py` generates the point-in-time replay for `short_term_fill_degradation_ge_15`. Latest replay is `paper_only_collecting` with `5` candidate rows, `0` champion-matched rows, `0` exact-priced rows, and `5` unpriced/non-executable rows; the paper monitor remains `collecting` with `0` fresh rows. Verification: `uv run --locked python -m py_compile scripts\pending_audit_candidates.py scripts\validate_pending_scan_candidates.py scripts\log_scan_picks.py scripts\replay_short_term_filter_point_in_time.py scripts\audit_trading_desk_negative_trade_decisions.py scripts\build_current_policy_historical_picks_audit.py python-backend\main.py options_profit_gate.py options_profit_flywheel.py`, `uv run --locked python -m unittest tests.test_daily_all_lanes_audit tests.test_log_scan_picks tests.test_tracked_positions_api tests.test_trading_desk_negative_trade_decision_audit tests.test_short_term_filter_point_in_time_replay tests.test_options_profit_gate tests.test_options_profit_cycle -v`, `uv run --locked python -m unittest tests.test_options_api_e2e.OptionsAlgorithmApiE2ETests.test_options_profit_status_endpoint_overlays_current_tracked_positions_health -v`, `node --test tests\trading-desk\position-evidence.test.js`, `uv run --locked python scripts\replay_short_term_filter_point_in_time.py --no-write --json`, `uv run --locked python scripts\replay_short_term_filter_point_in_time.py`, `uv run --locked python scripts\monitor_current_policy_entry_filter_paper.py --no-write`, `npm run options:replay:short-term-filter -- --no-write`, `npm run verify:typecheck`, `npm run verify:docs`, and `npm run lint`.
- Added Point 1 auth boundaries for the architecture/readability remediation loop. Browser-facing state-changing and tool routes now call `requireLocalOperator(req)` before body parsing; `src/lib/operator-auth.ts` uses server-only `OPTIONS_LOCAL_OPERATOR_TOKEN`, `x-options-operator-token`, `Authorization: Bearer ...`, or an HttpOnly local operator session cookie. Added `GET/POST /api/operator/session` as the documented Next-only session surface, kept `OPTIONS_BACKEND_API_TOKEN` as a separate Next-to-FastAPI bridge token, and changed the FastAPI bridge-token comparison to constant-time. Mutation-intent headers remain required audit labels but are not authorization. Verification: `node --test tests/ui/operator-auth.test.js`, `node --test tests/trading-desk/mutation-intent.test.js`, `node --test tests/strategy-lab/replay-intent.test.js`, `uv run --locked python -m unittest tests.test_backend_bridge_auth -v`, `npm run verify:typecheck`, `npm run lint`, `uv run --locked python -m py_compile python-backend\main.py scripts\generate_route_parity.py`, and `npm run docs:route-parity`.
- Added Point 2 route mutation inventory to the generated route map. `scripts/generate_route_parity.py` now derives lifecycle, auth boundary, mutation-intent label, route contract, store, and owner fields from mounted Next route source and route-contract modules, renders browser-route and backend-only FastAPI auth/mutation tables in `docs/route-parity.md`, and fails `verify:docs` when a mutating mounted Next route lacks local operator auth. Backend-only FastAPI routes are classified as direct surfaces protected by `OPTIONS_BACKEND_API_TOKEN` when configured. Added direct handler tests proving `/api/scan`, `/api/predictions/grade`, and `/api/tools/[name]` reject missing or wrong local operator auth before body parsing or bridge calls. Verification: `uv run --locked python -m unittest tests.test_route_parity_generator -v`, `uv run --locked python -m py_compile scripts\generate_route_parity.py`, `node --test tests/ui/operator-auth.test.js`, `npm run docs:route-parity`, and `npm run verify:docs`.
- Added Point 3 proof/evidence contract centralization. Canonical proof classes, research/backfill marker tokens, trusted/untrusted exit basis tokens, and frontend display group precedence now live in `data/contracts/proof-evidence-contract.json`, with backend predicates in `python-backend/proof_contract.py` and frontend constants in `src/lib/trading-desk/proofContract.ts`. `python-backend/main.py` now uses the backend proof predicates for grouped summaries and `/api/proof-summary`; `positions_service.py` consumes canonical proof-class constants; `positionEvidence.ts` consumes shared proof strings and token lists. Added `docs/proof-evidence-contract.md` as the semantic owner. Verification: `uv run --locked python -m unittest tests.test_proof_contract tests.test_positions_review_engine tests.test_tracked_positions_api -v`, `node --test tests/trading-desk/position-evidence.test.js`, `uv run --locked python -m py_compile python-backend\proof_contract.py python-backend\positions_service.py python-backend\main.py`, and `npm run verify:typecheck`.
- Tightened Point 3 after backend satisfaction review. Stored-row production proof now re-checks exact selection source, verified source scan lineage, OPRA source, executable entry evidence, trusted closed exit, and calculable realized P&L; `options_profit_gate.py` and `options_profit_flywheel.py` now call the shared proof-grade closed predicate instead of local proof/source-label checks. Verification: `uv run --locked python -m unittest tests.test_proof_contract tests.test_options_profit_gate tests.test_options_profit_cycle tests.test_tracked_positions_api -v` and `node --test tests/trading-desk/position-evidence.test.js`.
- Tightened Point 3 again after satisfaction reviewers found frontend and stale-metadata drift. The shared contract now includes entry-proof gates, required quote-freshness fields, and research/backfill identity fields for Python creation/classification, Python stored-row predicates, and TypeScript display. Backend creation-time and proof-grade stored rows reject missing/stale quote freshness and stored rows treat `closed_at` as closed for exit/P&L enforcement, while frontend `positionEvidence.ts` no longer treats a bare `live_scan_exact_contract` string or opaque `backfill_audit_id` row as production proof. Verification: `uv run --locked python -m unittest tests.test_proof_contract tests.test_options_profit_gate tests.test_options_profit_cycle tests.test_tracked_positions_api -v`, `node --test tests/trading-desk/position-evidence.test.js`, and `npm run verify:typecheck`.
- Closed Point 3 after final satisfaction debate. Creation-time proof classification now requires an explicit trusted OPRA source label from the shared proof contract source fields regardless of provider/test mode, and the docs explicitly name row-level and source-snapshot backfill/migration identity fields such as `backfill_audit_id`, `position_migration_id`, and `position_migrated_at_utc` as production-proof blockers. Final reviewer pass returned backend, frontend, and metrics/docs PASS. Verification: `uv run --locked python -m unittest tests.test_proof_contract tests.test_positions_review_engine tests.test_tracked_positions_api tests.test_options_profit_gate tests.test_options_profit_cycle -v` (`111` tests), `node --test tests/trading-desk/position-evidence.test.js`, `npm run verify:typecheck`, `npm run verify:docs`, and `git diff --check` with only line-ending warnings.
- Added Point 4 scanner creation safety centralization. Scanner-origin tracked-position and suggested-trade creates now require archived source lineage, source caps enforcement, explicit source `creation_eligible=true`, no source creation blockers, a current guardrail rerun, current caps enforcement, explicit current `creation_eligible=true`, no current creation blockers, and final proof eligibility. Scheduled auto-track now fails closed on unknown market state, unavailable exposure snapshots, caps-off exposure, non-auto-track lanes, env kill switch, blocked guardrails, `creation_eligible` not exactly `true`, and per-pick creation blockers, while fill-attempt audit rows preserve the gate state for later validation. Added `data/contracts/scanner-creation-safety-contract.json` and `docs/scanner-creation-safety-contract.md` as the semantic owner, with proof-scope values tested against `supervised_scan.py` constants. Verification: `uv run --locked python -m unittest tests.test_scanner_creation_contract tests.test_tracked_positions_api tests.test_suggested_trades_api tests.test_log_scan_picks tests.test_daily_all_lanes_audit tests.test_playbook_discovery tests.test_options_api_e2e -v` (`132` tests), `npm run verify:docs`, `npm run verify:typecheck`, and `git diff --check` with only line-ending warnings.
- Added Point 5 evidence recording observability. Tracked-position create, duplicate create, review, and close mutations now return `position_event_persistence` with recorded, failed, or skipped status and error metadata; failed lifecycle-event persistence logs a forward-evidence health event when possible, and `/api/backtest/forward-evidence` exposes position-event failure counts plus opened/review ledger counts. `/api/scan` now reports scan-capture health-log append failures as `forward_truth_event_log_error` instead of swallowing them. This is operational observability only and does not relax proof eligibility or promote failed evidence rows. Verification: `uv run --locked python -m unittest tests.test_tracked_positions_api.TrackedPositionsApiTests.test_create_position_reports_position_opened_event_failure tests.test_tracked_positions_api.TrackedPositionsApiTests.test_duplicate_create_reports_skipped_position_event_persistence tests.test_tracked_positions_api.TrackedPositionsApiTests.test_review_positions_reports_position_event_failure tests.test_tracked_positions_api.TrackedPositionsApiTests.test_close_position_reports_position_event_failure -v`, `uv run --locked python -m unittest tests.test_tracked_positions_api tests.test_options_api_e2e tests.test_forward_options_ledger -v` (`105` tests), `uv run --locked python -m py_compile python-backend\main.py tests\test_tracked_positions_api.py`, `npm run verify:typecheck`, `npm run verify:docs`, and `git diff --check` with only line-ending warnings.
- Added Point 6 FastAPI router split pilot. `python-backend/main.py` now stays the composition root while `python-backend/predictions_routes.py` owns prediction read/grade/delete handlers and `python-backend/tools_routes.py` owns tool dispatch. `python-backend/backend_route_context.py` keeps those support routers late-bound to the loaded backend module so tests and fixtures can still patch globals after `TestClient` creation, and docs now record the no-`main.py`-import router rule plus the remaining larger route families still awaiting extraction. Route parity continues to see extracted top-level `@router.*` decorators. Verification: `uv run --locked python -m unittest tests.test_backend_support_routes tests.test_backend_profile_routes tests.test_route_parity_generator tests.test_backend_bridge_auth tests.test_options_api_e2e -v` (`63` tests), `uv run --locked python -m py_compile python-backend\main.py python-backend\backend_route_context.py python-backend\predictions_routes.py python-backend\tools_routes.py tests\test_backend_support_routes.py scripts\generate_route_parity.py`, `npm run docs:route-parity`, `npm run verify:docs`, `npm run verify:typecheck`, and `git diff --check` with only line-ending warnings.
- Added Point 7 application-service pilot. `/api/proof-summary` now calls decorator-free `python-backend/proof_summary_service.py`, which assembles loop-health, claim-readiness, forward-evidence, and tracked-position proof counts through `BackendRouteContext` without importing canonical `main.py` or owning proof semantics. `python-backend/main.py` remains the HTTP adapter and composition root, while docs now distinguish route adapters, application services, domain/proof contracts, and repositories. Verification: `uv run --locked python -m unittest tests.test_proof_summary_service tests.test_tracked_positions_api tests.test_proof_contract tests.test_forward_options_ledger tests.test_options_api_e2e -v` (`118` tests), `uv run --locked python -m py_compile python-backend\main.py python-backend\backend_route_context.py python-backend\proof_summary_service.py tests\test_proof_summary_service.py`, `npm run verify:docs`, `npm run verify:typecheck`, and `git diff --check` with only line-ending warnings.
- Added Point 8 scanner pipeline stage map. `supervised_scan.py` now exposes the canonical descriptive scanner stages while `data/contracts/scanner-creation-safety-contract.json` and `docs/scanner-creation-safety-contract.md` name the same playbook, raw candidate, drop diagnostic, policy, guardrail, managed selection, payload, lineage, proof, and creation-disposition boundaries. The map is documentation/contract metadata only and does not add public API fields or change scan behavior. Living docs now point future agents at the scanner stage owner, and `tests/test_scanner_creation_contract.py` keeps runtime stage order and emitted fields in parity with the JSON contract. Verification: `uv run --locked python -m unittest tests.test_scanner_creation_contract -v`, `uv run --locked python -m unittest tests.test_options_api_e2e tests.test_daily_all_lanes_audit tests.test_playbook_discovery tests.test_log_scan_picks tests.test_scanner_creation_contract -v` (`93` tests), `uv run --locked python -m py_compile supervised_scan.py tests\test_scanner_creation_contract.py`, `npm run verify:typecheck`, and `npm run verify:docs`.
- Added Point 9 replay/profit modularization. Replay/profit readback assembly for cached backtest reports, metric truth, experiments, profitability forensics, stability, live policy, exit audit, comparison, and summary now lives in decorator-free `python-backend/replay_profit_service.py` using `BackendRouteContext`; `python-backend/main.py` remains the FastAPI route adapter and cache primitive owner. Added `docs/replay-profit-contract.md` as the ownership map for replay orchestration, scanner policy, proof/profit gates, profit-cycle state, and options-profit status, with living-doc links and a durable decision that this split does not change replay math, proof semantics, DB state, or public API payloads. Verification: `uv run --locked python -m unittest tests.test_replay_profit_contract tests.test_replay_profit_service -v` (`7` tests), `uv run --locked python -m unittest tests.test_replay_profit_contract tests.test_replay_profit_service tests.test_options_api_e2e tests.test_options_profit_gate tests.test_options_profit_cycle -v` (`76` tests; existing psycopg ResourceWarnings only), `node --test tests\strategy-lab\replay-intent.test.js`, `uv run --locked python -m py_compile python-backend\main.py python-backend\replay_profit_service.py tests\test_replay_profit_service.py tests\test_replay_profit_contract.py`, `npm run verify:docs`, and `npm run verify:typecheck`.
- Added Point 10 repository interfaces. `python-backend/repository_contracts.py` now defines structural Protocols for the shared Trading Desk position repository shape, tracked-position repositories, suggested-trade repositories, and optional tracked-only capabilities such as `list_compact_positions` and `profit_status_snapshot`. Factory return annotations now point at the Protocols, `positions_service.review_open_positions` accepts the shared repository interface, and the stale SQLite tracked-position fallback docstring now says explicit tests/legacy tools only. Added `docs/repository-contract.md` as the semantic owner for Postgres tracked positions, SQLite suggested trades, unavailable sentinels, memory/test doubles, and proof/readback boundaries. No SQL, DB fallback, route payload, or proof behavior changed. Verification: `uv run --locked python -m unittest tests.test_repository_contract -v`, `uv run --locked python -m py_compile python-backend\repository_contracts.py python-backend\positions_repository.py python-backend\suggested_trades_repository.py python-backend\positions_service.py tests\test_repository_contract.py`, `uv run --locked python -m unittest tests.test_repository_contract tests.test_positions_repository_schema tests.test_tracked_positions_api tests.test_suggested_trades_api tests.test_positions_review_engine tests.test_proof_summary_service tests.test_options_api_e2e -v` (`146` tests), `node --test tests\trading-desk\mutation-intent.test.js`, `npm run verify:docs`, `npm run verify:typecheck`, and `git diff --check` with only line-ending warnings.
- Added Point 11 versioned repository migrations. `python-backend/repository_migrations.py` now owns a versioned Trading Desk repository migration manifest plus checksum-guarded `repository_schema_migrations` ledger helpers. Postgres tracked positions, SQLite suggested trades, and explicit test/legacy SQLite tracked positions keep their existing idempotent `init_schema()` DDL and legacy column repair behavior, then record the current schema as a baseline migration after bootstrap succeeds. Added `docs/repository-migrations.md` as the semantic owner and linked it from the repository, storage, architecture, project-context, decisions, and index docs. This does not add Alembic, constraints, indexes, route changes, proof changes, scanner behavior, AI commodity migration work, or a tracked-position SQLite fallback. Verification: `uv run --locked python -m unittest tests.test_repository_migrations tests.test_positions_repository_schema tests.test_repository_contract -v` (`22` tests), `uv run --locked python -m py_compile python-backend\repository_migrations.py python-backend\positions_repository.py python-backend\suggested_trades_repository.py tests\test_repository_migrations.py tests\test_positions_repository_schema.py`, `uv run --locked python -m unittest tests.test_tracked_positions_api tests.test_suggested_trades_api tests.test_proof_summary_service -v` (`46` tests), `node --test tests\trading-desk\mutation-intent.test.js`, `npm run verify:docs`, and `npm run verify:typecheck`.
- Added Point 12 repository constraint ownership. `python-backend/repository_constraints.py` now maps Trading Desk repository invariants by enforcement layer: DB-enforced primitive relational safety, API/service validators, proof-contract-owned semantics, and deferred DB checks. `docs/repository-constraints.md` is the semantic owner and explicitly keeps proof truth out of SQL while deferring broad status/direction/numeric `CHECK` constraints until a read-only audit is clean. `scripts/audit_repository_constraints.py` audits current stores without writes or fallbacks, and `SQLiteSuggestedTradesRepository._connect()` now enables `PRAGMA foreign_keys=ON` so the already-declared suggested-review foreign key is actually enforced for future writes. No broad Postgres `CHECK`, SQLite table rebuild, proof/scanner/replay behavior, route payload, AI commodity, options-history, or tracked-position SQLite fallback change was added. Verification: `uv run --locked python -m unittest tests.test_repository_constraints tests.test_repository_migrations tests.test_positions_repository_schema -v` (`20` tests), `uv run --locked python -m py_compile python-backend\repository_constraints.py scripts\audit_repository_constraints.py python-backend\suggested_trades_repository.py tests\test_repository_constraints.py`, `uv run --locked python scripts\audit_repository_constraints.py --json` (`sqlite_suggested_trades=pass`, `postgres_tracked_positions=skipped` because `DATABASE_URL` is unset), `uv run --locked python -m unittest tests.test_tracked_positions_api tests.test_suggested_trades_api tests.test_proof_contract tests.test_scanner_creation_contract -v` (`60` tests), `node --test tests\trading-desk\mutation-intent.test.js`, `npm run verify:docs`, and `npm run verify:typecheck`.
- Added Point 13 repository index ownership. `python-backend/repository_indexes.py` now maps existing Trading Desk repository indexes and deferred candidates by store, table, read path, and uniqueness posture. `docs/repository-indexes.md` is the semantic owner and makes indexes read-path/performance support only, not proof semantics, scanner safety, route behavior, or constraints. `scripts/audit_repository_indexes.py` audits current index metadata without writes or fallbacks. Following the debate, no new Postgres or SQLite index DDL was added; composite status/fill/id, tracked-review id-tiebreaker, and closed-at/profit indexes remain deferred until row counts or query plans justify a migration/maintenance plan. Verification: `uv run --locked python -m unittest tests.test_repository_indexes tests.test_repository_migrations tests.test_repository_constraints tests.test_positions_repository_schema -v` (`25` tests), `uv run --locked python -m py_compile python-backend\repository_indexes.py scripts\audit_repository_indexes.py tests\test_repository_indexes.py`, `uv run --locked python scripts\audit_repository_indexes.py --json` (`sqlite_suggested_trades=pass`, `postgres_tracked_positions=skipped` because `DATABASE_URL` is unset), `uv run --locked python -m unittest tests.test_tracked_positions_api tests.test_suggested_trades_api tests.test_proof_summary_service tests.test_options_api_e2e -v` (`88` tests), `node --test tests\trading-desk\mutation-intent.test.js`, `npm run verify:docs`, and `npm run verify:typecheck`.
- Ran the negative regular-options pick audit as a six-slice subagent pass plus local no-write artifact checks. Consensus: promoted entry guardrails already learned away most historical research/backfill losses; `short_term_fill_degradation_ge_15` is the only surviving residual entry-filter hypothesis, and it remains paper-only because walk-forward is mixed and the forward monitor still has `0` fresh rows. No live scanner selection, proof bar, or exit-policy change was promoted. Implemented one scoped creation-safety fix: scheduled scan auto-track now skips any pick whose scan metadata has `creation_eligible=false`, non-empty `creation_blockers`, or a blocked guardrail decision before building a tracked-position payload. Verification: `uv run --locked python scripts\analyze_current_policy_entry_filters.py --no-write`, `uv run --locked python scripts\validate_current_policy_entry_filter_walkforward.py --no-write`, `uv run --locked python scripts\monitor_current_policy_entry_filter_paper.py --no-write`, `uv run --locked python scripts\build_current_policy_historical_picks_audit.py --no-write`, `uv run --locked python scripts\build_current_policy_cohort_health.py --no-write`, `uv run --locked python scripts\analyze_trading_desk_profitability_guardrails.py --no-write`, `uv run --locked python scripts\audit_trading_desk_negative_trade_decisions.py --no-write`, `uv run --locked python scripts\replay_trading_desk_exit_policies.py --no-write`, `uv run --locked python scripts\audit_trading_desk_legacy_missed_closes.py --no-write`, `uv run --locked python -m py_compile scripts\log_scan_picks.py tests\test_log_scan_picks.py`, and `uv run --locked python -m unittest tests.test_log_scan_picks -v`.

## 2026-06-03

- Added a Today's Picks strip above the Open Trades table. It uses the existing trusted signal/entry-date normalization, stays lane-aware, shows today-picked open trades as compact highlight tiles, and includes a Show Today button that applies the Today entry-date filter. Verification: `npm run verify:typecheck`, `npm run lint`, `npm run build`, and browser QA against clean production Next `http://localhost:3020` confirmed the strip renders above the tracked-options table, the Show Today control applies the filter, and page horizontal overflow remains `0`. The already-running dev ports were unhealthy from a stale/mixed Next dev bundle, so visual QA used the clean production server.
- Changed the regular scanner portfolio/exposure guardrail family from hard blockers to visible cautions. Existing positions, same-ticker/exact-spread exposure, max concurrent positions, daily/weekly loss, open executable drawdown, position/portfolio cost-risk, sector/regime caps, and correlated-index caps no longer suppress otherwise proof-eligible picks; proof, lane-scope, execution-label, liquidity, and replay-promoted entry-quality failures remain hard blockers. Verification: focused `uv run --locked python -m unittest tests.test_strategy_audit... -q` (`7` guardrail tests), `uv run --locked python -m unittest tests.test_options_api_e2e -q` (`42` tests), and `uv run --locked python -m unittest tests.test_tracked_winner_primary_lane tests.test_trading_desk_profitability_guardrails -q` (`5` tests). The broader adjacent suite timed out after `124s`, so focused checks were used.
- Fixed the scanner-to-creation safety path across the regular supervised lane family after subagent debate. Playbooks now carry lane-wide validation/tracking/proof metadata, production scans default to portfolio caps on, scheduled auto-track requires caps-enforced exposure plus an auto-track lane, and pending live validation includes all regular validation-enabled lanes instead of a hardcoded Bullish Pullback path. Scanner-origin tracked-position and suggested-trade creates now require verified archived forward-scan lineage, caps-enforced source state, no creation blockers, a current guardrail rerun, and proof eligibility; manual research/backfill rows must opt into `manual_paper`/`manual_broker`. Added open executable drawdown gating from fresh executable reviews, expanded scanner UI fallback lanes, and added quote evidence hashes to side-aware zero-bid replay. Verification: `python -m pytest tests/test_log_scan_picks.py tests/test_daily_all_lanes_audit.py tests/test_options_api_e2e.py tests/test_tracked_positions_api.py tests/test_suggested_trades_api.py tests/test_strategy_audit.py tests/test_playbook_discovery.py tests/test_side_aware_zero_bid_replay.py -q` (`219` passed, `37` subtests), `python -m py_compile supervised_scan.py scripts/log_scan_picks.py scripts/pending_audit_candidates.py scripts/validate_pending_scan_candidates.py python-backend/main.py python-backend/positions_service.py`, and `npm run verify:typecheck`.
- Removed hierarchy framing from active scanner code, UI fallbacks, tests, living docs, and latest generated readbacks. `bullish_pullback_observation` is now described as a technical/scheduled routing fallback only when no playbook is supplied, surfaced as `Bullish Pullback`, with `regular_peer_strategy` role and `candidate` cohort role rather than any product-priority role. `tracked_winner_primary` remains a stable playbook ID but is labeled `Tracked Winner` and documented/tested as a peer lane; `regular_bearish_put_primary` remains a stable ID but is labeled `Regular Bearish Put`. Renamed scanner ordering metadata to `lane_display_order`, replaced omitted-playbook constants with fallback naming, and refreshed the all-lane guardrail artifact, profit-capture queue, operating scorecard, and lane-data shortfall latest artifacts so current readbacks use peer-lane wording. AI commodity / commodity-infrastructure options is documented as a separate proof-first strategy lane, not part of the regular-options lane family. Verification: `python scripts\audit_regular_guardrail_starvation.py --include-commodity --top-limit 8`, `python scripts\build_regular_options_profit_capture_queue.py`, `python scripts\build_regular_profitability_operating_scorecard.py`, `python scripts\lane_data_shortfall_report.py`, focused `python -m pytest ... -q` (`34` passed), `npm run verify:docs`, and `python -m py_compile supervised_scan.py wfo_optimizer.py auto_scan.py scripts\audit_regular_guardrail_starvation.py scripts\audit_tracked_winner_profile.py scripts\audit_zero_pick_days_current_main_lane.py scripts\ensure_scan_picks_ran.py scripts\lane_data_shortfall_report.py scripts\log_scan_picks.py scripts\record_options_forward_truth.py scripts\run_ai_commodity_opra_progress.py scripts\run_regular_options_all_planned_sleeves.py scripts\run_regular_options_multilane_portfolio.py python-backend\main.py`.
- Ran the AI commodity OPRA capture guard. Started with `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest`; the bare Python command timed out, so the leftover process from this run was stopped and the same helper was rerun through the repo virtualenv. The readback returned `run_next_execution_command=true`, `guarded_command_decision_status=ready_to_run_primary_next_execution`, and `guarded_command_decision_safe_to_execute_now=true`, so ran exactly `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29`, then ran the required final readback. Outcome remains `not_verified`: generated artifact `2026-06-03T14:15:57Z`, capture status `no_rows_captured`, all `24` target-date symbols still missing, exact Alpaca OPRA shared quote dates stayed `3` / `100`, scan candidates stayed `0`, raw drop reasons remain recorded for `24` symbols, local exact store already matches the proof window and cannot advance history depth, and baseline no-progress is detected. The final guard still advertises the same `2026-05-29` capture command as ready, but the target has repeatedly failed with no rows captured; repair the full-universe capture path before looping it. Profitability remains unverified; exact replay remains blocked until sufficient Alpaca OPRA bid/ask history, with full replay still projected for `2026-10-15` if forward captures resume successfully.
- Reran the AI commodity OPRA capture guard later the same morning. Started with `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest`, observed `run_next_execution_command=true` for the same guarded `2026-05-29` capture target, ran exactly `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29`, and then ran the required readback. Latest artifact `2026-06-03T14:23:18Z` remains `not_verified`: capture status `no_rows_captured`, all `24` target-date symbols still missing, exact Alpaca OPRA shared quote dates still `3` / `100`, scan candidates still `0`, `scan_drop_reason_audit_status=raw_drop_reasons_recorded`, `scan_drop_reason_count=24`, local exact store already matches the proof window, local refresh cannot advance history depth, and `snapshot_updated_since_is_backfill_capability=false`. The final guard still advertises the same command as ready, but this is another no-progress loop; repair the full-universe Alpaca OPRA capture path before repeating it. Profitability remains unverified and cannot be claimed without exact Alpaca OPRA bid/ask replay evidence.
- Reran the AI commodity OPRA capture guard in the afternoon. The initial bare `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest` timed out, so the readback was rerun through the repo virtualenv, then the advertised guarded command was run exactly as `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29`, followed by the required final readback. Latest artifact `2026-06-03T20:15:25Z` remains `not_verified`: capture status `no_rows_captured`, all `24` target-date symbols still missing, exact Alpaca OPRA shared quote dates still `3` / `100`, scan candidates still `0`, `scan_drop_reason_audit_status=raw_drop_reasons_recorded`, `scan_drop_reason_count=24`, `last_execution_status=failed`, and `next_evidence_action=repair_full_scan_universe_capture_and_proof_alignment`. The guard still advertises the same command as ready, but this is another no-progress loop; repair the full-universe Alpaca OPRA capture path before repeating it. Profitability remains unverified and cannot be claimed without exact Alpaca OPRA bid/ask replay evidence.
- Reran the AI commodity OPRA capture guard again. Started with `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest`, observed `run_next_execution_command=true`, `guarded_command_decision_status=ready_to_run_primary_next_execution`, and `guarded_command_decision_safe_to_execute_now=true`, then ran exactly `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29` followed by the required readback. Latest artifact `2026-06-03T20:22:59Z` remains `not_verified`: capture status `no_rows_captured`, all `24` target-date symbols still missing, exact Alpaca OPRA shared quote dates still `3` / `100`, scan candidates still `0`, `scan_drop_reason_audit_status=raw_drop_reasons_recorded`, `scan_drop_reason_count=24`, `last_execution_status=failed`, `snapshot_updated_since_is_backfill_capability=false`, and `next_evidence_action=repair_full_scan_universe_capture_and_proof_alignment`. The guard still advertises the same command as ready, but this is another no-progress loop; repair the full-universe Alpaca OPRA capture path before repeating it. Profitability remains unverified and cannot be claimed without exact Alpaca OPRA bid/ask replay evidence.

## 2026-06-02

- Repaired the high-priority lower-tier profit-capture rows without lowering proof bars. Imported missing exact ThetaData OPRA/NBBO rows for the repair set (`190` unique requested items, `93` normalized rows, `91` new DB rows plus `2` duplicates), reran the full Bullish Pullback ticker sleeve round (`171` rows / all `59` tickers), rebuilt confidence/ticker audits, reran the full all-planned sleeve suite (`44` implemented variants), and reran `tracked_winner_chain_native_qqq_time80_research` after another exact-fill iteration (`43` additional imported rows; final time80 replay `118` priced / `215` candidates, `54.9%` coverage, PF `1.49`). Updated `scripts/run_regular_options_multilane_portfolio.py` to point IWM and time80 at repaired artifacts, and updated `scripts/build_regular_options_symbol_sleeves.py` to ignore stale `latest_partial.json` all-planned artifacts after a newer full refresh. Added explicit `selection_readiness` routing to `scripts/build_regular_options_profit_capture_queue.py`: Tier A is `paper_review_candidate`, Tier B is `watch_repair_only`, clear fresh signatures are `historical_signature_only`, blocked rows are `blocked_guardrail_only`, and quarantine rows are `do_not_chase`. Latest queue: `97` rows, `15` Tier A, `82` Tier B, `16` high-priority repairs, `6` fresh scan matches, `4` blocked-but-interesting rows, and `173` quarantine rows. Subagent debate agreed no broad GOOGL promotion and no live picks from this artifact alone; NEM is the clean Tier A paper-review opportunity, while GOOGL remains watch/repair or quarantine because unresolved/zero-bid issues remain. Verification so far: `uv run --locked python -m py_compile scripts\build_regular_options_profit_capture_queue.py scripts\build_regular_options_symbol_sleeves.py scripts\run_regular_options_multilane_portfolio.py tests\test_regular_options_profit_capture_queue.py tests\test_regular_options_symbol_sleeves.py`, `uv run --locked python -m unittest tests.test_regular_options_profit_capture_queue tests.test_regular_options_symbol_sleeves -v`, and regenerated the symbol-sleeve matrix, profit-capture queue, multi-lane report, Bullish Pullback audits, all-planned suite, and operating scorecard.
- Repaired the daily no-pick audit path so it no longer relies on Bullish Pullback alone. Added `scripts/ensure_daily_all_lanes_audit_ran.py`, extended `scripts/audit_regular_guardrail_starvation.py` with `--include-commodity` and per-playbook ticker-scope reporting, wired `scripts/run_scan_picks.bat` and `scripts/run_scan_picks_safety_net.bat` to require an all-supervised daily artifact, and added `npm run options:audit:all-lanes`. Added `scripts/pending_audit_candidates.py` plus `scripts/validate_pending_scan_candidates.py` so clear approved-lane audit rows are preserved as `pending_live_validation` and only promoted by a market-hours rerun with portfolio caps enabled. Fixed `scripts/log_scan_picks.py` proof ordering so scheduled auto-track records forward scan lineage before position creation, and disabled auto-track when the market is not open. Forced the audit for `2026-06-02`: `14` / `14` supervised playbooks completed, AI Commodity included, all configured ticker scopes audited with default `watchlist_size=59`, `6` read-only diagnostics returned after-hours (`2` clear Swing and `4` blocked scout/control), and the `2` clear Swing rows were queued as pending validation. Verification: `python -m pytest tests/test_daily_all_lanes_audit.py tests/test_zero_pick_all_lanes_audit.py tests/test_log_scan_picks.py -q`, `python -m py_compile scripts\audit_regular_guardrail_starvation.py scripts\ensure_daily_all_lanes_audit_ran.py scripts\pending_audit_candidates.py scripts\validate_pending_scan_candidates.py scripts\log_scan_picks.py`, and `python scripts\ensure_daily_all_lanes_audit_ran.py --date 2026-06-02`.
- Added the regular options profit-capture queue after subagent debate agreed the next move was evidence visibility and proof-hardening rather than broad stop changes or lowered proof bars. `scripts/build_regular_options_profit_capture_queue.py` combines the symbol-sleeve matrix, current-policy historical symbol/lane buckets, and the fresh guardrail-starvation audit into `data/profitability-lab/regular-options-profit-capture-queue/latest.*` and `docs/regular-options-profit-capture-queue.md`. Latest readback: `98` research/paper queue rows, `15` Tier A clean exact rows, `83` Tier B profitable watch/repair rows, `18` high-priority evidence repairs, `6` fresh scan signature matches, `4` blocked-but-interesting rows, and `164` quarantine/do-not-chase rows. Clear fresh SPY/QQQ swing rows now surface as Tier C signature matches, and GOOGL tracked-winner/Bullish Pullback evidence is visible as high-priority Tier B repair/watch rather than hidden or mislabeled as clean proof. Wired the queue into `scripts/build_regular_profitability_operating_scorecard.py` and regenerated `docs/regular-options-operating-scorecard.md`; live scanner guardrails, stop policy, and broker behavior are unchanged. Verification: `uv run --locked python -m py_compile scripts\build_regular_options_profit_capture_queue.py tests\test_regular_options_profit_capture_queue.py scripts\build_regular_profitability_operating_scorecard.py tests\test_regular_profitability_operating_scorecard.py`, `uv run --locked python -m unittest tests.test_regular_options_profit_capture_queue -v`, manual invocation of `tests.test_regular_profitability_operating_scorecard.test_scorecard_marks_product_progress_but_keeps_proof_blocked`, `uv run --locked python scripts\build_regular_options_profit_capture_queue.py --no-write`, `uv run --locked python scripts\build_regular_options_profit_capture_queue.py`, `uv run --locked python scripts\build_regular_profitability_operating_scorecard.py --no-write`, and `uv run --locked python scripts\build_regular_profitability_operating_scorecard.py`.
- Ran the Trading Desk loss-control iteration with subagent debate before and after implementation. Consensus was to stop tuning broad exits from the historical paper set, test entry avoidance first, and promote nothing until fresh forward evidence exists. Added `scripts/analyze_current_policy_entry_filters.py` and `tests/test_current_policy_entry_filter_lab.py`; latest lab artifacts under `data/forward-tracking/current_policy_entry_filter_lab_latest.*` and report `docs/current-policy-entry-filter-lab.md` identify `short_term_fill_degradation_ge_15` as the only useful paper candidate. It blocks `9` historical current-policy rows, avoids `5` rows `<= -50%`, avoids `3` rows `<= -90%`, loses `2` winners, and lifts the kept historical cohort to avg `+61.01%`. Added `scripts/monitor_current_policy_entry_filter_paper.py` and `tests/test_current_policy_entry_filter_paper_monitor.py`; latest monitor artifacts under `data/forward-tracking/current_policy_entry_filter_paper_monitor_latest.*` and report `docs/current-policy-entry-filter-paper-monitor.md` are `collecting` with `0` fresh rows since `2026-06-02`. Wired the monitor into `scripts/build_regular_profitability_operating_scorecard.py`, regenerated `docs/regular-options-operating-scorecard.md`, and kept the scanner policy unchanged. Verification: `uv run --locked python -m py_compile scripts\analyze_current_policy_entry_filters.py tests\test_current_policy_entry_filter_lab.py scripts\monitor_current_policy_entry_filter_paper.py tests\test_current_policy_entry_filter_paper_monitor.py scripts\build_regular_profitability_operating_scorecard.py tests\test_regular_profitability_operating_scorecard.py`, `uv run --locked python -m unittest tests.test_current_policy_entry_filter_lab tests.test_current_policy_entry_filter_paper_monitor -v`, manual invocation of `tests.test_regular_profitability_operating_scorecard.test_scorecard_marks_product_progress_but_keeps_proof_blocked`, `uv run --locked python scripts\analyze_current_policy_entry_filters.py --no-write`, `uv run --locked python scripts\monitor_current_policy_entry_filter_paper.py --no-write`, and `uv run --locked python scripts\build_regular_profitability_operating_scorecard.py --no-write`. The pytest-style scorecard test could not be run with `python -m pytest` in the locked environment because `pytest` is not installed.
- Added the all-regular-lanes current-policy entry-filter walk-forward validator. `scripts/validate_current_policy_entry_filter_walkforward.py` reads the exact-contract current-policy stop-grid rows, freezes `short_term_fill_degradation_ge_15`, tests chronological train/latest-holdout folds, month folds, lane-specific fill-degradation diagnostics, and the broad all-lane fill-degradation `>=15%` diagnostic. Latest artifacts under `data/forward-tracking/current_policy_entry_filter_walkforward_latest.*` and report `docs/current-policy-entry-filter-walkforward.md` show status `mixed_walkforward_watch_not_promoted`: frozen short-term passes overall and the `2026-05` holdout, but `2026-04` is `winner_damage_too_high`, and the broad all-lane fill rule is rejected after losing `10` winners. Wired the read into `scripts/build_regular_profitability_operating_scorecard.py` and regenerated `docs/regular-options-operating-scorecard.md`. Verification: `uv run --locked python -m py_compile scripts\validate_current_policy_entry_filter_walkforward.py tests\test_current_policy_entry_filter_walkforward.py scripts\build_regular_profitability_operating_scorecard.py tests\test_regular_profitability_operating_scorecard.py`, `uv run --locked python -m unittest tests.test_current_policy_entry_filter_walkforward -v`, manual invocation of `tests.test_regular_profitability_operating_scorecard.test_scorecard_marks_product_progress_but_keeps_proof_blocked`, `uv run --locked python scripts\validate_current_policy_entry_filter_walkforward.py --no-write`, and `uv run --locked python scripts\build_regular_profitability_operating_scorecard.py --no-write`.
- Added the current-policy historical stop-grid replay for the Trading Desk loss problem. `scripts/replay_current_policy_historical_stop_grid.py` maps current-policy realized rows back to tracked positions, replays trusted exact-contract OPRA/NBBO daily close-check spread exits, writes latest/timestamped JSON/CSV/Markdown artifacts, and renders `docs/current-policy-historical-stop-grid.md`. Added focused coverage in `tests/test_current_policy_historical_stop_grid.py` for actionable deep-loss reduction, first-priced-already-through-stop classification, stop-policy summaries, and entry-avoidance signal aggregation. Latest readback: `112` / `112` current-policy realized rows replayed with `0` unresolved; baseline avg `+53.54%`, median `+50.60%`, `29` negatives, `8` rows `<= -90%`; `stop_80` is the only non-destructive daily close-check candidate, with avg `+53.69%`, `29` negatives, `7` rows `<= -90%`, `9` stop hits, and `0` winner flips. Tighter `stop_50`, `stop_60`, and `stop_70` reduce more near-total losses but add negatives and/or flip winners, so live stops stay unchanged and the next work shifts to entry-filter validation plus minute-level stop evidence. Verification: `uv run --locked python -m py_compile scripts\replay_current_policy_historical_stop_grid.py tests\test_current_policy_historical_stop_grid.py`, `uv run --locked python -m unittest tests.test_current_policy_historical_stop_grid -v`, `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py --no-write`, and `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py`.
- Tightened the current-policy historical stop-grid coverage readback to prove unresolved state by ticker, not only in aggregate. `scripts/replay_current_policy_historical_stop_grid.py` now writes `coverage.by_ticker`, `all_tickers_resolved`, and unresolved ticker counts into the JSON and renders a per-ticker coverage table in `docs/current-policy-historical-stop-grid.md`. Latest regenerated readback: `112` / `112` current-policy rows replayed, `17` tickers, `0` unresolved ticker buckets, and `0` unresolved rows for every ticker. Verification: `uv run --locked python -m py_compile scripts\replay_current_policy_historical_stop_grid.py tests\test_current_policy_historical_stop_grid.py`, `uv run --locked python -m unittest tests.test_current_policy_historical_stop_grid -v`, `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py --no-write`, and `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py`.
- Extended the stop-grid audit with a separate annual replay-backed exact cohort so the Apr/May tracked slice is no longer mistaken for a full-year sample. `scripts/replay_current_policy_historical_stop_grid.py` now reads the regular multi-lane exact stack, reconstructs position-shaped rows for stop-grid simulation without mutating the tracked-position store, and labels the cohort as replay-backed evidence rather than broker/live tracked fills. Latest regenerated readback: tracked/current-policy slice `112` replayed, `0` unresolved across `17` tickers; annual replay-backed cohort `234` replayed, entry window `2025-08-14` to `2026-03-24`, exit window `2025-09-09` to `2026-04-27`, and `0` unresolved across `37` tickers. The annual grid rejects broad stop tightening because every tested stop from `50%` through `90%` reduces average P&L and flips winners. Verification: `uv run --locked python -m py_compile scripts\replay_current_policy_historical_stop_grid.py tests\test_current_policy_historical_stop_grid.py`, `uv run --locked python -m unittest tests.test_current_policy_historical_stop_grid -v`, `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py --no-write --json`, and `uv run --locked python scripts\replay_current_policy_historical_stop_grid.py`.
- Extended the Trading Desk exit-policy replay with an executable stop grid (`stop_60`, `stop_70`, `stop_80`, `stop_90`) and deep-loss diagnostics. `scripts/replay_trading_desk_exit_policies.py` now reports loss buckets (`<= -50%` through `<= -99%`), per-policy bucket deltas versus baseline, stop-trigger summaries, and stop-trigger lane/ticker impact. Converted `tests/test_trading_desk_exit_policy_replay.py` to `unittest` so the repo's normal Python discovery runs it, and added coverage for stop-grid registration plus deep-loss / stop-impact metrics. Regenerated `data/forward-tracking/trading_desk_exit_policy_replay_latest.json`, `data/forward-tracking/trading_desk_exit_policy_replay_latest.csv`, and `docs/trading-desk-exit-policy-replay-2026-05-31.md`. Latest stored executable-review replay: baseline `107` rows, avg `+39.06%`, median `+43.72%`, `23` negatives, and `2` rows `<= -90%`; positive-delta stop-grid variants reduce `<= -90%` to `1` but raise negatives to `24` and flip `2` stored winners, so no broad live stop change is promoted. Verification: `uv run --locked python -m py_compile scripts\replay_trading_desk_exit_policies.py`, `uv run --locked python -m unittest tests.test_trading_desk_exit_policy_replay -v`, `uv run --locked python -m unittest discover -s tests -p "test_trading_desk_exit_policy_replay.py" -v`, `uv run --locked python scripts\replay_trading_desk_exit_policies.py --no-write`, `uv run --locked python scripts\replay_trading_desk_exit_policies.py`, and `npm run verify:docs`.

## 2026-06-01

- Ran the AI commodity OPRA capture guard. Started with `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest`, observed the fresh-scan guard open, ran exactly `python scripts/run_ai_commodity_opra_progress.py --skip-capture`, then ran the required readback. The readback opened the primary capture guard, so ran exactly `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-29` and then ran the required final readback. Outcome remains `not_verified`: the fresh scan made no material progress (`0` live candidates, quote freshness `fresh_or_not_age_limited`, raw drop reasons for `24` symbols), exact Alpaca OPRA shared quote dates stayed `3` / `100`, the `2026-05-29` capture event failed/no-progress with `exact_capture_progress_outcome.status=exact_capture_progress_failed_or_not_observed`, and missed capture dates since the latest shared date are `2026-05-26`, `2026-05-27`, and `2026-05-28`. The final readback still advertises the same `2026-05-29` force-capture as ready, but it was not looped again in this run after the no-progress attempt. Profitability remains unverified; exact replay remains blocked until sufficient Alpaca OPRA bid/ask history, now projected at `2026-10-15` for full replay if forward captures resume successfully.
- Extracted the Trading Desk tracked-position and suggested-trade data/review/paging surface from `PredictionsView.tsx` into `src/components/predictions/useTradingDeskRecords.ts`. The new hook owns open/closed record state, closed-page loading, explicit review mutations, merge/apply helpers, and stale-review tracking while preserving the existing read-route windows, `compact=1` payloads, and mutation-intent headers. `PredictionsView.tsx` dropped from `1723` to `1283` lines; the focused hook is `510` lines. Static mutation/read-route tests now read both the parent component and hook so the route-contract checks follow the source split. Post-change scorecard regenerated at `2026-06-01T07:11:39Z` with route payload and safety state unchanged: `321,783` frontend bytes, `49.1 ms` backend max duration, `48` open rows, `0` executable close-ready rows, `1` open review-required row, `1` stale suggested trade, starvation still `upstream_zero_candidate_scan_pressure`, and AI commodity still `3` / `100` shared dates. Runtime smoke on temporary Next `3015` and backend `8115` exercised Open, Tracked Stocks, and archive-gated Paper on desktop `1440x1000` and mobile `390x844` with `0` console/page errors, no page-level horizontal overflow, and the Paper stale-review warning still visible; artifact: `tmp/trading-desk-hook-qa-20260601T0711Z/browser-hook-qa-summary.json`. Verification: `npm run verify:typecheck`, `node --test tests/trading-desk/mutation-intent.test.js`, `node --test tests/trading-desk/position-evidence.test.js tests/trading-desk/suggested-trades-ui.test.js`, `npm run lint`, `npm run docs:route-parity`, `npm run verify:docs`, and `python scripts/build_regular_profitability_operating_scorecard.py --json`.
- Verified the active multi-lane performance goal against fresh artifacts and tightened `docs/autoresearch/active-options-performance-goal.md` so browser QA slices must exercise desktop/mobile, archive-gated tabs, and expanded drawer/detail states that own acceptance signals. Fresh browser QA on temporary Next `3014` and backend `8114` opened Archive > Live Scan, expanded `Evidence & guardrails`, and confirmed `TRACKED DB READY` in the truth-health card on desktop `1440x1000` and mobile `390x844`, with `0` console/page errors and no page-level horizontal overflow. Artifacts: `tmp/browser-qa-20260601T0646Z/browser-qa-expanded-summary.json`, `desktop-1440x1000-live-scan-expanded.png`, and `mobile-390x844-live-scan-tracked-db.png`.
- Tightened `docs/autoresearch/active-options-performance-goal.md` with an explicit continuation guard, stale-priority guard, and required lane-status table. Future resumed goal runs must re-read the newest user request and current artifacts, cite the artifact that makes one slice highest priority, and treat named rows/routes/sleeves/time gates from older goal text as hypotheses until fresh evidence confirms them. Baseline before the edit: scorecard regenerated at `2026-06-01T06:42:53Z`, frozen autoresearch score line stayed `score: 0.00`, and AI commodity readback stayed time-gated until `2026-06-01T08:10:00-06:00`. Verification: `npm run verify:docs` and `git diff --check`.
- Added the current-policy cohort health / paper-only readback layer. `scripts/build_current_policy_cohort_health.py` reads the current-policy historical picks audit, writes timestamped/latest artifacts under `data/forward-tracking/`, and renders `docs/current-policy-cohort-health.md`. The report explains why the original April winners and the recent negative UI slice can both be true: overall current-policy rows remain positive (`112` priced, avg `+53.54%`, median `+50.6%`), showcase month `2026-04` is strong (`70` priced, avg `+81.17%`, median `+71.82%`, `8.6%` negative rate), but recent `2026-05` degraded (`42` priced, avg `+7.49%`, median `-4.6%`, `54.8%` negative rate) and latest week `2026-W21` is broken (`3` priced, avg `-82.06%`, `100.0%` negative rate). Closed Trades current-policy stats now show Current Avg, Showcase Month, Recent Month, Recent Median, and Cohort State, with current state `Paper-only` once the full policy history is loaded. Verification: `uv run --locked python -m unittest tests.test_current_policy_cohort_health -v`, `uv run --locked python -m py_compile scripts/build_current_policy_cohort_health.py`, `uv run --locked python scripts/build_current_policy_cohort_health.py --json`, `node --test tests/trading-desk/position-evidence.test.js`, and `node --test tests/trading-desk/mutation-intent.test.js`.
- Added a current-policy historical-picks replay layer for the Trading Desk. `scripts/build_current_policy_historical_picks_audit.py` now replays closed rows through today's promoted repair-lane entry guardrails, writes latest/timestamped artifacts under `data/forward-tracking/`, and renders `docs/current-policy-historical-picks-audit.md`. Closed Trades now defaults to `Current policy`, adds `Learned away` beside raw `Realized P&L` and `Truth-grade`, and closed row status cells show whether a row would still be taken today, was learned away, needs evidence, or sits outside the promoted replay scope. The `Current policy` and `Learned away` views now keep loading closed-history pages until the policy read is complete, so the first `100` newest closed rows cannot masquerade as full-history performance. Latest readback: `488` closed rows, `400` current-policy scope rows, `112` `would_take_today` rows at avg `+53.54%` and `25.9%` negative rate, versus `274` `blocked_by_current_policy` rows at avg `-17.56%` and `63.8%` negative rate. Verification: `uv run --locked python -m unittest tests.test_current_policy_historical_picks_audit -v`, `uv run --locked python -m py_compile scripts/build_current_policy_historical_picks_audit.py`, `node --test tests/trading-desk/position-evidence.test.js`, `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, and `uv run --locked python scripts/build_current_policy_historical_picks_audit.py`.
- Added the repeatable regular-options per-symbol sleeve matrix. `scripts/build_regular_options_symbol_sleeves.py` now merges scan/playbook config, exact intraday replay artifacts, Bullish Pullback ticker/confidence artifacts, all-planned lane-lab artifacts, Trading Desk guardrails, and open/suggested risk audits into timestamped/latest JSON under `data/profitability-lab/regular-options-symbol-sleeves/`, then renders `docs/regular-options-symbol-sleeves.md` from that JSON. Latest readback: `60` tracked symbols, `335` symbol-lane rows, classification counts `keep=25`, `watch=60`, `quarantine=79`, `rejected=85`, `needs-paper=86`; Bullish Pullback carriers are `AAPL`, `COP`, `CVX`, `GOOGL`, `IWM`, `JNJ`, `LLY`, `NEM`, `UNH`, and `XOM`; no high-beta row clears the real-crusher sample/coverage bar. Verification: `uv run --locked python -m unittest tests.test_regular_options_symbol_sleeves -v`, `uv run --locked python -m py_compile scripts/build_regular_options_symbol_sleeves.py`, and `uv run --locked python scripts/build_regular_options_symbol_sleeves.py --no-write --json`.
- Tightened the Trading Desk open-row safety signal for display-only `SELL` states. Shared evidence logic now classifies open reviews as executable `SELL`, non-executable `SELL`, missing review, unpriced review, or hold; the open status cell shows `Review quote` for non-executable/display-only `SELL` reviews instead of `Close now`. This keeps position `104` visibly review-required until a fresh executable exit quote exists while preserving executable zero-price SELL handling. Verification: `node --test tests/trading-desk/position-evidence.test.js`.
- Tightened the Paper Ideas review safety signal for missing suggested-trade reviews. Open paper rows now render the shared review-action `Status` cell, the summary strip separates `Needs Review` from `Close-ready`, and non-executable or missing-review rows use a manual-close action instead of close-ready styling. This keeps suggested trade `138` visibly review-required until it has fresh executable review evidence. Verification: `node --test tests/trading-desk/position-evidence.test.js tests/trading-desk/suggested-trades-ui.test.js`, `npm run trading-desk:test`, and `npm run verify:typecheck`.
- Pruned detail-only fields from compact closed tracked-position list rows. Compact closed pages now omit stored reviews, entry quote snapshots, source scan bookkeeping, and share-safe/detail fields while preserving row status, executable exit price, realized P&L, and source quote time needed by the Closed Trades table. A fresh temporary audit against Next `3005` and backend `8104` passed `11` / `11` probes and reduced frontend total payload from `622,703` to `465,904` bytes; the first `100` closed tracked rows dropped from about `414 KB` to `275,004` backend bytes / `273,882` Next bytes. Verification: `python -m pytest tests/test_tracked_positions_api.py -q`, `python -m py_compile python-backend\main.py tests\test_tracked_positions_api.py`, `python scripts\audit_trading_desk_api_performance.py --base-url http://localhost:3005 --backend-url http://localhost:8104 --json`, and `python scripts\build_regular_profitability_operating_scorecard.py --json`.
- Optimized the `/api/options-profit/status` tracked-health overlay. The route now uses a narrow repository `profit_status_snapshot()` for open/closed counts and proof-eligible realized metrics instead of loading full open/closed tracked-position rows and latest-review payloads. A fresh temporary audit against Next `3008` and backend `8107` passed `11` / `11` probes; `/api/options-profit/status` backend duration fell from `183.2 ms` to `38.9 ms`, and the backend max duration is now the first compact closed tracked-position page at `54.2 ms`. Verification: `python -m unittest tests.test_options_api_e2e.OptionsAlgorithmApiE2ETests.test_options_profit_status_endpoint_uses_narrow_tracked_snapshot_when_available tests.test_options_api_e2e.OptionsAlgorithmApiE2ETests.test_options_profit_status_endpoint_overlays_current_tracked_positions_health -v`, `python -m pytest tests/test_positions_repository_schema.py tests/test_options_api_e2e.py -q`, `python scripts\audit_trading_desk_api_performance.py --base-url http://localhost:3008 --backend-url http://localhost:8107 --json`, and `python scripts\build_regular_profitability_operating_scorecard.py --json`.
- Further compacted closed tracked-position windows. Compact closed reads now use a repository `list_compact_positions()` path that skips the latest-review join, preserves the small lane/date/current-policy guardrail metrics, and replaces verbose per-row provenance strings with `compact_evidence` booleans consumed by the Trading Desk evidence classifier. Current-code FastAPI TestClient evidence reduces the first `100` closed-row backend payload from the last full-audit `275,004` bytes to `171,667` bytes, with median `x-python-backend-duration-ms` `34.8` across five in-process requests. Full Next-route audit rerun is still pending because local dev-server orchestration on temporary ports timed out despite the servers becoming ready; the leftover listeners were stopped. Verification: `python -m unittest tests.test_tracked_positions_api.TrackedPositionsApiTests.test_compact_closed_positions_use_narrow_repository_path tests.test_tracked_positions_api.TrackedPositionsApiTests.test_compact_closed_positions_omit_detail_only_fields -v`, `python -m pytest tests/test_tracked_positions_api.py tests/test_positions_repository_schema.py tests/test_options_api_e2e.py tests/test_trading_desk_api_performance.py tests/test_regular_profitability_operating_scorecard.py -q`, `node --test tests/trading-desk/position-evidence.test.js`, `npm run trading-desk:test`, and `npm run verify:typecheck -- --pretty false`.
- Reran the full Trading Desk API performance audit after the second compact closed pass using fresh temporary Next `3012` and backend `8112`. The route audit passed `11` / `11` probes, rebuilt the operating scorecard, and confirmed the closed-history payload reduction in the real route layer: frontend total payload fell from `466,024` to `362,655` bytes, the first `100` compact closed tracked rows fell to `171,667` backend bytes / `170,715` Next bytes, backend max duration header fell to `39.6 ms`, and the largest measured payload shifted to open tracked positions at `180,284` backend bytes / `179,768` Next bytes. Verification: `python scripts\audit_trading_desk_api_performance.py --base-url http://127.0.0.1:3012 --backend-url http://127.0.0.1:8112 --timeout 20 --json`, `python scripts\build_regular_profitability_operating_scorecard.py --json`.
- Pruned compact open-row provenance while preserving lane, contract, quote timestamp, executable-review, share-safe, and compact evidence semantics. Open compact rows now drop detail-only source evidence strings and redundant latest-review fields, while `compact_evidence` carries migrated/research/comparable flags for the Trading Desk classifier. FastAPI TestClient evidence lowered `/api/positions?status=open&compact=1` from `180,284` to `139,603` backend bytes. A fresh full route audit against Next `3013` and backend `8113` passed `11` / `11` probes, lowered frontend total payload from `362,655` to `321,783` bytes, and rebuilt the operating scorecard; the first compact closed page remains the largest payload at `171,667` backend bytes / `170,715` Next bytes. Verification: `python -m py_compile python-backend\main.py tests\test_tracked_positions_api.py`, `python -m pytest tests/test_tracked_positions_api.py -q`, `python -m pytest tests/test_positions_repository_schema.py tests/test_options_api_e2e.py tests/test_trading_desk_api_performance.py tests/test_regular_profitability_operating_scorecard.py -q`, `node --test tests/trading-desk/position-evidence.test.js`, `python scripts\audit_trading_desk_api_performance.py --base-url http://127.0.0.1:3013 --backend-url http://127.0.0.1:8113 --timeout 20 --json`, and `python scripts\build_regular_profitability_operating_scorecard.py --json`.
- Added `docs/autoresearch/active-options-performance-goal.md`, a reusable multi-lane `/goal` prompt for end-to-end active-options performance work. The prompt starts every run from the operating scorecard and AI commodity readback, then orders follow-up across truth/mutation boundaries, Trading Desk runtime, open/suggested risk, live scan starvation, Trading Desk profitability, regular multi-lane clean proof, Bullish Pullback harness work, AI commodity OPRA capture, and architecture reductions so future loops do not collapse the problem back to Lane A alone.
- Tightened `docs/autoresearch/active-options-performance-goal.md` after a live check of whether the goal was behaving as intended. The prompt now includes a per-turn operating loop, explicit guard-state triage, expected-proof selection before editing, cross-lane blocker ledger guidance, dashboard/report update criteria, and a requirement-by-requirement completion ledger so future goal runs stay multi-lane and measurable instead of becoming open-ended useful-work loops.
- Added a goal-health check to `docs/autoresearch/active-options-performance-goal.md` so each future loop must report the active lane map, identify the one selected measurable slice, and state why untouched lanes are time-gated, artifact-blocked, or lower priority instead of silently drifting back to a single-lane audit.
- Fixed the tracked DB truth-health mismatch in `/api/options-profit/status`. The route still serves the stored options-profit status artifact for the proof gate, but now overlays `measurement_gate.checks.tracked_positions` from the current runtime positions repository used by `/api/positions`, including open/closed counts and proof-eligible realized metrics. Current-code FastAPI TestClient evidence reports `tracked_available=true`, `48` open positions, `488` total closed positions, and `1` proof-eligible realized closed row while the stored gate remains `blocked`. Verification: `uv run --locked python -m unittest tests.test_options_api_e2e.OptionsAlgorithmApiE2ETests.test_options_profit_status_endpoint_overlays_current_tracked_positions_health -v`, the four focused options-profit status endpoint tests, and an in-process FastAPI route probe for `/api/options-profit/status`, `/api/positions?status=open&compact=1`, and `/api/proof-summary`.
- Changed the Trading Desk Closed Trades default from strict `Truth-grade` to `Realized P&L`, matching the operator goal of seeing closed rows with executable entry, trusted executable exit, and calculable realized P&L first. `Truth-grade` remains available as the stricter production-proof slice, while historical paper/research rows with trusted exits can now appear in the default realized-P&L review. Updated the evidence module, tracked-position tab summary/filter copy, and focused Trading Desk evidence tests. Verification: `node --test tests/trading-desk/position-evidence.test.js`, `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, and `npm run verify:docs`.
- Reworked the Trading Desk Tracked Stocks tab from a raw stock/row-count table into an action watchlist. The tab now foregrounds tickers needing close-now review, open P&L, realized P&L, missing exit-proof counts, and compact lane/date context; opening Tracked Stocks also loads the first closed-position window so realized P&L appears without visiting Closed first. Browser sanity on `http://localhost:3004` showed `Tracked Stocks (23+)`, `100+` closed rows loaded, `66+` priced exits, and no console errors. Verification: `npm run verify:typecheck`, `npm run lint`, `npm run trading-desk:test`, and `npm run verify:docs`.
- Added a shared client JSON response reader for browser-to-Next calls. `src/lib/client-json.ts` now owns timed fetches and JSON parsing, including explicit HTML/non-JSON response diagnostics for proxy or dev-server failures, and `PredictionsView.tsx` now uses it for scanner, truth-health, tracked-position, suggested-trade, close/review, and prediction-grading requests instead of raw `response.json()` parsing. Added `tests/ui/client-json.test.js` coverage for successful JSON, payload error messages, and HTML proxy failures. Browser sanity on a fresh `http://localhost:3004` tab showed the default Open Trades board loaded with `48` open rows and no visible tracked-position parse failure; the in-app browser log buffer still retained an older `localhost:3000` parse error from before this change. Verification: `npm run verify:typecheck`, `npm run lint`, `npm run ui:test`, `npm run trading-desk:test`, `npm run verify:docs`, and `git diff --check`.
- Split the archive-gated live scanner out of `PredictionsView.tsx` and loaded it dynamically. `src/components/predictions/ScannerTab.tsx` now owns the scanner evidence/guardrail drawer, replay-policy controls, manual trade/paper-idea form, and memoized scanner table rows/column contracts, so the default Open/Closed Trading Desk path no longer carries the full scanner render body in the parent module. Browser sanity on `http://localhost:3004` confirmed Archive > Live Scan dynamically mounted, displayed the scanner controls/evidence drawer, and settled to an enabled Refresh Scan button; the current dev instance still logs the pre-existing tracked-position JSON parse error on page load. Verification: `npm run verify:typecheck`, `npm run lint`, `npm run trading-desk:test`, `npm run ui:test`, `npm run verify:docs`, and `git diff --check`.
- Split the Trading Desk tracked-position Open/Closed board out of `PredictionsView.tsx`. `src/components/predictions/TrackedPositionsTab.tsx` now owns tracked-position filters, summary cards, row rendering, and closed-row pagination UI, while `src/components/predictions/trackedPositionUtils.tsx` owns shared date, lane, contract, and ticker cell helpers used by tracked positions, scanner, close dialogs, and tracked-stock summaries. This keeps the default Trading Desk path focused on active position review while further reducing the `PredictionsView.tsx` hot surface. Verification: `npm run verify:typecheck`, `npm run lint`, `npm run trading-desk:test`, `npm run verify:docs`, and `git diff --check`.
- Split the Trading Desk paper-ideas surface out of `PredictionsView.tsx` and made it a dynamically loaded archive component. `src/components/predictions/SuggestedTradesTab.tsx` now owns Paper Ideas rendering, `src/components/predictions/tradingDeskCells.tsx` owns shared position/suggested-trade cell renderers and share-safe review helpers, and `src/components/predictions/TradingDeskCompactStat.tsx` owns the small stat tile reused by Paper Ideas and tracked positions. `PredictionsView.tsx` is down to `3300` lines in this worktree and no longer carries the paper-idea tab body. Verification: `npm run verify:typecheck`, `npm run lint`, and `npm run trading-desk:test`.
- Split the Trading Desk tracked-stock rollup out of the `PredictionsView.tsx` monolith. `src/components/predictions/TrackedStocksTab.tsx` now owns the tracked-stock view and memoized row rendering, `src/components/predictions/tradingDeskFormat.ts` owns shared money/date/P&L formatting, and `PredictionsView.tsx` builds the tracked-stock summary once for both the tab label and tab body instead of recomputing it inside the view. Verification: `npm run verify:typecheck`, `npm run lint`, and `npm run trading-desk:test`.
- Extended the operating scorecard from a regular-options-only readback into an active multi-lane dashboard. `scripts/build_regular_profitability_operating_scorecard.py` now consumes `data/ai-commodity-infra/progress/latest.json` and the generated `docs/regular-options-operating-scorecard.md` shows AI commodity OPRA readiness beside Trading Desk guardrails, open risk, suggested-close risk, starvation, API latency, payload, and cache stats. Current AI commodity read in the scorecard: `3` / `100` exact shared Alpaca OPRA dates, `0` live/proof candidates, failed `2026-05-26` capture target with `24` missing symbols, and filters locked until the guarded `python scripts/run_ai_commodity_opra_progress.py --skip-capture` event is allowed. Verification: `python -m pytest tests/test_regular_profitability_operating_scorecard.py -q`, `python -m py_compile scripts/build_regular_profitability_operating_scorecard.py`, `python scripts/build_regular_profitability_operating_scorecard.py --json`, `npm run verify:docs`, and `git diff --check`.
- Added a read-only Trading Desk API performance audit and wired it into the regular operating scorecard. `scripts/audit_trading_desk_api_performance.py` probes the Next route layer and FastAPI backend for open/default and closed-windowed tracked positions, suggested trades, proof status, and market-data cache stats; it records elapsed time, `x-python-backend-duration-ms`, payload bytes, row counts, page windows, and cache counters under `data/forward-tracking/trading_desk_api_performance_latest.json`. Trading Desk and proof-status Next read routes now forward the backend duration header without changing JSON bodies, and Trading Desk list reads can request `compact=1` to strip heavy snapshot/debug fields while preserving UI-critical provenance, quote, evidence, and review fields. Initial current-code run against Next `3004` and backend `8103`: `11` / `11` probes passed, frontend payload total dropped to `622,703` bytes, backend max duration header was `109.8 ms`, and the first `100` compact closed tracked positions were the largest measured payload at about `414 KB` instead of the earlier `886 KB` full payload. Verification: `python -m pytest tests/test_trading_desk_api_performance.py tests/test_regular_profitability_operating_scorecard.py -q`, `python -m pytest tests/test_tracked_positions_api.py tests/test_trading_desk_api_performance.py -q`, `python -m py_compile python-backend/main.py scripts/audit_trading_desk_api_performance.py scripts/build_regular_profitability_operating_scorecard.py`, `node --test tests/trading-desk/mutation-intent.test.js`, `npm run verify:typecheck`, `npm run lint`, `python scripts/audit_trading_desk_api_performance.py --base-url http://localhost:3004 --backend-url http://localhost:8103 --json`, and `python scripts/build_regular_profitability_operating_scorecard.py --json`.
- Removed duplicated desktop/mobile row rendering from `FinTable`. The shared table component now chooses one responsive surface at runtime: a desktop table at wide viewports or mobile cards below the table breakpoint, with stable memoized column/classification state and test-only render modes for deterministic coverage. This halves the mounted row/action tree for dense Trading Desk tables at any one viewport while preserving the explicit mobile title/subtitle/priority/hidden-column contract. Browser sanity on `http://localhost:3002` with backend `8101` confirmed `1280px` renders `1` table and `0` mobile cards, while `390px` renders mobile cards and `0` tables, with no current-url console warnings/errors. Verification: `npm run ui:test`, `npm run verify:typecheck`, `npm run lint`, `npm run verify:docs`, and `git diff --check`.
- Wired the new list windows into the Trading Desk UI. `PredictionsView.tsx` now loads open tracked positions and open suggested trades by default, defers closed tracked positions and closed paper ideas until their Closed tabs are opened, and pages older closed rows in `100`-row batches with `Load More`; Tracked Stocks now labels closed-row totals as on-demand until the archive is loaded. Added Trading Desk source tests proving read routes forward `limit` / `offset` and the component no longer uses the old all-row grouped fetch. Browser sanity on `http://localhost:3002` with a current backend on `8101` confirmed the Open view leaves Closed uncounted, opening Closed shows `Closed (100+)`, `Load More` remains reachable even when the truth-grade filter hides the first page, and loading another page advances to `Closed (200+)` with no current-url console warnings/errors. Verification: `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, `npm run verify:docs`, `python -m pytest tests/test_tracked_positions_api.py tests/test_suggested_trades_api.py tests/test_positions_repository_schema.py -q`, and `git diff --check`.
- Added backend and Next-route list windowing for tracked positions and suggested trades. `/api/positions` and `/api/suggested-trades` now accept `limit` and `offset`, pass those through Postgres/SQLite/memory repositories, and return page metadata so the UI can move toward lazy-loading closed/history-heavy rows instead of pulling the full archive on every refresh. Verification: `python -m pytest tests/test_tracked_positions_api.py tests/test_suggested_trades_api.py tests/test_positions_repository_schema.py -q`, `uv run --locked python -m py_compile python-backend/main.py python-backend/positions_repository.py python-backend/suggested_trades_repository.py tests/test_tracked_positions_api.py tests/test_suggested_trades_api.py tests/test_positions_repository_schema.py`, `npm run verify:typecheck`, `npm run lint`, and `npm run verify:docs`.
- Added lightweight FastAPI timing telemetry for backend performance work. Every Python backend response now carries `x-python-backend-duration-ms`, giving the Trading Desk, scan, review, and evidence routes a comparable latency readout before payload or query changes. Verification: `python -m pytest tests/test_suggested_trades_api.py -q` and `uv run --locked python -m py_compile python-backend/main.py tests/test_suggested_trades_api.py`.
- Added a small in-process connection pool for the Postgres tracked-position repository. `PostgresTrackedPositionsRepository` now reuses successful connections across requests while rolling back and discarding failed connections, reducing repeated connection setup on hot Trading Desk position reads/reviews without changing route contracts or proof semantics. Verification: `python -m pytest tests/test_positions_repository_schema.py -q` and `uv run --locked python -m py_compile python-backend/positions_repository.py tests/test_positions_repository_schema.py`.
- Added a read-only suggested-trade close-risk audit and wired it into the regular operating scorecard. `scripts/audit_suggested_trade_close_risk.py` reads `chat_history.db` without calling state-changing review endpoints, separates executable `SELL` evidence from non-executable marks, and flags stale/missing paper-idea reviews. The current artifact, `data/forward-tracking/suggested_trade_close_risk_latest.json`, reports `1` open suggested trade (`AAA`, id `138`) with no stored review, `0` executable close-ready suggested rows, and `0` non-executable close-risk rows; the scorecard now surfaces that as a refresh-required paper idea rather than a close claim. Verification: `python -m pytest tests/test_suggested_trade_close_risk.py tests/test_regular_profitability_operating_scorecard.py -q`, `uv run --locked python -m py_compile scripts/audit_suggested_trade_close_risk.py scripts/build_regular_profitability_operating_scorecard.py tests/test_suggested_trade_close_risk.py tests/test_regular_profitability_operating_scorecard.py`, `python scripts/audit_suggested_trade_close_risk.py`, and `python scripts/build_regular_profitability_operating_scorecard.py`.
- Promoted the read-only open-position safety audit into the regular operating scorecard. `scripts/audit_regular_open_position_risk.py` now writes timestamped/latest JSON artifacts under `data/forward-tracking/` and includes actionable row details that separate executable `SELL` evidence from display-only/non-executable `SELL` marks. The current artifact keeps position `104` visible as a `stored_non_executable_sell` / `priced_display_only_last` SBUX row with `exit_execution_price=null`, so the next safe action is a fresh explicit executable review rather than auto-close. `scripts/build_regular_profitability_operating_scorecard.py` now consumes `regular_open_position_risk_latest.json` and the generated dashboard shows `48` open regular rows, `47` fresh executable reviews, `1` fresh unpriced review, `0` executable close-ready rows, and `1` review-required non-executable row. Verification: `uv run --locked python scripts/audit_regular_open_position_risk.py`, `uv run --locked python scripts/build_regular_profitability_operating_scorecard.py --json`, `python -m pytest tests/test_regular_open_position_risk.py tests/test_regular_profitability_operating_scorecard.py -q`, and `uv run --locked python -m py_compile scripts/audit_regular_open_position_risk.py scripts/build_regular_profitability_operating_scorecard.py`.
- Upgraded `scripts/audit_regular_guardrail_starvation.py` from console-only readback to a stable operating artifact writer. It now writes timestamped JSON, `data/forward-tracking/regular_guardrail_starvation_latest.json`, and `docs/regular-guardrail-starvation-audit.md`, emits a compact default console summary, and classifies the distinction between guardrail starvation and upstream zero-candidate pressure. The latest all-regular run completed `13` / `13` playbooks with `0` candidates, `0` returned picks, no guardrail-starved playbooks, and status `upstream_zero_candidate_scan_pressure`; top drops were direction filter (`115`), option liquidity (`96`), momentum (`72`), history/liquidity (`60`), and tech score (`33`). The operating scorecard now consumes this audit and changes the guardrail next action to avoid loosening promoted entry guardrails for the current no-pick state. Verification: `python scripts/audit_regular_guardrail_starvation.py --top-limit 8`, `python scripts/build_regular_profitability_operating_scorecard.py --json`, `python -m pytest tests/test_regular_guardrail_starvation_audit.py tests/test_regular_profitability_operating_scorecard.py -q`, and `python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/build_regular_profitability_operating_scorecard.py`.
- Added `scripts/audit_trading_desk_legacy_missed_closes.py` and `tests/test_trading_desk_legacy_missed_closes.py` to close the narrow follow-up on legacy rows `26`, `39`, and `44`. The read-only audit writes `data/forward-tracking/trading_desk_legacy_missed_close_audit_latest.json` and `docs/trading-desk-legacy-missed-close-audit-2026-06-01.md`. Current result: all three rows diagnose as `stale_or_non_autoclosing_review_path`, with `current_action_required_count=0`, so they are historical stale-policy/application diagnostics rather than evidence for a current auto-close bug or broad exit-policy change. The operating scorecard now consumes this audit and changes the next action to preserving the rows as diagnostics. Verification: `python scripts/audit_trading_desk_legacy_missed_closes.py --json`, `python scripts/build_regular_profitability_operating_scorecard.py --json`, `python -m pytest tests/test_regular_profitability_operating_scorecard.py tests/test_trading_desk_legacy_missed_closes.py -q`, and `python -m py_compile scripts/build_regular_profitability_operating_scorecard.py scripts/audit_trading_desk_legacy_missed_closes.py`.
- Added `scripts/build_regular_profitability_operating_scorecard.py`, `tests/test_regular_profitability_operating_scorecard.py`, and the generated dashboard `docs/regular-options-operating-scorecard.md` so the active regular-profitability goal has a single readback for "are we seeing results?" The scorecard combines the latest experiment-scoped frozen evaluator result, Trading Desk profitability guardrails, negative-decision audit, and exit-policy replay. Current status is `visible_product_profitability_progress_but_proof_still_blocked`: promoted Trading Desk guardrails keep `130` rows / `116` priced at avg `+53.08%`, median `+46.4%`, and `25.0%` negative rate versus baseline avg `+5.21%`, median `-1.58%`, and `50.4%` negative rate, while proof-grade autoresearch remains blocked at clean count `0`, Lane A conservative PF `0.92`, and zero-bid exit rate `43.24%`. Updated `docs/autoresearch/regular-options-goal.md` so every loop must start/end with the scorecard and produce a measured delta or retire the branch. Verification: `python scripts/build_regular_profitability_operating_scorecard.py --json`, `python -m pytest tests/test_regular_profitability_operating_scorecard.py -q`, and `python -m py_compile scripts/build_regular_profitability_operating_scorecard.py`.
- Added causal Lane A memory variants to `scripts/run_bullish_pullback_next_round.py` and wired them into `scripts/run_regular_options_goal_experiment.py`: selected short-leg expiry/strike exit-failure cooldown, ticker symbol-health memory, and a combined memory branch. Ran the focused experiment batch with `--no-append-ledger`, so evaluator artifacts stayed experiment-scoped at `data/profitability-lab/regular-options-autoresearch/experiments/20260601T020317Z/summary.json`. The best branch, `lane_a_goal_stop200_time75_symbol_health90_backfill`, improved effective coverage to `99.06%` and unresolved candidates to `3`, with PF `2.13` and avg `+27.22%`, but it remains rejected for production: clean count `0`, scout count `191`, zero-bid exit rate `43.24%`, and Lane A conservative PF `0.92` below the `1.30` gate. Verification: `python -m pytest tests/test_regular_options_goal_experiment.py -q`, `python -m py_compile scripts/run_bullish_pullback_next_round.py scripts/run_regular_options_goal_experiment.py`, and the focused `python scripts/run_regular_options_goal_experiment.py --variant ... --lookback-years 1 --no-append-ledger --json` run.
- Completed the regular-lane sector ETF data unblock and first focused replay. Imported the remaining bounded ThetaData v3 `1h` chunks for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` through batch `1883`, bringing all six ETFs to `252` shared trusted quote dates from `2025-05-22` through `2026-05-22`; then added targeted `10:10` entry and min-DTE-0 `15:45` exit backfills through batch `1920` for the sector replay candidates. The planner now reports `ready_for_sector_replay`, `2,651,299` trusted sector ETF rows, and no data-depth blockers. Added regular-sector all-planned variants for XLE, XLF, KRE, SMH, TLT, and sector rotation while deliberately excluding GLD/commodity-lane sleeve implementation. Focused partial batch `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260601T015109Z/summary.json` rejected the simple sector shapes: SMH priced `17` of `20` candidates at PF `0.49`, avg `-15.45%`, and stress PF `0.32`; the regular sector stack priced `16` of `23` candidates at PF `0.27`, avg `-32.36%`, and stress PF `0.19`; TLT/XLE/XLF/KRE produced no current candidates. Verification: `python scripts/plan_regular_sector_etf_imports.py --json`, `python scripts/run_regular_options_all_planned_sleeves.py --lookback-years 1 --only ...sector variants...`, `python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `python -m pytest tests/test_regular_options_all_planned_sleeves.py -q`.
- Ran the Section 1 implementation pass for runtime architecture and request flow. Extended `scripts/generate_route_parity.py` so the generated route map now includes active client-component `/api/*` fetches and `npm run verify:docs` fails if a browser fetch does not resolve to a mounted Next route, if a browser fetch uses an absolute `/api/*` URL that bypasses Next, or if a mirrored Next route lacks a FastAPI decorator. Regenerated `docs/route-parity.md`, updated runtime/architecture docs, and added `tests/test_route_parity_generator.py` for fetch-path extraction, dynamic route matching, missing-route validation, and absolute-URL bypass validation. Verification: `uv run --locked python -m unittest tests.test_route_parity_generator -v`, `npm run docs:route-parity`, and `npm run verify:docs`.
- Ran the Section 10 implementation pass for verification gaps. Reproduced the stale options API E2E scan-count failures, then updated `tests/test_options_api_e2e.py` so scan fixtures match the current guardrail contract: Bullish Pullback default-playbook coverage uses a current keep-list ticker, the blocked-top-pick backfill test uses a non-quarantined replacement candidate, and the golden replay snapshot now asserts that blocked guardrail candidates are filtered out by default. Verification: focused `uv run --locked python -m unittest` for the three repaired tests and the full `tests.test_options_api_e2e.OptionsAlgorithmApiE2ETests` suite (`38` tests).
- Ran the Section 9 implementation pass for shared UI components and mobile UX. Extended `src/components/ui/FinTable.tsx` with explicit mobile-card hierarchy props (`mobileTitleCol`, `mobileSubtitleCol`, `mobilePriorityCols`, `mobileHiddenCols`, and `mobileActionCol`) and applied them to live scanner, tracked-position, paper-idea, Strategy Lab replay, and profile changelog tables so mobile cards keep the decision/action fields first instead of inheriting desktop column order. Added `tests/ui/fin-table.test.js` and `npm run ui:test` with React server rendering coverage. Verification: `npm run ui:test`, `npm run strategy-lab:test`, `npm run verify:typecheck`, and `npm run lint`.
- Ran the Section 8 implementation pass for monolithic Python files. Extracted the FastAPI profile surface from `python-backend/main.py` into `python-backend/profile_routes.py`, covering `GET /api/profile`, `GET /api/profiles`, `PUT /api/profile`, `GET /api/changelog`, and `GET /api/risk`. Added standalone router tests plus an integration check that `main.py` mounts the router, and updated `scripts/generate_route_parity.py` so extracted `@router.*` routes remain visible in generated docs. Verification: `uv run --locked python -m unittest tests.test_backend_profile_routes -v`, targeted backend E2E checks for backtest/status, `uv run --locked python -m py_compile python-backend/main.py python-backend/profile_routes.py scripts/generate_route_parity.py tests/test_backend_profile_routes.py`, `npm run docs:route-parity`, `npm run verify:docs`, `npm run strategy-lab:test`, `npm run verify:typecheck`, and `npm run lint`.
- Ran the Section 7 implementation pass for replay, Strategy Lab, and research-pipeline boundaries. Added `src/lib/strategy-lab/replayIntent.ts` with explicit Strategy Lab route contracts, response lifecycle headers, and mutation intents. `POST /api/backtest` now requires `x-strategy-lab-mutation: run_replay_backtest`, `PUT /api/profile` requires `x-strategy-lab-mutation: save_strategy_profile`, and passive replay/profile/changelog GET routes return ownership headers instead of sharing the same ambiguous surface as writes. A reviewer found the changelog lifecycle gap, so `/api/changelog` is now included as a passive `strategy_profile_files` read. Added `npm run strategy-lab:test` and focused route/component coverage; existing Trading Desk route tests were updated for the shared API utility import. Verification: `npm run strategy-lab:test`, `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, and `npm run verify:docs`.
- Fixed the Section 6 proof-review blocker in the live scan to tracked-position flow. `POST /api/positions` now verifies `source_scan_*` lineage against the forward-evidence ledger before `positions_service.py` can classify a row as `live_scan_exact_contract`, and exact-looking payloads with fabricated or price-mutated lineage remain proof-ineligible. Added regressions for verified lineage, forged lineage, price-mutated lineage, and browser scan-pick-to-position lineage preservation.
- Ran Section 6 of the code-audit remediation goal for live scan to position flow. `POST /api/scan` now annotates returned picks with forward-evidence session/event/run/timestamp provenance after ledger recording succeeds, and tracked-position proof classification now requires verified scan lineage before a row can become `live_scan_exact_contract`. Added regressions proving a browser-created position preserves scan provenance, verified lineage removes the lineage blocker, and exact-looking payloads without scan lineage remain proof-ineligible; updated the TypeScript scan/position contracts and recorded `5` of `6` spawned reviewer agreement in the remediation run report. Verification: targeted `uv run --locked python -m unittest` coverage for scan provenance and tracked-position proof guards, `npm run trading-desk:test`, `npm run verify:typecheck`, `npm run lint`, and `npm run verify:docs`. Full E2E scan-count tests still expose pre-existing/stale fixture failures unrelated to the provenance path and remain a follow-up for Section 10.
- Ran Section 5 of the code-audit remediation goal for proof and evidence integrity. Added backend research/backfill contamination detection in `positions_service.py` so exact-looking research, historical replay, migration, or backfill picks persist as `proof_class=ineligible` before live-exact or manual/broker proof classification can run. Hardened grouped position proof summaries in `python-backend/main.py` so stale `proof_eligible=true` rows with research/backfill markers are excluded from `summary.*.proof`. Added backend regressions for research/backfill exact rows, manual-exact precedence, and stale grouped-summary contamination, then recorded `6` of `6` reviewer agreement in the remediation run report. Verification: `uv run --locked python -m unittest tests.test_positions_repository_schema tests.test_tracked_positions_api -v`, `npm run trading-desk:test`, `npm run verify:typecheck`, and `npm run lint`.
- Ran Section 4 of the code-audit remediation goal for data lifecycle and store ownership. Added `src/lib/trading-desk/storeOwnership.ts`, route response lifecycle/store headers, and Trading Desk tests that distinguish Postgres-backed tracked positions from SQLite-backed suggested paper ideas. A reviewer found a real backend ownership blocker, so `create_positions_repository(None)` now fails closed with `UnavailableTrackedPositionsRepository` instead of silently creating a writable SQLite tracked-position store. Updated `docs/api-and-storage.md`, `docs/architecture-overview.md`, and the remediation run report with `6` of `6` reviewer agreement. Verification: `npm run trading-desk:test`, `uv run --locked python -m unittest tests.test_positions_repository_schema -v`, `npm run verify:typecheck`, `npm run lint`, and `npm run verify:docs`.
- Ran Section 3 of the code-audit remediation goal for read-versus-mutate semantics. Added typed Trading Desk mutation intents in `src/lib/trading-desk/mutationIntent.ts`, guarded all tracked-position and suggested-trade create/review/close API routes with `x-trading-desk-mutation`, and changed `PredictionsView.tsx` passive loads to explicit read-only calls while manual refresh uses `{ notify: true, review: "force" }`. Added `npm run trading-desk:test` with route behavior checks for missing/wrong/correct mutation intent and updated the section report with `6` of `6` reviewer agreement. Verification: `npm run trading-desk:test`, `npm run verify:typecheck`, and `npm run lint`.
- Ran Section 2 of the code-audit remediation goal against the Trading Desk UX monolith. Extracted Trading Desk evidence grouping, production/research separation, trusted-exit checks, price selection, fee-aware P&L math, and closed-outcome summaries into `src/lib/trading-desk/positionEvidence.ts`; `PredictionsView.tsx` now consumes that pure module for proof/P&L semantics. A six-reviewer pass found one proof blocker, so the default closed `truth_grade` view was tightened to production-proof rows only and research/backfill rows with executable exits remain in explicit research slices. Added focused Node coverage in `tests/trading-desk/position-evidence.test.js` and recorded the section report in `docs/autoresearch/code-audit-remediation-run-2026-06-01.md`. Verification: `npm run verify:typecheck`, `npm run lint`, and `node --test tests\trading-desk\position-evidence.test.js`.
- Started the regular-lane-only profitability CEO goal and spawned read-only worker threads for kept losers, open-position risk, clean-sleeve triage, and guardrail starvation. Added `scripts/audit_regular_open_position_risk.py`, a read-only open regular-position risk audit that uses stored position/review fields only and does not call state-changing review endpoints. First run reported `48` open regular rows, `47` with fresh executable reviews, `15` negative marked rows, and `1` stored non-executable SELL signal (`id=104`). Verification: `uv run --locked python scripts/audit_regular_open_position_risk.py` and `uv run --locked python -m py_compile scripts/audit_regular_open_position_risk.py`.
- Added `scripts/audit_regular_guardrail_starvation.py`, a read-only regular-lane scan-funnel audit that runs every non-commodity playbook with blocked guardrail candidates visible and portfolio caps disabled by default. First full run completed `13` regular playbooks with `0` raw candidates, `0` guardrail-blocked candidates, and upstream drop pressure led by direction filters (`115`), option liquidity (`96`), momentum (`72`), history/liquidity (`60`), and tech score (`33`), so the current no-pick state is not guardrail starvation. Verification: `uv run --locked python scripts/audit_regular_guardrail_starvation.py --no-write`, `uv run --locked python scripts/audit_regular_guardrail_starvation.py --no-write --playbooks bullish_pullback_observation`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py`.
- Continued the regular profitability goal by enhancing `scripts/audit_regular_guardrail_starvation.py` with compact per-filter drop details, ticker samples, and `market_open_at_run`, then reran the all-regular no-write audit. The zero-pick state remained upstream rather than guardrail-driven: top details were puts blocked from call-only lanes (`65`), thin underlying history/liquidity (`60`), calls blocked from put-only lanes (`50`), Bullish Pullback shape absent (`47`), and stale/wide option-liquidity drops (`32`). Advanced the sector ETF data unblock with a bounded ThetaData v3 import for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` from `2025-11-06` through `2025-11-12`: batch `1859`, `53,764` trusted rows, `0` duplicates, `0` rejects. The planner now reports `136` shared sector ETF quote dates and `1,408,340` trusted rows, still `ready_to_import_sector_etfs` / `thin_required_history`, so no sector sleeve replay is allowed yet. Verification: `uv run --locked python scripts/audit_regular_guardrail_starvation.py --no-write --top-limit 6`, `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-11-06 --date-to 2025-11-12 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, and `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`.
- Continued the sector ETF trusted-data import while ThetaTerminal was still reachable. Added two bounded `1h` chunks for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1860` for `2025-10-30` through `2025-11-05` imported `54,227` trusted rows, and batch `1861` for `2025-10-23` through `2025-10-29` imported `49,210` trusted rows. Both had `0` duplicates, `0` rejects, and `0` errors. The planner now reports `146` shared sector ETF quote dates, earliest `2025-10-23`, and `1,511,777` trusted rows, still `ready_to_import_sector_etfs` / `thin_required_history`, so no sector sleeve replay or clean-count claim is allowed. The frozen evaluator remains unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-10-30 --date-to 2025-11-05 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-10-23 --date-to 2025-10-29 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1862` for `2025-10-16` through `2025-10-22` imported `45,192` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `151` shared sector ETF quote dates, earliest `2025-10-16`, and `1,556,969` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-10-16 --date-to 2025-10-22 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1863` for `2025-10-09` through `2025-10-15` imported `50,350` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `156` shared sector ETF quote dates, earliest `2025-10-09`, and `1,607,319` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-10-09 --date-to 2025-10-15 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1864` for `2025-10-02` through `2025-10-08` imported `50,972` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `161` shared sector ETF quote dates, earliest `2025-10-02`, and `1,658,291` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-10-02 --date-to 2025-10-08 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1865` for `2025-09-25` through `2025-10-01` imported `47,633` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `166` shared sector ETF quote dates, earliest `2025-09-25`, and `1,705,924` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-09-25 --date-to 2025-10-01 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1866` for `2025-09-18` through `2025-09-24` imported `47,897` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `171` shared sector ETF quote dates, earliest `2025-09-18`, and `1,753,821` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-09-18 --date-to 2025-09-24 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1867` for `2025-09-11` through `2025-09-17` imported `52,177` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `176` shared sector ETF quote dates, earliest `2025-09-11`, and `1,805,998` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-09-11 --date-to 2025-09-17 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1868` for `2025-09-04` through `2025-09-10` imported `60,992` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `181` shared sector ETF quote dates, earliest `2025-09-04`, and `1,866,990` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-09-04 --date-to 2025-09-10 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1869` for `2025-08-28` through `2025-09-03` imported `49,996` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `185` shared sector ETF quote dates, earliest `2025-08-28`, and `1,916,986` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-08-28 --date-to 2025-09-03 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1870` for `2025-08-21` through `2025-08-27` imported `59,096` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `190` shared sector ETF quote dates, earliest `2025-08-21`, and `1,976,082` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-08-21 --date-to 2025-08-27 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1871` for `2025-08-14` through `2025-08-20` imported `56,284` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `195` shared sector ETF quote dates, earliest `2025-08-14`, and `2,032,366` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-08-14 --date-to 2025-08-20 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Continued the sector ETF trusted-data import with one additional bounded `1h` chunk for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`: batch `1872` for `2025-08-07` through `2025-08-13` imported `52,988` trusted rows with `0` duplicates, `0` rejects, and `0` errors. The planner now reports `200` shared sector ETF quote dates, earliest `2025-08-07`, and `2,085,354` trusted rows. The status remains `ready_to_import_sector_etfs` / `thin_required_history`; sector ETF sleeves stay blocked until `ready_for_sector_replay`, and the frozen evaluator remained unchanged at `score: 0.00`, clean count `0`, scout count `234`, and Lane A conservative PF `0.85`. Verification: `uv run --locked python scripts/import_thetadata_options_nbbo.py --symbols GLD,TLT,XLE,XLF,SMH,KRE --date-from 2025-08-07 --date-to 2025-08-13 --snapshot-kind intraday --start-time 09:45 --end-time 15:55 --interval 1h --min-dte 21 --max-dte 45 --strike-range 20 --right both --theta-url http://127.0.0.1:25503 --timeout 30 --json`, `uv run --locked python scripts/plan_regular_sector_etf_imports.py --json`, `uv run --locked python scripts/evaluate_regular_options_autoresearch.py --no-write --score-line`, and `uv run --locked python -m py_compile scripts/audit_regular_guardrail_starvation.py scripts/audit_regular_open_position_risk.py`.
- Added `docs/autoresearch/code-audit-remediation-goal.md`, a reusable six-subagent code-audit remediation goal prompt that iterates through architecture, UX, read/write semantics, data lifecycle, proof integrity, live scan flow, replay pipelines, monoliths, shared UI, and verification gaps until each section has a long-term solution and at least `4` of `6` reviewer agents agree it is addressed. Linked it from `docs/index.md`.

## 2026-05-31

- Redesigned the Trading Desk `Tracked Stocks` tab from a generic table into a compact stock watchlist. The view removes the bulky metric-card row, keeps a quiet inline summary, shows every tracked ticker once, groups open/closed/total counts, collapses lane families into capped chips with `+N` overflow, and keeps average P&L readable without a runaway horizontal table. Verification: `npm run lint`, `npm run verify:typecheck`, and in-app browser checks on `http://localhost:3000/` with no console warnings/errors.
- Cleaned up the Trading Desk closed-row table so it no longer exposes raw quote/review/exit-reason columns or reuses open-trade `Close now` status on closed positions. Closed tracked-position rows now show the smaller product-facing set: ticker, signal, trade, taken date, entry, exit price, realized P&L, and a plain `Closed` status with date; closed paper ideas received the same treatment. Verification: `npm run lint`, `npm run verify:typecheck`, and in-app browser desktop/mobile checks on `http://localhost:3000/` with no console warnings/errors.
- Removed the noisy tracked-trade diagnostic card stack from the Trading Desk Open/Closed view: evidence mix, lane-quality, and guardrail-readback panels no longer render on the primary board. Row-level evidence badges, summary stats, lane/date filters, and scanner/archive diagnostics remain available. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run verify:docs`, and in-app browser desktop/mobile checks on `http://localhost:3002/` with no console warnings/errors; the temporary dev server was stopped after QA.
- Made the Trading Desk Closed Trades view accuracy-first by default. Closed rows now default to a truth-grade executable slice requiring an executable entry, trusted executable exit basis, realized P&L, and no lifecycle-only / proof-ineligible / legacy-unclassified provenance. Added Closed table evidence filters for truth-grade, all closed, historical paper, lifecycle-only, unpriced, and legacy rows, and changed closed summary cards to foreground truth-grade count, win rate, and average P&L while keeping research/backfill visible on demand. Verification: `npm run verify:typecheck` and `npm run lint`. A local dev server started on `http://localhost:3001` because `3000` was occupied, but browser automation could not run because the bundled Playwright package was missing `playwright-core`; the temporary dev server processes were stopped.
- Replaced the primary Trading Desk third tab with `Tracked Stocks`, a stock-level rollup of all tickers in the tracked-position store. The tab lists each tracked stock once with open/closed row counts, latest signal/trade dates, current open status, lane families, evidence mix, and average tracked P&L; the live scanner remains available under the archive toggle. Updated living docs for the new primary view shape. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run verify:docs`, and in-app browser desktop/mobile checks on `http://localhost:3002/` with no console warnings/errors; the temporary dev server was stopped after QA.
- Simplified the Trading Desk tracked-position table so historical/paper rows no longer show target/stop/time-exit plan details. The tracked row now uses `Signal` for the entry lane family plus the signal date, moves Hold/Close-now into `Status`, and keeps the historical-paper provenance badge visible without the extra migrated-row detail line. Verification: `npm run lint`, `npm run verify:typecheck`, and in-app browser desktop/mobile checks on `http://localhost:3000/` with no console warnings/errors.
- Added a read-only Trading Desk exit-policy replay at `scripts/replay_trading_desk_exit_policies.py`, generated `docs/trading-desk-exit-policy-replay-2026-05-31.md`, `data/forward-tracking/trading_desk_exit_policy_replay_20260531T233603Z.json`, and latest JSON/CSV pointers, with tests in `tests/test_trading_desk_exit_policy_replay.py`. The replay uses stored executable review rows only and excludes unpriced/display-only/last-trade/midpoint rows. It found `107` replayable regular Trading Desk rows, baseline avg `+37.28%` and median `+35.79%`, and no promotable broad exit rule. Global profit harvest, global trailing giveback, shorter time exits, and stored-SELL following all reduced average executable P&L; `stop_70` and current-policy replay stayed research-only because they increased negatives and flipped stored winners to losses. Legacy rows `26`, `39`, and `44` remain a narrow missed-auto-close/application audit target. Verification: `python -m py_compile scripts\replay_trading_desk_exit_policies.py`, `python -m pytest tests\test_trading_desk_exit_policy_replay.py -q`.
- Added a reproducible Trading Desk negative-trade decision audit at `scripts/audit_trading_desk_negative_trade_decisions.py`, generated `docs/trading-desk-negative-trade-decision-audit-2026-05-31.md`, `data/forward-tracking/trading_desk_negative_trade_decision_audit_20260531T222431Z.json`, latest JSON/CSV pointers, and tests in `tests/test_trading_desk_negative_trade_decision_audit.py`. The audit classifies `213` negative rows by evidence quality, entry rationale, current guardrail hits, and executable review history: `167` negatives hit promoted entry guardrails, `1` row had non-negative executable evidence before its first negative review, and legacy rows `26`, `39`, and `44` had positive executable `SELL` reviews before the final negative outcome. Verification: `python -m py_compile scripts\audit_trading_desk_negative_trade_decisions.py` and `python -m pytest tests\test_trading_desk_negative_trade_decision_audit.py -q`.
- Implemented replay-backed Trading Desk profitability guardrails across the regular repair lanes (`short_term`, `swing`, `bullish_momentum`, and Bullish Pullback). Added `scripts/analyze_trading_desk_profitability_guardrails.py`, generated `docs/trading-desk-profitability-guardrails-2026-05-31.md` plus latest JSON under `data/forward-tracking/`, and promoted only the all-row-passing entry rules into `supervised_scan.py`: debit over `45%` width, fill degradation `>=20%`, worst-leg bid/ask spread `>=20%`, lane-specific ticker quarantines, Bullish Pullback keep-bucket enforcement, and Bullish Pullback `ret5 >= -2`. The replay rejected momentum-chase blocking because it removed too many winners. Verification: `python -m py_compile supervised_scan.py scripts/analyze_trading_desk_profitability_guardrails.py`, `python -m pytest tests/test_trading_desk_profitability_guardrails.py tests/test_tracked_winner_primary_lane.py -q`, and `npm run verify:docs`.
- Implemented the first product-facing slice of the all-lanes repair backlog in the Trading Desk tracked-position surface. Summary cards now separate production-proof rows from research/paper rows instead of blending them into one win-rate or average-P&L read; the view also adds an evidence-mix strip, a lane-quality panel for `short_term`, `swing`, `bullish_momentum`, and Bullish Pullback, and read-only guardrail readbacks for high debit, fill degradation, wide legs, momentum chase, and Bullish Pullback ret5 risk. Extended `ScanPick` typing for the diagnostic fields used by those panels. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run verify:docs`, and in-app browser checks on `http://localhost:3000/` for open/closed Trading Desk panels with zero console errors.
- Turned the all-lanes negative-trade audit into a repair backlog at `docs/main-product-lane-quality-system-2026-05-31.md`, using the existing full negative ledger, repair metrics, tracked-position backup, and read-only subagent slices. The readback ranks `short_term`, `swing`, `bullish_momentum`, and `bullish_pullback_observation` as the P1 repair lanes; preserves the `0` negative live-exact row finding; separates research/backfill from production-proof metrics; calls out provenance-safe Trading Desk summary cards; and records candidate guardrail probes such as debit-over-45%-width, SLB/NVDA quarantine, Bullish Pullback bucket enforcement, fill-degradation/worst-leg-spread checks, momentum-chase filters, and a separate legacy missed-auto-close audit for rows `26`, `39`, and `44`. Verification: `npm run verify:docs`.
- Followed up the worker/context-window audits and implemented the remaining proof-integrity guardrails that were safe to land now. `scripts/run_regular_options_goal_experiment.py` now writes frozen-evaluator artifacts inside each experiment batch by default instead of overwriting global `data/profitability-lab/regular-options-autoresearch/latest.json`; overwriting the global latest now requires explicit `--write-global-latest`. `scripts/run_regular_options_multilane_portfolio.py` now falls back to run-level metrics when a sparse authoritative metrics block omits fields such as `win_rate_pct`, avoiding false `0.0%` lane win rates. Verification: `python -m pytest tests\test_regular_options_goal_experiment.py tests\test_regular_options_multilane_portfolio.py -q` (`12` passed) and `python -m py_compile scripts\run_regular_options_goal_experiment.py scripts\run_regular_options_multilane_portfolio.py`.
- Accepted the worker proof-label recommendation and renamed the current multi-lane proof status from misleading `portfolio_candidate` to `count_candidate` in regenerated reports, while keeping the frozen evaluator backward-compatible with older artifacts that still use the legacy label. Regenerated `data/profitability-lab/regular-options-multilane/latest.json`, `latest.md`, and `docs/regular-options-multilane-2026-05-30.md`; the frozen evaluator remains unchanged at clean count `0`, scout count `234`, Lane A conservative PF `0.85`. Verification: `python -m py_compile scripts\run_regular_options_multilane_portfolio.py scripts\evaluate_regular_options_autoresearch.py`, `python -m unittest tests.test_regular_options_multilane_portfolio tests.test_regular_options_autoresearch_evaluator tests.test_regular_options_all_planned_sleeves -q` (`29` tests), `python scripts\evaluate_regular_options_autoresearch.py --no-write --score-line`, and `npm.cmd run verify:docs`.
- Advanced the sector ETF trusted-data unblock after verifying GOOGL/NVDA, IWM, and first liquidity-first tracked-winner shapes are already stopped by proof gates. Imported twenty-five additional bounded ThetaData v3 `1h` chunks across `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` after the initial one-day validation; the latest two chunks were batch `1856` for `2025-11-28` through `2025-12-04` with `64,135` trusted rows and batch `1857` for `2025-11-20` through `2025-11-26` with `64,793` trusted rows. Both had `0` duplicates, `0` rejects, and `0` errors. Sector ETF readiness now has `1,293,277` trusted rows and `126` shared quote dates from `2025-11-20` through `2026-05-22`, with every required ETF at `100.0%` executable rows. The post-import planner confirms `thin_required_history` and `ready_to_import_sector_etfs`, so no sector sleeve replay or clean-count claim is allowed and the next chunk is `2025-11-13` through `2025-11-19` while ThetaTerminal remains reachable. Verification: `.venv\Scripts\python.exe scripts\plan_regular_sector_etf_imports.py --json` and `.venv\Scripts\python.exe scripts\evaluate_regular_options_autoresearch.py --no-write --score-line --experiment-id sector-import-check-after-1857 --hypothesis "Sector ETF trusted-data import check after batch 1857."` (unchanged: clean count `0`, scout count `234`, Lane A conservative PF `0.85`), plus `npm run verify:docs`.
- Unblocked the sector ETF ThetaData path from the worker handoff. Started local ThetaTerminal v3 on `127.0.0.1:25503`, updated `scripts/plan_regular_sector_etf_imports.py` so the retired `/v2/system/status` `410 Gone` response is treated as a reachable v3 terminal, switched emitted sector commands from invalid legacy interval `60` to Theta v3 interval `1h`, and raised dry-run timeout to `30` seconds. The emitted GLD dry run succeeded, then a bounded one-day write validation imported trusted intraday rows for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` on `2026-05-22`: batch `1832`, `13,457` imported rows, `0` duplicates, `0` rejects. The planner now reports `ready_to_import_sector_etfs`; all six symbols are present but thin at `1` shared quote date, so sector sleeves remain blocked until the full import reaches `ready_for_sector_replay`. Verification: `python -m pytest tests\test_regular_sector_etf_import_plan.py -q` (`5` passed), `python scripts\plan_regular_sector_etf_imports.py --json`, and the one-day `scripts/import_thetadata_options_nbbo.py` import above.
- Added tracked-position review policy versioning to saved review metrics. New reviews now carry `review_policy_version=profit_first_stop90_v1` and `max_live_review_stop_loss_pct=90.0`, so future negative-trade audits can separate current profit-first review policy from stale historical `50%` stop reason text. Verification: `python -m unittest tests.test_positions_review_engine.PositionsReviewEngineTests.test_review_holds_loss_inside_configured_90_percent_stop -v`, `python -m py_compile python-backend\positions_service.py`, and `npm run verify:docs`.
- Added Trading Desk evidence badges to tracked-position lane cells so operators can distinguish `Live exact`, `Research backfill`, `Historical paper`, `Lifecycle-only`, and `Legacy/unclassified` rows directly in open/closed position tables. The classifier reads existing `proof_class`, backfill, migration, research-only, and executable-exit fields from each position snapshot, and TypeScript now models those snapshot fields. Verification: `npm run verify:typecheck` and `npm run lint`.
- Broadened the negative-trade audit from the narrow Bullish Pullback lane to the full Trading Desk regular supervised product lane and wrote `docs/main-product-lane-negative-trade-audit-2026-05-31.md`. Generated full negative ledgers at `data/forward-tracking/main_product_lane_negative_trade_audit_20260531.json` and `.csv`. Current Postgres read: `536` tracked positions, `213` negative, `62` unknown/unpriced, `5` live exact tracked rows, and `0` negative live exact rows; `208` of `213` negatives are research/backfill rows. No state-changing review endpoint was called.
- Audited negative tracked rows for the main Bullish Pullback lane in Postgres and wrote `docs/main-lane-negative-trade-audit-2026-05-31.md`. The audit found `62` tracked Bullish Pullback rows: `23` positive/flat, `26` negative, and `13` unknown/unpriced lifecycle rows. The two live exact-contract Bullish Pullback rows were not negative; every negative row was research/backfill or migrated historical paper, with `22` of `26` negatives outside the current keep bucket. No state-changing review endpoint was called.
- Added `scripts/plan_regular_sector_etf_imports.py` and `tests/test_regular_sector_etf_import_plan.py` so the sector ETF layer has a repeatable read-only coordinator instead of a prose-only blocker. The planner audits trusted intraday ThetaData readiness for `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE`, checks the local ThetaTerminal status endpoint, and emits one-day dry-run plus full import commands without importing rows. Current local run: `status=blocked_theta_unavailable`, all six sector ETFs need import, `IWM` control readiness is `252` trusted quote dates, and `http://127.0.0.1:25503/v2/system/status` refused the connection. Verification: `python -m pytest tests\test_regular_sector_etf_import_plan.py -q` (`3` passed), `python -m py_compile scripts\plan_regular_sector_etf_imports.py`, and `python scripts\plan_regular_sector_etf_imports.py --json`.
- Verified the sector ETF data blocker after IWM and liquidity-first probes failed promotion. Current trusted intraday `data/options-validation/options_history.db` readiness is `IWM=252` quote dates from `2025-05-22` through `2026-05-22`, while `GLD`, `TLT`, `XLE`, `XLF`, `SMH`, and `KRE` each have `0` trusted intraday rows. A one-day GLD dry-run import with `scripts/import_thetadata_options_nbbo.py` failed because `http://127.0.0.1:25503` refused the connection, so sector ETF sleeves are blocked on starting/licensing ThetaTerminal or another trusted OPRA/NBBO source before replay work can be honest.
- Added and tested the first causal `liquidity_first_spread` replay variant, `tracked_winner_liquidity_first_contract_hygiene_v1`, using entry-time quote continuity, bid/ask width, short-leg bid, one-step inside short-leg selection, and tradability-score gates rather than post-exit hindsight. The all-planned partial artifact is `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260531T185933Z/summary.json`. It is rejected: `102` strict-new exact trades would close the count gap, but standalone PF was `0.81`, avg `-5.22%`, quote coverage `58.0%`, unpriced `74`, stress PF `0.53`, and worth status `not_worth_current_shape`. Verification: `python -m pytest tests\test_wfo_sleeve_selection.py -q` (`5` passed), `python -m pytest tests\test_chain_native_continuity.py tests\test_regular_options_all_planned_sleeves.py -q` (`23` passed), `python -m py_compile scripts\run_regular_options_all_planned_sleeves.py`, a JSON probe confirmed the replay artifact contains diagnostic-only spread fields, and `python scripts\evaluate_regular_options_autoresearch.py --no-write --score-line` confirmed the frozen evaluator still rejects the stack (`score: 0.00`, clean count `0`, Lane A conservative PF `0.85`).
- Added `scripts/repair_historical_backfill_realized_pnl.py` and applied it to the Postgres tracked-position store so historical research/backfill rows realize P&L at the first executable historical suggested-close point rather than at a lifecycle placeholder date. The repair replays stop, target, profit-harvest/giveback, and time-exit checks from entry through expiry/as-of using trusted ThetaData OPRA/NBBO exact spread quotes, updates only rows with executable close evidence, and leaves missing rows visibly unpriced when the exact exit leg is unavailable. Latest report: `data/forward-tracking/historical_suggested_close_realized_pnl_repair_v1_latest.json`; backups include `data/tracked_positions.pre-historical_suggested_close_realized_pnl_repair_v1-20260531-131756.json` and `data/tracked_positions.pre-historical_suggested_close_realized_pnl_repair_v1-20260531-132506.json`. Applied result across the two repair passes: `98` rows updated, final dry-run idempotence reported `369` already-correct executable suggested closes, and closed missing P&L fell from `102` to `61` exact-quote gaps (`54` all-lanes, `7` main-lane). Verification: `python -m py_compile scripts\repair_historical_backfill_realized_pnl.py python-backend\positions_repository.py scripts\migrate_main_lane_backfills_to_positions.py`, `uv run --locked python -m unittest tests.test_positions_repository_schema -v`, `python -m pytest tests\test_zero_pick_all_lanes_audit.py -q`, and dry-run idempotence with `python scripts\repair_historical_backfill_realized_pnl.py --as-of-date 2026-05-31`.
- Added the all-lanes zero-pick current-algorithm audit runner at `scripts/audit_zero_pick_days_all_lanes.py` and removed the unsafe implicit bullish-pullback replay fallback from non-pullback lane audits. Applied the all-lanes audit after the operator clarified that would-have-picked rows should be tracked for learning: `14` / `14` supervised scan lanes completed, with `1,496` signal candidates, `1,228` exact historical candidates, and `425` research/backfill picks appended to the forward scan/fill logs. Generalized `scripts/migrate_main_lane_backfills_to_positions.py` for `--playbook-id all` and migrated those rows into `425` historical paper positions: `398` closed and `27` open as of `2026-05-31`, with all `425` scan/fill rows linked back to tracked position IDs. Latest reports: `data/forward-tracking/all_lanes_zero_pick_current_algo_audit_latest.json` and `data/forward-tracking/all_lanes_zero_pick_position_migration_v1_latest.json`. Verification: `python -m pytest tests\test_zero_pick_all_lanes_audit.py -q`, `uv run --locked python -m unittest tests.test_positions_repository_schema -v`, `python -m py_compile scripts\audit_zero_pick_days_current_main_lane.py scripts\audit_zero_pick_days_all_lanes.py scripts\migrate_main_lane_backfills_to_positions.py python-backend\positions_repository.py`, and in-app browser verification on `http://localhost:3000/` showed `Open (51)`, `Closed (485)`, and lane chips for the all-lanes tracked rows with zero console errors.
- Followed up both active worker windows and implemented their actionable regular-options recommendations. `scripts/run_regular_options_all_planned_sleeves.py` now exposes `iwm_small_cap_risk` through the existing `sleeve_ticker_iwm` ticker-sleeve hook plus IWM-only risk-on call and risk-off put WFO variants. The latest IWM partial all-planned artifact is `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260531T185035Z/summary.json`: the per-ticker IWM sleeve is PF `2.47` but only `11` exact trades at `73.3%` coverage and no strict-new contribution, the WFO call probe is `18` exact trades at PF `1.25`, `100.0%` coverage, `7` strict-new rows, and stress PF `0.87`, and the WFO put probe is unprofitable with `20.0%` coverage. `wfo_optimizer.py` now persists replay-side diagnostic-only `selected_spread`, `top_spread_alternatives`, `fill_degradation_vs_mid`, and entry mid/ask-bid debit fields for chain-native spread rows, so liquidity-first analysis can use historical artifacts without changing proof gates. Verification: `python -m pytest tests\test_chain_native_continuity.py -q` (`9` passed), `python -m pytest tests\test_regular_options_all_planned_sleeves.py tests\test_lane_lab.py -q` (`22` passed), `python -m pytest tests\test_historical_truth_lane.py -q` (`23` passed), `python -m py_compile wfo_optimizer.py scripts\run_regular_options_all_planned_sleeves.py`, and a JSON probe confirmed the latest IWM call run contains the diagnostic fields.
- Repaired closed tracked-position realized P&L handling. The tracked and suggested trade repositories now persist gross/net P&L fields when creating pre-closed rows, and closed-row normalization recalculates canonical realized P&L from entry/exit execution prices when an exit price exists. Repaired the local Postgres tracked-position store after backing it up to `data/tracked_positions.pre-realized-pnl-repair-20260531T184725Z.json`: `87` closed rows audited, `75` priced rows repaired to canonical P&L, and `12` historical lifecycle-only rows intentionally remain unpriced because no trusted historical exit quote exists. Verification: `uv run --locked python -m unittest tests.test_positions_repository_schema -v`, `uv run --locked python -m py_compile python-backend\positions_repository.py python-backend\suggested_trades_repository.py tests\test_positions_repository_schema.py`, and a local closed-position audit reported `inconsistent_priced_count=0`.
- Canonicalized Trading Desk lane display around lane families. Tracked trades now show `Bullish Pullback` instead of splitting the same regular lane by legacy label variants plus source labels, and the top filter panel now uses clickable lane chips to filter open/closed trades by lane family. Updated the scanner playbook dropdown and living docs to use the shorter Bullish Pullback label. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run verify:docs`, and in-app browser checks at desktop/default and 390px mobile width with zero console warnings/errors.
- Tightened the Trading Desk row UX for tracked positions and paper ideas. Open-row actions now sit at the sticky right edge instead of splitting the Signal/Plan/Quote sequence, the controls use compact icon-and-label buttons with `Close` replacing the wider `Mark Closed`, the exit plan cell is a smaller target/stop/time strip, and sticky action cells are right-aligned with a stable compact width. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run verify:docs`, and in-app browser checks at mobile and wide-desktop widths with zero console errors.
- Updated the Trading Desk tracked-position table to mark positions taken in the last `24` hours, expose a lane/source selector derived from stored playbook, cohort, promotion, selection, and event provenance, and replace separate repetitive Target/Stop columns with one exit-plan cell showing target, effective stop cap, and time exit. The UI keeps the current profit-first `90%` live-review stop cap rather than trusting stale saved review metrics, while still showing tighter configured stops when rows have them. Verification: `npm run lint`, `npm run verify:typecheck`, and in-app browser checks on `http://localhost:3000/` confirmed the Plan column, lane/source options and filter interaction, last-24h stat, no Next error overlay, and zero console errors.
- Ran the AI commodity OPRA capture guard. Started with `python scripts/run_ai_commodity_opra_progress.py --next-execution --from-latest`, observed `run_next_execution_command=true`, ran exactly `python scripts/run_ai_commodity_opra_progress.py --force-capture --target-date 2026-05-26`, then ran the required post-command readback. The capture still made no material progress: exact Alpaca OPRA shared quote dates stayed `3` / `100`, the previous proof event reports `capture_status_after=no_rows_captured`, all `24` target-date symbols remained missing, and `exact_capture_progress_outcome.status` is `exact_capture_progress_failed_or_not_observed`. The final guard state is waiting until `2026-06-01T08:10:00-06:00` for `python scripts/run_ai_commodity_opra_progress.py --skip-capture`; profitability remains unverified and filter work remains locked until exact Alpaca OPRA bid/ask replay gates unlock.
- Followed up the worker-window regular-options audits and closed the stale handoff. `scripts/run_regular_options_all_planned_sleeves.py` now keeps same-`lane_id` lane-lab specs visible when a replay variant exists but the lane-lab spec still needs paper/forward evidence, so `relative_strength_pullback` and `bearish_put_debit_spread` no longer disappear from the blocked/planned ledger. Updated `docs/PROJECT_CONTEXT.md`, `docs/DECISIONS.md`, `docs/NEXT_STEPS.md`, and `docs/lane-lab-lanes.md` to record that targeted side-aware replay rejected tracked-winner no-SPY at conservative PF `0.62` and GOOGL/NVDA at PF `0.67`, so the next clean-count work should pivot to IWM and blocked-lane conversion instead of GOOGL/NVDA survivability. Verification: `python -m pytest tests\test_regular_options_all_planned_sleeves.py -q` (`10` passed), `python -m py_compile scripts\run_regular_options_all_planned_sleeves.py`, `python -m pytest tests\test_lane_lab.py -q` (`9` passed), and a no-write GOOGL/NVDA side-aware probe confirmed the current local ThetaTerminal service is unavailable (`127.0.0.1:25503` connection refused), so the targeted `20260531T074502Z` artifact remains the economic verdict.
- Clarified the regular stock-options count state across living docs and generated report wording. `docs/current-state.md`, `docs/PROJECT_CONTEXT.md`, `docs/NEXT_STEPS.md`, `docs/DECISIONS.md`, and `docs/lane-lab-lanes.md` now distinguish `234` multi-lane count feasibility, `157` clean replacement baseline, `130` bullish-pullback/autoresearch fallback, `0` gap versus `0` clean trades, and the separate meanings of `200` and `300`. Updated `scripts/run_regular_options_multilane_portfolio.py` so regenerated Markdown leads with count passed / quality pending instead of a potentially misleading proof heading, then regenerated `data/profitability-lab/regular-options-multilane/latest.json`, `data/profitability-lab/regular-options-multilane/latest.md`, and `docs/regular-options-multilane-2026-05-30.md`. Verification: `python -m py_compile scripts\run_regular_options_multilane_portfolio.py`, `python -m pytest tests\test_regular_options_multilane_portfolio.py tests\test_regular_options_autoresearch_evaluator.py tests\test_regular_options_all_planned_sleeves.py tests\test_lane_lab.py -q` (`26` passed), and `npm run verify:docs` passed.
- Switched tracked-position live review back to profit-first stop behavior. `MAX_LIVE_REVIEW_STOP_LOSS_PCT` and scanner fallback stops now use `90%`, the Strategy Lab stop slider can reach/default to `90%`, and regression coverage now confirms a `-55%` executable drawdown stays `HOLD` while a roughly `-90%` executable drawdown becomes `SELL`/`Close now`. Verification: `python -m unittest tests.test_positions_review_engine -v`, `npm run lint`, `npm run verify:typecheck`, and `npm run build:clean` passed; the first build attempt failed because a running dev server was sharing `.next`, then passed after stopping the dev process.
- Simplified the Trading Desk open-trade dashboard for friend-facing copy review. Removed contract-size-dependent cost/risk dollars, internal `SELL`/proof/unpriced KPI cards, and raw recommendation wording from the visible stat strip; Open now shows open trade count, average live P&L, closed-trade win rate, and average closed P&L, with `SELL` rendered as the plain-language `Close now` signal in rows and dialogs. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run build:clean`, and an HTTP 200 load check on `http://localhost:3000` passed; initial `npm run build` hit stale `.next` output for `/api/positions/review` and passed after the clean rebuild. In-app browser visual verification was unavailable because the `iab` browser backend was not advertised.
- Ran a six-subagent frontend makeover review for the main app shell, Trading Desk, and Strategy Lab surfaces. Implemented the majority-approved first pass: scrollable main content, `Trading Desk` / `Strategy Lab` naming, removed the contradictory `Crypto Pilot` header badge, compressed sidebar copy to navigation plus risk snapshot, shortened desk tabs, collapsed scanner evidence/guardrails, reduced tracked-position KPI density, merged tracked-position lane tables into one table with a `Lane` column, added mobile card rendering for `FinTable`, calmed the palette/buttons/metric cards, made Strategy Lab replay-first, and added better tab ARIA wiring. Verification: `npm run lint`, `npm run verify:typecheck`, `npm run build`, and an HTTP 200 load check on `http://localhost:3000` passed. In-app browser automation was unavailable in this session.
- Added global Truthfulness And Challenge Rules to `C:\Users\kalec\AGENTS.md` and `C:\Users\kalec\CLAUDE.md`. Added repo-level `AGENTS.md` for options-chatbot, recorded the scoped Markdown placement audit in `docs/markdown-audit-2026-05-31.md`, and updated `README.md`, `docs/index.md`, `docs/DECISIONS.md`, and `docs/NEXT_STEPS.md` so global behavior, repo memory, generated evidence, and historical research files have clear ownership.
- Audited tracked-position stop-loss behavior after live paper rows showed losses at or near `-100%`. Confirmed the live review engine was honoring raw stored `stop_loss_pct=90`, causing executable positions down more than `50%` to remain `HOLD`. Added a live-review risk cap of `50%` in `python-backend/positions_service.py` while preserving the configured stop in metrics, allowed verified executable zero exits to auto-close paper positions at `0.0`, disabled automatic UI review POSTs during passive positions/suggestions polling, and added regression coverage for `-55%` SELL versus `-49%` HOLD plus zero-exit auto-close. Verification: `python -m py_compile python-backend/positions_service.py`, `python -m unittest tests.test_positions_review_engine -v` (`43` tests), and `npx eslint src/components/predictions/PredictionsView.tsx --max-warnings=0` passed.
- Added `scripts/audit_zero_pick_days_current_main_lane.py` to audit market days with zero daily picks and/or zero main-lane picks against the current `bullish_pullback_observation` selection logic. Applied the audit as research backfill: `60` exact historical call-vertical picks were appended to `data/forward-tracking/scan_picks.jsonl` and `data/forward-tracking/fill_attempts.jsonl`, with `18` `research_backfill` ledger sessions in `data/options-validation/forward_tracking.db`. Latest report: `data/forward-tracking/main_lane_zero_pick_current_algo_audit_latest.json`.
- Added `scripts/migrate_main_lane_backfills_to_positions.py` and applied the explicit paper-position migration for those `60` zero-pick audit rows into the Postgres tracked-position store used by the UI. Result: `27` open historical paper positions and `33` closed by historical time-exit, with `21` closed rows carrying trusted historical spread-exit P&L and `12` lifecycle-only closed rows where one or both exit-leg quotes were unavailable. Updated `scan_picks.jsonl` and `fill_attempts.jsonl` with tracked position IDs. Latest report: `data/forward-tracking/main_lane_zero_pick_position_migration_latest.json`; pre-migration snapshot: `data/tracked_positions.pre-main-lane-zero-pick-migration-20260530-232927.json`.
- Updated the open tracked-position UI so executable `Exit P&L` / `Est. exit` are shown as the primary values and non-executable `Mark P&L` / `Mark Value` are secondary. This keeps wide bid/ask spread effects visible without treating optimistic mark values as the main managed P&L.
- Fixed long option/spread P&L percent handling so net return cannot display below `-100%` after fees. Backend P&L snapshots now include fees in the return denominator and cap full-loss rows at `-100%`, the frontend fallback math matches that behavior, and the affected open `SBUX` review was refreshed from `-101.0012%` to `-100.0%`.
- Ran a contrarian portfolio-count scan across `612` trusted intraday regular stock-options run artifacts. Raw artifact aggregation can exceed `300` strict entry-date + ticker + direction exact keys: adding `data/options-validation/runs/20260526_023904_tracked_winner_chain_native_qqq_time80_research_intraday.json` to the current `234` stack adds `120` strict-new exact trades for a `354`-trade exact-priced scenario at combined PF `2.07` and avg `+23.75%`, but that tracked-winner artifact has only `53.8%` quote coverage and `103` unpriced candidates, so it is a count challenge rather than production-quality evidence.
- The same scan refuted the easy "just include all clean/reference sleeves" argument. The clean 100%-covered bullish-pullback reference adds only `17` strict-new rows over the current stack, and those incremental rows are weak at PF `1.04`; broader S/A/B and energy/defensive refills add about `25` strict-new rows but their incremental rows are negative or flat. Existing bearish/range/volatility intraday probes add count but fail profitability, while clean coverage>=97.5 and unpriced=0 artifacts union to only `134` strict keys.
- Added a frozen regular stock-options autoresearch evaluator at `scripts/evaluate_regular_options_autoresearch.py`, tests at `tests/test_regular_options_autoresearch_evaluator.py`, and the `/goal` prompt at `docs/autoresearch/regular-options-goal.md`. The evaluator is the fixed judge for future Karpathy-style goal loops and must not be edited during a strategy experiment.
- Ran the first evaluator baseline with `python scripts/evaluate_regular_options_autoresearch.py --refresh-multilane --experiment-id baseline-current-stack --hypothesis "Current regular stock-options stack before Lane A survivability filters." --append-ledger --score-line`. The baseline row was appended to `data/profitability-lab/regular-options-autoresearch/ledger.jsonl` and latest outputs were written under `data/profitability-lab/regular-options-autoresearch/`. Current score line: `score: 0.00`, `research_score: 177.74`, status `scout_or_blocked`, clean count `0`, scout count `234`, PF `2.16`, avg `+26.76%`, effective side-aware coverage `96.71%`, unresolved `14`, zero-bid exit rate `41.99%`, Lane A conservative PF `0.85`.
- Tightened the regular stock-options multi-lane portfolio readback so the `200` target is treated as a quality-gated milestone rather than production approval. The latest report now shows `234` trusted intraday exact trades after strict entry-date + ticker + direction dedupe, PF `2.16`, avg `+26.76%`, and gap `0` to `200`, with count status `passed` but overall status `quality_pending`.
- Ran Lane A missing-contract classification and exact ThetaData import loops on the refreshed chain-native ret20=4 branch. One-day and bounded lookahead passes imported new trusted exact rows, but the final rerun `data/options-validation/runs/20260530_191945_lane_a_chain_native_ret20_4_stop200_time75_rerun4_v1_intraday.json` still has `155` exact trades, `137` unpriced candidates, `53.1%` quote coverage, PF `3.59`, and avg `+42.34%`.
- Classified the remaining latest Lane A misses: `111` unique missing exact leg/date items still classify as `provider_no_match_exact_contract_with_same_expiry_chain`, so the residual coverage debt is structural/provider no-match evidence rather than a simple local import oversight.
- Ran a contrarian raw ThetaData probe against all `111` classified latest Lane A missing leg/date items. All `111` were exact short-leg rows present at `15:55` with `bid=0` and `ask>0`; the current importer/classifier drops them because executable-row validity requires `bid > 0`. This reframes the residual debt from pure provider absence to a zero-bid short-leg exit modeling problem.
- Added `scripts/run_side_aware_zero_bid_replay.py` and `tests/test_side_aware_zero_bid_replay.py` to replay Lane A missing-exit candidates with side-aware zero-bid handling instead of treating `bid=0, ask>0` short-leg rows as absent. The latest artifact is `data/profitability-lab/side-aware-zero-bid/latest_lane_a_side_aware_zero_bid.json`.
- Ran the side-aware zero-bid replay against `127` Lane A `missing_exit_quote_for_leg` candidates. Conservative long-bid/short-ask mode priced `126` of `127`, with `118` priced rows using at least one zero-bid exit quote; those side-aware rows alone were PF `0.11`, avg `-66.59%`, and win rate `16.7%`. Combining them with the original `155` priced Lane A trades gives `281` priced trades, `11` unpriced, `96.2%` coverage, PF `0.85`, and avg `-6.51%`. Even midpoint zero-bid mode leaves combined Lane A weak at PF `1.11` and avg `+3.79%`, so the missing exits are economic losses rather than harmless import gaps.
- Ran robustness for the latest Lane A artifact. Priced exact trades stay strong under 5%/side slippage stress at PF `2.65` and after top-5 winner removal at PF `3.21`; rolling exact test windows are positive, but the robustness status remains blocked because unpriced test candidates remain.
- Updated `scripts/run_regular_options_multilane_portfolio.py` to point at the latest Lane A artifact, attach Lane A robustness, add a `200` quality gate, render the side-aware zero-bid diagnostic, and keep the tracked-winner intraday rerun visible as a rejected scout. Updated `tests/test_regular_options_multilane_portfolio.py` so tests require count success to remain separate from production readiness.
- Regenerated `data/profitability-lab/regular-options-multilane/latest.json`, `data/profitability-lab/regular-options-multilane/latest.md`, and `docs/regular-options-multilane-2026-05-30.md` with the current `234`-trade quality-gated readback.
- Verification for the latest regular multi-lane work: `python -m py_compile scripts/run_regular_options_multilane_portfolio.py scripts/run_side_aware_zero_bid_replay.py scripts/import_thetadata_run_candidate_chains.py scripts/import_missing_replay_quotes_from_thetadata.py scripts/classify_missing_replay_contracts.py` and focused pytest coverage including `tests/test_side_aware_zero_bid_replay.py` passed.
- Ran a contrarian regular-lane audit for uncounted stock-options sleeves. The strongest contrary artifact was the trusted intraday `tracked_winner_chain_native_qqq_time80_research` family because older artifacts had 100+ novel exact trades versus the current 234 stack, but a current trusted-intraday rerun wrote `data/options-validation/runs/20260530_202046_tracked_winner_chain_native_qqq_time80_research_intraday.json` and failed promotion at `102` exact trades, PF `1.36`, avg `+9.52%`, `51.8%` coverage, and `95` unpriced candidates.
- Added the tracked-winner intraday rerun to `scripts/run_regular_options_multilane_portfolio.py` as a visible rejected scout and regenerated the regular multi-lane report. Classifying its latest misses found `77` provider no-match exact contract items, so this does not overturn the quality-gate-first decision.
- Added `scripts/run_regular_options_goal_experiment.py` and `tests/test_regular_options_goal_experiment.py` so Lane A variants can be run, robustness-tested, side-aware zero-bid replayed, temporarily inserted into the regular multi-lane stack, scored by the frozen autoresearch evaluator, and appended to the regular-options autoresearch ledger without editing the judge.
- Ran the first Lane A autoresearch goal pass against entry survivability filters. `lane_a_goal_stop200_time75_shortprior3_shortbid10_backfill` was the best of the simple entry filters at `research_score: 163.07`, effective coverage `99.09%`, unresolved `3`, PF `2.01`, and scout count `194`, but Lane A conservative zero-bid PF stayed weak at `0.88` and zero-bid exit rate stayed above `42%`. The liquidity-score replacement was similar at `research_score: 160.78`, coverage `99.09%`, unresolved `3`, and Lane A conservative PF `0.87`.
- Ran the second Lane A repair pass against the actual conservative zero-bid failure mode. A lower debit/width plus broad bad-zero-ticker exclusion probe reached Lane A conservative PF `1.36`, and the looser `lane_a_goal_bad_zero_ticker_exclusion_debit45_npicks8` probe reached `research_score: 236.19` with Lane A conservative PF `1.30`, but neither recovered enough exact Lane A count: the best count-recovery probe had only `81` exact Lane A trades, so Lane A was not counted and the portfolio fell back to the `130`-trade core.
- Tested whether existing clean/high-PF bullish sleeves could replace Lane A. Counting the clean exact reference with the core reaches only `157` strict-deduped trades; adding the high-PF, balanced, clean, and 130-trade cluster artifacts still tops out at `158` because they overlap heavily. Current trusted-intraday tracked-winner reruns also failed as replacements: `tracked_winner_chain_native_qqq_time65_research` reran at `109` exact trades but PF `1.04`, while no-XLK and SPY/QQQ time variants stayed below promotion.
- Added `scripts/run_regular_options_all_planned_sleeves.py` and `tests/test_regular_options_all_planned_sleeves.py` to run every currently implemented planned regular stock-options sleeve end-to-end instead of ranking planned ideas by hand. The merged latest audit is `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/latest.json` and batch `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260531T044317Z_merged_all_planned/summary.json`: `24` implemented variants tested, `0` run failures, `30` lane-lab specs explicitly blocked/not yet testable, and the base core-plus-clean-reference stack remains `157` exact trades with a `43`-trade gap to `200`.
- The all-planned audit found no currently implemented planned sleeve that both closes the `43`-trade gap and clears quality. Count-closing scouts fail economics or coverage: tracked-winner QQQ adds `109` novel exact trades but only PF `1.04`, `54.2%` coverage, and stress PF `0.68`; tracked-winner no-XLK adds `83` with PF `1.57` but only `49.7%` coverage and stress PF `1.10`; volatility-expansion calls add `44` but lose money at PF `0.54`; high-beta momentum adds `44` at PF `0.20`; range-breakout calls add `14` at PF `0.36`. The best profitable overlap/repair sleeves remain coverage-blocked or too redundant: move-bucket refill has `135` exact trades at PF `2.03` but only `6` strict-new rows over the `157` stack and `91.2%` coverage.
- Ran a four-subagent contrary review of the all-planned audit. The review agreed that all implemented planned sleeves had been tested, rejected any claim that all planned lane-lab specs were disproven, and identified tracked-winner contract survivability plus blocked-spec conversion as the next real profitability blockers.
- Added a repair/unlock pass to `scripts/run_regular_options_all_planned_sleeves.py`: current trusted ThetaData readiness is now derived from `data/options-validation/options_history.db`, `IWM` is no longer treated as thin, `relative_strength_pullback` has a replay entry-signal hook, and six additional current/new sleeve variants were run. The merged latest audit is now `data/profitability-lab/regular-options-autoresearch/all-planned-sleeves/20260531T053337Z_merged_all_planned_plus_repairs/summary.json`: `30` implemented variants tested, `0` run failures, `29` blocked/not implemented lane-lab specs, and the clean base remains `157` with a `43`-trade gap to `200`.
- The best new count/quality candidate is `tracked_winner_chain_native_googl_nvda_time65_all_sleeves`: `49` strict-new exact trades, PF `1.98`, avg `+18.27%`, and stress PF `1.39`, but only `68.1%` quote coverage with `23` unpriced candidates, so it is not countable until contract coverage is repaired. The broader no-SPY tracked-winner variant adds `67` strict-new trades at PF `1.85` but has only `62.0%` coverage and stress PF `1.24`; `tracked_winner_cheap_debit_continuity_v1` adds `98` strict-new trades but misses quality at PF `1.43`, `50.8%` coverage, and stress PF `0.97`.
- Rejected the current shapes of the newly runnable alternatives: fast volatility-expansion calls were PF `0.13`, relative-strength pullback was PF `0.34`, and narrowed bearish index puts were only `8` strict-new trades with `34.8%` coverage. Missing-contract classifiers on the tracked-winner repairs found exact same-expiry chain rows present but no executable exact selected contract rows: `15` missing GOOGL/NVDA items for the best tracked-winner repair, `29` for no-SPY, and `76` for cheap-debit continuity.
- Verification for the repair/unlock pass: `python -m py_compile wfo_optimizer.py scripts\run_regular_options_all_planned_sleeves.py scripts\imported_intraday_robustness.py` and `python -m pytest tests\test_regular_options_all_planned_sleeves.py tests\test_imported_intraday_robustness.py tests\test_lane_lab.py -q` passed (`15` tests).

## 2026-05-30

- Added `scripts/run_regular_options_multilane_portfolio.py` and `tests/test_regular_options_multilane_portfolio.py` to build a regular stock-options multi-lane portfolio report from existing replay artifacts without using AI commodity, crypto, Polymarket, or day-trading lanes.
- Generated `data/profitability-lab/regular-options-multilane/latest.json`, `data/profitability-lab/regular-options-multilane/latest.md`, and `docs/regular-options-multilane-2026-05-30.md`. After adding the refreshed Lane A chain-native ret20=4 replay and tightening dedupe to entry date + ticker + direction, the combined proof-grade regular stock stack is `227` trusted intraday exact trades, PF `1.99`, avg `+23.25%`, and gap `0` to the `200` target.
- Added `scripts/import_thetadata_run_candidate_chains.py` to import ThetaData NBBO chains for replay candidate ticker/date/right pairs without letting existing call rows mask missing put rows.
- Imported `822,615` trusted intraday put-chain rows for the liquid/index bearish candidate set, imported exact missing exit rows, and reran bearish, range-breakout, and volatility-expansion probes. The bearish put lane priced `73` exact trades but failed at PF `0.21`; range and volatility probes also stayed below profitability gates, so they are documented as rejected intraday scouts rather than counted.
- Reran the older Lane A branch as `data/options-validation/runs/20260530_165354_lane_a_chain_native_ret20_4_stop200_time75_rerun_v1_intraday.json`, improving it to `145` exact trades at PF `3.02` while retaining `147` unpriced candidates as coverage debt.
- Verification for the regular multi-lane work: `python -m py_compile scripts/run_regular_options_multilane_portfolio.py scripts/import_thetadata_run_candidate_chains.py` and `python -m pytest tests/test_regular_options_multilane_portfolio.py tests/test_bullish_pullback_layer_stack.py tests/test_bullish_pullback_confidence_tiers.py tests/test_bullish_pullback_ticker_audit.py -q` passed (`22` tests).

## 2026-05-29

- Added `scripts/build_bullish_pullback_layer_stack.py` and `tests/test_bullish_pullback_layer_stack.py` to freeze the next bullish-pullback exact-evidence layers into `data/profitability-lab/bullish-pullback-observation/layer-stack/latest.json` and `docs/bullish-pullback-layer-stack-2026-05-29.md`.
- The frozen layer stack keeps the high-confidence S/A/B queue (`108` exact, PF `4.86`), high-PF cluster (`113` exact, PF `4.34`), high-coverage cluster (`115` exact, PF `2.57`), clean cluster component (`95` exact, PF `3.36`), clean exact mixed branch (`129` exact, PF `2.20`, `100%` coverage), count-expanded branch (`130` exact, PF `2.04`, `97.7%` coverage), high-PF 130-trade reference (`130` exact, PF `2.53`), and S-only timecluster watch (`95` exact, PF `3.71`).
- Reran the defensive refill scout `sleeve_next_defensive_refill_v1` after the layer-stack audit, classified its misses, attempted a ThetaData exact-fill import, reran the replay, then reran classifier and robustness. The exact-fill summary `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260530T000025Z.json` returned `497` normalized rows but imported `0` new trusted rows because all were duplicates; the post-import replay stayed at `138` candidates, `130` exact, `8` unpriced, `94.2%` coverage, PF `2.15`, and 5%/side stress PF `1.60`.
- Defensive refill is not promoted: classifier still finds `10` provider no-match exact-contract legs (`PM` `5`, `WMT` `3`, `UNH` `1`, `JNJ` `1`) with `0` exact executable rows on the missing dates, and the incremental WMT/PM tier is only `22` exact trades at PF `1.36` and avg `+7.66%`.
- Verification for the layer-stack work: `python scripts/build_bullish_pullback_layer_stack.py`, `python -m py_compile scripts/build_bullish_pullback_layer_stack.py`, and `python -m pytest tests/test_bullish_pullback_layer_stack.py tests/test_bullish_pullback_confidence_tiers.py tests/test_bullish_pullback_ticker_audit.py -q` passed (`16` tests).
- Ran the next bullish-pullback profitability iteration across frozen ETF/index, high-beta, defensive/refill, REIT, industrial, and combined move-bucket variants, added the variant definitions to `scripts/run_bullish_pullback_sleeves.py`, and wrote the tested rounds under `data/profitability-lab/bullish-pullback-observation/sleeves/`.
- Started licensed ThetaData v3 on `127.0.0.1:25503`, reran exact missing-contract imports, and kept all imported rows source-labeled as trusted `thetadata_opra_nbbo_1m` intraday OPRA/NBBO.
- Imported trusted ThetaData rows for the move-bucket and expanded-count gaps: exact import `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260529T042508Z.json` imported `3,676` rows, broad UNH/CAT/PM/WMT/JNJ imports added over `5.7M` rows, and high-count import `20260529T050747Z.json` imported `6,743` rows. Required second exact-fill cycles were duplicate-only (`700` and `2,824` duplicate rows).
- Verified paid-data readiness after imports: all `59` required underlyings have `252` shared trusted quote dates from `2025-05-22` through `2026-05-22`; latest readiness is `ready_for_exact_replay` with `12,981,701` trusted intraday rows and latest trusted batches `1787` through `1793`.
- Reran affected replays after each import cycle. `sleeve_pf59_coverage_a_refill_v1` remains the best count/coverage branch at `133` candidates, `130` exact trades, `3` unpriced, `97.7%` coverage, PF `2.04`, rolling test PF `2.35`, and 5%/side stress PF `1.53`.
- Best new move-bucket scout after imports was `sleeve_next_move_bucket_refill_v1`: `148` candidates, `135` exact trusted trades, `13` unpriced, `91.2%` coverage, PF `2.03`, and 5%/side stress PF `1.49`; it was not promoted because the small count gain materially worsens coverage and falls just below the stress gate.
- High-count expansion after imports reached `165` exact trades in `sleeve_pf59_s_themeA_no_ticker_bans_v1`, but that branch failed at PF `1.49`, 5%/side stress PF `1.12`, `90.7%` coverage, and rolling-OOS watch status. Higher-PF expansions (`146` exact at PF `2.23` and `139` exact at PF `2.50`) also failed coverage and rolling-OOS gates.
- Missing-contract classifiers after imports found the remaining misses are provider no-match exact OCC contracts with same-expiry trusted chain rows present and `0` exact executable rows found, not unresolved local import work.
- Robustness retests confirmed the best clean 100%-covered branch is `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timecombo55_50_75_mixed_v1`: `129` exact trades, PF `2.20`, rolling test PF `2.51`, 5%/side stress PF `1.67`, and no unresolved candidates. The best 130-trade PF branch is `sleeve_winner_cluster_exit_balanced_quoted_v1`: `130` exact trades, PF `2.53`, rolling test PF `2.77`, and 5%/side stress PF `1.91`, but it remains blocked by `3` unresolved candidates.
- Updated `docs/bullish-pullback-next-layer-profitability-2026-05-29.md` and regenerated `data/profitability-lab/bullish-pullback-observation/next-layer-summary-2026-05-29.json` to record the successful ThetaData imports, reruns, classifier evidence, stop rationale, and next actions.
- Audited living Markdown docs against the current route map, bullish-pullback artifacts, and AI commodity progress readback.
- Fixed the broken living-doc link in `docs/autoresearch/automation-handoff.md` and added `docs/markdown-audit-2026-05-29.md` to record audit scope, findings, and verification.
- Updated `README.md`, `docs/index.md`, `docs/current-state.md`, `docs/NEXT_STEPS.md`, `docs/PROJECT_CONTEXT.md`, `docs/lane-lab-lanes.md`, `docs/paid-options-data-import-checklist.md`, and `docs/architecture-audit.md` so they reference the current bullish-pullback ticker audit and the latest AI commodity runbook state.

## 2026-05-28

- Implemented reusable sleeve support for `bullish_pullback_observation`: configured allocation groups, `Unknown` sector `ticker_bucket` handling, per-ticker/index caps, tier-aware daily selection, execution-aware preflight/backfill diagnostics, tradability scoring, and result breakdowns by sleeve/tier.
- Added `scripts/run_bullish_pullback_sleeves.py` to derive active symbols from `data/options-lanes/universes/bullish_pullback_observation.json`, preserve `CMCSA` exclusion, run pooled/theme/per-symbol sleeve sweeps, and write artifacts under `data/profitability-lab/bullish-pullback-observation/sleeves/`.
- Added `scripts/classify_missing_replay_contracts.py` to classify selected unpriced replay legs against trusted local ThetaData OPRA/NBBO rows.
- Ran the full theme/per-ticker sleeve sweep at `data/profitability-lab/bullish-pullback-observation/sleeves/sleeve_round_20260528T025450Z.json`; the best useful grouped sleeves were energy (`29` exact, PF `2.38`), defensive (`54` exact, PF `2.21`), liquid core (`52` exact, PF `1.67`), healthcare (`40` exact, PF `1.63`), and the broad alpha sector-fix (`115` exact, PF `1.59`).
- Added and tested `sleeve_profit_theme_stack_v1`, the current leading profitability sleeve family, at `data/options-validation/runs/20260527_210147_sleeve_profit_theme_stack_v1_intraday.json`: `180` candidates, `122` exact priced trades, `58` unpriced, `67.8%` quote coverage, PF `1.75`, avg `+16.54%`, median `+21.61%`, and win rate `66.4%`.
- Robustness artifact `data/profitability-lab/imported-intraday-robustness/imported_intraday_robustness_sleeve_profit_theme_stack_v1_20260528T030206531210Z.json` shows the stack remains positive after top-winner removal (`117` trades, PF `1.47` after removing top 5), 5%/side slippage stress (`122` trades, PF `1.25`, avg `+6.54%`), and the only rolling test window (`26` exact trades, PF `1.67`, avg `+17.26%`), but strict promotion is still blocked by unpriced selected candidates.
- Missing-contract classification for the profit-theme stack found `51` missing selected legs, all classified as `provider_no_match_exact_contract_with_same_expiry_chain`; misses are concentrated in short-leg exit quotes rather than entry-chain availability.
- Reran the broad baseline with current code and local env as `data/options-validation/runs/20260527_211058_bullish_pullback_observation_intraday.json`; the control stayed weak with `21` exact trades at PF `0.83`.
- Tested stricter continuity and exit variants: `sleeve_profit_theme_stack_survival_v1` improved coverage to `69.7%` but reduced PF to `1.60`, `sleeve_profit_theme_stack_target120_v1` reduced PF to `1.49`, and `sleeve_profit_theme_stack_time40_v1` failed at PF `0.68`.
- Verification run: `python -m pytest tests/test_wfo_sleeve_selection.py tests/test_chain_native_continuity.py tests/test_historical_options_store.py tests/test_imported_intraday_robustness.py tests/test_strategy_audit.py tests/test_historical_truth_lane.py::HistoricalTruthLaneTests::test_daily_imported_backtest_uses_shared_quote_calendar_to_avoid_peer_gaps -q` passed (`123` tests).
- Added bounded lookahead support to `scripts/import_missing_replay_quotes_from_thetadata.py`; the helper can now request a small calendar-day window after each missing replay quote date while preserving the exact OCC contract.
- Ran the winner-ticker sleeve branch from the active `bullish_pullback_observation` universe, keeping `CMCSA` inactive and deriving all eligible symbols from the universe JSON. The best high-PF count variant was `sleeve_winner_clean_plus_liquid_v1` at `data/options-validation/runs/20260527_235823_sleeve_winner_clean_plus_liquid_v1_intraday.json`: `127` candidates, `106` exact trades, `21` unpriced, `83.5%` coverage, PF `4.84`, avg `+41.70%`, and 5%/side stress PF `3.42`, but rolling OOS stayed in watch status because the second window had `11` unpriced test candidates.
- Promoted `sleeve_winner_clean_plus_liquid_no_cat_pm_v1` as the current profitability-first lead at `data/options-validation/runs/20260528_000613_sleeve_winner_clean_plus_liquid_no_cat_pm_v1_intraday.json`: `119` candidates, `103` exact trades, `16` unpriced, `86.6%` coverage, PF `4.34`, avg `+40.59%`, median `+39.36%`, win rate `78.6%`, worst trade `-99.89%`, max drawdown `248.06` pct-points, and 5%/side stress PF `3.11`.
- Robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_clean_plus_liquid_no_cat_pm_v1.json` passed the available rolling OOS window (`21` exact test trades, PF `4.13`, avg `+45.84%`, `0` unpriced test candidates), stayed positive after top-5 winner removal (`98` trades, PF `3.75`), and stayed positive under worst symbol/month holdouts (`NEM` removal PF `3.79`; August removal PF `3.34`).
- Exact-fill loops for the no-CAT/no-PM lead stopped with no new imports. `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T060650Z.json` found `10` normalized rows but all were already duplicates, leaving the remaining misses classified as provider no-match exact-contract gaps rather than local import gaps.
- Missing-contract classification for the no-CAT/no-PM lead found `16` missing selected legs, all `provider_no_match_exact_contract_with_same_expiry_chain`, concentrated in `IWM` (`4`), `GOOGL` (`3`), `UNH` (`2`), `JNJ` (`2`), and single misses in `PLD`, `NEM`, `XLK`, `LLY`, and `AAPL`.
- Skeptical subagent audit agreed with a conditional paper/live-shadow-only recommendation for `sleeve_winner_clean_plus_liquid_no_cat_pm_v1`; the audit preferred `sleeve_winner_clean_plus_liquid_no_cat_v1` for raw profit but preferred no-CAT/no-PM for coverage hygiene, and flagged unresolved provider no-match exits, NEM/GOOGL/month concentration, thin rolling OOS, and too few dense/promotable exact trades as the key strict-promotion blockers.
- Added selected-contract-aware exit-quote failure memory scopes to the research runner, including short-contract and short-expiry/strike bucket keys, so future variants can skip/backfill based on observed contract-level misses instead of broad ticker memory.
- Tested the hard-prior quote-continuity branch. `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_v1` is now the cleaner profitability-first lead at `data/options-validation/runs/20260528_001854_sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_v1_intraday.json`: `119` candidates, `104` exact trades, `15` unpriced, `87.4%` coverage, PF `4.09`, avg `+39.46%`, median `+39.36%`, win rate `77.9%`, and 5%/side stress PF `2.94`.
- Robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_v1.json` passed the available rolling OOS window (`21` exact test trades, PF `4.22`, avg `+46.13%`, `0` unpriced test candidates), stayed positive after top-5 winner removal (`99` trades, PF `3.53`), and stayed positive under worst symbol/month holdouts (`NEM` removal PF `3.56`; August removal PF `3.14`).
- The stricter tradability sibling `sleeve_winner_clean_plus_liquid_no_cat_pm_trad70_v1` improved full-sample economics to `100` exact trades, PF `4.61`, and `87.0%` coverage, but stayed behind the lead because rolling OOS retained `1` unpriced test candidate.
- A final bounded exact-fill pass for the prior1/trad70 variants wrote `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T062153Z.json`; it requested `57` unique OCC/date items, normalized `10` rows, imported `0` new rows, and confirmed the newly found rows were duplicates.
- Verification run after the latest sleeve, selected-contract memory, and import-helper changes: `python -m pytest tests/test_wfo_sleeve_selection.py tests/test_chain_native_continuity.py tests/test_historical_options_store.py tests/test_imported_intraday_robustness.py tests/test_strategy_audit.py tests/test_historical_truth_lane.py::HistoricalTruthLaneTests::test_daily_imported_backtest_uses_shared_quote_calendar_to_avoid_peer_gaps -q` passed (`124` tests).
- Tested short-leg continuity, short-delta, and one-strike-inside variants suggested by the short-leg provider no-match audit. They did not beat the hard-prior lead on exact count/coverage; the best short-prior/backfill variant reached `101` exact trades but still had `18` unpriced candidates.
- Added fixed-time spread exit variants and `spread_exit_monitoring_mode="time_only"` support. Intermediate quoted-exit variants improved the count/economics split: `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly50_v1` produced `116` exact trades, PF `2.59`, and `3` unresolved candidates; `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly55_v1` produced `112` exact trades, PF `3.04`, and `7` unresolved candidates, with all priced exits using bid/ask `time_exit` fills.
- Fixed the time-only spread simulator so a scheduled fixed exit attempts expiry-date bid/ask quotes before falling through to intrinsic settlement, and added a regression test in `tests/test_chain_native_continuity.py`.
- Reran the late fixed-exit variants after that fix. `sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly75_v1` now has `118` exact trades, `0` unresolved candidates, PF `2.16`, avg `+37.71%`, rolling test PF `1.70`, and `49` expiration-settlement exits that must be labeled separately from quoted exits.
- Final quoted-exit robustness favors `timeonly55`: artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_winner_clean_plus_liquid_no_cat_pm_prior1_timeonly55_v1.json` passed the available `50/20` rolling window with `30` exact test trades at PF `2.24`, avg `+28.58%`, and no unpriced test candidates; it also stayed positive under 5%/side stress at PF `2.26`, top-5 winner removal at PF `2.44`, and worst-ticker removal at PF `2.50`.
- Bounded exact-fill/classification for the `50/55` fixed-time sleeves wrote `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T065705Z.json`; it imported `1` new trusted row and left the remaining unresolved contracts as provider no-match or endpoint failures.
- Tested causal contract-selection variants on top of the `timeonly55` lead: `shortscore075`, `shortprior5_backfill15`, `shortinside1`, and `trad90`. They did not improve the promoted count/coverage balance: the best PF sibling reached `109` exact trades at PF `3.65` but worsened quote coverage to `91.6%`, while the stricter tradability sibling fell to `84` exact trades. The branch is not promoted because entry-time continuity/inside-strike changes did not solve the remaining provider no-match exits.
- Added `spread_time_exit_basis="calendar_elapsed"` support for imported spread replay and a regression test for elapsed-calendar fixed exits. The integrated calendar `55%` branch improved quote coverage to `98.2%` but reduced PF to `1.80`, so it is not promoted ahead of the trading-session fixed-exit branches.
- Added ticker-cluster fixed-exit sleeves. The high-PF branch `sleeve_winner_cluster_exit_50_55_60_no_pld_xlk_v1` produced `113` exact quoted trades, `7` unresolved provider no-match candidates, `94.2%` coverage, PF `4.34`, avg `+49.78%`, and passed the available rolling OOS/stress/holdout checks except for unresolved candidates.
- Added higher-coverage balanced cluster sleeves. `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_v1` produced `115` exact quoted trades, `1` unresolved JNJ provider no-match candidate, `99.1%` coverage, PF `2.57`, avg `+34.29%`, rolling test `36` exact trades at PF `2.59`, and 5%/side stress PF `1.95`.
- Added the clean quoted subset `sleeve_winner_cluster_exit_balanced_quoted_no_unh_xlk_pld_jnj_v1`: `95` exact quoted trades, `0` unresolved candidates, `100.0%` quote coverage, PF `3.36`, avg `+44.07%`, and 5%/side stress PF `2.56`; it is useful as a clean component sleeve but remains below the `100`-trade target.
- Ran a bounded exact-fill pass for the cluster branches at `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T074150Z.json`; it normalized `1` duplicate UNH row, imported `0` new rows, and left the remaining misses as provider no-match exact-contract gaps.
- Final verification run: `python -m pytest tests/test_wfo_sleeve_selection.py tests/test_chain_native_continuity.py tests/test_historical_options_store.py tests/test_imported_intraday_robustness.py tests/test_strategy_audit.py tests/test_historical_truth_lane.py::HistoricalTruthLaneTests::test_daily_imported_backtest_uses_shared_quote_calendar_to_avoid_peer_gaps -q` passed (`129` tests).
- Added `scripts/analyze_bullish_pullback_confidence_tiers.py` and `tests/test_bullish_pullback_confidence_tiers.py` to turn exact quoted sleeve runs into S/A/B/C/Blocked confidence tiers over the full `59`-symbol active universe. Latest report: `data/profitability-lab/bullish-pullback-observation/confidence/latest.json`.
- Reworked the all-59 expansion pass after subagent review. Broad forced refills added trades but weakened reliability: `sleeve_pf59_s_ab_timecluster_v1` reached `141` exact trades at PF `2.39` but only `86.5%` coverage and weak A/B refills; `sleeve_pf59_s_themeA_no_ticker_bans_v1` reached `161` exact trades at PF `1.58`; `sleeve_pf59_coverage_clean_v1` reached `146` exact trades at PF `1.67`; and `sleeve_pf59_s_a_energy_defensive_v1` reached `135` exact trades at PF `2.96` but only `87.1%` coverage.
- Initial best expanded-count branch was `sleeve_pf59_coverage_a_refill_v1` at `data/options-validation/runs/20260528_085047_sleeve_pf59_coverage_a_refill_v1_intraday.json`: `133` candidates, `130` exact quoted trades, `3` unresolved provider no-match candidates, `97.7%` quote coverage, PF `2.04`, avg `+24.56%`, median `+35.86%`, and all priced exits are bid/ask `time_exit` fills. The 2026-05-29 v3 rerun supersedes this artifact for current-state references.
- Robustness artifact `data/profitability-lab/imported-intraday-robustness/latest_sleeve_pf59_coverage_a_refill_v1.json` passed the available `50/20` rolling window with `34` exact test trades at PF `2.35`, avg `+33.33%`, and `0` unpriced test candidates. It stayed positive under 5%/side stress at PF `1.53`, after top-5 winner removal at PF `1.65`, and after worst-month removal at PF `1.69`, but the robustness status remains `blocked` because `3` full-sample candidates are still unresolved.
- Missing-contract classification for `sleeve_pf59_coverage_a_refill_v1` found `3` provider no-match exact-contract gaps (`WMT` `2`, `JNJ` `1`) with same-expiry chains present. The initial bounded lookahead import wrote `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260528T145332Z.json`; later 2026-05-29 v3 imports reran this path with ThetaTerminal available and still returned `0` exact rows for those WMT/JNJ contracts.
- Latest confidence-tier report over the full active universe keeps only proven quoted cluster trades as tradable: S tier `72` trades, PF `5.98`, avg `+59.68%`; A tier `27` trades, PF `3.57`, avg `+40.65%`; B tier `9` trades, PF `2.78`, avg `+39.26%`; combined S/A/B `108` trades, PF `4.86`, avg `+53.22%`. C/Blocked broad-sleeve evidence is kept as scout-only because C is `85` trades at PF `0.43` and Blocked is `30` trades at PF `0.64`.
- Verification run after the confidence-tier script and all-59 refill additions: `python -m pytest tests/test_bullish_pullback_confidence_tiers.py tests/test_wfo_sleeve_selection.py tests/test_chain_native_continuity.py tests/test_imported_intraday_robustness.py -q` passed (`17` tests).
- Added `scripts/audit_bullish_pullback_ticker_evidence.py` and `tests/test_bullish_pullback_ticker_audit.py` to turn exact trusted per-ticker evidence into a keep/move/research/remove decision table for all `59` active `bullish_pullback_observation` symbols.
- Generated the per-ticker audit at `docs/bullish-pullback-ticker-audit-2026-05-29.md` and `data/profitability-lab/bullish-pullback-observation/ticker-audit/latest.json`. Final buckets: keep current lane `10` symbols (`IWM`, `AAPL`, `GOOGL`, `UNH`, `LLY`, `JNJ`, `XOM`, `CVX`, `COP`, `NEM`), move to separate lanes `10`, remove from current queue `9`, and keep research/data-needed `30`.
- Subagent slice audits reviewed index/growth, financial/consumer/defensive, healthcare/energy/materials, and industrial/REIT/high-beta symbols. The reconciled audit keeps strategic high-beta failures such as `NVDA`, `AMZN`, and `TSLA` out of current bullish-pullback picks but routes them to a separate high-beta hypothesis instead of calling the tickers bad.
- The per-ticker audit confirms the trade-count issue is real: high-confidence S/A/B evidence has `108` exact trades at PF `4.86`, and the count-expanded all-59 branch has `130` exact trades at PF `2.04`, leaving a `70` to `92` exact-trade gap to the desired `200+` annual cadence.
- Verification run after the ticker-audit additions: `python -m pytest tests/test_bullish_pullback_ticker_audit.py tests/test_bullish_pullback_confidence_tiers.py -q` passed (`12` tests).

## 2026-05-27

- Ran the required full active-universe `bullish_pullback_observation` ThetaData intraday OPRA/NBBO baseline with `n_picks=5`, pessimistic pricing, trusted imported data only, and `min_imported_calendar_dates=252`; active universe count was `59`, `CMCSA` was excluded, and all active symbols had trusted intraday coverage.
- Verified the broad baseline remained research-weak on exact contracts: `334` candidates, `174` priced, `134` unpriced, `59.9%` quote coverage, and only `21` exact-contract priced trades at PF `0.83`.
- Ran bounded exact-fill loops and variant searches around chain-native spread selection, pullback filters, SPY regime, short-leg bid, exits, daily selection, and max 20-day signal return.
- Best 100+ trade candidate was `lane_a_shortbid05_ret5_actual_pullback_final`: `105` exact trades, PF `1.56`, avg `+13.24%`, `71.9%` quote coverage, but blocked by unpriced candidates and failed rolling OOS PF/avg.
- Best OOS-improved candidate was `lane_a_shortbid05_ret20cap13_final_bulkfill`: `91` exact trades, PF `1.83`, avg `+17.14%`, `72.2%` quote coverage, rolling OOS priced exact PF `1.72`, and stopped exact-fill loop with remaining misses classified as ThetaData no-match/provider issues; it remains blocked by exact trade count below the 100 target, unresolved candidates, and rolling OOS unpriced candidates.
- Added a reporting-only fix so unpriced replay rows preserve `unpriced_reason` as `non_promotable_reason` instead of being mislabeled as nearest-listed.
- Added trusted-only filtering to exact quote, entry-contract, continuity, and closing-quote imported replay lookups, then added a regression test so research/free-vendor rows cannot satisfy `trusted_only` entry lookups.
- Added timestamp-safe numeric parquet date coercion and replay-only entry execution backfill controls (`execution_backfill_enabled`, `execution_backfill_scan_depth`) so ranked candidates can skip known entry-time chain-native spread failures and backfill from the same day's ranked list without using future exit quotes.
- Re-ran the required active-universe baseline as `data/options-validation/runs/20260527_073823_bullish_pullback_observation_intraday.json`; it preserved the weak control result with `334` candidates, `174` priced, `134` unpriced, `59.9%` coverage, and `21` exact trades at PF `0.83`.
- Best final near-miss was `lane_a_shortbid05_excl_oos_unpriced_plus3_backfill15` at `data/options-validation/runs/20260527_083004_lane_a_shortbid05_excl_oos_unpriced_plus3_backfill15_intraday.json`: `105` exact trades, PF `1.88`, avg `+18.43%`, rolling OOS test `28` exact trades at PF `1.65` and avg `+15.40%`, and 5%/side slippage stress still positive at PF `1.34`.
- Stopped the exact-fill loop for that near-miss at `data/options-validation/thetadata-nbbo/thetadata_exact_missing_intraday_20260527T143016Z.json`; the pass requested `36` unique OCC/date items, imported `0` normalized rows, and left misses classified as ThetaData no-match/provider issues.
- Terminal status for this research round is `BLOCKED`, not paper/live-shadow ready: the near-miss still has `40` unpriced selected candidates, `72.4%` quote coverage, only `12` promotable dense exact-contract trades against the `25` target, and final skeptical/statistical subagent audits rejected promotion.

## 2026-05-25

- Added `docs/agent-worktree-hygiene.md` and linked it from the living docs so agents know when to branch, push big changes, and report required untracked files.
- Added `docs/weekly-bug-audit-loop.md` with the reusable six-agent audit goal prompt, agent slices, verification ladder, delivery rules, and weekly automation prompt.
- Audited the living Markdown docs against the current route tree, FastAPI decorators, package scripts, storage artifacts, and generated AI commodity progress readbacks.
- Updated route docs to include the backend-only `DELETE /api/predictions/{pred_id}` endpoint and the mirrored `forward-evidence` / `exit-audit` backtest routes.
- Updated current-state docs for the AI commodity exact OPRA lane: `3` / `100` shared quote dates, `24` aligned scan/proof symbols, no latest live candidates, and next gated actions on `2026-05-26`.
- Corrected stale day-trading notes so they match this worktree's actual code and tests.
- Added `scripts/generate_route_parity.py`, `npm run docs:route-parity`, and `npm run verify:docs` so route docs can be regenerated and checked from source.
- Moved dated roadmap and audit records into `docs/archive/` and promoted `DECISIONS`, `runtime-request-flow`, and the paid-data checklist in the docs index.

## 2026-05-22

- Ran the guarded fresh OPRA scan command `python scripts/run_ai_commodity_opra_progress.py --skip-capture` inside its allowed window and followed with `--next-execution --from-latest`.
- Refreshed the paid-data readiness audit for `alpaca_opra_daily_snapshot` at the 100 shared-date threshold; status remains `partial` with `thin_required_history`.
- Audited local credentials, option stores, Alpaca/Theta import paths, and local ThetaTerminal availability without printing secret values.
- Added `scripts/import_thetadata_options_nbbo.py` and tests so a licensed ThetaData v3 OPRA NBBO source can be imported into the existing exact bid/ask store.
- Wrote a commodity-lane historical NBBO acquisition plan under `data/ai-commodity-infra/progress/`.
- Restored the missing Codex automation TOML for `ai-commodity-opra-capture`; latest lane readback reports `automation_healthy=true`.
- Added and ran `scripts/probe_alpaca_options_history_access.py`; authenticated Alpaca access supports historical option bars/trades and latest OPRA quote/snapshot surfaces, but `GET /v1beta1/options/quotes` returned 404 for historical bid/ask backfill.
